"""LLM-as-judge。**只能进文档，不能进 CI 闸门。**

为什么与 `eval.metrics` 分开放
------------------------------
两者的可信度不是一回事。`eval.metrics` 那些数是从报告正文里**读**出来的，
同样的输入永远得到同样的值，所以它们可以作为一条"破了就红"的线。
判官指标不是：同一个输入换一次采样，裁判可能改判。

把它放进 CI 会产生一种很坏的绿——某次它碰巧全给了"支持"，闸门就过了，
而下一次同样好的报告可能因为裁判心情不好而红。**一条会随机红的闸门，
很快就会被人加上 `continue-on-error`，然后它就再也不起任何作用了。**

所以规矩是：

- 报告判官指标时，**必须同时写明四个数**：样本量、温度、裁判模型、跑了几次。
  少一个，那个数就没法被复现，也就没法被质疑——而一个无法被质疑的数字
  在作品集里是负资产。
- `--repeat N` 报**均值与离散度**。只报均值等于把"这个数有多不稳"藏起来。

裁判用哪个档位
--------------
用 `core` 档（最强的那一档）。裁判比被评的对象弱的话，
它判的是"我能不能写出这样的东西"，不是"这段东西好不好"。
代价因此比跑流水线本身还高——这是判官指标只跑一次的另一个原因。

**裁判也是 provider。** 这里不出现任何厂商名：`registry.get_llm()` 拿到的是
当前配置的适配器，换一家裁判就是换一个环境变量。
"""
