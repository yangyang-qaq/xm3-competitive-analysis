"""xm3 评测层。

与 `backend/` 分开放，是因为它回答的问题不一样：
`backend/tests/` 问「这段代码按我写的规则跑吗」，`eval/` 问
「这套东西跑出来的东西好到什么程度」。前者有唯一正确答案，后者只有一个数，
而且那个数会随模型、prompt、搜索源变化——所以它不该混进单元测试里，
也不该用单元测试的绿/红来表达。

两个模块：

- `eval.metrics` —— 从报告正文里**读**指标（不重算），再跨 query 聚合。
- `eval.run_eval` —— 黄金集跑批 + 闸门判定。

`eval/judges/` 放 LLM-as-judge：它们有采样噪声，只能进文档，不能进 CI 闸门。
"""
