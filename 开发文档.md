# 开发文档

> 按阶段记录 xm3 是怎么长出来的。每个阶段结束时都有**可运行、可验证**的东西，
> 而不是"写了但还没接上"。
>
> 每章五节：**数据库设计 / 后端 API / 前端 / 涉及文件 / 验证方法**。
> 某个阶段没动那一层时，那一节直说"本阶段无"，不拿别的内容填满它。
>
> 问题与踩坑另记在 [`问题记录.md`](问题记录.md)（55 条），这里只记**做了什么、怎么验的**。

**累计**：约 5 周全职。阶段 0–6 结束就有"能演示的最小完整闭环"，
7–8 补全对标能力，9–10 是质量与文档。

---

## 阶段 0：骨架与契约（1–2d）

先把"两边能起来、能互相说话"这件事做完，再写任何业务代码。
这一阶段刻意不实现任何功能——它产出的是**约束**。

### 0.1 数据库设计

**本阶段无。** 数据库推迟到阶段 2：那时才知道要存什么。
先建表再写代码，会产生一批"建了但没人写"的表
（这是阶段 8 真的发生过的事，见 8.1）。

### 0.2 后端 API

只有一条：

```
GET /health → {status, version, llm_provider, llm_configured, ...}
```

**它不做任何外部调用。** 语义必须是廉价且永远可用——
它是前端"后端起没起"的探针。真实连通性探测是后来的
`/api/providers/health`，两者刻意分开：
一个回答"我们的进程活着吗"，一个回答"外面那些服务活着吗"。

返回体来自 `settings.describe()`，**不含任何密钥**，
只有一个 `llm_configured` 布尔值表示 key 配没配。

### 0.3 前端

Vite + React 19 + TS + Tailwind 4 起在 **3500**，
`vite.config.ts` 里把 `/api` 与 `/health` 代理到 **8020**。

两处刻意的配置：

```ts
server: {
  port: 3500,
  strictPort: true,   // 端口被占时直接失败，而不是悄悄换一个
}
```

`strictPort` 的理由：代理指向固定的 8020，
如果前端悄悄换到 3501，代理仍然对，但**你打开的那个地址不是你以为了那个**——
而它看起来一切正常。

`types/` 下的类型定义**先写**（`domain.ts` / `events.ts` / `report.ts`），
此时还没有任何东西产生它们。这是有意的前置：类型是前后端之间的契约，
先定契约再写两端，比"先写后端再补类型"少一轮返工。

端口选 8020 / 3500 的理由是上游 Verda 占了 8010 / 3400，
**两套系统要能同时跑**——这在对照参考实现时是刚需。

### 0.4 涉及文件

```
README.md  CLAUDE.md  LICENSE（MIT）  .gitignore
backend/pyproject.toml   backend/app/main.py   backend/app/core/config.py
frontend/vite.config.ts  frontend/src/App.tsx  frontend/src/types/*.ts
```

### 0.5 验证方法

| 检查 | 期望 |
|---|---|
| `uvicorn app.main:app --port 8020` | 起得来，`/health` 返回 200 |
| `npm run build` | 过（`tsc -b && vite build`） |
| `pytest -q` | 绿，0 用例 |
| `npm test` | 绿，0 用例 |

> 最后两项"绿着 0 用例"是这一阶段唯一有争议的地方。
> 保留它们是因为：**从第一天起，"绿"就必须是一个能跑的结论**，
> 而不是"还没有测试所以没红"。中途插入一个测试框架，
> 会顺便带来一批"第一批用例怎么写"的临时决定。

---

## 阶段 1：Provider 层（mock 驱动，2–3d）

**这是整个项目的技术核心**，所以它排在数据层和流水线之前。
理由：适配层如果是后加的，就会变成"在已有的流水线上抽出接口"——
而那意味着所有厂商方言已经漏进编排层了，抽的时候要靠读代码找回来。

### 1.1 数据库设计

**本阶段无。** Provider 层不持久化任何东西。

### 1.2 后端 API

**本阶段无。** 这一层先做成可被 CLI 与测试直接调用的库，
HTTP 暴露推迟到阶段 3（`/api/providers`）——
接口的形状要等真实适配器写完才定得下来。

### 1.3 前端

**本阶段无。**

### 1.4 做了什么

三个 Protocol，方言全部下沉到适配器：

| 类型 | 归一化的输入输出 | 关键决定 |
|---|---|---|
| `LLMProvider` | `chat(messages, *, tier: "core"\|"aux"\|"fast", ...)` | **只接受档位，不接受模型名** |
| `SearchProvider` | `search(SearchQuery) -> list[SearchHit]` | `Freshness` 是五档枚举，不是天数 |
| `Fetcher` | `fetch(url, *, fallback_snippet="") -> FetchedPage` | `degraded` 默认 **`True`** |

两处"默认值即态度"的地方：

- `freshness` 用 `Literal["any","day","week","month","year"]` 而不是 `days: int`。
  它是**意图**，而各家对"一个月"的实现不一样（博查 `oneMonth`、
  Tavily `days: 30`、有的按自然月）。写 `30` 会让意图提前被一个数字钉死。
- `FetchedPage.degraded` 默认 `True`——**默认值取"这次抓取不可信"**。
  忘了设的后果是"少一条好证据"，而不是"一条坏证据被当成好的"。

配套四件：

1. `registry.py`：装饰器注册 + `lru_cache` 构造。
2. `errors.py`：`ProviderError(retryable)` 及 9 个子类，
   HTTP 状态码到异常的映射**在一张表里**（`_BY_STATUS`），
   不在各适配器里各写一份。"怎么判断是不是限流"只有一处实现。
3. `retry.py`：唯一的 `with_backoff()`，退避是指数偏慢的
   （4/8/15/25 秒）——限流窗口通常是分钟级，隔一秒重试只是再撞一次。
4. `cassette.py`：录制/回放，`CASSETTE_MODE=off|record|replay`。

`CassetteMiss` **继承 `HarnessError`** 是有意的：回放时缺记录
不是"provider 挂了"，是"夹具不全"，它必须穿过降级逻辑直接抛出去。
否则一次录漏会表现成"这一次跑得有点降级"，而不是"夹具缺一条"。

### 1.5 涉及文件

```
backend/app/providers/base.py       # 三个 Protocol + 归一化类型
backend/app/providers/registry.py   # 注册与构造
backend/app/providers/errors.py     # 错误分类 + 状态码映射表
backend/app/providers/retry.py      # RetryPolicy + with_backoff
backend/app/providers/cassette.py   # 录制 / 回放
backend/app/providers/mock.py       # 三个 Mock provider（确定性、零成本）
backend/tests/unit/providers/       # 方言翻译、错误映射、退避
```

### 1.6 验证方法

| 检查 | 期望 |
|---|---|
| `pytest tests/unit/providers -q` | 绿 |
| record → replay 往返 | 同一份输入两次跑，输出逐字节相同 |
| 缺录制时 `CASSETTE_MODE=replay` | 抛 `CassetteMiss`，**不联网** |

第三条是这一层最重要的性质：**它让"跑测试 = 零成本"成为一条
由代码保证的事实**，而不是一句约定。

---

## 阶段 2：数据层 + 流水线主干（3–4d）

里程碑：**核心闭环跑通，零 API 花费。**

### 2.1 数据库设计

9 张表，`PRAGMA user_version` + 有序迁移列表（**不用 Alembic**）。

| 表 | 存什么 |
|---|---|
| `tasks` | 任务记录（query / mode / status / stage / 澄清问答 / 主题与品牌） |
| `reports` | 报告正文（整份 JSON 一列）+ 需要筛选排序的标量列 |
| `evidences` | 每次任务采到的证据，主键 `(task_id, evidence_id)` |
| `traces` | 每次 LLM / 搜索 / 抓取调用，主键 `(task_id, span_id)` |
| `task_events` | SSE 事件日志，供断线续发 |
| `report_feedback` | 人工批注 |
| `subscriptions` | 订阅（**没有写入方**，见 8.1） |
| `expert_stats` | 专家统计（**没有写入方**，见 8.1） |
| `report_team` | 专家↔报告关联（阶段 5 加的回填，阶段 8 用上） |

三个连接配置：**WAL**、**`busy_timeout`**、**线程局部连接**。
理由是流水线要通过 `asyncio.to_thread` 扇出采集，
而 `sqlite3` 的连接对象不能跨线程共享。

三个值得写进文档的取舍（完整版在 [`docs/DATA_MODEL.md`](docs/DATA_MODEL.md)）：

1. **报告正文整份 JSON 存一列**，另加所有需要筛选排序的标量列。
   读放大无所谓（一次访问一份报告），拆成 12 张规范化表只会换来 JOIN 风暴。
2. **`report_feedback` 用代理主键 `feedback_id`**。
   上游 Verda 用 `report_id` 做主键，每次提交覆盖上一次，
   「人工修正率」永远只能看快照、看不出趋势。
3. **`save_report(rep)` 从报告对象内部取 `task_id`**，不接受调用方传参。
   Verda 的 `save_report(rep, task_id="")` 用 `INSERT OR REPLACE`
   把关联抹掉——这里让它在结构上不可能发生。

两个 Verda 没有的列：`evidences.credibility_breakdown`（可解释评分）
与 `evidences.matched_dimensions`（维度覆盖的真相源）。

### 2.2 后端 API

**本阶段无 HTTP 暴露**，只有 CLI：

```
python -m scripts.run_pipeline_cli "对比 Notion 与 Obsidian" --mode quick --provider mock
```

这是**刻意的**：核心闭环先以"一个能打印完整事件流的命令行"的形式存在。
有了它，阶段 6 的 SSE 只是把同一股事件流换一个出口，
而不是在那时才第一次把流水线接起来。

### 2.3 前端

**本阶段无。** 前端在阶段 6 之前不碰流水线。

### 2.4 流水线的形状

唯一真相源是 `core/pipeline/stages.py`：

| key | label | weight | optional |
|---|---|---|---|
| `intake` | 需求理解 | 6 | 否 |
| `orchestrator` | 专家调度 | 8 | 否 |
| `collect` | 联网采集 | 34 | 否 |
| `analyze` | 分析研判 | 30 | 否 |
| `audit` | 质量审计 | 6 | 否 |
| `rework` | 返工补采 | 8 | **是** |
| `write` | 报告撰写 | 8 | 否 |

`weight` 是**进度条的权重**，不是耗时估计——
`collect` 占 34%、`analyze` 占 30%，因为它们确实是这个系统里最重的两步。

这个常量同时供给三处：SSE 的首个 `node_update`、
前端 DAG（`GET /api/pipeline/stages`）、以及**一个文档一致性测试**
（解析 [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) 的 mermaid 块，
断言节点顺序等于这个常量）。

**把"文档与代码不符"变成结构上不可能**，代价只是 40 行解析代码。
上游 Verda 的架构文档把质检与写作的顺序写反了——那不是笔误，
是"文档和代码各自演化"这个结构的必然产物。

### 2.5 涉及文件

```
backend/app/db/connection.py  migrations.py  schema.sql  repo/{tasks,reports,evidences,traces,events}.py
backend/app/core/models.py            # Evidence / Claim / Issue / Envelope
backend/app/core/schemas/             # base.py（容错原语）+ feature_tree/pricing/persona/completeness
backend/app/core/evidence/            # credibility / relevance / textquality / fetch / citations / sourcetypes
backend/app/core/pipeline/            # stages.py + intake/orchestrator/collect/analyze/audit/write/assemble
backend/app/core/analysis/            # sentiment / charts / metrics / matrix
backend/scripts/run_pipeline_cli.py
```

### 2.6 验证方法

| 检查 | 期望 |
|---|---|
| `pytest -q` | 全绿，**零 API 花费** |
| `run_pipeline_cli ... --provider mock` | 吐出完整事件流 + 一份合规的 `report.json` |
| 报告合规性 | 每条论点有证据引用；质量门有结论 |

这一阶段结束时 **`--provider mock` 就能出一份结构完整的报告**。
之后所有阶段都在这条已经跑通的路径上加深，
而不是在一条从没跑通过的路径上继续加东西。

---

## 阶段 3：接真实 provider（2–3d）

### 3.1 数据库设计

**无新表。** 迁移 v2 在这里加了一列：
`traces.cached_prompt_tokens`（缓存命中的输入 token 数）——
它会进成本计算，因为缓存命中的 token 单价低得多，不算这一列成本会虚高。

### 3.2 后端 API

两条新路由：

| 路由 | 内容 |
|---|---|
| `GET /api/providers` | 当前生效的 provider、能力矩阵、今日成本 |
| `GET /api/providers/health` | 真实连通性探测，`?scope=active\|all` |

两处刻意的设计：

- `/api/providers/health` 的 `ok` **只统计 `probed: true` 的条目**。
  没配 key 的 provider 不算失败——它是"没启用"，不是"坏了"。
  把它算成失败会让"我没配 Tavily"表现成"系统有一处坏了"。
- `/api/providers` 里 `cost` 那条查询失败时**吞掉异常返回全 0**，
  而不是 500。理由是成本栏是页面上的一个角落，它挂掉不该让整页打不开。
  日志里有记录。

### 3.3 前端

**本阶段无。** 这两条路由先给 CLI 与人工 `curl` 用，
前端到阶段 8 的仪表盘才消费它们。

### 3.4 接了什么

| provider | 状态 |
|---|---|
| `llm/deepseek.py` | 默认，实测调用 |
| `llm/zhipu.py` | 适配器完整，**未真实调用过** |
| `search/bocha.py` | 默认，实测调用 |
| `search/tavily.py` | 适配器完整，**未真实调用过** |
| `fetch/http.py` | trafilatura + BeautifulSoup 兜底 |
| `llm/fallback.py` | `LLM_FALLBACK` 的降级链 |

降级链有两条规则值得单说：

- **致命错误会闩锁（latch）**：`AuthFailed` / `QuotaExhausted` /
  `ProviderNotConfigured` 一旦出现，后续调用**不再尝试主 provider**。
  key 无效不会在 3 秒后自己变好，每次去撞只是把每次调用都拖慢一个超时。
- **`chat_stream` 不做中途切换**：流已经吐了一半，换一家接着吐
  只会得到一段语义断裂的文本。宁可让这一次失败。

`name` 是 `f"{主}>{副}"`，`summary()` 在 `/api/providers/health` 里能看到。
**降级了多少次是一个可见的数**，不是日志深处的一行。

同时写下能力协商的第一条（也是目前唯一一条）策略：
`calls.py` 的 `adapt_query()`——provider 不支持站点过滤时，
把域名折进查询词并**计数**（`site_filter_folded`）。
不拼 `site:` 前缀（那本身就是一种厂商方言），同时清空 `sites`
（不清的话适配器还会尝试过滤，然后返回 0 条）。

### 3.5 涉及文件

```
backend/app/providers/llm/{openai_compat,deepseek,zhipu}.py
backend/app/providers/search/{bocha,tavily}.py
backend/app/providers/fetch/http.py      providers/fallback.py
backend/app/api/routes_providers.py      backend/app/core/pipeline/calls.py
backend/scripts/record_cassettes.py      backend/tests/fixtures/cassettes/
```

### 3.6 验证方法

| 检查 | 期望 |
|---|---|
| `pytest -m live` | 2 次真实调用，绿（**必须显式开启**） |
| 同一份 CLI 换 `--provider deepseek` | 出真报告，记下成本与耗时 |
| `record_cassettes.py` | 开跑前打印预估 token/成本，要求 `--yes` |
| 方言翻译 | `httpx.MockTransport` 断言**请求体的形状** |

现有 cassette：`llm.deepseek.jsonl`（98 条）、`search.bocha.jsonl`（143 条）、
`fetch.http.jsonl`（68 条）。录制清单在 `MANIFEST.json`：
5 条黄金查询、90 次 LLM 调用、571,355 prompt tokens、
101,236 completion tokens、成本 **$0.1326**。

---

## 阶段 4：48 专家 + 动态组队（2d）

### 4.1 数据库设计

**无新表。** 名册**不进数据库**——它来自 `experts.json`（代码资产），
理由是放进库里就要面对"库里的名册和仓库里的名册不一致"，
而这个不一致**没有任何东西会报错**。

### 4.2 后端 API

| 路由 | 内容 |
|---|---|
| `GET /api/experts` | `?level=` `?group=`，返回 `{items, total, rosterSize, byLevel, byGroup}` |
| `GET /api/experts/{id}` | 专家详情 + **他参与过的报告** |

三处口径上的决定：

- `rosterSize` 是**名册总人数**（48），不受过滤影响；`total` 是筛选后的。
  页头写"48 位专家"用的是前者——用 `total` 的话筛选之后页头会变成"12 位专家"。
- 排序：层级 → 分组 → 参与度降序 → **id 升序**。
  同分时按 id，否则每次刷新名册的顺序都在跳。
- 过滤在服务端做。48 人一次全发也就几十 KB，
  但前端自己筛意味着筛选逻辑有两份，而多一份就会漂。

`GET /api/experts/{id}` 走的是 `reports_of_expert()`（`report_team` 与
`reports` 的连接），**不是"拉 200 份报告再自己过滤"**——
后者为了回答"他都参与过哪几次"要读 200 × 172 KB 的正文，
而这里一个正文字段都不需要。

### 4.3 前端

**本阶段无。** 名册页在阶段 8。

### 4.4 名册怎么来的

拆成两半：

- **`experts_seed.yaml`**：手写的语义内核，约 8 行/人，
  `id / level / group / name / role_title / one_liner / skills /
  knowledge_base / knowledge_tags`。**每一条都是面试里讲得出的内容。**
- **`gen_experts.py`**：确定性生成机械字段
  （`avatarColor` / `badgeColor` 用 `hash(id) % palette` 稳定取色、
  `domainIcon` 按 group 查表、`stats` 初值）。

直接手写 48 条 JSON 是 1500+ 行复制粘贴——语义对了，
但会引入错别字、重复 id、以及"改了 YAML 忘了改 JSON"的漂移。

**"生成而非手写"要成立，得有一条测试证明它是幂等的**：
`test_experts.py` 里"重复生成字节一致"那条钉住了这一点。
没有它的话，"生成"和"随机"在结果上无法区分。

名册由 **46 条用例**守护：恰好 48 人、id 唯一且匹配 `^L[123]-\d{3}$`、
分布 3/9/24/12、level 与 group 一致、无空必填字段、
每人 `knowledge_tags` ≥ 3、`one_liner` ≤ 40 字、生成幂等。

### 4.5 组队：两道关

```python
_ROLE_SPECS = (
    ("lead",        "L3", 1, (...)),   # 组长，恰好 1 人
    ("strategists", "L2", 3, (...)),   # 战略层，最多 3 人
    ("executors",   "L1", 4, (...)),   # 执行层，最多 4 人
)
```

**第一道关：id 得在名册里。** 模型会写出不存在的号。
不校验的话它会一路走进 `expert_stats` 表，污染"每位专家参与了多少任务"，
而且**没有任何地方会报错**——统计页上的数字看起来完全正常，
只是有一行属于一个不存在的专家。

**第二道关：层级得对。** 48 人名册里有 36 个执行层，
"号是真的但人被放错了层"因此比"号是编的"更常见——
一个战略层的人被指派成组长，在数据上和一次正常调度长得一模一样。

这一条**原先漏了**，那是一个真的漏洞：校验只查"这个 id 在不在名册里"，
不查"它在哪一层"。名册从 15 人扩到 48 人之后，
这个漏洞只会更容易被踩到。

`TeamOutcome` 把调度问题分成**四类**，不合成为一个数：

| 字段 | 含义 | 这是谁的错 |
|---|---|---|
| `phantom` | 名册里查不到的 id | 模型的错，它在编 |
| `wrong_level` | id 存在但层级不对 | 模型的错，它没读层级要求 |
| `over_limit` | 合法但超出上限被截掉 | **我们的取舍** |
| `fallback_roles` | 整类回退到默认队伍 | 调度没生效 |

原先这四种被合成描述成"N 个专家 id 不存在于名册"——
而其中只有第一种是真的不存在。实测那次报告里的"有 11 个专家 id 不存在于名册"，
里面就混着后两种。**观测通道说的每一句话都必须是真的。**

### 4.6 涉及文件

```
backend/app/data/experts_seed.yaml      # 手写名册
backend/app/data/experts.json           # 生成产物
backend/app/data/loader.py              # load_experts / expert_by_id / roster_digest
backend/scripts/gen_experts.py
backend/app/core/pipeline/dispatch.py   # 两道关 + TeamOutcome
backend/app/api/routes_experts.py
backend/tests/unit/test_experts.py      # 46 条
```

### 4.7 验证方法

| 检查 | 期望 |
|---|---|
| `pytest tests/unit/test_experts.py -q` | 46 条全绿 |
| 重复运行 `gen_experts.py` | 输出**逐字节一致** |
| 用一份含伪造 id 的 cassette | 伪造被过滤，回退到确定性默认队伍，四类问题分别计数 |

---

## 阶段 5：质检返工闭环 + 可观测（3d）

### 5.1 数据库设计

**无新表。** 这一阶段新增了 `report_team` 的**回填逻辑**
（迁移 v4），它属于阶段 8 需要的数据准备，但写在这里。

### 5.2 后端 API

| 路由 | 内容 |
|---|---|
| `GET /api/tasks/{id}/trace` | span 树 + 每任务 seq + 成本 |

### 5.3 前端

**本阶段无。** TracePanel 在阶段 8。

### 5.4 可观测怎么做的

`contextvars` 无侵入埋点：阶段代码**不需要手动传 tracer**
（那会让每个函数签名都多一个参数），
而是用 `ContextVar` 持有当前 span，`call` 的包装器自动捕获输入输出、耗时、成本。

代价是 `contextvars` 在 `asyncio.to_thread` 里**不会自动传递**——
线程池的工作线程有自己的上下文，所以包装器必须在进入线程前显式 copy。
这一处如果漏了，症状是"某些调用的 trace 不见了"，**而不是报错**。

`observability/` 提供两条独立的东西：

- **span 树**（给人看，`/api/tasks/{id}/trace`）
- **事件 journal**（给前端推，SSE）

分开的理由：两者的消费方式完全不同。span 树是"一次取回、静态渲染"，
journal 是"持续追加、带 seq 可续传"。合成一个结构会让两边都别扭。

### 5.5 返工闭环

`audit` 阶段产出 Issue 列表 → `decide_rework()` 判断是否返工 →
返工后 `metrics_after - metrics_before` 就是**这条铁律唯一可量化的证据**。
没有这个差值，"返工有效"就只是一句话。

`Issue` 与"日志里的一行警告"的区别是有 `suggestion` 与 `targets`：
有了它们，Issue 可以驱动**一次具体的返工**（补采哪个维度的哪些 query）；
没有的话，问题清单就只是一份抱怨。

`ISSUE_KINDS` 里 9 种问题，每一种都对应一条明确的返工动作。
`issue_id` 由 `kind + 维度 + 品牌 + detail` 摘要生成——
**同一轮里重复发现的问题不会被记成两条**。

### 5.6 涉及文件

```
backend/app/core/observability/trace.py    events.py
backend/app/core/pipeline/audit.py         runner.py
backend/app/core/analysis/metrics.py
backend/app/api/routes_tasks.py
```

### 5.7 验证方法

三个 fixture 测试：

| 测试 | 断言 |
|---|---|
| 证据稀薄触发返工 | `metrics_after > metrics_before` |
| 维度覆盖 | 有证据锚定的维度 ÷ 计划维度数（**有反回归测试**） |
| 同一 task_id 两个并发订阅 | 只跑一次流水线（用 provider 调用计数断言） |

第三条是流水线注册表存在的原因，也是上游 Verda 最严重的问题所在：
它的 `/api/tasks/{id}/stream` 在请求处理器里直接调 `run_pipeline`，
没有注册表。`EventSource` 断线自动重连、或开第二个标签页，
就会对同一 `task_id` 再跑一遍，重复写入证据、trace、专家统计。

---

## 阶段 6：SSE + 工作台（4–5d）

里程碑：**浏览器里能跑完整一次。**

### 6.1 数据库设计

`task_events` 从阶段 2 建好起，到这里才真正被写入与读取。
它存每个任务的事件，按 `(task_id, seq)`。

### 6.2 后端 API

| 路由 | 内容 |
|---|---|
| `POST /api/tasks` | 建任务 → **201** |
| `GET /api/tasks` | 分页列表 |
| `GET /api/tasks/{id}` | 快照（只读） |
| `POST /api/tasks/{id}/clarify` | 提交澄清答案 |
| `GET /api/tasks/{id}/stream` | **SSE**，纯读 |
| `GET /api/modes` | 三档模式参数 |
| `GET /api/pipeline/stages` | DAG 节点 |

三个反直觉但有意的地方：

1. **`POST /api/tasks` 会等到 intake 阶段跑完才返回。**
   因为澄清问题必须在那之前生成出来——否则前端拿到的
   `clarifyQuestions` 永远是空的。
2. **intake 失败时返回的仍然是 201**，body 里 `status: "failed"`。
   任务确实被创建了，它的失败是一个状态而不是一个请求错误。
   用 4xx/5xx 表达它会让前端把"任务建好了但第一阶段失败"
   和"请求根本没发出去"混成一件事。
3. **它不是一个幂等接口**——建任务会真的开始跑流水线。

SSE 的帧格式：

```
id: 37
event: thought
data: {"seq":37,"taskId":"TK-...","type":"thought","createdAt":"...","thought":{...}}

```

`id:` 是事件**按任务的** seq（从 1 开始）。
上游 Verda 用的是全局计数器，两个并发任务的 seq 会交错，
而决策回放的滑杆恰恰依赖 seq 排序。

浏览器原生 `EventSource` 会自动把最后收到的 `id` 放进重连时的
`Last-Event-ID` 头——**整条断线续传链路不需要任何客户端代码**。
`Last-Event-ID` 优先于 `?from_seq=`。

其余细节：15 秒心跳发注释帧 `: keep-alive\n\n`（注释帧不被 `EventSource`
当事件派发，但能让中间的代理不掐连接）；
响应头带 `X-Accel-Buffering: no`（nginx 部署必需，不加的话事件会在最后
一次性到达，看起来像"流没生效"）。

两条不同的服务路径：runner 在内存里 → 订阅 journal，**带心跳**，
一直挂着直到任务结束；runner 不在（进程重启过）→ 从 `task_events`
回放历史，**没有心跳**，历史推完就关流。
第二条是有意的：回放路径是"把已经发生过的事情讲一遍"，
它有个明确的终点，不需要保持连接。

### 6.3 前端

12 条路由分两组：

```
侧边栏组（AppShell 内）      /  /library  /knowledge  /experts  /experts/:expertId  /dashboard
全屏沉浸组                   /clarify/:taskId  /workspace/:taskId  /report/:reportId
                             /trace/:taskId  /graph/:reportId
兜底                         *
```

**SSE 摄入是这一阶段最关键的设计**，也是相对 Verda 的主要改动。
Verda 每个事件都 `set({thoughts: [...s.thoughts, d]})`——
一次 O(n) 拷贝 + 一次渲染。800 条思维 + 400 条证据流进来时肉眼可见地卡。

xm3 分三层：

```ts
// store/taskStore.ts
const THOUGHT_CAP = 800      // 环形上限
const SPAN_CAP = 1200
const FLUSH_FALLBACK_MS = 250 // 后台标签页里 rAF 不触发时的兜底
```

1. `seq <= lastSeq` 直接丢弃（断线续传去重）
2. 推进**模块级可变缓冲**，用 `requestAnimationFrame` 合并成一帧一次 `set()`
3. 环形上限 + 证据走虚拟列表

`lib/sse.ts` 是薄封装：`onmessage` 里解析、记录 `lastSeq`、转发。
**出错不主动 close**，交给 `EventSource` 自动重连，服务端按
`Last-Event-ID` 补发。

`types/events.ts` 用**判别联合**而非 `unknown` + `as`。
Verda 的类型转换会静默丢掉新事件新增的字段；判别联合下，
服务端加了一种事件而前端没跟上，`tsc` 会报错。

### 6.4 涉及文件

```
backend/app/api/routes_tasks.py
backend/app/core/pipeline/runner.py     # TaskRunner + ensure_runner + 事件 journal
backend/app/core/observability/events.py
frontend/src/lib/sse.ts                 frontend/src/store/taskStore.ts
frontend/src/store/taskEvents.ts        frontend/src/hooks/useTaskStream.ts
frontend/src/pages/{ClarifyPage,WorkspacePage,HomePage}.tsx
frontend/src/components/{pipeline/StageDag,stream/ThoughtStream,evidence/EvidenceStream}.tsx
contracts/sse_events.json               contracts/task_states.json
```

### 6.5 验证方法

```
浏览器走一遍：首页选模式 → 澄清问答 → 工作台看 DAG/思维流/证据流
```

| 检查 | 期望 |
|---|---|
| 跑到一半断网 10 秒再恢复 | **无缺口、无重复** |
| 跑到一半刷新页面 | 工作台从 journal 恢复 |
| 开两个标签页看同一任务 | 流水线只跑一次 |
| `contracts/sse_events.json` | 后端跑一遍真实 Mock 流水线断言推出去的字节与它一致 |

最后一条是双向的：后端断言**推出去的字节**，
前端断言**类型定义没有多写少写一个字段**。

---

## 阶段 7：报告页（4–5d）

### 7.1 数据库设计

新增一张表 `report_feedback`（代理主键 `feedback_id`）。

理由是上游 Verda 用 `report_id` 做主键，每次提交覆盖上一次，
「人工修正率」永远只能看快照、看不出趋势。
换代理主键之后，`report_feedback` 是**只增不改**的——
没有 UPDATE 路径。

### 7.2 后端 API

| 路由 | 内容 |
|---|---|
| `GET /api/reports` | 列表，每项是**摘要** |
| `GET /api/reports/{id}` | 完整正文 + 批注 |
| `POST /api/reports/{id}/feedback` | 批注，只增不改 |
| `POST /api/reports/{id}/refine` | 按批注深化本节 |
| `GET /api/reports/{id}/export` | `?format=md\|json` |

几处细节：

- **列表刻意不含 `data`**：quick 档一份报告的 `data` 有几 MB，
  列表页要 20 份。这不是优化，是"列表页能不能打开"的问题。
- `refine` **会重写整份报告**（一次 UPDATE，不是改一个字段），
  并在 `report_feedback` 里记一条 `kind="refine"`。
  前端的做法是拿返回值里的 `section` **就地替换那一节**，
  而不是重新拉整份报告。
- `refine` 返回的 `phantomCitations` 是这次重写里被丢弃的、
  模型编造出来的引用编号个数——**它不是"出错"，是幻觉抑制在工作的计数**。
- 导出返回 `text/plain; charset=utf-8`，**不是** `text/markdown`——
  后者会让浏览器直接下载文件，而演示时更希望它显示出来。
  文件名走 RFC 6266 的 `filename*=UTF-8''...`，中文标题不会乱码。

### 7.3 前端

报告页拆成 17 个组件，按"一块内容一个文件"分：

| 块 | 组件 |
|---|---|
| 骨架 | `ReportHeader` / `ReportToc` / `ReportCatalog` / `ReportSection` / `ReportPanel` |
| 内容 | `Prose` / `ReportMatrix` / `ReportStructured` / `ReportClaims` |
| 图表 | `ReportChart` / `ReportCharts` / `ReportChartGrid` |
| 质量 | `ReportQuality` / `ReportAudit` / `ReportMetrics` / `SentimentPanel` |
| 交互 | `SectionAnnotator` / `EvidenceAside` |
| 附录 | `ReportAppendix` |

两处值得说的：

- **`ReportChart` 与 `ReportCharts` 是两个文件**，这是刻意拆的。
  `ReportChart` 是按需引入 ECharts、管 ResizeObserver、
  画不出来时给一句说明的单图组件；`ReportCharts` 负责把一批图排成网格。
  拆开之后，"报告页一张图都不显示"这个缺陷（问题记录 41）
  的成因才看得清楚——`ReportChart` 写得很完整，
  **而没有任何地方 import 它**。
- `lib/reportCitation.ts` 负责把正文里的 `[12]` 变成可点的引用。
  与 `EvidenceAside` 的联动靠它给出的 evidence_id，
  而不是靠下标——下标会因为过滤而错位。

### 7.4 涉及文件

```
backend/app/api/routes_reports.py   backend/app/core/report/{export,refine,citation_index}.py
backend/app/db/repo/reports.py
frontend/src/pages/ReportPage.tsx   frontend/src/components/report/*.tsx
frontend/src/lib/{reportCitation,reportMatrix,chartOptions,format}.ts
frontend/src/types/report.ts
```

### 7.5 验证方法

| 检查 | 期望 |
|---|---|
| 报告页端到端渲染真报告 | 所有块都渲染出来 |
| `lib/reportCitation.test.ts` / `chartOptions.test.ts` / `reportMatrix.test.ts` | 绿 |
| 导出 | 得到可读的 Markdown，中文不乱码 |
| 批注一节 → 「按批注深化」 | 该节被重写，`report_feedback` 多一条记录 |
| Playwright 冒烟 | 真浏览器里打开报告页并截图 |

最后一条是**唯一**能证明图真的画出来了的测试：
jsdom 没有 canvas，所以报告页那些 ECharts 在单测层面结构上不可见。

---

## 阶段 8：回放 / 图谱 / 仪表盘 / 文库 / 知识库（4–5d）

### 8.1 数据库设计

迁移 **v3**：知识库筛选面的覆盖索引，
让 `COUNT(DISTINCT evidence_id)` 不必回表。

迁移 **v4**：`report_team` 关联表 + 回填。
原先"某位专家参与过几次"是**读全部报告正文、解析 JSON 数出来的**，
一份报告 172 KB，48 个人就是读几十 MB。
加一张关联表之后，那是一个 `COUNT(*)`。

回填是纯 SQL，踩了一个坑（问题记录 45）：
`json_each` 与单参数的 `json_type` **会把参数当 JSON 文档解析**，
遇到普通字符串直接抛 `malformed JSON`。
而队伍里到处都是普通字符串（`"team": {"lead": "L3-001"}`）。
更麻烦的是**迁移是启动时跑的**——一份形状不对的老报告能让整个应用起不来。

> **两张表在这一阶段建了但没有写入方**：`subscriptions` 与 `expert_stats`。
> 订阅功能（`GET/POST/DELETE /api/subscriptions`）**没有实现**，
> `/api/experts` 的 `stats` 字段因此一直是名册里的初值。
> 这不是"暂时看不出来"——`expert_stats` 里那些 0 是"还没被量过"，
> 不是"参与次数是 0"。完整的缺口表在
> [`docs/DATA_MODEL.md`](docs/DATA_MODEL.md) 的「已知缺口」一节。

### 8.2 后端 API

| 路由 | 内容 |
|---|---|
| `GET /api/evidences` | 知识库列表，**一行 = 一个去重后的来源** |
| `GET /api/evidences/facets` | 来源类型分面 + 品牌分面 |
| `GET /api/dashboard` | `coverage` / `runs` / `byMode` / `correction` + `recent` |

三处口径决定：

- `GET /api/evidences` 的一行**不是一条证据记录**，是一个去重后的来源。
  同一篇文章被 9 次调研引用过就是一行，`taskCount: 9`。
- `facets` 同时给 `total`（去重后的来源数）与 `mentions`（**行数**）。
  两个口径都要有，页头写的是"`total` 条来源（去重后）· 累计被引用 `mentions` 次"。
  分面与列表**必须同一个口径**，否则会出现"选项里写着 24 条、点进去只有 3 条"——
  那种不一致看起来像 bug，实际是两处各算各的。
- 仪表盘的 `crossValidationRate` 是**加权比率**（`Σ 交叉验证过的论点 ÷ Σ 论点`），
  不是"每份报告的交叉验证率的平均"。后者会让只写了一句话的报告
  和写了 30 条论点的报告权重相同。

**没有** `GET /api/evidences/{id}` 详情路由，因为 `evidences` 表没有
全局唯一的 id——主键是 `(task_id, evidence_id)`，同一个 `evidence_id`
可以在多个任务里各有一行，且各行的维度、品牌、交叉印证分都不同。
**"这条证据"单独拿出来是一个不完整的概念。**

### 8.3 前端

| 页面 | 内容 |
|---|---|
| `GraphPage` | d3-force 力导向图：品牌 ↔ 维度 ↔ 证据 ↔ 专家 |
| `TracePage` | span 树 |
| `DashboardPage` | 四组聚合数字 + 最近 10 份 |
| `LibraryPage` | 报告文库 |
| `KnowledgePage` | 知识库，带可信度分面 |
| `ExpertsPage` / `ExpertDetailPage` | 名册与详情 |

`lib/graph.ts` 里有一条性质值得单说：**同一份正文两次构图、
两次布局，结果逐字节相同**。之所以能做到，是因为 `d3-force`
用自带的 LCG（`d3-force/src/lcg.js`）而不是 `Math.random`。
没有这条性质，"图谱每次刷新都在动"就无法被测试断言。

仪表盘的每个数字在界面上都带一个 `hint`（公式说明）。
**一个没有出处的数字不如不显示。**

### 8.4 涉及文件

```
backend/app/db/migrations.py（v3/v4）   backend/app/db/repo/evidences.py
backend/app/api/routes_{evidences,dashboard}.py
frontend/src/pages/{GraphPage,TracePage,DashboardPage,LibraryPage,KnowledgePage,ExpertsPage,ExpertDetailPage}.tsx
frontend/src/lib/{graph,replay,period}.ts
frontend/src/components/trace/TracePanel.tsx
```

### 8.5 验证方法

| 检查 | 期望 |
|---|---|
| 拖动决策回放滑杆 | 被引证据面板跟随 |
| `lib/graph.test.ts` | 两次构图逐字节相同 |
| 仪表盘数字与 DB 对得上 | 手写 SQL 核对 |
| `lib/replay.test.ts` / `period.test.ts` | 绿 |

第二条是这一阶段最有价值的测试：它把"图谱是确定性的"
从一句设计意图变成一条能被证伪的断言。

---

## 阶段 9：评测体系 + CI 门禁（3d）

### 9.1 数据库设计

**本阶段无新表**，但有一个重要决定：
评测**不写真库**。22 次流水线每次都会落库，落进 `backend/data/xm3.db`
的话，一次评测就往"我的调研"里灌 22 份没人看过的报告——
而那个库本来就已经被测试污染过（见 [`修补文档.md`](修补文档.md) D4b）。

所以在**任何 `Settings` 被构造之前**把 `DB_PATH` 指到一个临时文件，
跑完删掉。顺带一个好处：临时库每次是空的，
所以**评测结果不依赖"库里原来有什么"**，换个机器跑出来的数一样。

### 9.2 后端 API

**本阶段无。** 评测是 CLI。

### 9.3 前端

**本阶段无。**

### 9.4 做了什么

四种取数方式，各自回答不同的问题：

| 方式 | 跑什么 | 回答的问题 | 花钱 |
|---|---|---|---|
| `--gate` | 黄金集 × mock | 结构性行为有没有退步 | 0 |
| `--replay` | 有录制物的 × cassette | 真实适配器那条路径还能不能走通 | 0 |
| `--live` | 黄金集 × 真实 provider | 现在的质量到底是多少 | 有 |
| `--from-db` | 库里已有的报告 | 昨天那批报告长什么样 | 0 |

**指标一个都不重算**：`compute_metrics` 已经算过一遍，
结果跟着报告落库在 `body["metrics"]`。评测层再算一遍就会有
**两份"维度覆盖率"的定义**，然后它们在某个边界上分叉——
这个坑在本项目里已经踩过两次（问题记录 37）。

**读不到就报缺，不当成 0**：`MissingMetrics` 是一个单独的异常类型，
因为"没测到"和"测出来是 0"必须能被区分开。
聚合同理——`_pooled()` 在分母为 0 时返回 `None` 而不是 0。

**闸门是契约，不是快照**。阈值分两类：

- **机制背书的**（`hallucinationRate` / `phantomCitations` /
  `unsupportedClaimRate` 全为 0）——这些数**在结构上**就该是 0，
  一旦非 0 说明那层机制坏了，不是模型今天状态不好。
- **质量判断的**（`dimensionCoverage ≥ 0.6` 等）——取值在
  "实测最低值"与"明显坏了"之间，**不贴着实测最低值写**。
  贴着写会让闸门在正常波动下变红，**而它变红一次就会被调松一次**。

CI 拆成五个 job（`backend` / `frontend` / `eval` / `eval-replay` / `docs`），
因为它们**失败的含义不同**，混成一个 job 之后一个红叉说不清是哪一类问题。

### 9.5 涉及文件

```
eval/{run_eval,metrics}.py   eval/{golden,thresholds}.yaml
eval/judges/{faithfulness,rubric}.py   eval/baseline.json   eval/reports/
.github/workflows/ci.yml
backend/tests/unit/test_docs_order.py
```

### 9.6 验证方法

| 检查 | 期望 |
|---|---|
| `python -m eval.run_eval --gate` | 确定性且为绿，退出码 0 |
| 破坏一个阈值 | 退出码 1，并打印那条 `why` |
| `--live --repeat 3 --judge` | 产出写进 [`项目总结.md`](项目总结.md) 的数字 |
| CI 五个 job | **配置已就绪，尚未执行过**——仓库还没有 `git init`，所以没有任何一次运行可引用 |

最后一条原先写的是"全绿"，**那是从"配置写完了"推出来的，不是跑出来的**。
真实状态是：配置文件齐了，而它一次都没有被触发过。
本机跑 CI 里那条 ruff 命令的结果曾经是 5 条错误（即 CI 会停在第一步），
现已修好并加了一条本机守卫（见 [`问题记录.md`](问题记录.md) 的问题 50）。
**在有人真的 `git init` + push 之前，这里只能写"未执行"。**

"不联网"的保证有三层，这三层是配置层面的、可读的：
`pytest` 默认排除 `-m live`；cassette 缺录制**直接抛异常**，绝不静默联网；
`eval --gate` 用 mock、`eval --replay` 走真实适配器 + 回放。

少了这三层，"CI 跑绿"就变成一句**需要花钱才能相信的话**。

---

## 阶段 10：压测、文档、打包（3d）

### 10.1 数据库设计

**本阶段无新表。** 但压测**用一个独立的库**（`data/loadtest.db`）：
压测会灌进大量任务与事件，混进 `xm3.db` 会把"我的调研"淹掉——
和评测那条同一个理由。

### 10.2 后端 API

**本阶段无新路由。**

### 10.3 前端

**本阶段无新页面。** 产出是 `frontend/e2e/` 的 Playwright 截图，
它同时被 CI 作为 artifact 上传。

### 10.4 压测

`loadtest/locustfile.py`，**四个场景**：

| 场景 | 干什么 |
|---|---|
| `read` | 报告列表 / 详情 |
| `stream` | 挂 SSE 消费全部事件 |
| `create` | 建任务 + 澄清 + 消费 |
| `mixed` | 默认，权重 Read 5 / Stream 3 / Create 1 |

**真正测的是 SSE 扇出与并发订阅下的 SQLite 写路径**，
所以自带 `FORCE_MOCK_PROVIDER=1`——**确保被测瓶颈不是 LLM**。
关注 p95 首事件时延、events/sec、以及是否出现 `database is locked`
（在 WAL + 线程局部连接下不应出现）。

场景选择用环境变量 `LOADTEST_SCENARIO=read|stream|create|mixed`。

> 这里踩过一次（问题记录 43）：第一版用 Locust 的 `--tags`，
> 而用户类上**没有** `@tag` 装饰器，于是 20 秒里刷了 334 行
> `No tasks defined on ReadUser`——**而它读起来像"压测跑完了，没有任务"**。
> 一个"跑完了"的假象比一个报错更危险。

### 10.5 启动脚本

四份，因为 Windows 上的 Bash 与 PowerShell 各有各的坑：

| 文件 | 环境 |
|---|---|
| `scripts/dev.sh` / `stop.sh` | Git Bash |
| `scripts/dev.ps1` / `stop.ps1` | PowerShell |

`dev.sh` 的头注释记着三个 Windows 陷阱：

1. **`python3` 是坏的**（指向 Microsoft Store 的占位程序），
   所以按候选列表探测解释器而不是直接写 `python3`；
2. **venv 路径随平台不同**（`Scripts/python.exe` vs `bin/python`）；
3. **绝不用 `--reload`**（理由见阶段 3 的 uvicorn 一节）。

端口守卫用 `curl` 探测而不是查进程表（**端口可能被不叫 uvicorn 的东西占着**），
并且**失败而不是换端口**——与前端 `strictPort` 同一个理由。

`stop.sh` 的顺序是：按 PID 文件杀 → **按端口复核** → 端口兜底。
PID 复核对不对是因为**PID 会被复用**：一个过期的 PID 文件里的数字
可能已经被分配给了无关进程，而**一个裸 PID 无法证明归属**。
最终验收是端口，不是 `kill` 的退出码。

`.dev/`（PID 与日志）进 `.gitignore`，因为它是**本机状态**：
PID 换一台机器就没有意义，日志每次都变。入库只会制造冲突。
跑完**不删**这个目录，留着给事后看。

### 10.6 文档

| 文件 | 内容 |
|---|---|
| [`README.md`](README.md) | 陌生人照它跑得起来 |
| [`CLAUDE.md`](CLAUDE.md) | 给 AI 助手看的项目说明 |
| [`开发文档.md`](开发文档.md) | 本文件 |
| [`技术栈.md`](技术栈.md) | 选型、代价、负面清单 |
| [`项目总结.md`](项目总结.md) | 技术统计表（面试武器） |
| [`面试问答集.md`](面试问答集.md) | 预写答案 |
| [`问题记录.md`](问题记录.md) | 55 条真实踩坑 |
| [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) | 架构与流水线，顺序由测试守护 |
| [`docs/AGENTS.md`](docs/AGENTS.md) | 48 专家、消息协议、四条铁律 |
| [`docs/PROVIDERS.md`](docs/PROVIDERS.md) | 方言对照表 + 能力协商 |
| [`docs/API.md`](docs/API.md) | 21 条路由的字段与口径 |
| [`docs/DATA_MODEL.md`](docs/DATA_MODEL.md) | 9 张表 + 迁移史 + 已知缺口 |
| [`docs/EVAL.md`](docs/EVAL.md) | 评测体系与它做不到的事 |

**文档链接有测试守着**（`backend/tests/unit/test_docs_links.py`）。
这件事本身值得说：这份仓库的 README 曾经同时链向五个**还不存在**的文件，
目录表读起来完整、专业，点进去全是 404。

这类缺陷之所以难被发现，是因为它**不报错**：
不是语法错误（编辑器不标红）、不是代码（`pytest` / `ruff` / `tsc`
谁也不看 markdown）、不在运行时路径上（没有任何请求会 500）。
唯一的发现方式是"有人真的去点了那个链接"——
而写文档的人刚刚写完，**他最不可能去点**。

测试扫 15 份入口文档里的相对链接，断言目标存在；
另有三条守着它自己不空转（链接数量下限、坏链识别自证、
根目录文档集与计划一致）。

**同一类缺陷还有一个变种：文档里的测试条数。**
链接错了点进去是 404，条数错了**读起来完全正常**——
`921` 与 `924` 在形态上没有区别，**精确感与正确性无关**。
这个数在四份文档之间漂过九轮（917 → 921 → 924 → 925 → 926 → 939 → 941 → 943 → 945），
所以现在由 [`backend/tests/unit/test_docs_counts.py`](backend/tests/unit/test_docs_counts.py)
逐句对着 `pytest --collect-only` 断言。
它**故意不覆盖 [`问题记录.md`](问题记录.md)**：那篇里的 `917` 是历史数，
当时就是对的，全文搜索的守卫会把正确的历史判成错误。
**它的边界也写在文件里**——抓得到"改了 README 忘了改技术栈"，
抓不到"改了代码、四份文档全忘了改"。详见问题 48 的补记 §48.7。

**后来又加了同一形状的第二组：`问题记录.md` 有多少条。**
它比测试条数更频繁地漂——**每写完一条新问题就要动六处**
（README / 修补文档 / 开发文档两处 / 项目总结两处），
而写问题 52 的时候六处全忘了改。现在这六句也逐句断言，
并且顺带断言问题编号是**从 1 连续**的：跳号时"最大的那个号"
就不等于条数，而这个守卫会照着两个不同的量比对，还显得很有道理。

### 10.7 涉及文件

```
loadtest/locustfile.py   loadtest/reports/*.html
scripts/{dev.sh,dev.ps1,stop.sh,stop.ps1}
.github/workflows/ci.yml   backend/tests/unit/test_docs_links.py
README.md  CLAUDE.md  开发文档.md  技术栈.md  项目总结.md  面试问答集.md  问题记录.md
docs/*.md
```

### 10.8 验证方法

| 检查 | 期望 |
|---|---|
| `locust -f loadtest/locustfile.py --headless -u 20 -r 4 -t 2m` | 报告存 `loadtest/reports/`，**无 `database is locked`** |
| `bash scripts/dev.sh` | 后端 8020、前端 3500 起来 |
| 端口被占时 | **退出码 1 并说明**，不换端口 |
| `bash scripts/stop.sh` | 按端口复核，最终验收是端口而不是退出码 |
| `git status` | **`backend/.env` 不在待提交列表里** |
| 照 README 从零 clone + 配置 + 启动 | 陌生人能跑通 |

---

## 阶段依赖关系

```
0 骨架与契约
└─ 1 Provider 层（mock 驱动）
   └─ 2 数据层 + 流水线主干  ← 核心闭环里程碑（零 API 花费）
      ├─ 3 接真实 provider
      │  └─ 4 48 专家 + 动态组队
      │     └─ 5 质检返工闭环 + 可观测
      │        └─ 6 SSE + 工作台  ← 浏览器里能跑完整一次
      │           └─ 7 报告页
      │              └─ 8 回放 / 图谱 / 仪表盘 / 文库 / 知识库
      │                 └─ 9 评测体系 + CI 门禁
      │                    └─ 10 压测、文档、打包
```

**顺序有两处是刻意的：**

1. **Provider 层在流水线之前。** 适配层如果是后加的，
   就会变成"在已有的流水线上抽出接口"——而那意味着所有厂商方言
   已经漏进编排层了，抽的时候要靠读代码找回来。
2. **质检在写作之前。** 沿用上游 Verda **代码**的实际顺序
   （其文档写反了）。理由：让返工发生在最贵的步骤
   （12 章并行撰写）之前。

---

## 一句话总结

十个阶段里，**真正承重的只有两个**：

- **阶段 2** 让核心闭环以"一个能打印完整事件流的命令行"的形式跑通，
  从此每个阶段都在一条已经跑通的路径上加深；
- **阶段 6** 把同一股事件流换个出口交给浏览器，
  于是"能看到它在做什么"从一件事后补的界面变成了产品的一部分。

其余八个阶段都在做同一件事：**把"应该发生的事"变成"可以被证伪的断言"**。
