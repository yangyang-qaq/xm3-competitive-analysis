# 技术栈

> 一个竞品分析系统选了哪些技术，以及**每一条为什么选它、代价是什么**。
>
> 这份文档里没有"因为流行"这个理由。凡是写不出代价的选型，都是还没想清楚。

---

## 一、选型原则

三条，按优先级排：

1. **能用标准库解决的，不引入依赖。**
   这条最容易被当成教条，所以给一个具体的边界：
   **只有当依赖能省掉"一个我自己写会写错的东西"时才引入**。
   `trafilatura` 值得引入（正文抽取有一百种边界情况）；
   `left-pad` 不值得。中间地带的判据是：*我能不能说出它在什么情况下会出错？*
   说不出来，说明它做的事情我已经理解了，那就自己写。

2. **每一个依赖都要有一个提问能指向它。**
   面试里被问"为什么用 X"时，答案不能是"大家都用"。
   所以 `backend/pyproject.toml` 里每一行都有注释说明它是干什么用的，
   而且**每一条都真的被 import 过**——
   上游项目 Verda 的 `requirements.txt` 里 `langgraph` / `langchain-core` /
   `matplotlib` / `networkx` 四个依赖在代码中零引用，这份文档不想重蹈覆辙。

3. **选型要能解释代价。** 一项技术的好处大家都知道，坏处才见功力。
   下面每一项都带一个「代价」小节，第五节还有一份**负面清单**。

### 依赖表有多小

| 位置 | 运行时依赖 | 开发依赖 |
|---|---|---|
| 后端 | **11 个** | 6 个 |
| 前端 | **8 个** | 15 个 |

后端 11 个运行时依赖跑完整个系统：LLM 调用、搜索、正文抽取、
48 专家名册、SQLite 持久化、SSE、评测。

---

## 二、逐层总览

```
┌─────────────────────────────────────────────────────────────┐
│  前端  React 19 · Vite 8 · TypeScript 6 · Tailwind 4          │
│        Zustand 5 · React Router 7 · ECharts 6 · d3-force 3    │
├─────────────────────────────────────────────────────────────┤
│  HTTP  FastAPI · SSE（原生 EventSource + Last-Event-ID）      │
├─────────────────────────────────────────────────────────────┤
│  后端  FastAPI · uvicorn · pydantic 2 · httpx                 │
│                                                              │
│  Provider 适配层  ← 流水线里不出现任何厂商名                   │
│  SQLite（stdlib sqlite3，无 ORM、无 Alembic）                 │
├─────────────────────────────────────────────────────────────┤
│  质量  pytest 945 · vitest 314 · Playwright 1 · Locust        │
│        eval 22 条黄金集 + 2 个 LLM 裁判                        │
└─────────────────────────────────────────────────────────────┘
```

**这张图不写行数**：它的职责是画清层与依赖方向，行数是另一样东西。
规模数字只在 [`项目总结.md`](项目总结.md) 的技术统计表里出现一次——
理由见 [`问题记录.md`](问题记录.md) 的问题 48。

---

## 三、后端

### Python 3.11+（实测 3.12.10）

**为什么**：`contextvars`（无侵入埋点的基础）、`asyncio.to_thread`、
类型标注与 `Literal` 都是这条线的语言特性。项目不靠新语法，
但 `requires-python = ">=3.11"` 是真的下限，不是随手写的。

**代价**：3.11 之前 `asyncio.to_thread` 没有，改造要动 30 多处。
另外**本机 `python3` 是坏的**（Windows 上指向 Microsoft Store 的占位程序），
所有脚本必须显式找解释器——`scripts/dev.sh` 为此写了一个
按候选列表探测的函数，而不是直接写 `python3`。

### FastAPI

**为什么**：
1. **SSE 是最主要的理由。** 这个系统的核心界面是一条事件流，
   FastAPI 的 `StreamingResponse` 直接吃一个 async 生成器，
   而断线续传只需要自己写 `id:` / `event:` / `data:` 三行。
   换成 Django 要在同步视图和 ASGI 之间绕一圈；
   换成 Flask 则要额外处理异步上下文。
2. **pydantic 模型即文档。** 21 条路由的入参出参都是 `BaseModel`，
   校验、序列化、OpenAPI 一次写完。422 的报错形状是免费的。
3. **依赖注入够用。** `Depends()` 用来取配置与连接，不需要框架级的 DI。

**代价**：
- FastAPI 只做 HTTP，**不做任何编排**。流水线、任务注册表、
  事件日志都得自己写——这是刻意的（见下），但确实比"用 Celery"多写代码。
- 版本升级偶尔会因为 Starlette 的改动而需要动中间件。

### uvicorn（`[standard]`）

**为什么**：ASGI 服务器，FastAPI 的默认搭配。`[standard]` 带上
`httptools`（更快的 HTTP 解析）、`watchfiles`（热重载）和 `uvloop`。
uvloop 带平台标记，**Windows 上根本不装**（实测 `import uvloop` 直接
`ImportError`）——所以这个项目在本机跑的一直是默认事件循环，
性能收益没拿到。开发机是 Windows，这条代价是实打实的。

**代价，也是本项目最值得写进笔记的一条**：
**`--reload` 与 SQLite 放在被监视目录里会互相打架。**

每次写库 → 文件变化 → 重载 → 进程重启 → 任务上下文全丢。
表现不是报错，而是**流水线永远跑不完**，看起来像"卡住了"。
真要热重载必须 `--reload-dir app`，把这个目录排除在数据目录之外。
`scripts/dev.sh` 里 `WATCH=1` 走的就是这条路，默认不开。

### pydantic 2 + pydantic-settings

**为什么**：`pydantic` 用于所有 API 边界与报告正文的强制转换
（`core/schemas/` 有一层容错 coercer，LLM 吐出的畸形 JSON 靠它兜住）；
`pydantic-settings` 读 `.env`，一条 `Settings` 类管所有配置。

`pydantic-settings` 相对 `os.environ.get` 的**唯一实质好处**是：
配置项有类型、有默认值、缺失时报错位置明确。
一个拼错的 `os.environ.get("DB_PAHT")` 会静默拿到 `None`。

**代价**：LLM 的输出经常不是合法 JSON（多了个逗号、少了个括号、
被截断了），而 `pydantic` 的报错对这种情况不够友好。
所以 `core/schemas/base.py` 里写了一套**容错原语**：
先修再校验，修不好才降级。**这层是 pydantic 带来的必要成本。**

### httpx

**为什么**：`requests` 不支持 async，而这个系统的采集阶段是
"6 个维度 × 若干 query 并发打搜索接口"。用 `httpx.AsyncClient`
复用连接池。

**代价**：一处踩过的坑——上游 Verda 每条 query 新建一个 `httpx.Client`，
6 品牌 × 6 角度 × 4 平台 ≈ **150 个连接池**。xm3 全程复用单 client。

### openai（**只作为协议客户端**）

**为什么**：DeepSeek / 智谱 / Moonshot 等一大批服务都提供
**OpenAI 兼容的 HTTP 协议**。用官方 SDK 当客户端，省掉自己拼
`/v1/chat/completions` 的请求体、SSE 解析、重试。

`pyproject.toml` 里这一行专门写了注释：
**`# 仅作为 OpenAI 兼容协议的客户端，不代表只支持 OpenAI`**。
不写的话，读依赖表的人会以为这个项目绑定了某一家。

**代价**：SDK 的报错类型是自己的（`openai.AuthenticationError` 等），
所以 `providers/llm/openai_compat.py` 里有一层映射，
把它们翻译成项目自己的 `ProviderError` 子类。
**这一层不能省**——不翻译的话，"是不是限流"就会变成
对 SDK 异常类名的依赖，换一家实现就崩。

### trafilatura + beautifulsoup4 + lxml

**为什么**：采集到的页面要抽正文。`trafilatura` 在中文新闻/博客上
效果明显好于自己写的正则或 `readability` 的通用实现；
`beautifulsoup4` 作为兜底（`trafilatura` 返回空时退到
"去掉 script/style 之后的可见文本"）；`lxml` 是前两者的解析后端。

**代价**：**三个依赖做一件事**，这是后端依赖表里最重的一处。
理由是正文抽取的失败率直接决定 `degradedRate` 这个指标，
而"只拿到摘要"的证据在报告里是要被标记的。三条路都失败时才标降级——
换句话说，多两个包换来的是**这个指标更少被误报**。
三个包约 20 MB，值。

### PyYAML

**为什么**：48 位专家的名册 `experts_seed.yaml` 是手写的语义内核
（每人约 8 行）。YAML 支持注释和多行字符串，
而 JSON 不支持——名册里每条 `skills` / `knowledge_base` 都需要
一句"为什么这个人是这个专业"的说明。

**代价**：`yaml.safe_load` 是必须的（默认的 `load` 可以构造任意对象）。
另外 YAML 的缩进敏感会带来"多一个空格就换了层级"的错，
所以名册有 46 条测试钉住结构（恰好 48 人、层级分布、id 格式、生成幂等）。

### SQLite（标准库 `sqlite3`）

**为什么**：
1. **单文件、零部署。** 这个项目的目标是"陌生人 clone 下来能跑"，
   引入 Postgres 就得附带一句"先起一个数据库"。
2. **读者只有一个进程。** 写入方是流水线（通过 `asyncio.to_thread` 扇出），
   读出方是 API。没有多机、没有主从。

配置是三项：**WAL**（读写不互相阻塞）、**`busy_timeout`**
（写冲突时等而不是立刻抛 `database is locked`）、**线程局部连接**
（`sqlite3` 的连接对象不能跨线程共享）。

**代价**：

| 代价 | 说明 |
|---|---|
| 写并发是全局串行的 | WAL 下写仍然只能有一个。本项目写路径不多（任务状态、事件、报告），压测实测无 `database is locked` |
| 没有 `JSONB` 索引 | 报告正文整份存一列，按字段筛选只能靠额外列（见下） |
| 迁移要自己写 | 没有 Alembic，用 `PRAGMA user_version` + 有序迁移列表 |
| 全文检索弱 | FTS5 可用但没接，`?q=` 现在是 `LIKE` 模糊匹配 |

### 不用 ORM，不用 Alembic

**为什么**：
- **不用 ORM**：9 张表、查询都是"按 task_id 取"或"分页列表"，
  没有复杂关系。ORM 会带来一层"这句 SQL 到底发了什么"的翻译成本，
  而报告正文整份存 JSON 的读法也不适合映射成对象。
  更重要的是：**面试里能讲清楚的 SQL 比讲清楚的 ORM 抽象更有价值。**
- **不用 Alembic**：它解决的问题是"多人协作下迁移文件的分支与合并"。
  单人项目里 `PRAGMA user_version` + 一个有序列表
  （`db/migrations.py`）就够了，而且**能一眼读完**。
  迁移历史 v1–v4 全部记在 [`docs/DATA_MODEL.md`](docs/DATA_MODEL.md)。

**代价**：没有自动生成的迁移，改表要手写 SQL 并自己保证幂等。
v4 那次迁移就踩了坑——一段纯 SQL 的回填用了 `json_each` / `json_type`，
而这两个函数**会把参数当 JSON 文档解析**，遇到普通字符串直接抛
`malformed JSON`。而迁移是**启动时跑的**，
一份形状不对的老报告能让整个应用起不来。（详见
[`问题记录.md`](问题记录.md) 问题 45）

### contextvars

**为什么**：这是"全程可观测"这条铁律的实现基础。
阶段代码不需要手动传 tracer（那会让每个函数签名都多一个参数），
而是用 `contextvars.ContextVar` 持有当前 span，
`observability/trace.py` 的包装器自动捕获输入输出、耗时、成本。

**代价**：`contextvars` 在 `asyncio.to_thread` 里**不会自动传递**——
线程池的工作线程有自己的上下文。所以埋点包装器必须在进入线程前
显式 copy 上下文。这一处如果漏了，症状是"某些调用的 trace 不见了"，
而不是报错。

### pytest 945 条 / ruff / mypy（未启用）

**为什么 pytest**：`pytest-asyncio`（`asyncio_mode = "auto"`，
不用每个用例都写 `@pytest.mark.asyncio`）、`pytest-cov`、
以及**自定义 marker `live`**——联网且花钱的用例必须显式
`pytest -m live` 才跑，`addopts` 里默认 `-m 'not live'`。
这条加上 cassette 的默认模式，保证"跑测试 = 零成本"。

`pyproject.toml` 的 `pythonpath = [".", ".."]` 里那个 `".."`
（仓库根）不是可选项：它让 `import eval` 在测试里可用。
理由写在注释里——`eval/run_eval.py` 是**唯一**决定
"这套东西算不算合格"的地方，而它自己出过两次错
（一个永远为假的判断、一句印错的 provider），两次都是靠人肉看输出发现的。
**一个不被测试的裁判，绿得再好看也只是好看。**

**为什么 ruff**：一个工具替代 `flake8` + `isort` + `pyupgrade`，
配置 8 行。`line-length = 100` 但 `ignore = ["E501"]`——
**行长由格式化工具管，不该由 linter 报错**。

**mypy 的代价（这一条是缺口，不是优点）**：
`mypy>=1.13` 装在 `dev` extras 里，`[tool.mypy]` 也配了，
**但 CI 里从来没跑过**。它现在是一个"装了但没用"的依赖——
和 `Settings.llm_max_retries` 是同一类问题。
要么接进 CI，要么删掉。写在负面清单里。

### Locust

**为什么**：压测这个系统真正要测的不是 HTTP 吞吐，
而是**SSE 扇出与并发订阅下的 SQLite 写路径**。
Locust 的用户类是 Python，可以直接构造"建任务 → 澄清 → 消费整条流"
这种有状态场景，而 `ab` / `wrk` 这类只能打单个 URL。

**代价**：Locust 的 `--tags` 用法踩过一次——
写了 `--tags read` 而用户类上**没有** `@tag` 装饰器，
结果是 20 秒里刷了 334 行 `No tasks defined on ReadUser`，
而它**读起来像"压测跑完了，没有任务"**。
场景选择最后改用环境变量 `LOADTEST_SCENARIO=read|stream|create|mixed`
（默认 `mixed`），权重 Read 5 / Stream 3 / Create 1。

---

## 四、前端

### React 19.2

**为什么**：这个界面本质是**一条不断增长的流 + 若干派生视图**。
React 的"状态 → 视图"模型让"证据流新增一条，图谱和指标面板跟着变"
不需要手写同步逻辑。19 的具体好处是 `useTransition` 可以把
"事件洪峰时的重渲染"标成可中断的。

**代价**：**SSE 洪峰下的重渲染是真实的性能问题。**
上游 Verda 每个事件都 `set({thoughts: [...s.thoughts, d]})`——
一次 O(n) 拷贝 + 一次渲染，800 条思维 + 400 条证据流进来时肉眼可见地卡。
xm3 的解法有三层（`store/taskStore.ts`）：

1. `seq <= lastSeq` 直接丢弃（断线续传去重）
2. 推进**模块级可变缓冲**，用 `requestAnimationFrame` 合并成
   一帧一次 `set()`（`FLUSH_FALLBACK_MS = 250` 兜住后台标签页）
3. 环形上限：`THOUGHT_CAP = 800`、`SPAN_CAP = 1200`，
   证据走虚拟列表

### Vite 8

**为什么**：`npm run dev` 秒起、HMR 快，且**代理配置就是几行**
（`/api` 与 `/health` 转到 8020）——前端代码里不需要出现后端地址。
`strictPort: true` 是刻意的：**端口被占时直接失败，而不是悄悄换一个**，
否则代理会指向错误的后端而无人察觉。

**代价**：构建产物有一块 571 kB 的 chunk（ECharts）。
已经按路由做了动态 import（`ReportChartGrid` 单独成块），
但 ECharts 本身还没有按图表类型细分引入。见负面清单。

### TypeScript 6（`strict` + 更严的几项）

**为什么**：接口形状是这个项目最容易出错的地方
（21 条路由、11 种 SSE 事件、报告的十几个结构化块）。
打开的不只是 `strict`：

| 选项 | 挡住什么 |
|---|---|
| `noUncheckedIndexedAccess` | `arr[0]` 可能是 `undefined` |
| `noUnusedLocals` / `noUnusedParameters` | 死变量 |
| `erasableSyntaxOnly` | 会生成运行时代码的 TS 语法（enum / 参数属性） |
| `verbatimModuleSyntax` | 类型导入与值导入混用 |

`erasableSyntaxOnly` 值得单独说：它禁掉 `enum`，逼着用
`as const` 对象 + 联合类型——后者在运行时不产生代码，
也让 `types/events.ts` 里的**判别联合**成为可能。

**为什么用判别联合而不是 `as`**：上游 Verda 的事件类型转换
（`unknown` + `as`）会**静默丢掉新事件新增的字段**。
判别联合下，服务端加了一种事件而前端没跟上，`tsc` 会报错。

**代价**：
1. `tsconfig.app.json` 的 `types` 只开 `["vite/client"]`，**不开 `node`**。
   这是刻意的——它让"在浏览器代码里用了 `process`"变成编译错误。
   代价是测试里不能用 `node:fs`，读文件要走 `import.meta.glob`。
2. `tsc -b` 是构建的一部分（`build` = `tsc -b && vite build`），
   类型不过就构建不出来。这会让构建稍慢，但**类型在这个项目里
   是可信度的一部分**，不能只当编辑器提示。

### Tailwind 4（`@tailwindcss/vite`，CSS-first）

**为什么**：**没有 `tailwind.config.js`**。v4 的配置写在 CSS 里：

```css
@import 'tailwindcss';

@theme {
  --color-canvas: #f6f2ea;
  --color-panel: #fffdf9;
  /* ... */
}
```

好处是**设计令牌与使用它的样式在同一个文件里**，
不需要在 JS 配置和 CSS 之间来回跳。前端是"暖色浅色"的
案头研究台风格（米色纸面 + 白卡片 + 鼠尾草绿强调色），
令牌只有一层，改一处全站生效。

`src/index.css` 里每个颜色令牌都标了**它被当成什么用**，
因为从深色换到浅色时最容易坏的不是色值而是假设——
比如 `text-canvas` 原本的意思是"在强调色块上写字"，
浅色下如果拿它往白卡片上写字就会得到一片空白。

**代价**：v4 与 v3 的配置方式不兼容，网上大量 v3 的答案直接套会出错。
另外 `@theme` 里定义的令牌**不参与摇树**，全量进 CSS——
本项目 CSS 产物 35.7 kB（gzip 6.9 kB），可以接受。

### Zustand 5

**为什么**：状态管理只需要它做一件事——**把 SSE 事件流变成可渲染的状态**。
Zustand 的 store 是普通对象 + 订阅，没有 Provider 嵌套、
没有 reducer 的样板，而 `store/taskStore.ts` 里那些
"模块级缓冲 + rAF 合帧"的写法必须能直接操作 store 之外的可变状态，
这在 Redux 的模型里是别扭的。

**代价**：没有中间件生态，没有 devtools 的时间旅行（除非接插件）。
另外 Zustand 的 `set` 是浅比较，**嵌套对象更新要自己保证引用变化**——
这个项目里报告数据是整体替换的，所以没踩到。

### React Router 7

**为什么**：12 条路由分两组——侧边栏组（`/`、`/library`、`/knowledge`、
`/experts`、`/dashboard`）与**全屏沉浸组**（`/clarify/:taskId`、
`/workspace/:taskId`、`/report/:reportId`、`/graph/:reportId`、
`/trace/:reportId`）。后者要脱离侧边栏，用嵌套路由 + 布局路由表达很自然。

**代价**：v7 把 `react-router-dom` 与 `react-router` 合并了，
导入路径跟 v6 的教程不一样。目前只用了最基础的
`createBrowserRouter` + `Outlet`，没有用它的 loader/action——
数据获取是自己写的 `useAsync`（见下）。

### ECharts 6

**为什么**：报告页有功能矩阵、定价对比、雷达图、趋势图，
还有仪表盘上的聚合图。ECharts 的**配置对象模型**适合
"图表规格由后端算好、前端只负责渲染"这个分工——
`analysis/charts.py` 产出的 `chart spec` 是一个 JSON，
前端 `chartOptions.ts` 把它翻译成 option。

**代价**：
1. **体积最大的一块。** 已按需引入 + 路由级动态 import，
   但仍是 571 kB（gzip 193 kB）的独立 chunk。
2. **jsdom 里没有 canvas**，所以报告页的图在单元测试层面
   **结构上不可见**。这就是那条 Playwright 冒烟存在的原因——
   "图表整块没接线"这个缺陷真的发生过（`问题记录.md` 问题 41）。

### d3-force 3（**只用了这一部分**）

**为什么**：知识图谱要力导向布局。`d3-force` 是那个算法的参考实现，
而且**确定性**——它用自带的 LCG（`d3-force/src/lcg.js`）而不是
`Math.random`，所以"同一份正文两次构图、两次布局，
结果逐字节相同"这条测试才成立。

**代价，也是本阶段真的踩到的一个坑**：
一开始 `package.json` 里写的是 `d3`（元包），而代码里 import 的是
`d3-force`。**它一直能跑**，因为 `d3@7` 恰好依赖 `d3-force`，
npm 把它提升到了 `node_modules` 顶层。
`tsc` / `oxlint` / `vitest` / `vite build` / CI **五层全绿**——
没有一层会问"这个包你声明了吗"。
已改成显式声明 `d3-force` + `@types/d3-force` 并删掉 `d3`：
`node_modules` 里的 d3\* 包从 **31 个降到 4 个**。
详见 [`问题记录.md`](问题记录.md) 问题 46。

### oxlint（而不是 ESLint）

**为什么**：ESLint 9 的 flat config + 一堆 plugin + 类型感知规则，
配置本身就是一个项目。oxlint 是一个二进制，零配置能跑，
`oxlint src --deny-warnings` 一行。这个项目要 lint 挡住的是
"未使用的变量、可疑的条件、被 await 忘记的 promise"这类，
不需要类型感知规则（那是 `tsc` 的活）。

**代价**：规则少。没有 `eslint-plugin-react-hooks` 的
"依赖数组检查"——**这个是真的少了**，`useEffect` 依赖写错它不会报。
目前的补偿只是"自定义 hook 少"（`useAsync` 与 `useTaskStream` 两个），
**而且 `useAsync` 没有测试**。这是一个真实的缺口，不是"暂时看不出来"。

### vitest 5 + jsdom + Testing Library / Playwright

**为什么**：vitest 与 Vite 共用配置（`defineConfig` 来自
`vitest/config`，插件数组是同一个），`test.include` 写死成
`src/**/*.test.{ts,tsx}`——**刻意不用 vitest 的默认 glob**，
因为默认 glob 会把 `e2e/*.spec.ts` 收进来，
而那些文件 import 的是 `@playwright/test`，在 jsdom 里跑会报
"没配置 browser"这种与代码无关的错。
写死成 `src/` 而不是 `exclude: ['e2e/']`：将来多一个目录时，
默认行为是"不被收进来"，而不是"又混进一堆要排查的东西"。

Playwright 只有 **1 条用例**，它证明的是别处证明不了的那件事：
真浏览器里图真的画出来了。CI 会把截图作为 artifact 上传——
作品集里那张报告页截图就是从这儿出的。

**代价**：Playwright 要装 chromium（CI 上加 `--with-deps`），
本地首次 `npm ci` 之后要 `npx playwright install chromium`。
另外 vitest 每次新建一遍 jsdom 环境（实测 17 次、160 秒、占 72% 时间），
`pool: 'vmThreads'` 能改善，但会牺牲文件间隔离，暂时没换。

---

## 五、自主选型 vs 继承自 Verda

这份项目是"参照架构、从零手写"的，所以逐项分清哪些是沿用的、
哪些是改的，是有意义的。

| 层 | 沿用 Verda 的架构 | xm3 自主决定 |
|---|---|---|
| 流水线 | 三档模式、三层专家、`intake→…→done` 的阶段划分 | **质检在写作之前**（Verda 的代码是这顺序，文档写反了）；拆掉单次 9000 token 的巨调用 |
| Provider | "适配层隔离厂商方言"这个想法 | 归一化 Protocol 的具体形状（`Freshness` 五档、`Tier` 三档）、能力协商、类型化错误、`RetryPolicy` |
| SSE | 11 种事件类型、`id:` 帧 | **按任务的 seq**（Verda 是全局计数器）、`task_events` 表 + `Last-Event-ID` 补发 |
| 埋点 | `contextvars` 无侵入 | span 树与事件 journal 拆成两个东西 |
| 存储 | 线程局部连接 + WAL + `busy_timeout`、报告整份 JSON 存一列 | `PRAGMA user_version` 迁移、`report_team` 关联表、`credibility_breakdown` 列 |
| 前端 | React + Vite + TS + Tailwind + Zustand + ECharts | `store` 的三层流控（seq 去重 / rAF 合帧 / 环形上限）、判别联合事件类型 |
| 依赖 | —— | **最小依赖表**，四个零引用依赖直接不要 |
| 语言 | Python 3.11+ | 同为 3.11+，但运行时是 3.12 |

**没有沿用的东西**（都是有意的）：

| Verda 有 | xm3 为什么不要 |
|---|---|
| `api/` 目录下手工维护的第二份后端（Vercel） | 单人项目维护两份后端是纯负担 |
| 字符串匹配 `"429" in str(err)` 判限流 | 换成类型化的 `ProviderError` |
| 相关性过滤写在搜索适配器内部 | 移出到 `core/evidence/relevance.py`——**否则换搜索源时它会静默消失** |
| `enable_demo_fallback: bool = True` 死配置 | 删掉 |
| `_full_text` 挂在对象上但没声明（`asdict()` 会丢） | 显式字段 |

---

## 六、已知代价与负面清单

**这一节是这份文档里最该被读的部分。** 一份只讲好处的技术栈文档，
在面试里撑不过第二个追问。

### 已知代价

| 代价 | 说明 | 为什么不改 |
|---|---|---|
| 报告正文整份存一列 | 一份报告 `data` 有几 MB，按字段筛选只能靠额外列 | 读放大无所谓（一次访问一份），拆 12 张表换来 JOIN 风暴 |
| SQLite 写并发串行 | WAL 下写仍只能有一个 | 压测无 `database is locked`；换 Postgres 就要"先起一个数据库" |
| ECharts chunk 571 kB | 报告页首屏要多下一块 | 已按路由动态 import；再细要按图表类型引入，收益递减 |
| jsdom 里没有 canvas | 图表在单测层面不可见 | 用一条 Playwright 补，而不是给 jsdom 装 `canvas` |
| vitest 每文件新建 jsdom | 160 秒、72% 的时间 | 换 `pool: 'vmThreads'` 会牺牲隔离 |
| 前端没有 hooks 依赖数组检查 | oxlint 不做类型感知规则 | 只靠"hook 少"（2 个）兜着，`useAsync` 还没有测试 |
| 三个包做正文抽取 | trafilatura + bs4 + lxml ≈ 20 MB | `degradedRate` 直接由抽取成功率决定，值 |
| 没有 ORM / Alembic | 改表要手写幂等 SQL | v4 那次迁移因此踩了 `json_each` 的坑（应用起不来） |

### 负面清单（明确**没有**做到的事）
1. **CI 从来没有执行过。** `.github/workflows/ci.yml` 写好了五个 job，
   但仓库**还没有 `git init`**，所以它一次都没有被触发。
   更糟的是：本机跑 CI 里那条 ruff 命令是 **5 条错误**——
   也就是说它一旦真的跑起来，会停在 backend job 的第一步，
   后面那几百条测试一条都不会开始。（已修，并加了一条本机守卫。）
   **配置写得再完整，在一个没有执行入口的地方提供的保证是零。**
   详见 [`问题记录.md`](问题记录.md) 的问题 50。

按"读者可能以为做到了"的程度排序：

2. **`mypy` 装了但 CI 里从来没跑过。** `dev` extras 里有它、
   `[tool.mypy]` 也配了，而 `.github/workflows/ci.yml` 的后端 job
   只跑 `ruff` 与 `pytest`。它现在是一个纯装饰的依赖。
   **要么接进 CI，要么删掉**——留着比删掉更糟，
   因为它让人以为类型被检查过。

3. **`Settings.llm_max_retries` 没有被任何代码读取。**
   重试次数实际由 `providers/retry.py` 的 `RetryPolicy` 决定。
   改那个字段不会有任何效果。

4. **`zhipu` / `tavily` 的适配器写完了，但没有被真实调用过。**
   方言映射是照着公开文档写的，测试用 `httpx.MockTransport`
   断言请求体的形状，**不是**对真实接口的一次成功调用。

5. **没有 `serper` 适配器**，尽管文档里拿它当"另一种方言"的例子，
   而且开发机的 `.env` 里留着 `SERPER_API_KEY=` / `SERPER_BASE_URL=`
   两行空配置。**后者应该删掉——配置项是一种承诺。**

6. **流式调用不录制、不可回放。**
   `ReplayProvider.chat_stream()` 直接抛 `CassetteMiss`。
   目前 SSE 推的是流水线事件而不是 token 流，所以不影响任何测试——
   但哪天要做 token 级流式，这条路是堵的。

7. **能力协商只实现了 `site_filter` 一项。**
   `freshness_filter` / `long_snippet` / `max_results_per_call` /
   `json_mode` / `thinking_toggle` / 上下文上限都还没有对应的策略分支。
   它们在类型上有声明、在 `describe_all()` 里能查到，
   但流水线还不会根据它们改变行为。

8. **黄金集 22 条里录过 5 条，现在回放得出来 4 条。** 其余 18 条只在
   mock 下跑过——而 mock 的三个 provider 都不看 query，所以它们的
   "证据形态差异"实际上从未被真实模型吃过。
   **账和事实差的那一条是 `vague-coffee-market`**：
   `MANIFEST.json` 记着录过它，`--replay` 现在报"没录制"——
   录制品对不上 key 了（改过提示词就会这样，机制见问题 38）。
   值得注意的是**它是这 5 条里唯一走澄清路径的**，而那正是最可能
   因为一次提示词改动而整条作废的一步。**能指出是哪一条，
   但指不出是哪一次请求**——详见 [`问题记录.md`](问题记录.md) 的问题 51。

9. **判官指标没有基线可比。** `eval --live --repeat 3` 跑过一次
   （15 份报告 · $0.39025，值在 [`项目总结.md`](项目总结.md) 的判官指标一节），
   但那是**一次性的手工跑**：`eval/baseline.json` 的 `judges` 字段仍然是空的
   （它来自 `--replay`，而判官只配 `--live` 跑），且**没有任何东西会自动
   拿新结果去比它**。"引用忠实度比上次高了"这句话现在没有依据——
   **不是因为缺数，是因为只有一个数。**

10. **跨平台未验证。** 本地那 13 项**确定性指标**连跑多遍逐行相同，
   换到 ubuntu runner 会不会因并发搜索的完成顺序影响证据编号，
   没有验过。**说"逐项相同"时要限定住范围**：`durationMs` /
   `firstEvidenceMs` 每次都不同——它们量的是墙钟，本来就不该相同。

11. **`subscriptions` 与 `expert_stats` 两张表没有写入方。**
    表建了、读取路径写了（部分），但没有代码往里写。
    完整的缺口表在 [`docs/DATA_MODEL.md`](docs/DATA_MODEL.md) 的「已知缺口」一节。

---

## 七、一句话总结

技术栈不是一份清单，是**一组带着代价的决定**：

- 依赖表压到**后端 11 个、前端 8 个**，代价是正文抽取和迁移要自己盯；
- 用 **SQLite 而不是 Postgres**，换来的是"clone 下来就能跑"；
- 用 **provider 适配层**换掉了流水线里的厂商名，代价是多一层间接；
- 在前端做**三层流控**，因为 800 条思维流进来时"能跑"和"不卡"是两件事；
- 把每一处的**代价**都写下来，因为一份讲不出代价的选型，
  在第二个追问下就会露出它其实没被想过。
