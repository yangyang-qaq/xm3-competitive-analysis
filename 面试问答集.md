# 面试问答集

> 这份文档只有一个用途：**在被追问之前，先把答案想清楚。**
>
> 每个问题分四段：**一句话**（开口就先说这一句）、**展开**（对方点头再往下）、
> **追问预演**（"那如果……"）、**诚实边界**（这件事我没做到什么程度）。
> 第四段是最重要的——面试里真正扣分的不是"没做过"，
> 而是**说了做过、被追问三层之后露馅**。

数字全部来自 [`项目总结.md`](项目总结.md) 的技术统计表，与仓库里跑出来的东西一致。
凡是没有实测支撑的，写的是"未测"，不是估计。

---

## 一、五个必答题

### 1. 为什么要做 provider 适配层？不嫌多一层吗？

**一句话**：因为不做这层，"换一家搜索源"就不是改配置，而是改流水线——
而流水线是整个仓库最贵、测试最厚的部分。

**展开**

问题不是"改起来累"，是**厂商方言漏进了编排层**。参考实现里编排层直接读
`settings.zhipu_model_core`、直接传 `freshness="oneYear"`、直接拼 `include="douyin.com"`。
方言一旦漏上去，会同时发生四件事：

| 后果 | 具体表现 |
|---|---|
| 换 provider 要动最贵的地方 | 采集与分析是测试最厚的两个模块 |
| 每加一家就多一个分支 | `if provider == "bocha": ...` 会长在业务逻辑里 |
| 厂商错误码散落各处 | 429 要靠 `"429" in str(err)` 这类字符串匹配去认 |
| **业务逻辑悄悄住进适配器** | 相关性过滤写在搜索适配器里，换一家它就消失了 |

最后一条最危险，因为它**不报错**。所以
[`backend/app/core/evidence/relevance.py`](backend/app/core/evidence/relevance.py)
是独立模块，不在任何适配器里
（[`backend/app/providers/base.py`](backend/app/providers/base.py) 的
`SearchProvider.search` docstring 里写了这句话，就是为了防止有人后来"顺手优化"进去）。

解法是三个归一化 Protocol + 一份方言对照表。表格在
[`docs/PROVIDERS.md`](docs/PROVIDERS.md)，最能说明问题的是 `freshness="any"` 那一行：
博查要显式发 `noLimit`，Tavily 要**完全不发这个键**（发 `null` 会被拒）。
同一个语义，一边是"显式说不要限制"，另一边是"别说这个事"。

**追问预演**

> **"这不就是 ports & adapters 吗？"**
> 是。这个模式本身不新，值得说的只有两点：一是我能指出它在这个项目里
> 具体拦住了什么（上面那张表）；二是它带来一个我一开始没预料到的收益——
> **既然所有外部调用都经过同一层，就可以在这一层包一层录制/回放。**
> 这是本项目验证成本为零的原因，见 [`backend/app/providers/cassette.py`](backend/app/providers/cassette.py)。

> **"多一层不影响性能吗？"**
> 它是一次函数调用，包住的是几百毫秒到几秒的网络请求。
> 这个量级的差异不需要测——**我不打算用一个没测过的数字去回答性能问题**。

> **"能力不一致的 provider 怎么办？"**
> 能力协商。`capabilities.site_filter=False` 时，调用方显式退化成
> "把域名折进查询词"，并且**计数**——这个数一路走到报告指标里，
> 前端读到的键是 `siteFilterFolded`
> （[`backend/app/core/analysis/metrics.py`](backend/app/core/analysis/metrics.py)）。
> 降级是允许的，静默降级不是。

**诚实边界**

- 适配器里的 `zhipu` 与 `tavily` **写完了但没有被真实调用过**——
  验证过的是 mock / `httpx.MockTransport` 级别的方言翻译。
- 能力协商**只实现了 `site_filter` 一项**，
  `freshness_filter` / `long_snippet` 只声明了、没有消费方。
- 两条都在 [`技术栈.md`](技术栈.md) 的负面清单里。

---

### 2. 为什么用 contextvars 埋点，而不是传参或者装饰器？

**一句话**：因为埋点参数一旦靠传，**漏传一次不会报错，只会让成本表少一笔**——
而成本表少一笔比多一笔危险得多。

**展开**

流水线要扇出到线程（`asyncio.to_thread`）。如果 tracer 靠参数传递，
每一个函数签名都要多一个跟业务无关的参数，而**任何一次重构忘了往下传，
那次调用的成本就静默消失了**。

`contextvars` 的语义恰好对上：`asyncio.to_thread` **会复制当前上下文**，
所以在子线程里跑的 provider 调用自动看得见发起它的那个 tracer 与 span 栈，
父 span 也就自动接上了（[`backend/app/core/observability/trace.py`](backend/app/core/observability/trace.py)）。

**必须主动说的限制**：contextvars 是单向的。在子线程里 push 的 span
不会传回父协程的栈。这正是我要的（同一个 span 不该出现在两条时间线上），
但它意味着**跨线程的父子关系靠的是"复制时的那个快照"**——
子线程里再开新协程不受影响。

同一层里还有两个刻意的设计：

- **`seq` 按任务，不按全局。** 参考实现用一个全局 `_SEQ` 计数器，
  两个并发任务的事件序号会交错，而决策回放的滑杆恰恰按 seq 排序——
  交错之后"第 40 秒发生了什么"就不可回答了。
- **`_finish()` 里先落收集器、再通知订阅者。** 少了那一句 `_spans` 永远是空的，
  而这不是崩溃：trace 事件照常推送、工作台看得见每次调用，
  但 `snapshot()` 是空的、`traces` 表一条不写、报告里印着"token 0 / $0.0000"。
  **读起来像是"这个系统很省钱"，而不是"埋点没记上"。**

**追问预演**

> **"为什么不用 OpenTelemetry？"**
> 认真权衡过，没选。OTel 要引入 SDK + exporter + collector + 一个后端（Jaeger），
> 而我要回答的问题只是"这一次调用花了多少钱、是谁发起的、成功了没有"。
> 自建这一层能覆盖，而且它跟 `traces` 表、跟 SSE 的 trace 事件是同一份数据——
> OTel 的 span 模型和成本归因不是一回事，套上去还要写一层转换。
> **但这是个有条件的结论**：如果团队里已经有 OTel 后端，我会 export 过去而不是自建。

> **"为什么不用装饰器？"**
> 两个理由。装饰器只能包函数，包不住"这次调用在做什么决策"这类语义
> （`purpose` / `evidence_ids`）；而且它会把调用点藏起来——
> 读 `analyze.py` 的人应该看得见"这一步在被观测"，而不是靠猜。

> **"埋点会不会漏？"**
> 会，所以有两道防线：模块级 `span()` 在没有 tracer 时**返回一个游离 span**
> 而不是 `None`，调用点可以无条件写 `sp.detail["hits"] = 3`，
> 不必到处判断 `if sp is not None`（**埋点代码里的分支越多，漏埋的概率越高**）；
> LLM 用量走唯一的收口函数 `record_llm_usage()`，
> 不存在"某个分支忘了记 token"的位置。

**诚实边界**

- 这一层**没有采样**：每次调用都记。任务量上去之后 `traces` 表会长得很快，
  **而清理策略没写**——这一条之前没被记进任何一份文档，
  我在准备这份问答时才发现自己是靠"印象里有"在说它。
- span 是**进程内**的。多进程部署时父子关系会断，那需要 trace context 传播
  （W3C traceparent 或自己塞进消息头），现在没有。

---

### 3. 为什么质检排在写作之前？

**一句话**：因为返工的成本等于"要重做多少下游步骤"——
把质检往前挪一格，返工从"重写全部章节"变成"补采几条 + 重跑一次分析"。

**展开**

流水线顺序是
`intake → orchestrator → collect → analyze → audit →(rework) → write`
（[`backend/app/core/pipeline/stages.py`](backend/app/core/pipeline/stages.py)）。

写作是最贵的一步：章节要逐节生成，每一节都要带上证据上下文。
质检排在它之后的话，发现证据不足时返工的是**全部章节**；
排在之前，返工只是补采加重跑分析。

**这段值得讲的其实不是这个决定本身，而是它是怎么来的。**
参考项目的**文档**写的是"分析 → 撰写 → 质检"，**代码**里实际是"分析 → 质检 → 撰写"
——文档写反了。所以这个顺序不是我发明的，是我读它代码读出来的。

而"文档与代码不符"这件事本身是可修的：这里有一条测试解析
`docs/ARCHITECTURE.md` 的 mermaid 块，断言节点顺序等于 `PIPELINE_STAGES`
（[`backend/tests/unit/test_docs_order.py`](backend/tests/unit/test_docs_order.py)）。
**一条测试就永久消灭了这类脱节**，比"这次记得改一下文档"可靠。

**追问预演**

> **"质检在写作前，那文章本身的幻觉谁来查？"**
> 分两层，这正是顺序能成立的原因：
> `audit` 阶段管的是**证据够不够**（证据密度、维度覆盖），
> 不是**文章写得好不好**。写法层面的幻觉由另一条机制拦：
> 模型给出的每个 `evidence_id` 都由
> [`backend/app/core/evidence/citations.py`](backend/app/core/evidence/citations.py)
> 依据**实际存在的证据**校验——**不采信模型的自我陈述**，
> 编造的引用被丢弃并计数。这一条与阶段顺序无关，所以写作在后也不漏。

> **"返工之后要重跑分析吗？"**
> 两条路径，由 `Issue` 决定：补采证据（回 `collect`）或重做分析（回 `analyze`）。
> `decide_rework()`（[`backend/app/core/pipeline/audit.py`](backend/app/core/pipeline/audit.py)）
> 把问题清单翻译成**具体的搜索目标**，而不是一句"证据不足请补充"。

> **"返工次数用完了但问题还在怎么办？"**
> 留档不返工，并且**在报告里说清楚是哪一种**。
> `decide_rework` 返回的三条不返工理由都会被打印：
> 没问题 / 问题不够严重 / **轮次已用尽**。
> 第三种尤其要说——"到上限了但还是有问题"和"问题已经解决了"是完全不同的两件事。

**诚实边界**

- 标注为 `minor` 的 Issue **只记录、不触发返工**，这个门槛（`_REWORK_SEVERITIES`）
  是判断，不是测量。
- **返工的有效性只有一个实测样本**：单份报告的返工触发率与
  `metrics_after - metrics_before` 有记录，但**没有跨报告统计**。
  所以"返工确实提升了质量"这句话，我只有一次运行的依据。

---

### 4. 为什么把整份报告 JSON 存成一列？

**一句话**：因为这份数据的访问模式是"一次访问一份、整份一起读"，
拆成 12 张规范化表只会换来 JOIN 风暴。

**展开**

`reports` 表的形状（[`backend/app/db/migrations.py`](backend/app/db/migrations.py)）是：
**凡是要筛选、排序的，都提成了标量列**（`report_id` / `task_id` / `query` /
`mode` / `subject` / `brands` / `generated_at`），剩下的是"只有拿到报告才需要"的部分，
全在 `data` 里。列表页只查标量列，详情页才读 `data`。

**代价，说具体的三条**：

1. **不能在 SQL 层查报告内容。** "哪几份报告提到了 Notion"这类问题答不了。
2. **`data` 有几 MB。** 所以文库列表**刻意不返回正文**——
   这不是遗漏，是这个取舍的必然结果。
3. **改数据结构要走代码而不是 DDL。** 迁移时要写转换函数，SQL 帮不上忙。

**什么时候会改主意**：当出现"跨报告查询"这个真实需求时。
现在知识库页做的是**去重后的来源**，那是靠把可查询的部分单独建表
（`evidences`）实现的，而不是去查 `reports.data`——这条路径已经在了。

**追问预演**

> **"这不就是反模式吗？"**
> 这是这个答案的关键。反模式的成立条件是有**跨行的查询/统计需求**。
> 我这里所有统计（覆盖、成本、修正率）走的都是 `evidences` / `traces` /
> `tasks` 这些规范化表，`reports.data` 只被"读一份"用到。
> **如果我判断错了，代价是我不能 SQL 查报告内容**——
> 一个已知的、写进负面清单的代价，不是我不知道。

> **"那为什么不干脆存成文件？"**
> 三个理由：它与 `task_id` 的关联要在同一个事务里；
> 9 张表已经共用一个 SQLite，多一套文件系统要自己管路径、清理、并发；
> 存一列是"用数据库管一份不打算在里面查的文档"，是个合理的中间点。

> **"JSON 存一列，那 schema 演进怎么办？"**
> 迁移是手写的有序列表（v1–v4），每一步带一个转换函数，
> 用 `PRAGMA user_version` 记进度。不上 Alembic 的理由是它对这个规模太重，
> **代价是迁移要自己写对**——这一点写在 [`技术栈.md`](技术栈.md) 里。

**诚实边界**

- `data` 的大小随模式变化很大，quick 档与 expert 档不是一个量级。
  **没有做过压缩**，也没有做过"大报告的分页读取"。
- 9 张表里有 **2 张没有写入方**（`subscriptions` / `expert_stats`），
  相关的订阅接口从未实现。这写在 [`开发文档.md`](开发文档.md) 里，
  不是"忘了说"。

---

### 5. 规模放大 100 倍会怎么改？

**一句话**：先反问"是哪 100 倍"——用户数、报告数、还是并发任务数，
它们的瓶颈完全不是同一个地方。

**展开**

按**并发任务数 × 100** 来答（这是最难的假设），要改四处：

| 现在 | 100 倍下 | 为什么 |
|---|---|---|
| 进程内 `TaskRunner` + `ensure_runner` | 任务队列 + 租约（Postgres `SKIP LOCKED` / Redis） | 现在"同一个 task 只跑一次"靠进程内的注册表，多进程下失效 |
| SQLite（WAL） | Postgres | WAL 下并发读没问题，但**写是单写者**，会串行 |
| 进程内订阅者列表 | 跨进程广播（Redis pub/sub 或 PG `LISTEN/NOTIFY`） | SSE 扇出现在是内存里的一个集合 |
| 一刀切的模型档位 | 前缀缓存 + 分级模型 + quick 档做默认 | 成本随任务数线性涨，而这笔钱是真实的 |

**不会变的部分**（这才是最值得说的）：

- **provider 适配层不随规模变形**——它按 provider 数量增长，不按任务数增长。
- **cassette 不变**——它包在适配层上，与并发无关。
- **四条铁律的校验逻辑不变**——`enforce_citations` 是纯函数。
- **断线续传不用重做**——`task_events` 已经是持久化 journal 了，
  多进程下补发路径照走，只是"谁在写"从本进程变成队列里的 worker。

**追问预演**

> **"那你会先做哪一个？"**
> 先测，不改。给现在这套加指标——**events/sec、SQLite 写等待、首事件时延**
> ——然后在 100 倍负载下看哪个先塌。我猜是 SQLite 写，但
> **"我猜"不是依据**，而这三条路径压测里已经测过了
> （[`loadtest/locustfile.py`](loadtest/locustfile.py)），加负载就能重跑。

> **"为什么 SQLite 当初是合理的选择？"**
> 单机、单写者、读多写少、要免运维——这四条当时都成立。
> 现在压测 5,365 请求 0 失败、**全程没有 `database is locked`**，
> 说明在目标负载内它对得住。**但我知道它的失效点在哪**：
> 写并发一旦超过 WAL 单写者的吞吐就会开始排队，
> 那不是"偶尔慢一下"，是随负载陡增的。

> **"成本那一条，具体怎么降？"**
> 实测前缀缓存命中率只有 **0.8%**——因为几个章节是**并行**扇出写的，
> 每个章节都是自己那份前缀的第一个写者，谁也不等谁，于是谁也命中不了。
> 要提命中率就得**刻意安排调用顺序**（把公共前缀部分串行化），
> 那是在拿延迟换钱。这是个真实的取舍，不是"加个缓存"。

**诚实边界**

- 这一整题是**推理，不是实测**。我没有在 100 倍负载下跑过，
  也没有做过容量规划。能给的只是"哪一处会先断，以及为什么"。
- 跨平台也没验证过：本地那 13 项**确定性指标**连跑多遍逐行相同，
  **换到 ubuntu runner 会不会因为并发搜索的完成顺序影响证据编号，没有验过。**
  （"逐项相同"只覆盖确定性指标；`durationMs` 与 `firstEvidenceMs` 每次不同，
  它们量的是墙钟。）

---

## 二、关于这套系统本身的高频追问

### "幻觉抑制到底靠什么？"

**不靠让模型诚实，靠不给它谎报的机会。**

| 层 | 机制 | 为什么可信 |
|---|---|---|
| 引用 | 每个 `evidence_id` 由 `enforce_citations()` 依据**实际存在的证据**校验 | 不采信模型的自我陈述——它说引用了，得真有一条 |
| 置信度 | 高置信要求 ≥2 个**独立域名** | 用平台归属表算"独立"：同一篇文章被 5 家门户转载算 1 个信源 |
| 图表 | 每个 chart spec 与 grid 单元格都带 `evidence_ids` | 铁律一之下图表也是论点，不能例外 |

**"被丢弃的引用为什么不算错误？"**
批注深化返回的 `phantomCitations` 是**幻觉抑制在工作的计数**，
不是"这次出错了"。这两个含义混在一起的仪表盘会让人以为系统不稳定。

### "mock 闸门那么绿，能说明质量吗？"

**不能，而且我在文档里写明了它不能。**

22 条黄金查询在 mock 下**是同一种输入跑了 22 遍**——mock 的三个 provider
都不看 query。实测跑三条不同 query，指标**逐项相同**：
216 条证据、33 个信源、4 个维度、覆盖率 1.00。
（说得再准一点：相同的是这些确定性指标，`durationMs` 之类量墙钟的不在其中。）

所以那张表证明的是**结构性行为没退步**，不证明质量。
质量那句话只能由 `--replay`（真实录制）与 `--live` 说。
（[`docs/EVAL.md`](docs/EVAL.md) 开头就有一个醒目的警告框写着这件事。）

**闸门里有一个数值得单说**：`crossValidationRate` 的 `min=0`。
它不是噪声，是**由构造决定的**——某些黄金查询的证据结构就该是单源的。
把它设成"必须 > 0"会逼着系统去编造交叉验证。

### "48 个专家是不是噱头？"

**它解决的问题很具体，但它不是"48 个 agent 并行工作"。**

- **真问题**：48 个角色的资料塞进 prompt 会撑爆上下文，所以
  `roster_digest()` 给调度器的是**限长的紧凑名册**。
- **真收益**：组队是**可解释**的——报告里能看到谁参与了、为什么；
  而一个大 prompt 里"谁负责定价"只能靠模型自己记住。
- **两道关**：id 得在名册里，**且层级得对**。48 人名册里有 36 个执行层，
  "号是真的但人被放错了层"比"号是编的"更常见。

**这里有一个我踩过的真坑，值得讲**：模型把 `"L3-002 首席分析官"` 整个字符串
抄了回来，而最初的代码拿整串去查字典，查不到就记成 `phantom`——
**于是一个真实存在的人被报成了"不存在"**，6 次录制里的 4 次，
三个角色全部回退到了默认队伍。修法是从第 0 个空格切开再查。
这也是为什么调度问题分四类计数（`phantom` / `wrong_level` / `over_limit` /
`fallback_roles`）而**不合成一个数**——只有第一种是"模型在编"。

### "断线续传怎么做的？客户端代码有多少？"

**零行客户端代码。** 每帧带 `id: <按任务的 seq>`，浏览器原生 `EventSource`
自动把它放进重连时的 `Last-Event-ID` 头，服务端按它补发。
`lib/sse.ts` 只做解析和记录 `lastSeq`，**出错不主动 close**（主动 close 反而会
关掉浏览器自带的重连）。服务端 15 秒一次注释帧心跳防代理超时。

前端摄入分三层处理洪峰（[`frontend/src/store/taskStore.ts`](frontend/src/store/taskStore.ts)）：
`seq` 去重 → `requestAnimationFrame` 合帧 → 环形上限（思维 800 条、span 1200 条）。
**参考实现每个事件都做一次 `[...thoughts, d]`**，那是 O(n²) 拷贝加上每事件一次渲染。

### "`ensure_runner` 是干什么的？"

**修的是参考实现最严重的一个问题**：它的 `/api/tasks/{id}/stream` 在请求处理器里
直接调 `run_pipeline`，没有注册表。于是 `EventSource` 断线自动重连、
或者开第二个标签页，就会对**同一个 task_id 再跑一遍**——重复写入证据、trace、
专家统计。

`ensure_runner` 把它变成**一次执行 + N 次广播**
（[`backend/app/core/pipeline/runner.py`](backend/app/core/pipeline/runner.py)）。
压测的 `stream` 场景刻意让所有用户抢**同一批**任务，
**那个场景就是这句设计的验证**：1,774 请求、0 失败。

### "cassette 是什么？怎么保证不静默联网？"

适配层的副作用：**既然所有外部调用都经过同一层，就能在这一层包一层录制/回放。**
CI 里跑的是**同一份 `run_pipeline` 代码**，只换了 provider 实现——
不需要为测试写一套平行的"假流水线"。

关键设计：**`CassetteMiss` 继承 `HarnessError`**。
回放时缺记录**不是**"provider 挂了"，而是"夹具不全"。
它必须穿过降级逻辑直接抛出去，否则一次录漏会表现成
"这一次跑得有点降级"，而不是"夹具缺一条"。

---

## 三、取舍与不足（最容易被问翻的部分）

规则：**每个决定都要能说出代价是什么、什么情况下会改主意。**

| 决定 | 代价 | 什么情况下改主意 |
|---|---|---|
| 整份报告 JSON 存一列 | 不能 SQL 查报告内容；`data` 几 MB | 出现真实的跨报告查询需求时 |
| SQLite 不上 ORM | SQL 全是裸的，迁移要自己写对 | 换 Postgres 时（裸 SQL 反而是优势） |
| 不上 Alembic | 迁移是手写有序列表，错误没有工具兜底 | 多人同时改 schema 时 |
| 自建 trace 而非 OTel | 没有采样、没有跨进程传播 | 团队已有 OTel 后端时 |
| 文档链接由测试守护 | 多一条测试要维护，且它只覆盖相对链接 | 不打算改，这条是净收益 |
| 48 专家走名册文件而非一个大 prompt | 多一层生成与校验（`gen_experts.py` + 幂等测试） | 名册规模再大一个量级时 |

### 负面清单里最该主动说的三条

1. **`mypy` 装了，但 CI 里从来没跑过。** 类型检查是零收益的摆设。
2. **`zhipu` / `tavily` 适配器写完了，没有被真实调用过。**
   "换一家 provider 只写一个文件"这句话的验证强度是
   **mock 级别**，不是生产级别。
3. **判官指标没有基线可比。** `eval --live --repeat 3` 跑过了（15 份报告、
   花 $0.39025），实测值都在 [`项目总结.md`](项目总结.md) 的判官指标一节里。
   但那个数**只跑过一次**，而且**不进 CI**——`eval/baseline.json` 的
   `judges` 字段仍然是空的。所以"这次比上次高了"这句话**依然没有依据**：
   不是因为没有数，是因为只有一个数。

第三条建议**主动说**。它是我在 [`项目总结.md`](项目总结.md) 的
「未测的项」里自己标出来的，面试官如果从别处发现它，性质完全不同。
**主动说还顺带展示了另一件事**：这个指标我第一次跑出来是错的
（判官少吐一个右括号，5 份报告被判成 0.00/5），修好之后**重跑了全部 15 份**
而不是只报那 5 份——见 [`问题记录.md`](问题记录.md) 问题 49。

### 还有哪些"没做完"

| 项 | 状态 |
|---|---|
| `subscriptions` / `expert_stats` 两张表 | **没有写入方**；订阅接口从未实现 |
| 三档模式的成本对比 | 只有 `quick` 有实测成本，`expert` 只在 mock 下跑过 |
| 跨平台 | 未验证（见上） |
| 真实 provider 压测 | 有意没做——**被测瓶颈不该是 LLM 的排队时间** |
| 优雅停机回调 | 停机时正在跑的任务不会恢复，只会停在半路 |

---

## 四、"这个数字是怎么来的？"

被问数字时，**永远给出来源**。给不出来源的数字不如不给。

| 数字 | 来源 |
|---|---|
| pytest **947** 条（含 live 共 953） | `pytest --collect-only` |
| vitest **314** 条 + Playwright **1** 条 | vitest 输出 / `npm test` |
| 约 **56,000** 物理行 / **232** 个源文件 | 逐目录数出来的；**报的是物理行，不是"代码行"** |
| 单份报告 **$0.029157** | `eval --replay`，真实 DeepSeek + 博查的录制品 |
| 录制一次性 **$0.1326** | 5 条黄金查询、90 次 LLM 调用 |
| **引用忠实度 0.975**（最低 0.875、离散 0.0345） | `eval --live`，15 份报告 · 184 条判定，逐条在 `live-judge.json` 的 `judges.faithfulness.detail` 里 |
| **可用性 3.36/5**（离散 0.304） | 同上，`judges.rubric`；5 维各自的均分也在那 |
| 判官 **$0.39025** / **23.4 分钟** | `overview.totalCostUsd` / `wallSeconds`；**裁判模型 tier=`core`、`temperature=0.0`** |
| 压测 **5,365** 请求 / **0** 失败 | Locust，`-u 20 -r 4 -t 2m`，mock provider |
| **21** 条路由 / **9** 张表 / **11** 种事件 | 路由模块与 `migrations.py` 数出来的 |

**为什么报"物理行"不报"代码行"**：跨语言没法一致地算——
Python 有 docstring、TS 有块注释，两边的"代码行"不是一个东西。
**两个不能比的数放在一张表里比，比不报更糟。**

**被问倒时的规则**：说"这个数是 X，来源是 Y；如果 Y 不算数，那我只能说 Z"。
**不要现场估一个数然后当成实测值说出来。**

---

## 五、我要问面试官的问题

用来判断对面团队的技术水位（按锋利程度排序）：

1. **"你们的 prompt 改一次，怎么知道是变好还是变坏？"**
   —— 没有评测集的团队会答"看效果"。
2. **"你们的 LLM 成本能归因到具体哪一次调用吗？"**
   —— 答不上来的，通常也没有成本意识。
3. **"你们的重试逻辑集中在哪一层？"**
   —— 散在各处的，说明没有 provider 抽象。
4. **"上一份线上事故报告里，最根本的原因是设计还是操作？"**
   —— 答"操作"的，可能没有复盘文化。

---

## 六、这份文档的维护规则

1. **每个数字都要能在仓库里找到出处。** 找不到就删掉，或者标「未测」。
2. **答不上来的问题要写进来**，标"当时没答上来"，然后去把它想清楚。
   这份文档的价值不在于已经写好的部分，在于**它记录了我还没想清楚的地方**。
3. **只写真实做到的程度。** 这里最贵的失败不是"没做过"，
   而是"说了做过、被追问三层之后露馅"——那会让前面所有真话一起失去可信度。
4. **文档与代码的脱节由测试守。** 这份文档里的相对链接会被
   [`backend/tests/unit/test_docs_links.py`](backend/tests/unit/test_docs_links.py) 检查；
   流水线顺序由 [`backend/tests/unit/test_docs_order.py`](backend/tests/unit/test_docs_order.py) 检查。
