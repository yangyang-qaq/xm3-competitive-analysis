# 项目总结

> xm3 — 个人 AI 竞品分析系统
> 一个证据驱动、全程可观测、带返工闭环的多 Agent 深度调研系统
>
> 这份文档里每个数字都是**跑出来的**，不是设计值。
> 凡是没有实测支撑的，都标了「未测」而不是给一个估计。

---

## 一、项目概述

### 它是什么

给它一句调研需求——"对比 Notion 与 Obsidian"——它会：

把这句话拆成调研对象与候选人品牌（**需求理解**）→ 按主题从 48 位虚拟专家里
挑一个小队、规划调研维度（**专家调度**）→ 按维度与平台并发搜索、抓正文、
给每条证据打可解释的可信度分（**联网采集**）→ 从证据里提取论点、
功能矩阵、定价表、用户画像（**分析研判**）→ 检查证据密度与维度覆盖，
产出能驱动返工的问题清单（**质量审计**）→ 不达标就补采重做（**返工补采**）→
逐章撰写并挂载引用（**报告撰写**）→ 落库、生成报告页、决策回放与知识图谱。

**整个过程通过 SSE 实时推到浏览器**：你能看到每个阶段在做什么、
正在检索什么、拿到了什么证据、花了多少钱。

### 为什么做它

用 LLM 做竞品调研，最容易出的不是"写不出来"，而是**写得太顺**——
模型会流畅地编造市场份额、编造定价、编造用户评价，而你无法分辨哪一句有据可依。

所以这个系统的核心不是"让模型写报告"，而是**给这套流程加上四道约束**，
并且让每一条约束都有一个可被证伪的检验方式：

| 铁律 | 含义 | 怎么保证它在真的执行 |
|---|---|---|
| **无证据不立论** | 每个论点必须挂到具体证据上 | 引用的 evidence_id 由 `enforce_citations()` 依据**实际存在的证据**校验——**不采信模型的自我陈述**。编造的引用被丢弃并计数 |
| **交叉验证** | 高置信结论需要 ≥2 个独立域名支撑 | 用一张平台归属表算"独立"：同一篇文章被 5 家门户转载算 1 个信源 |
| **返工闭环** | 质量不达标就打回重做，不降低标准 | 返工后 `metrics_after - metrics_before` 是这条铁律**唯一可量化的证据** |
| **全程可观测** | 每次调用的输入输出、耗时、成本、决策依据都可回放 | contextvars 无侵入埋点；且**观测通道自己说的每句话都必须是真的** |

铁律一与二的副产品就是最能量化"这份报告有多少是编的"的两个数：
**幻觉引用率**与**引用忠实度**。

### 定位

**求职作品。** 三个后果：

1. **每一行都能解释。** 没有复制任何参考实现的代码——
   参考项目是 AGPL-3.0（传染性强），本项目是 MIT，
   架构思路借鉴，代码全部重写。
2. **每个选择都写下了代价。** 见 [`技术栈.md`](技术栈.md) 的「已知代价与负面清单」。
3. **文档齐全且互不脱节。** 文档之间的链接、文档与代码的顺序，都有测试守着。

---

## 二、功能模块

### 调研流水线（7 个阶段）

`intake → orchestrator → collect → analyze → audit → rework → write`

`rework` 是唯一的可选阶段。每一阶段的权重进进度条：
采集 34%、分析 30%、其余各 6–8%——因为那两步确实是这个系统里最重的。

**三档模式**，参数全部来自 `core/modes.py` 一处：

| | 品牌 | 维度 | 每维最少证据 | 独立信源 | 章节 | 搜索上限 | 返工额度 | 返工轮数 | 抓取 | 档位 |
|---|---|---|---|---|---|---|---|---|---|---|
| `quick` | 3 | 4 | 3 | 2 | 4 | 24 | 8 | 1 | 12 | fast |
| `deep` | 4 | 6 | 4 | 2 | 6 | 64 | 24 | 2 | 40 | aux |
| `expert` | 6 | 8 | 6 | 3 | 8 | 160 | 48 | 3 | 100 | core |

`rework_search_calls` **单独报，不并进 `max_search_calls`**：
返工额度是一个额外的池子，合并显示会让人以为 quick 档比实际能搜得多。

### 48 位虚拟专家 · 三层分工

| 层级 | 人数 | 构成 |
|---|---|---|
| **L3 决策层** | 3 | 调研总监 / 首席分析官 / 质检总监 |
| **L2 战略层** | 9 | 竞争战略 / 定价与商业模式 / 用户研究 / 技术架构 / 增长与获客 / 渠道与生态 / 品牌与传播 / 供应链与交付 / 财务与资本市场 |
| **L1 执行层** | 36 | 24 个行业专家（AI 编程工具、新能源汽车、跨境电商…）+ 12 个职能专家（舆情分析、财报解读、专利情报、招聘信号…） |

每次任务**只挑一个小队**（1 名组长 + 最多 3 名战略 + 最多 4 名执行），
不是 48 人全上。挑选过程有**两道关**：id 得在名册里，
且**层级得对**——48 人名册里有 36 个执行层，
"号是真的但人被放错了层"比"号是编的"更常见。

调度问题分四类分别计数（`phantom` / `wrong_level` / `over_limit` /
`fallback_roles`），**不合成一个数**——因为其中只有第一种是"模型在编"。

### 事件流工作台

11 种 SSE 事件类型：`node_update` / `progress` / `thought` / `message` /
`evidence` / `trace` / `chart` / `image` / `report_ready` / `done` / `error`。

每帧带 `id: <按任务的 seq>`，浏览器原生 `EventSource` 自动把它放进重连时的
`Last-Event-ID` 头——**整条断线续传链路不需要任何客户端代码**。
服务端 15 秒发一次注释帧心跳，`X-Accel-Buffering: no` 保证 nginx 下不被缓冲。

前端摄入分三层处理洪峰：`seq` 去重 → `requestAnimationFrame` 合帧 →
环形上限（思维 800 条、span 1200 条）。

### 报告页

17 个组件：目录 / 章节 / 功能矩阵 / 定价对比 / 用户画像 / 图表 /
指标面板 / 质量门 / 审计发现 / 图集 / 术语表 / 降级横幅。
每个数字都带**出处**（公式说明），每个论点都能点开被引证据。

批注工具条支持"按批注深化本节"——会重写该节并补采证据，
返回的 `phantomCitations` 是这次重写里被丢弃的、模型编造出来的引用编号个数。
**它不是"出错"，是幻觉抑制在工作的计数。**

### 决策回放 / 知识图谱 / 仪表盘 / 文库 / 知识库

| 页面 | 内容 |
|---|---|
| 决策回放 | 拖滑杆走过 span，联动高亮被引证据 |
| 知识图谱 | d3-force 力导向：品牌 ↔ 维度 ↔ 证据 ↔ 专家。**同一份正文两次构图、两次布局，结果逐字节相同** |
| trace 页 | span 树 |
| 仪表盘 | 覆盖 / 运行 / 分档位 / 人工修正率 + 最近 10 份。每个数字带公式 hint |
| 文库 | 报告列表（**刻意不含正文**：quick 档一份报告的 `data` 有几 MB） |
| 知识库 | **一行 = 一个去重后的来源**，带可信度与来源类型分面 |

---

## 三、技术统计表

### 规模

| 项 | 物理行 | 其中注释行 | 文件数 |
|---|---|---|---|
| 后端 `app/` | **18,246** | 1,553 | 83 个 `.py` |
| 后端 `tests/` | **15,143** | 769 | 60 个 `.py` |
| 前端 `src/` | **20,245** | 1,054（另有块注释未计入） | 82 个 `.ts(x)` |
| `eval/` | 1,786 | 181 | 6 个 `.py` |
| `loadtest/` | 601 | 55 | 1 个 `.py` |
| **合计** | **约 56,000 行** | 约 3,600 行 | **232 个源文件** |

> **这张表是一个快照，不是一个事实。** 它数于后端 **945 条测试**那次——
> 用那个数当锚点，是因为它旁边就有一条守卫（`test_docs_counts.py`）
> 逐句比对 `pytest --collect-only`，所以**能判断这张表是不是已经旧了**。
> 行数本身没有守卫，也**故意不加**：它每次改代码都会变，一条天天红的
> 守卫最后一定会被人绕过去。详见 [`问题记录.md`](问题记录.md) 的问题 54。

**注释占比约 6%，但分布很不均**：后端 `app/` 是 8.5%（1,553 / 18,246），
前端约 5%。这些注释不是格式要求，是**设计理由**——
`credibility.py` 里每一档权重表、`retry.py` 里每一个退避值、
`citations.py` 里铁律的原文，都写在紧挨着代码的地方。
理由很直接：这个项目要能回答"为什么是 0.35 而不是 0.3"，
而那个答案如果只存在于某个文档里，它会在代码改动时静默过期。

### 测试

| 层 | 数量 | 默认是否跑 | 花钱 |
|---|---|---|---|
| pytest · `unit` | **674** | 是 | 0 |
| pytest · `integration` | **242** | 是 | 0 |
| pytest · `contract` | **27** | 是 | 0 |
| pytest · `e2e`（cassette） | 0（用例写在 `live` 里） | — | 0 |
| pytest · `live` | **6** | **否**（需 `-m live`） | 有 |
| **pytest 合计** | **945**（含 live 共 951） | | |
| vitest | **314**（17 个文件） | 是 | 0 |
| Playwright | **1**（真浏览器打开报告页并截图） | 是 | 0 |
| **前端合计** | **315** | | |

**跑测试默认零 API 花费**，由三条机制保证：`addopts` 排除 `-m live`；
cassette 缺录制**直接抛 `CassetteMiss`，绝不静默联网**；
CI 里 `eval --gate` 用 mock、`eval --replay` 走录制回放。

### 系统组成

| 项 | 数值 |
|---|---|
| HTTP 路由 | **21 条**（GET 16 / POST 5，没有 PUT / PATCH / DELETE） |
| 数据库表 | **9 张**，迁移 **v1–v4** |
| 流水线阶段 | **7 个**（1 个可选） |
| SSE 事件类型 | **11 种** |
| 前端路由 | **12 条**（6 侧边栏 + 5 全屏沉浸 + 1 兜底） |
| 虚拟专家 | **48 位**（3 / 9 / 36） |
| Issue 类型 | **9 种**，每种对应一条明确的返工动作 |
| 运行时依赖 | 后端 **11 个** / 前端 **8 个** |
| 黄金集 | **22 条**（21 条 queries + 1 条 ambiguity） |
| Locust 场景 | **4 个**（read / stream / create / mixed） |
| 问题记录 | **55 条** |
| 反证突变 | 后端 **49** · 前端 **56**（每条都声明了"该由谁抓住"，逐条跑一遍） |

### 单份报告的质量指标

**来源：`eval --replay`，4 条回放得出来的黄金查询之一 `dewu-vs-shihuo`。
这是真实 DeepSeek + 博查的录制输出，不是 mock。**
（`MANIFEST.json` 记的是录过 5 条——差的那条是 `vague-coffee-market`，
它对不上 key 了，见问题 51。）

| 指标 | 值 |
|---|---|
| 论点 / 已验证 | 12 / 12（无证据立论率 **0**） |
| 交叉验证过的论点 | 8（比率 **0.667**） |
| 独立信源 | **69** 个域名 |
| 证据 | **92** 条，其中降级 6 条（**6.52%**） |
| 维度覆盖 | **4 / 4（1.00）** |
| 搜索调用 | 32 次（原始命中 289 → 过滤后 117） |
| LLM 调用 | 20 次 |
| token | 150,563 |
| 成本 | **$0.029157** |
| 返工 | 1 轮，7 条 Issue |
| 幻觉引用率 | **0** |

回放得出来的 **4 条，4 条都带降级记录**（"3/12 篇正文抓取失败，
这些证据只有摘要"、"图表 2/3 张因没有证据链被不生成"）。
**一份"指标漂亮但降级了三处"的报告不该被当成好结果**，
所以评测的 `overview.degraded` 会把它们印出来。

### 判官指标（`--live`，15 份 · 真实调用）

**怎么跑的**：`python -m eval.run_eval --live --only <5 条> --repeat 3 --judge`。
真实 DeepSeek + 博查，`CASSETTE_MODE=off`（`--live` 会强制关掉回放，
否则会一边查表一边把结果标成"真跑"）。**裁判模型 `deepseek-flash`**
（与报告同档位 `core`，`DEEPSEEK_MODEL_CORE` 指到哪它就是哪个）、
**`temperature=0.0`**、同一条 query 跑 3 次。
本次总花费 **$0.39025**、墙钟 **23.4 分钟**、15 条契约全绿。

| 指标 | 值 | 样本 |
|---|---|---|
| **引用忠实度** | 均值 **0.975** · 最低 **0.875** · 离散 **0.0345** | 15 份报告 · 184 条判定 |
| 判定分布 | 支持 **175** · 部分支持 **9** · 不支持 **0** | — |
| **可用性 rubric** | 均分 **3.36/5** · 离散 **0.30** | 15/15 份解析成功 |

可用性五个维度各自的均分（每维 n=15）：

| 维度 | 均分 | 读数 |
|---|---|---|
| 诚实度 | **4.33** | 最高的那一维——降级与"没查到"都写在了报告里 |
| 结构完整性 | 3.87 | 该有的章节都在 |
| 论证充分性 | 3.13 | 部分结论只靠单一来源撑着 |
| 区分度 | 3.13 | 矩阵里有打平的行，没分出高下 |
| **可操作性** | **2.33** | **最低的那一维**——结论复述得多，明确"该选哪个"给得少 |

**这张表要说清楚三件事，否则它比不报更糟：**

1. **判官有采样噪声。** 上表那个 `0.30` 的离散度就是它有多不稳——
   同一个查询重复 3 次，可用性均分的极差最大到 **0.60**
   （`notion-vs-obsidian` 三次是 4.00 / 3.40 / 3.60；
   `dewu-vs-shihuo` 是 3.40 / 3.60 / 3.00）。
   注意这两个极差**比总分均值 3.36 与 3.20 的差距还大**——
   **所以"这次比上次高了 0.2"不构成结论。**
2. **判官不参与闸门。** 它是 `--judge` 单独触发的，红与不红都不拦 CI。
   理由见 [`docs/EVAL.md`](docs/EVAL.md)：一个带采样噪声、还要花钱的指标
   不适合做门禁。
3. **测量仪器自身的可靠性也要报。** 这 15 份里有 **3 份的判官输出是
   "补了一个右括号才读出来"的**（`repairedBraces: 3`）。
   补出来的分和一次答对的分数不完全是一回事——它记在了 JSON 里，
   而不是被抹平。详情见 [`问题记录.md`](问题记录.md) 的问题 49。

> **一条没能验证的**：判官的"重试"路径（输出没答完就再问一次）
> 在这一批里**一次都没用上**（`retried: 0`）。它只有单元测试的覆盖，
> 没有真实调用证明它管用。这不是缺陷，是**它还没被真实需要过**。

### 评测闸门（mock，22 条）

| 指标 | min | median | max | pooled |
|---|---|---|---|---|
| `hallucinationRate` | 0 | 0 | 0 | — |
| `phantomCitations` | 0 | 0 | 0 | — |
| `unsupportedClaimRate` | 0 | 0 | 0 | **0** |
| `evidences` | 216 | 216 | 216 | — |
| `independentDomains` | 33 | 33 | 41 | — |
| `crossValidationRate` | 0 | 1.0 | 1.0 | **0.8939** |

> ⚠️ **这 22 条在 mock 下是同一种输入跑了 22 遍。**
> mock 的三个 provider 都不看 query——实测跑三条不同 query，
> 上表这几项逐项相同（216 条证据、33 个信源、4 个维度、覆盖率 1.00）。
> 所以这张表证明的是**结构性行为没退步**，
> 不证明质量。质量那句话只能由 `--replay` 与 `--live` 说。
> 详见 [`docs/EVAL.md`](docs/EVAL.md)。

### 压测（mock provider，`-u 20 -r 4 -t 2m`）

| 场景 | 请求数 | 失败 | RPS | p50 | p95 | p99 |
|---|---|---|---|---|---|---|
| `read`（报告列表 / 详情） | 1,346 | **0** | 22.4 | 110 ms | 1,200 ms | 1,700 ms |
| `stream`（挂 SSE 消费全部事件） | 1,774 | **0** | 29.8 | 87 ms | 1,000 ms | 1,200 ms |
| `create`（建任务 + 澄清 + 消费） | 319 | **0** | 5.4 | 1,500 ms | 12,000 ms | 12,000 ms |
| `mixed`（默认，权重 5:3:1） | 1,926 | **0** | 16.1 | 280 ms | 3,000 ms | 4,000 ms |
| **合计** | **5,365** | **0** | | | | |

**四个场景全部 0 失败、0 异常，全程无 `database is locked`。**

这测的不是"能扛多少请求"——那不回答任何问题，因为请求之间的成本
差了三个数量级（列一次报告列表是查一次 SQLite，跑一个任务是几十次 LLM 调用）。
它测的是两条具体路径：

1. **SSE 扇出**：同一个 `task_id` 挂 N 个订阅者时首事件时延会不会塌。
   `stream` 场景刻意让所有用户抢**同一批**任务——
   参考实现在请求处理函数里直接跑流水线，于是"第二个订阅者"
   等于"第二次执行任务"；xm3 用 `ensure_runner` 把它变成
   一次执行 + N 次广播，**这个场景就是那句设计的验证**。
2. **SQLite 写路径**：WAL + 线程局部连接下，并发写会不会锁。

### 录制成本（真实调用，一次性）

| 项 | 数值 |
|---|---|
| 录制的黄金查询 | 5 条 |
| LLM 调用 | 90 次 |
| prompt tokens | **571,355** |
| completion tokens | **101,236** |
| 总成本 | **$0.1326** |

这笔钱换来的是：**整条真实流水线可以在 CI 里离线跑完，永久零成本。**

### 未测的项

| 项 | 状态 |
|---|---|
| 判官指标的**基线** | 跑过了（15 份 · $0.39025），值都在上面「判官指标」一节里。但**只跑过一次**，而且**不进 CI**——`eval/baseline.json` 的 `judges` 字段仍然是空的，所以"忠实度比上次高了"这句话**依然没有依据**：不是因为没有数，是因为只有一个数 |
| 判官的**重问路径** | `_MAX_ATTEMPTS = 2` 的第二次询问**在 15 份里一次都没触发过**（`retried: 0/15`）。它有单元测试覆盖（`fake.calls == 2`），但**没有真实调用过** |
| 三档模式的成本对比 | 未跑。只有 `quick` 档有实测成本；`expert` 档只在 mock 下跑过 |
| 跨平台 | 未验证。本地那 13 项**确定性指标**连跑多遍逐行相同，换到 ubuntu runner 会不会因为并发搜索的完成顺序影响证据编号，没有验过。**说"逐项相同"要限定住范围**——`durationMs` / `firstEvidenceMs` 每次都不同，它们量的是墙钟 |
| 真实 provider 的压测 | 未做。**压测有意只跑 mock**——被测瓶颈不该是 LLM 的排队时间 |

---

## 四、技术栈

**后端** Python 3.11+ · FastAPI · uvicorn · pydantic 2 · httpx ·
SQLite（标准库 `sqlite3`，WAL，**无 ORM、无 Alembic**）· trafilatura ·
PyYAML · openai SDK（仅作 OpenAI 兼容协议客户端）
**前端** React 19 · Vite 8 · TypeScript 6（strict）· Tailwind 4 ·
Zustand 5 · React Router 7 · ECharts 6 · d3-force 3
**测试** pytest · vitest · Playwright · Locust · ruff · oxlint
**默认 Provider** DeepSeek（LLM）· 博查（搜索）— **均藏在适配层后面**

选型理由与代价见 [`技术栈.md`](技术栈.md)。三条原则：
能用标准库解决的不用依赖；每个依赖都要有一个提问能指向它；
选型要能解释代价。

---

## 五、技术亮点

按"面试里最能撑住追问"排序。

### 1. Provider 适配层：流水线里不出现任何厂商名

这是整个项目的技术核心。问题陈述很具体：在采集循环里写
`httpx.post(url, json={"q": q, "include": "douyin.com", "freshness": "oneMonth"})`，
换一个搜索源就要回头改流水线。

问题不在"改起来累"，而在于**厂商方言漏进了编排层**——一旦漏进去，
换 provider 要动最贵、测试最厚的部分；每个新 provider 都要加一个
`if provider == ...`；厂商的限流/鉴权错误码散落各处；
**而"相关性过滤"这类业务逻辑会不小心写进适配器，换搜索源时静默消失**。

最后一条最危险，因为它不报错。

解法是三个归一化 Protocol + 一份**方言对照表**：

| 归一化 | 博查 | Tavily | Serper |
|---|---|---|---|
| `sites=("douyin.com",)` | `"include": "douyin.com"`（`\|` 连接） | `"include_domains": ["..."]` | 把 `site:` 拼进 query |
| `freshness="month"` | `"freshness": "oneMonth"` | `"days": 30` | `tbs` |
| `freshness="any"` | `"freshness": "noLimit"` | **整个键不发** | — |
| `limit` | `"count"`，上限 50 | `"max_results"`，上限 20 | 数组长度 |
| 鉴权 | Bearer 头 | `api_key` **在请求体里** | 头 |

最值得看的一格是 `any` 那一行：博查要显式发 `noLimit`，
Tavily 要**完全不发这个键**（发 `null` 会被拒）。
同一个语义，一个是"显式说不要限制"，一个是"别说这个事"。

**能力协商而非静默失败**：provider 不支持站点过滤时，
显式退化成"把域名折进查询词"并**计数**（`site_filter_folded`
出现在报告指标里）。降级是允许的，**静默降级不是**。

**接入一家新 provider 只写一个文件，流水线零改动。**

> **诚实说明**：`zhipu` / `tavily` 的适配器写完了但**没有被真实调用过**；
> 能力协商目前**只实现了 `site_filter` 一项**。这两条都在
> [`技术栈.md`](技术栈.md) 的负面清单里。

### 2. Cassette：让真实流水线可离线测

适配层带来一个副作用，而它是这个项目验证成本能压到零的原因：
**既然所有外部调用都经过同一层，那就可以在这一层包一层录制/回放。**

CI 里跑的是**同一份 `run_pipeline` 代码**，只换了 provider 实现。
不需要为测试写一套平行的"假流水线"，也不需要联网。

一个设计细节值得单说：`CassetteMiss` **继承 `HarnessError`**。
回放时缺记录**不是**"provider 挂了"，而是"夹具不全"。
它必须穿过降级逻辑直接抛出去，否则一次录漏会表现成
"这一次跑得有点降级"，而不是"夹具缺一条"。

### 3. 幻觉抑制：校验依据实际证据，不采信模型自述

```python
#: 下面两个字段由 evidence/citations.py 依据**实际存在的证据**计算，
#: 不采信模型的自我陈述。模型说"我引用了 3 条"和这 3 条真的存在是两回事。
verified: bool = False
cross_validated: bool = False
phantom_evidence_ids: list[str] = field(default_factory=list)
```

模型编造的引用被丢弃并计数，这个数就是**幻觉引用率**的分子。
铁律一还管到图表：**没有证据链的图一律不生成**并记一条降级——图表也是论点。

**但幻觉引用率为 0 不等于没有问题。** 它能发现"编号不存在"，
**发现不了"编号存在但内容不支持"**——后者才是真正会骗到人的那种：
报告里每个论点后面都挂着上标 `[12]`，点开也确实有第 12 条证据，
只是那条证据讲的是另一件事。铁律一在字面上成立，在实质上不成立。

所以还有 **LLM-as-judge 的引用忠实度**：三档判定（支持 / 部分支持 / 不支持），
固定种子抽 20 条。判"部分支持"是刻意的——只给两档会逼裁判在
"证据讲了这件事但数字对不上"和"证据讲的是另一件事"之间二选一，
而这两件事的严重程度差很远。

### 4. 结构性地消灭"文档与代码脱节"

流水线顺序由 `core/pipeline/stages.py` 单一维护，
**一个测试解析 `docs/ARCHITECTURE.md` 的 mermaid 块，
断言节点顺序等于那个常量**。改了顺序忘改文档，CI 就红。

文档链接也有测试守着，因为这份仓库的 README 曾经链向五个**还不存在**的文件。
这类缺陷难发现是因为它**不报错**：不是语法错误（编辑器不标红）、
不是代码（`pytest` / `ruff` / `tsc` 谁也不看 markdown）、
不在运行时路径上（没有任何请求会 500）。
唯一的发现方式是"有人真的去点了那个链接"——
而写文档的人刚刚写完，**他最不可能去点**。

两条测试都写了自证用例（喂一段已知内容，断言解析器真的会红），
以及防空转下限（"坏链列表为空"和"一个链接都没扫到"不能走同一个 assert）。

### 5. 观测通道自己的诚实度

这一条是这个项目里最特别的设计。四条铁律的第四条是"全程可观测"，
而它约束的**不只是"有没有埋点"，还有"观测数据本身是不是真的"**。

两个具体的例子：

- 调度问题原先被合成描述成"N 个专家 id 不存在于名册"。
  实测那次报告里的"有 11 个专家 id 不存在于名册"，
  里面混着"超出上限被截掉"和"整类回退默认队伍"——**只有一种是模型在编**。
  四类现在分开记，宁可多几个字段，也不把四类压成一个数。
- 名册是 `- L3-002 首席分析官` 这个形状喂进去的，模型很自然地
  **把整行抄回来**。原先拿整串去查字典，查不到就记进 `phantom`——
  于是**校验器把一个真实存在的人报成了不存在**，
  而报告里那句话读起来像是模型的错。修法是 `_extract_id()`，
  那段代码是**照着录制下来的真实响应写的**，不是照着想象中模型该有的
  输出格式写的。

### 6. 前端在事件洪峰下不卡

参考实现每个事件都 `set({thoughts: [...s.thoughts, d]})`——
一次 O(n) 拷贝 + 一次渲染。800 条思维 + 400 条证据流进来时肉眼可见地卡。

xm3 分三层：`seq` 去重（断线续传不重复）→ 模块级可变缓冲 +
`requestAnimationFrame` 合并成一帧一次 `set()`（250 ms 兜底后台标签页）
→ 环形上限 + 虚拟列表。

### 7. 评测：闸门是契约，不是快照

阈值分两类，因为它们**坏了意味着不同的事**：

- **机制背书的**（`hallucinationRate` 等恒为 0）——这些数**在结构上**
  就该是 0。一旦非 0，说明那层机制坏了，不是模型今天状态不好。
- **质量判断的**（`dimensionCoverage ≥ 0.6` 等）——取值在
  "实测最低值"与"明显坏了"之间，**不贴着实测最低值写**。
  贴着写会让闸门在正常波动下变红，**而它变红一次就会被调松一次，
  最后什么都不剩。**

聚合口径踩过一次坑并因此改对：`crossValidationRate` 原先按 min/max 聚合，
在黄金集上算出 `min = 0` 导致闸门变红——但那个 0 来自一份只有 3 条论点的报告，
3 条都没被交叉验证。一份报告 3 条论点，这个比率只能取 0 / 0.33 / 0.67 / 1，
取"22 份里的最小值"等于在问"有没有哪一份碰巧是 0"——
**这是个噪声问题，不是质量问题。** 比率类改用**合并**：`Σ分子 / Σ分母`。

### 8. 55 条真实的踩坑记录

[`问题记录.md`](问题记录.md) 每条按「现象 → 定位过程 → 根因 → 修复 → 验证」五段写，
编号是事件序号不是日记。挑四条最能说明问题：

- **问题 45**：为新代码写的第一版测试，一条是空的（两边都是空字典，
  断言成立）、一条会让应用起不来（`json_type` 把普通字符串当 JSON 解析
  直接抛 `malformed JSON`，而迁移是**启动时跑的**）。
  发现方式不是"读一遍觉得不对"，是**突变校验**。
- **问题 43**：压测脚本写完、跑一次就"绿"了，
  而它测的不是它自己声称的那个东西——`--tags` 不管用，
  Locust 照样实例化空的用户类，20 秒里刷 334 行
  `No tasks defined on ReadUser`，**读起来像"压测跑完了，没有任务"**。
- **问题 46**：`d3-force` 被 import 了而 `package.json` 里没有它。
  `tsc` / `oxlint` / `vitest` / `vite build` / CI **五层全绿**——
  没有一层会问"这个包你声明了吗"。
- **问题 47**：`eval --live` 从阶段 3 写好到阶段 10 之前**一次都没被执行过**——
  它把一个 LLM 的 provider 名填进了搜索和抓取两格，于是每次运行都死在
  "未注册的 搜索 provider"。它不花钱不是因为它省钱，**是因为它连第一个
  请求都没发出去**。修完第一次真跑：`$0.0232` 一份报告。
  这条的教训是**"这条路径被测过"必须带上"用哪个参数测的"**——
  那个抽象恰好只在 `mock` 这个退化取值上成立，而 CI 天天跑的就是 `mock`。

---

## 六、简历项目描述

### 一句话版

> 独立设计并实现一个多 Agent 竞品调研系统（Python 后端 18k 行 + React 前端 20k 行），
> 用 provider 适配层隔离 LLM / 搜索厂商方言，用 cassette 录制回放把真实流水线的
> 测试成本压到零，并以「引用强制 + 交叉验证 + 返工闭环 + 全程可观测」四条铁律
> 约束 LLM 的幻觉输出。

### 分点版（可直接放简历）

**xm3 · AI 竞品分析系统**（个人项目，2026.08–2026.09）

- **架构**：设计并实现 7 阶段调研流水线（需求理解 → 专家调度 → 联网采集 →
  分析研判 → 质量审计 → 返工补采 → 报告撰写），48 位虚拟专家分三层动态组队，
  产出带完整证据链的竞品分析报告；FastAPI + SQLite 后端，React 19 + TypeScript 前端，
  SSE 实时推送 11 类事件，支持断线续传与决策回放。

- **Provider 适配层**（核心亮点）：把 LLM / 搜索 / 抓取抽象为归一化 Protocol，
  **流水线代码中不出现任何厂商名**——换搜索源从"改流水线"变成"加一个文件"。
  实现能力协商（不支持的特性显式降级并计数，而非静默失败）与类型化错误重试
  （HTTP 状态码到异常的映射只有一处实现）。在此基础上实现 cassette 录制回放，
  **整条真实流水线可在 CI 离线跑完，零 API 花费**。

- **幻觉抑制**：建立四条质量铁律并用可证伪的方式落地——引用校验依据**实际存在的
  证据**而非模型自述；交叉验证按平台归属表判定独立信源；返工效果用
  `metrics_after - metrics_before` 量化；全部调用经 contextvars 无侵入埋点，
  输入输出、耗时、成本、决策依据均可回放。

- **工程**：**945 条 pytest + 314 条 vitest + 1 条 Playwright**，
  默认零 API 花费；22 条黄金集 + LLM-as-judge 评测闸门进 CI（5 个 job）；
  Locust 四场景压测 5,365 请求 **0 失败**、无 `database is locked`；
  写文档时顺手加了两条"结构与事实一致性"测试
  （文档链接可达、流水线顺序与架构文档一致）。

### 面试时最可能被追问的三点

| 追问 | 答案的落点 |
|---|---|
| "为什么要做适配层，直接调 API 不行吗？" | 问题不在改起来累，在于**厂商方言漏进编排层**——而一旦漏进去，相关性过滤这类业务逻辑会在换源时静默消失，不报错 |
| "怎么证明 AI 写的报告不是编的？" | 两个数：幻觉引用率（校验依据实际证据，不采信自述）与引用忠实度（LLM 裁判三档判定）。**并且我会说清楚前者的盲区**——它发现不了"编号存在但内容不支持" |
| "这个系统你自己信它吗？" | 打开 [`技术栈.md`](技术栈.md) 的负面清单：11 条明确没做到的事，**第一条是"CI 从来没执行过"**（仓库还没 `git init`），还包括"`mypy` 装了但 CI 从没跑过"、"两张表建了但没有写入方"、"22 条黄金集录过 5 条、现在只回放得出来 4 条" |

---

## 七、回顾

### 最花时间的不是写代码

十个阶段里真正承重的只有两个：**阶段 2** 让核心闭环以"一个能打印完整事件流的
命令行"的形式跑通，从此每个阶段都在一条**已经跑通过**的路径上加深；
**阶段 6** 把同一股事件流换个出口交给浏览器。

其余八个阶段在做同一件事：**把"应该发生的事"变成"可以被证伪的断言"**。

### 这个项目里最贵的一课

**"绿了"和"测到了"是两件事。**

第一版评测跑绿之后，我去做了突变校验，发现：
一个 22 条的闸门里有几条阈值方向解析错了（方向搞反的闸门**永远是绿的**）；
一个新写的等价性测试里九行语料有五行在验空集（`assert got == want`
在两边都是 `{}` 时同样成立）；一份压测报告显示"0 失败"，
而那 334 行 `No tasks defined` 混在聚合行里把结论变得没有意义。

三次都不是"代码写错了"，是**结论看起来是对的**。
所以后来所有守卫都加了两样东西：**自证用例**（喂已知内容，
断言它真的会红）和**防空转下限**（数量不能是 0）——
一条会在无人察觉时自己变绿的测试，比没有测试更糟。
