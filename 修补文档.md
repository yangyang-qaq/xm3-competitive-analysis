# 修补文档 · xm3

这份文档记录**已经知道、但还没修**的问题，以及**决定不修**的取舍。

它和 `问题记录.md` 的分工是：

| | 问题记录.md | 修补文档.md（本文件） |
|---|---|---|
| 记录的是 | 已经定位并修好的问题 | 还没修的问题 |
| 状态 | 已关闭 | 未关闭 |
| 读法 | 学"这类故障长什么样" | 知道"剩下多少活" |

条目分五类，**A 类不做完项目就不算完成**，B/C/D 类是"细节"，E 类是备忘。

当前基线：后端 `945 passed, 6 deselected`，前端 `314 passed / 17 文件`，
ruff 全库干净，`eval --gate` 22 条 12 项契约全绿，`eval --replay` 4 条，
反证脚本后端 `49/49`、前端 `56/56`（两边都是 0 漏掉、0 锚点失效、0 仪器崩，
逐字节还原）。
**阶段 0–10 全部完成。**

> **这一行曾经写着"815 passed / 310 / 阶段 0–7 已完成"，而它一直没人改。**
> 这正是 [`问题记录.md`](问题记录.md) 问题 48 记的那件事：
> 一个写在散文里的数字没有工具会去比对，改代码的人也没理由来看它。
> 现在后端那个数由 [`backend/tests/unit/test_docs_counts.py`](backend/tests/unit/test_docs_counts.py)
> 钉住（四份主文档里每一处写了条数的话都对它断言），
> **但这一行不在它管的四份文档里**——所以这一行仍然是靠人记得改的。
> 留着这个注记，是因为"哪些数有守卫、哪些没有"本身值得被读出来。

> **反证那两个数曾经也这样。** 这里原来写着"后端 `48/48`、前端 `47/47`"，
> 而 2026-09-20 真跑一遍拿到的是**后端 `46/49`**（其中一条是真漏，
> 见问题 52）与**前端 `56/56`**——前者说明那个数写下来之后**一次都没重跑过**，
> 后者说明**突变加了两组（回放 R1–R3、图谱 G1–G6）而这里没跟着改**。
> 顺带查出前端脚本本身的一个洞：**"测试没抓住"不影响退出码**
> （见问题 53），修完之后这两个数才真的算读数。

> 前端的 `260 / 14` → `293 / 15` 是阶段 8 的回放层带来的（`lib/replay.ts`）；
> `293 / 15` → `310 / 16` 是知识图谱带来的（`lib/graph.ts`），
> 那一层值得单独说一句：**它的三条缺陷全是拿真报告跑出来的，
> 单元测试一条都没抓到**——夹具里每条证据只命中一个维度，而真报告里不是。
> 所以 G1/G4 的 guard 用的夹具是照着真数据的形态**故意造出来**的。

> 提交环境注意：虚拟环境在 `backend/.venv`，**不在仓库根**。所以进到 `backend`
> 之后是 `./.venv/Scripts/python.exe`，不是 `../.venv/...`（本文档早期版本写错过）。

---

## 0. 先说一条会影响花钱顺序的结论

**阶段 4/5 会作废已经录好的 cassette，所以不要把 22 条黄金集录完再做这两个阶段。**

链路是这样的：

```
experts_seed.yaml / experts.json（名册）
  → roster_digest()                      # data/loader.py，上限 DIGEST_BUDGET=2600
  → plan_prompt / dispatch_prompt        # pipeline/dispatch.py:189, 230
  → 消息内容变了
  → _llm_key 变了                        # providers/cassette.py:87
  → 所有包含这两次调用的 cassette 全部 miss
```

`_llm_key` 只由请求内容决定（刻意不含 `purpose`），而名册是 prompt 的一部分。
临时名册 15 人换成 48 人，`roster_digest()` 的输出必然不同——**每一条 cassette 里的
`plan` 与 `dispatch` 两次 LLM 调用都会 miss**。回放时 miss 会抛 `CassetteMiss`
（绝不静默联网），所以后果不是"测错了"，而是"测试直接报错"。

**阶段 4 已于本轮落地**，那条 1 条黄金集（$0.01847）就此正式失效——这是第 0 节
预告过的沉没成本，不再另行抢救。

剩下的一半仍然成立：**阶段 5 若改动任何 prompt（返工提示词、审计提示词），
也会再次作废 cassette。** 所以黄金集的录制窗口在**阶段 5 结束之后**，不是现在。

判断"prompt 是否已冻结"的可执行办法。**没有 git，所以直接对喂给 prompt 的文件取摘要**
（见 A5：仓库还没 init）：

```bash
cd backend && sha256sum app/core/pipeline/prompts.py app/core/pipeline/dispatch.py \
    app/data/experts.json app/core/modes.py
```

跑两次，四行摘要全同 → 这四个文件没动 → prompt 没变 → 现在录黄金集不会白花钱。

**这个判据是故意偏保守的**：它比 `_llm_key` 严格。`_llm_key` 只看请求内容，
而这里连"改了注释"也会让摘要变。也就是说它可能误报"没冻结"（代价是多等一会儿），
但**不会误报"冻结了"**（代价是白花 $0.4）。两个方向的代价不对称，所以往保守
那边偏是对的。

`experts.json` 进这份清单，是因为它是 `roster_digest()` 的来源——第 0 节开头
那条链路的第一环。只盯着 `prompts.py` 会漏掉名册变化。

（顺带记一笔：`record_cassettes --dry-run` **不打印 cassette key**，它只估算成本。
这条命令给不了"prompt 冻没冻"的答案。）

---

## A 类 · 挡路的（不做完不算完成）

### A1. 专家名册还是临时版：15 人 → 48 人 —— ✅ **已关闭**

**关闭方式**（阶段 4，本轮完成）
名册改成"手写语义内核 + 确定性生成"的产物，三个文件各司其职：

```
experts_seed.yaml   ← 48 人手写语义内核（id/level/group/name/role_title/one_liner/skills/knowledge_base/knowledge_tags）
  → scripts/gen_experts.py   ← validate_seed() 把关 + 生成机械字段（配色/徽标/图标/stats）
  → app/data/experts.json    ← 提交进仓库的生成物
  → app/data/loader.py       ← 只读加载，不再持有任何常量表
```

`data/roster.py` 已删除，`context.py` / `dispatch.py` 的 import 改指 `app.data`。

**验收证据**（三条，都可复跑）

1. `python -m scripts.gen_experts --check` 返回 0：生成物与 seed 一致。
   `tests/unit/test_experts.py` 里有一条**幂等**测试——同一份 seed 生成两次
   逐字节相同。这一条让"生成而非手写"是个能站住的工程主张，而不是把
   不确定性从人手挪到了机器。
2. 名册分布由测试断言：L3 3 人 / L2 9 人 / L1 行业 24 + 职能 12 = 48 人。
3. **那条降级消失了**：跑一次完整的 mock 流水线，`degraded: []`、`repairs: []`，
   队伍三层齐全。原先的"有 11 个专家 id 不存在于名册，已替换为默认队伍"不再出现。
   全库测试 `403 → 469 passed`，且**扩容没有改动任何既有测试**。

**顺带发现并修掉的一个真漏洞**（见 `问题记录.md` 问题 12）
`validate_team()` 原先只查"id 在不在名册里"，**不查"它在哪一层"**，
于是一个 L2 的人可以被指派成组长而无人反对。这个洞在 15 人名册下不明显，
扩到 48 人之后**变得更容易踩到**——48 人里 36 个是执行层，
"模型挑了个执行层来当组长"从一个边角情况变成了一件常事。
现在两道关：id 必须存在，且层级必须与角色相符。

<details>
<summary>展开：关闭前的现象（保留，用于对照）</summary>

**现象**
`data/roster.py` 是阶段 2 立起来的"形状版"，只有 15 人：

| 层 | 现在 | 目标 | 缺口 |
|---|---|---|---|
| L3 决策 | 3 | 3 | ✅ |
| L2 战略 | 4 | 9 | +5 |
| L1 行业 | 4 | 24 | +20 |
| L1 职能 | 4 | 12 | +8 |
| **合计** | **15** | **48** | **+33** |

实跑时必然出现这条降级（已实测）：

```
专家调度：有 11 个专家 id 不存在于名册，已替换为默认队伍
```

**原因**：`validate_team()` 用 `expert_by_id()` 过滤非法 id
（`pipeline/dispatch.py:52`），名册越窄，模型按 `roster_digest()` 里的岗位名
推断出的 id 越容易落在名册外。这不是模型的错，是名册没给全。

**影响**
报告能跑通，但两件事不成立：

1. "48 专家三层分工"是简历上的核心卖点，现在只有 15 人。
2. 每一次演示的降级横幅里都会出现这一条——**降级横幅本身是好设计**
   （四铁律里的"全程可观测"），但它在这里暴露的是名册没做完，不是流程有问题。

**修法**（阶段 4，计划已定）
`experts_seed.yaml`（手写语义内核，约 8 行/人）+ `gen_experts.py`
（确定性生成机械字段）+ `loader.py` + 名册不变量测试。
`roster.py` 里的 15 个 id **一个都不能改**——id 已经写进 `expert_stats` 表
和报告数据，改它就是一次数据迁移。新名册是它的超集。

**验收信号**：跑一次完整流水线，这条降级消失。→ **已达成**，见上方证据 3。

</details>

---

### A2. 返工进得去、补不出来 —— ✅ **已关闭（问题 23）**

**现象**（实测，同一次真实运行）

```
返工补采：搜索预算已用尽，本轮没有发起任何搜索
返工补采：补采未新增证据，返工提前结束
已达返工上限 1 轮，仍有 2 项待补
```

**原因**：返工轮次没有独立预算。采集阶段的搜索预算在首轮就用完了
（`pipeline/collect.py:206`），返工阶段重新进采集时预算已经是 0，
于是"返工"这个动作发生了，但没有产生任何新证据
（`pipeline/orchestrator.py:199`），最后以"到上限"收场（`:231`）。

**影响**：**这是四铁律里唯一一条在真实运行中不成立的。**

铁律 3 是"返工闭环"，量化指标是 `返工提升 Δ = metrics_after − metrics_before`。
现在这个数恒为 0——不是"没有可补的"，是"没给预算"。更麻烦的是它在报告里
读起来像"补过了，没补到东西"，而事实是"没让它补"。

**修法建议**：把搜索预算拆成 `首轮预算 + 返工预算`，返工轮次从自己的额度里扣。
不要简单地把总预算调大——那会让"返工花掉了多少"变得无法归因，
而返工成本恰恰是最该被看见的一项。

**验收信号**：造一份证据稀薄的 fixture 触发返工，断言
`metrics_after > metrics_before`，且 trace 里返工轮次确实有搜索 span。

**已按上面的建议修掉**（详见 `问题记录.md` 问题 23）：`ModeConfig` 拆出
`rework_search_calls`（quick 8 / deep 24 / expert 48，约首轮池的三分之一），
`PipelineContext` 加 `rework_search_calls_made` 单独记账，
`build_queries(rework=True)` 读返工池。返工轮还**只跑审计指定的目标**，
不复跑首轮的通用查询——同样的查询文本会返回同一批 URL，
去重后一条新证据都进不来，白吃这个本来就不大的池子。

验证：25 条新用例（`test_collect_budget.py` 16 + `test_rework_loop.py` 9），
两条反证脚本 M1–M3 全红且逐字节还原。**三档模式的 `Δ` 现在都可能非 0 了。**

修这一条时顺带发现这个缺陷能活这么久的原因，值得单独记：
**它在测试里等于不存在**——没有一条用例让首轮池见底之后再进返工，
于是"返工拿不到预算"这个分支从来没被执行过。所以现在
`test_rework_loop.py` 的 `make_ctx` 里显式有 `ctx.search_calls_made =
ctx.mode.max_search_calls` 这一行：**先量"这段代码被执行过吗"，再去读它。**

---

### A3. ~~三条还没写的测试（阶段 5 的验收缺口）~~ —— 已补齐

三条都补上了，落在两个新文件里：

| 用例 | 落在哪 | 反证 |
|---|---|---|
| 返工触发 fixture（"触发时 Δ 为正"） | `tests/integration/test_rework_loop.py`（9 条） | M1/M2/M3 全红 |
| 维度覆盖反回归测试 | `tests/unit/test_dimension_coverage.py`（12 条） | C1/C2 全红，**同时钉住两个错答案**（算成 1.0 和算成 0.5 各说明换成了哪条错判据） |
| `degraded` 强断言 | `tests/integration/test_pipeline_mock.py`（2 条） | D1/D2 全红 |

**第三条有个坑，值得记下来**：干净跑完一条流水线时
`ctx.degraded_blocks` 与 `body["degraded"]` **都是空的**（实测），
所以 `==` 在装配把整份清单丢掉时同样成立——原计划的"强断言"直接写下去
会是一条**缺陷不可能出现**的测试。所以用例改用含糊需求跑
（触发"自动采用推荐选项"那条降级），先断言清单非空再比对。

顺带发现：`assemble.py` 里 `if problems:` 那段（含"校验后把降级补回正文"
那次修复，注释里自己写着这是个 bug）**此前没有任何测试**，一并补上了。
详见 `问题记录.md` 问题 24。

`MockLLMProvider.fail_purposes` 已经具备造这些场景的能力，缺的只是用例。

---

### A4. 前端几乎为零，且后端接口还没铺 —— ✅ **已关闭（阶段 6–8）**

**当时的现状**

- `frontend/src/pages/` 只有 `HomePage.tsx` / `NotFoundPage.tsx`。计划里是 12 个页面。
- `backend/app/api/` 只有 `routes_health.py` / `routes_providers.py`。
  缺 `routes_tasks`（含 SSE）、`routes_reports`、`routes_experts`、
  `routes_knowledge`、`routes_dashboard`、`pipeline/stages`。

阶段 6–8 的全部内容。这不是"修补"，是剩下的主线工作量，列在这里是为了让
"项目还差多少"有一个可数的答案。

**已关闭。** 12 个页面全在（Home / NotFound / Clarify / Workspace / Report /
Trace / Graph / Experts / ExpertDetail / Library / Knowledge / Dashboard），
后端 7 个路由模块，实测 `/openapi.json` 共 **21 个端点**：

```
GET  /health  /api/modes  /api/providers  /api/providers/health  /api/pipeline/stages
GET  /api/dashboard  /api/evidences  /api/evidences/facets  /api/experts  /api/experts/{id}
GET  /api/reports  /api/reports/{id}  /api/reports/{id}/export
POST /api/reports/{id}/feedback  /api/reports/{id}/refine
GET,POST /api/tasks  GET /api/tasks/{id}  POST /api/tasks/{id}/clarify
GET  /api/tasks/{id}/stream  GET /api/tasks/{id}/trace
```

**两处与计划不同，都是有意收的**（写在这里免得被当成遗漏）：

- **没有 `routes_knowledge.py`**。计划里那个"知识库"页的取数其实只有一种
  读法——按分面筛证据（`/api/evidences` + `/api/evidences/facets`）。
  单开一个 `/api/knowledge` 只是给同一份数据换个路由名，
  真正的分面逻辑会因此有第二份实现。`KnowledgePage` 直接吃这两个端点。
- **没有 `/api/subscriptions`**（订阅）。这是计划 API 表里唯一落空的一项。
  想清楚后发现它在这里没有真实语义：订阅要有一个"会自己变的对象"
  和一个送达渠道（邮件 / webhook / 站内提醒），而 xm3 是单机跑的，
  没有定时任务，报告不会自己变新。真做出来就是一个写着"已订阅"、
  永远不会响的按钮——比不做更坏。**这条不补，是决定，不是欠账。**

`/api/experts/workload` 也没有单独开：负载数据并进了 `/api/experts`
（每人带 `participation`），否则前端要为一个页面发两个请求再把结果对起来。

---

### A5. 没有版本控制：`.gitignore` 写了，但从来没 `git init`

**现象**（本轮写文档时发现的）

```
D:\Dekcop\xiangmu\xm3> git rev-parse --is-inside-work-tree
fatal: not a git repository (or any of the parent directories): .git
```

而 `.gitignore` 在阶段 0 就写好了（1026 字节，覆盖 `backend/data/*.db`、
`.env`、`node_modules` 等），`LICENSE` 也在。**目录结构齐了，只是从没 init 过。**

**影响**

1. **对求职作品来说，commit 历史本身是材料。** 「怎么一步步做出这个系统」
   在面试里是可以被追问的，而 `git log` 是最直接的证据。现在没有。
2. **一个具体的坏后果**：本轮"改坏了再改回来"的实验（`问题记录.md` 问题 12、14
   都在改源码做反证）**没有任何回滚保护**。我是靠手工备份 + `finally` 还原撑过去的，
   这次运气好，两次都 `restored: True`。这不是可以重复依赖的办法。
3. 第 0 节那条"prompt 冻没冻"的判据只能退化成 `sha256sum`（见第 0 节）。

**为什么它不是"细节"**

写这一段时（阶段 5 之前）的理由是"越晚 init，越难把历史整理成能看的样子"。
十个阶段做完之后又多了一条，而且这条更硬：

> **CI 从来没有执行过。**
> `.github/workflows/ci.yml` 写了五个 job，而它的唯一执行入口是 GitHub 上的
> push——没有 `git init` 就没有 push，没有 push 就没有 CI。
> 所以文档里"CI 五个 job 全绿"那句话是**推出来的，不是读出来的**。
> 更糟的是：本机跑 CI 里那条 ruff 命令是 5 条错误，
> 它一旦真的跑起来会停在 backend job 的第一步，后面那 945 条测试一条都不开始。
> （已修，并加了本机守卫；详见 [`问题记录.md`](问题记录.md) 的问题 50，
> 以及 [`技术栈.md`](技术栈.md) 负面清单的第 1 条。）

也就是说，这条欠账的后果**不是"没有 git log 看"**，而是
**一整层写在仓库里、看起来很完整的自动化保证，实际上一秒钟都没运行过**。
两者在文档里读起来一模一样。

**修法**

```bash
cd D:\Dekcop\xiangmu\xm3
git init
git add -A
git status          # ← 先看这一步：确认 .env 和 *.db 没被加进来
git commit -m "阶段 0–10：完整实现 + 评测 + 压测 + 文档"
```

**`git add -A` 之后必须先看 `git status`。** `backend/.env` 里是真实 API Key
（明文），一旦进了第一个 commit，就算之后删掉也还留在历史里。
`.gitignore` 应该已经挡住了它，但这一步是**验证**，不是走过场。

**关于历史**：现在 init 只能得到一个"阶段 0–10"的大 commit，前面的过程找不回来。
如果想让历史好看，可以按模块拆成若干个 commit（provider 层 / 数据层 / 流水线 /
名册 / SSE / 评测 / 文档），每个都附一句"为什么"。这比一个巨型 commit
在面试里有用得多，而代价只是多花十几分钟。

> **已执行（2026-09-21）。** 实际拆成了 8 个：骨架与契约 / provider / 数据层 /
> 流水线 / 名册 / API 与 SSE / 前端 / 评测与 CI 与文档。
> 拆的时候按依赖顺序排，所以中间某几个 commit 单独 `checkout` 出来未必能跑通
> 流水线——它们是**可读的模块切分，不是可二分的历史**，这一点写在最后一个
> commit 的正文里，免得有人拿它当 bisect 用。

**push 之前要做的第一件事：造一个"别人的机器"。**

上面那句"push 之后看 CI 绿不绿"是不够的——**CI 红了的时候，你应该已经在本地
先红过一次**。做法是把仓库克隆到一个空目录，在那里跑一遍测试：

```bash
git clone <仓库> /tmp/xm3-check && cd /tmp/xm3-check/backend && pytest -q
```

这一步真的抓到了东西（见 [`问题记录.md`](问题记录.md) 问题 55）：
克隆里是 `1 failed, 942 passed`，而本机是 `943 passed`。
差别只有一个——**克隆目录里没有 `backend/.env`**，于是一条默默依赖本机
密钥文件的用例在那里红了。也就是说 **CI 的 pytest job 从第一次跑就会红**，
而在做这一步之前，这件事**不可能被发现**：那个差别只在别人的机器上出现。

**push 之后要做的第二件事**：看 CI 是不是真的绿。
本机的 ruff 守卫（[`test_lint_clean.py`](backend/tests/unit/test_lint_clean.py)）
只覆盖 CI 的第一步，**`pytest` / `vitest` / `eval --gate` 三个 job 里
有没有别的坑，只有真跑过一次才知道**。
`vitest` 那 314 条尤其没被"克隆验证"覆盖到——它要 `npm install` 才跑得起来，
所以那一刻它仍然是**推出来的**，不是读出来的。

---

## B 类 · 还没建的工程件（阶段 9–10）—— ✅ **已补齐**

这一节写于阶段 5 之前，当时上面列的东西**一样都不存在**。
十个阶段做完之后逐项复核，全部落地：

| 当时缺的东西 | 现状 |
|---|---|
| `eval/judges/` | `faithfulness.py` + `rubric.py`（两个裁判），各有单元测试 |
| `eval/run_eval.py` | 四种模式：`--gate` / `--replay` / `--live` / `--from-db` |
| `loadtest/` | `locustfile.py` + 四个场景 + 报告存 `loadtest/reports/` |
| `scripts/dev.sh` / `dev.ps1` / `stop.sh` / `stop.ps1` | 4 个都在，先探端口再起，**端口被占时直接失败而不是换一个** |
| `docs/*.md` | 7 份：ARCHITECTURE / AGENTS / PROVIDERS / API / DATA_MODEL / EVAL / DECISIONS |
| `开发文档.md` / `技术栈.md` / `项目总结.md` / `面试问答集.md` | 4 份都在；加上 `问题记录.md`（55 条）与 `修补文档.md`（本文） |
| 顺序守护 | 不叫 `check_docs_order.py`，实现成了 `backend/tests/unit/test_docs_order.py`——**和别的测试放一起，`pytest` 一跑就带上，不需要额外记一条命令去跑** |

**只剩一行没关**：

| 缺的东西 | 现状 | 什么时候 |
|---|---|---|
| 黄金集录制 | 录过 **5** 条（`MANIFEST.json` 里 `measured.queries = 5`），**现在回放得出来 4 条** | 要用时说 |

其余 18 条只在 mock 下跑过，而 mock 的三个 provider 都不看 query——
所以它们的"证据形态差异"实际上**从未被真实模型吃过**。

**账和事实差一条，差的正是 `vague-coffee-market`。**
`MANIFEST.json` 记着录过 5 条（名单就在文件里），`--replay` 实际过闸 4 条——
那条录制品已经对不上 key 了。它是这 5 条里**唯一走澄清路径**的
（黄金集里标着「歧义」），而那是最容易因为一次提示词改动而整条作废的一步。
**能指出是哪一条，指不出是哪一次请求**——详见
[`问题记录.md`](问题记录.md) 的问题 51。
这一条同时写在 [`docs/EVAL.md`](docs/EVAL.md) 的「做不到的事」与
[`技术栈.md`](技术栈.md) 负面清单的第 8 条里。

**为什么有意不录满**：跑这 5 条的那一轮，流水线记下的用量是 90 次 LLM 调用、
571,355 prompt tokens、**$0.1326**——**注意这个数是流水线自己算的调用成本，
不是账单**（`MANIFEST.json` 自己在 `costUsdNote` 里标了这件事，
而同一轮里 `freshRecords` 全为 0，说明它一行新记录都没写进去）。
这一点在 [`问题记录.md`](问题记录.md) 的问题 39 教训 4 里专门写过：
**凡是会被读成金额的数字，旁边必须有它是什么的说明。**

更关键的是**回放质量取决于录制时的代码版本**——改一次提示词，旧录制品就作废
（问题 38 就是这么来的：改完提示词回放直接报 `CassetteMiss`）。
22 条全录意味着每次动提示词都要重付一遍。5 条够覆盖"正常 / 歧义 / 单源"
三种形态，**够用就不多买**。

**要录满时的命令**（可续录，已录的 key 会复用而不是重买）：

```bash
cd backend && CASSETTE_MODE=record ./.venv/Scripts/python.exe -m scripts.record_cassettes --yes
```

先 `--dry-run` 看预估，它会打印每条 query 的 token 与成本上界。

---

## C 类 · 已知取舍：**决定不修**

这一类要单独放，因为它们在面试里是加分项，而**动手改反而更差**。
写文档时照抄这里的理由即可。

### C1. cassette 回放不忠实于时间

实测（`问题记录.md` 问题 11）：一次真实录制与它的离线回放，33 个指标里
**31 个逐位相同**——成本、token、证据数、独立域名、维度覆盖、幻觉引用率、
质量门、连评审评语都一字不差。只有三个变了：`durationMs`（50859 → 139）、
`firstEvidenceMs`、`slowestCallMs`。因为回放不发网络请求。

**所以：在 cassette 上断言耗时，会得到一个恒绿的用例**（拿 0 和 108 比），
而它守的恰恰是性能。耗时类指标只能在 `pytest -m live` 里测。
`reportId` 同理不可断言——它是 `uuid4()`。

这是设计边界，不是缺陷。已写进 `providers/cassette.py` 的模块 docstring。

### C2. 汇率是一个有日期的常数

`_DEFAULT_FX_CNY_PER_USD = 6.7670`（`providers/llm/deepseek.py:71`），
来源是 2026-09-15 的中间价。汇率会漂。已提供 `DEEPSEEK_FX_CNY_PER_USD`
环境变量覆盖，非法值会 warning 并退回默认。

不变的原因：`cost_usd` 是**可比单位**，可比性来自"所有人用同一个常数"。
让它自动随行情浮动，历史报告之间就不可比了。

### C3. `_FLASH_MARKER = "flash"` 是子串判定

`providers/llm/deepseek.py:194`。已知代价写在注释里：将来若出现名字含 flash
但定价不同的变体（比如 flash-lite），它会被按 flash 计费。

选这个代价是因为另一侧更糟：**漏价是静默的成本 0**，而偏低的成本不会有人去复查。

### C4. 峰谷价按"调用结束时刻"判定

真实计费按 token 生成的时刻。跨过峰值边界的调用会被整体算成一边。
单次调用量级下误差可忽略，没有必要为它引入流式时间戳。

### C5. 缓存命中率只有 0.8%，这是结构性的

实测命中率 0.9%（另一次 0.8%）。原因不是配置问题：
**12 个章节是并行撰写的**，同一份前缀被同时发出去，缓存还没建立就已经全部请求完毕。
cache-hit 输入价 0.02¥/Mtok vs miss 1¥/Mtok，差 50 倍，所以这个数字看着刺眼。

但它是"并行换吞吐"的代价。要提高命中率就得改成串行或分批——
那是**拿墙钟时间换钱**，在交付前不该动。真要做，做法是把同一个品牌的所有章节
串成一条链，让第二个章节能吃到第一个建立的缓存。

### C6. 整份报告 JSON 存 `reports.data` 一列

读放大无所谓（一次访问读一份报告），拆成 12 张规范化表只会换来 JOIN 风暴。
代价写进 `技术栈.md` 的"已知代价"一节。

---

## D 类 · 小问题：攒着一起修，别单独开一轮

### D1. 降级文案把两类东西数成了一个数（**会误导**）—— ✅ **已关闭**

**关闭方式**：`TeamOutcome` 拆成四个字段，`describe()` 逐类说明。

原先 `dropped: list[str]` 一个列表塞了四种东西——模型编的 id、合法但超上限的 id、
层级不符的 id、整类回退的标记——而文案把它们统一说成
"有 N 个专家 id 不存在于名册，已替换为默认队伍"。其中**只有第一种真的不存在**。
实测那句"有 11 个专家 id 不存在于名册"就是这么来的，它听起来很确定，但是错的。

现在：

| 字段 | 含义 | 是不是"不存在" |
|---|---|---|
| `phantom` | 名册里查不到的 id | 是 |
| `wrong_level` | 存在但层级与角色不符 | **否**（模型没编，只是没读层级要求） |
| `over_limit` | 合法但超出该类上限被截掉 | **否**（这是我们的取舍，不是模型的错） |
| `defaulted_roles` | 整类回退到默认队伍的角色 | **否**（是回退，不是丢弃） |

守它的是一条**专门的反回归测试**：`test_a_wrong_layer_id_is_not_reported_as_a_phantom`
——把 `L2-007`（真实存在，品牌与传播分析师宋允）当组长送进去，
断言 `phantom == []` 且 `describe()` 里出现"层级不符"、**不出现**"不在名册"。

这条测试同时守着一个文档细节：`L2-007` 在 15 人名册时是个合法的幻造 id，
48 人名册里它成了真人。用一个"历史上确实不存在过"的号来守这条规则，
比随便编一个 `L9-999` 更贴近它要防的事。

<details>
<summary>展开：关闭前的原始记录</summary>

`pipeline/dispatch.py` 往 `dropped` 里塞了两种东西：

```python
dropped.append(expert_id)                # 模型编造的非法 id
dropped.append(f"<{role} 使用默认队伍>")   # 整类回退的标记
```

而文案是：

```python
ctx.degrade("专家调度", f"有 {len(dropped)} 个专家 id 不存在于名册，已替换为默认队伍")
```

这个比 A1 更小（A1 修完它就自然消失），但性质不同：A1 是缺内容，
这是**观测通道说了不准确的话**。四铁律里"全程可观测"的价值全在于
报告里的每一句话都能被相信，所以哪怕它自己会消失，也值得记一笔。

</details>

### D2. CLI 的 `node_update` 渲染读错了键名 —— ✅ **已关闭（实际范围比原文大得多）**

**原文说**：一处键名写错，兜底 `label` 掩盖了，影响是"极少数没有 label 的事件
会打印空阶段名"。

**实际是**：CLI 渲染器**六种事件全部渲染成空白**。原文只看到了它写错的那一处，
没有发现它错得系统。`NESTED_PAYLOADS` 表原本不存在，渲染器一律按"字段在顶层"
去读，而契约里六种事件的载荷是**嵌套**的：

| 事件 | 契约里的位置 | 从前印出来是 |
|---|---|---|
| `thought` | `data.thought.*` | `      · ：` |
| `message` | `data.message.*` | `      →  ⇒ ：` |
| `evidence` | `data.evidence.*` | `      [ ]    ? ` |
| `trace` | `data.span.*` | `      ⏱      0ms` |
| `chart` / `image` | `data.chart.*` / `data.image.*` | 空行 |

**还有另外三处，性质比键名写错更严重——它们都在"缺字段时给了一个默认判断"，
而不是不说：**

1. `done` 读顶层的 `elapsedMs` / `reworkRounds`，而契约已把这两个键移进 `metrics`。
   读不到就落回默认值 0，于是终端上**永远**印"耗时 0ms，返工 0 轮"——
   两个看起来像真实测量结果的 0。
2. `report_ready` 读不存在的 `quality`，读不到就当"未通过、完整度 0%"。
   于是一次**质量门通过**的报告在终端上印着"质量门=未通过"，
   而同一段输出里审计专家刚说过"质量门结论：通过"。**自相矛盾的两行字，
   读者只会相信错的那一行。**
3. 函数 docstring 里写着"渲染逻辑单独成函数并被测试覆盖"——**当时没有任何
   CLI 测试文件**。一句话把上面全部缺陷盖住了。现在这句话是真的了。

**根因值得单独记**：它没被发现，是因为 CLI 的"验收"就是看一眼有没有报错。
一个把整个叙事输出印成空白的 bug，在退出码上是完全干净的。
`tests/contract/test_sse_contract.py` 的 docstring 恰好拿 `node`→`stage` 这个改名
当过例子——它防的是**前端**读 `e.node`。CLI 是第三个消费者，没人查。

**修法与守卫**
新增 `tests/unit/test_cli_render.py`（32 条）。其中最有价值的一条是
`test_nested_payload_map_matches_the_contract`：它拿契约里的 `shape` / `required`
比对 `NESTED_PAYLOADS`。现在**同一件事有三份独立声明**——契约一份、
契约测试里的 `CARRIED_KEY` 一份、渲染器一份——三份必须对上。
刻意不把契约读进来当唯一真相源，读进来就失去了交叉验证。

**守卫已反证**（`scripts/falsify_cli_render.py`，见 `问题记录.md` 问题 14）：
把五个缺陷逐个放回去，每条守卫都真的变红。

| 变异 | 失败的测试数 | 证人 |
|---|---|---|
| 嵌套载荷按顶层读 | 5 | `test_a_thought_renders_its_expert_and_text` |
| `node_update` 读 `node` | 3 | `test_it_does_not_read_the_obsolete_node_key` |
| `done` 读顶层 | 2 | `test_it_reads_the_numbers_from_metrics` |
| `report_ready` 编造结论 | 3 | `test_it_does_not_invent_a_quality_verdict` |
| `NESTED_PAYLOADS` 与契约漂移 | 2 | `test_nested_payload_map_matches_the_contract` |

<details>
<summary>展开：关闭前的原始记录</summary>

`scripts/run_pipeline_cli.py`：

```python
node = data.get("node", "")       # ← 键名应该是 stage
```

而 `pipeline/runner.py` 明确写了：

> 键名是 `stage` 不是 `node`。事件里那个值就是一个 StageId。

现在不炸，是因为兜底 `data.get('label', node)` 拿到了 label。
表现是极少数没有 label 的事件会打印空阶段名。

**修复是一行，但值得记的是"测试没盖住一个错误的假设"**——
CLI 是阶段 2 的核心验收工具，它的测试却只断言退出码。

</details>

### D3. 澄清路径只被 `auto_clarify=True` 跑过

真实交互（用户填答案 → `POST /api/tasks/{id}/clarify` → 继续跑）
**没有任何测试覆盖**，因为那个接口还不存在（见 A4）。
现在 `clarify_answers` 只有测试用 `auto_clarify` 直接灌进去。

`intake.py` 里的需求澄清逻辑是否对"首轮问了、第二轮才拿到答案"这种
两段式输入成立，目前是**未验证的推断**。

### D4. 临时数据库文件

`backend/data/` 下有 `cli_smoke.db` / `golden.db` / `replay_check.db` / `xm3.db`，
以及 `reports/` 下十来个 `TK-*.json`（mock 跑出来的报告）。全部被 `.gitignore`
盖住（`backend/data/*.db`），不会入库，**不是问题**。

两个要留意的：

- **`golden.db`** —— 装着那条真实黄金集录制的落库结果。删了不花钱
  （cassette 在 `tests/fixtures/cassettes/`，不在这个库里），但重跑要重新花钱。**留着。**
- **`reports/TK-*.json`** —— 现在没用，但**别急着删**。阶段 9 的 Playwright 冒烟
  需要一份静态报告 JSON 当 fixture（计划里写的就是"用静态 fixture JSON
  打开 `/report/:id`"）。挑一份证据量正常的，提升到 `tests/fixtures/` 里**提交进仓库**——
  `data/` 下的东西是 gitignore 的，不进库就传不下去。挑的时候记得它来自 mock 运行，
  里面的品牌名是"示例调研对象"这类占位词，不是真实数据。

  > 阶段 7 那一半**已经用另一种方式还了**：`frontend/src/test/fixtures/realReport.ts`
  > 是从**真报告**（`GET /api/reports/RP-8b4e09569307`）裁出来的 TS 夹具，
  > 给 vitest 用。它和 Playwright 要的那份不是同一个东西（一个是模块、一个是 JSON 文件），
  > 但**来源可以共用**：等阶段 9 做冒烟时，从同一个真报告导出 JSON 即可，
  > 不必再去挑 mock 的 `TK-*.json`——真报告里的品牌名是真品牌，
  > 截图更能当作品集素材。

（本轮验收跑出来的 `phase4_check.db` 和 `_verify13.json` 已删除。）

### D4b. 真库 `xm3.db` 里的任务数是被测试污染的，还没清

问题 28 修好之前，测试往真库里插过一百多条假任务。现在库里：

| | 数量 |
|---|---|
| `tasks` 合计 | 131 |
| 其中 `done` | 13（**真的**，13 条全都有报告、有证据行） |
| 其中 `failed` | 118 |

**118 条失败任务全部是同一条 query**，按 query 分组一查就清楚：

```
 118  '对比 Notion 与 Obsidian 的定价与协作能力差异'   ← 测试常量 CLEAR_QUERY
   6  '对比 Notion 与 Obsidian'
   3  '对比 Notion 与 Obsidian 在团队协作场景下的差异'
   2  '对比 Notion 与 Obsidian 的定价与功能差异'
   1  '帮我看看那个笔记软件'                            ← 测试常量 VAGUE_QUERY
```

也就是说 131 行里**只有 12 行是人真的发起过的**，119 行是测试插的
（118 条 `CLEAR_QUERY` + 1 条 `VAGUE_QUERY`）。真报告那 13 条不受影响——
仪表盘的来源数 266、引用数 2426 都来自它们。

**影响**：竞争情报中心的"发起过 N 次调研"和"通过率"两个数现在是假的。
作品集演示时这两个数会被问到，讲不圆。

**还没做的原因**：删库是不可逆的，而仓库还没有 git（A5）。
清理之前先备份一份 `data/xm3.db`，再按上面那个判据删。
也**不要**为了好看把 `tasks` 清到和 `reports` 一样多——
两者本来就不是一个数，仪表盘上那个"分母是报告数不是任务数"的
口径提示（问题 27 的第二对）就没东西可指了。

### D5. `uvicorn --reload` + SQLite 这条坑还没在 xm3 上复现

`app/db/connection.py` 的模块 docstring 写着"一个 Windows 上的坑"，
那是**在 Verda 上实测的**，xm3 上还没复现过。

写 `问题记录.md` / `技术栈.md` 时必须说清来源。现在的措辞
（"参考实现的一个已知症状"）已经是对的，别在搬运时把"参考实现实测"
写成"本项目实测"——这是文档可信度的底线。

### D6. 「我的调研」列表拿不到报告 id，所以那页永远给不出报告链接

`TaskRecord.to_dict()`（`core/models.py`）**没有 `reportId`**，
而 `tasks` 表也没有这一列——报告与任务的关系只存在 `reports.task_id` 上。
于是 `GET /api/tasks` 的每一条里都没有报告 id，
而前端 `TaskRecord` 类型是照它逐字对齐的（域模型注释里写着为什么删掉了
那个字段），所以这一页结构上就没法渲染"去看报告"。

`GET /api/tasks/{id}`（快照）**有** `reportId`，所以工作台上那个链接是好的。

**这不是回归，是一处从没兑现的能力**：列表页从建立起就没有过这个链接。

> ### ✅ **已关闭**（2026-09-19）
>
> 上面那段的前提已经不成立了：这一页现在读的是 `GET /api/reports`
> （`ReportSummary` **带 `reportId`**），不是 `GET /api/tasks`。
> 每张卡片的标题是 `/report/{reportId}` 的链接，右侧另有「读报告」按钮，
> 以及 Markdown / JSON 导出。所以"拿不到报告 id"这个阻塞点消失了。
>
> `TaskRecord` 仍然没有 `reportId`、`tasks` 表仍然没有这一列，
> 而**这是对的**：报告与任务的关系本来就只在 `reports.task_id` 上，
> 往任务表里加一列冗余外键是给一个已经能回答的问题多存一份答案。
> D6 的正确结论不是"补那一列"，而是"这一页不该去问任务表"。

---

## E 类 · 已经关掉的（备忘，不需要动作）

- `contracts/sse_events.json` 里 `retryable` 的挂账——**已关闭**。
- `问题记录.md` 的 11 条，全部已修并验证。
- **A1** 名册 15 → 48（阶段 4）。
- **D1** 降级文案把四类问题数成一个数。
- **D2** CLI 渲染器六种事件渲染成空白（范围比原文大得多）。
- `validate_team()` 只查 id 存在、不查层级——**真漏洞**，已修，
  `问题记录.md` 问题 12。
- CLI 渲染器没有任何测试，而 docstring 声称有——`问题记录.md` 问题 13。
- **D6** 「我的调研」拿不到报告 id（那一页现在读 `/api/reports`，不读任务表）。
- **F1** `report_ready` 的 `title` 挂账——阶段 7 的结论是**不加**，已结清。
- `types/domain.ts` 的 `Issue` + `types/events.ts` 的 `TeamPreview`——
  两个都是死代码，且 `Issue` 的形状与服务端发的**不是同一样东西**。
  一并删除，换成照着 `Issue.to_dict()` 写的 `ReportAuditIssue`。见问题 30.3。
- **`/api/tasks/{id}/trace` 的三个口径问题**（2026-09-19）——
  `durationMs` 是并发相加而非任务耗时（实测 4.3 倍）、`failedCount` 把
  `degraded` 当成失败（24 条降级印成 24 次失败）、`span_tree` 的分支
  从没被真实数据走到。三条都已修，见 `问题记录.md` 问题 33。
  顺带补上这个接口此前**一条测试都没有**的空白（新增 19 条）。
- **`GET /api/reports/{id}/trace` 刻意不做**（2026-09-19，阶段 8 的决定）——
  `GET /api/reports/{id}` 已经返回 `taskId`，再开一条路径读同一批 span
  就是同一份数据有两个入口。契约文件与 `types/domain.ts` 里指向这条
  路径的注释都已按结论改写。
- **`scripts/_mutate_check.py` 会把混用行尾的文件悄悄归一**（2026-09-19）——
  改成按字节读、按原行尾写回，并把判定打在输出里。见问题 33.5。
  `问题记录.md` 本身也是这么被 `cat >>` 追加成 CRLF/LF 混用的，
  已统一为 LF（与其余 `.md` 一致，内容指纹未变）。
- **"113 与 117"：一个真数被接到了另一个问题上**（2026-09-19）——
  `firstFetchIndex` 的注释用了"第一轮证据盖章之前结束了多少条 span"那个数
  （113）去回答"第一个 fetch 排第几位"（117）。两个数都是真的，
  只是答的不是同一个问题，而差 3% 的错数最难发现。已改正，
  顺带量到一条更强的证据：证据盖章发生在轮次**开头**（比第一条 fetch
  开始还早 0.47 秒）。见问题 34。
- **前端自己发明了第二套"第几步"**（2026-09-19）——
  后端 `Tracer.spans()` 按 `spanId` 编号排（编号在 span 开始时发，
  所以它"就是"开始顺序），而 `lib/replay.ts` 第一版按 `startedAt` 排。
  两者**在结构上可以不一致**：编号在锁内发、时间戳在锁外取，
  并发开 span 时（搜索与抓取都是 `asyncio.to_thread` 扇出）那道缝足以让
  "编号 41 的时间戳晚于编号 42"。真库 15 个任务两种排法**当前完全一致**，
  所以是潜伏分歧而非正在错的行为——但它是两个定义。
  前端已改为编号优先、时间戳只做并列兜底；`buildTimeline` 里那层
  多余的"按 at 再排一遍"也删了（它制造第三种顺序，并让 `cumulativeMs`
  不再是前缀和）。后端补了一条与时间戳精度无关的守卫。见问题 35。
- **前端反证脚本里 3 条突变早就死了，而它照报成功**（2026-09-19）——
  `clearScheduled()` 与 `isSettled()` 两次重构各带走一到两条锚点，
  而"锚点失效"不进退出码，所以 47 条里实际只有 44 条在验证、
  退出码却是 0。已修：锚点失效现在让退出码变红，0 次与 >1 次分开报，
  **并加了开跑前的锚点预检**（唯一一种开跑前就能知道的失败，
  从"一小时后"提前到"一秒内"）。三条锚点已重新锚定，
  并发现"只删一半是抓不住的"。见问题 36。

---

## F 类 · 现在**不要**做的事

有人（包括未来的我）看到这份清单会想去动这几样，所以单独列出来：

1. ~~不要现在补 `report_ready` 的 `title` / `quality` / `completeness`。~~
   **挂账已到期并结清**（2026-09-19，阶段 7）：标题的结论是**仍然不带**——
   报告页直接读正文的 `subject`、没有时退回 `query`，真相源仍然只有一个；
   `quality` / `completeness` 也仍然不在事件里（它们随 `metrics` 与正文走）。
   契约里那段说明已按结论改写，不再是"挂账"。
   所以这一条现在的形态是：**不要为了"事件里看着更完整"而把它们补进去。**
2. **不要现在录完 22 条黄金集。** 见第 0 节——阶段 4/5 会全部作废。
3. **不要现在调缓存命中率。** 见 C5，那是拿时间换钱，且会动到 prompts，
   又一次作废 cassette。
4. **不要在 cassette 上断言耗时。** 见 C1，那会得到一个恒绿的用例。
5. **不要改 `experts_seed.yaml` 里任何已有 id。** 见 A1，那是数据迁移。
   `roster.py` 已删除，但 id 布局被锁进生成器并由测试断言：
   L3-001..003 / L2-001..009 / L1-001..024（行业）/ L1-025..036（职能）。
   改了就是改号，而号已经写进 `expert_stats` 表和历史报告。

---

## 附：「完成」的验收清单

把这份文档清空的标准（对齐计划里的端到端验证）：

- [x] A1 名册 48 人，实跑不再出现"专家 id 不存在于名册"降级
- [x] A2 返工能吃上预算，`metrics_after > metrics_before` 为真
      ——两个缺陷都修了（共用一个池子 + 复跑首轮查询），新增 16 条单元
      + 9 条集成测试，端到端守卫用**让首轮真的失败一个维度**的夹具逼出返工。
      反证三条突变全部抓住、逐字节还原。见问题 23
- [x] A3 三条测试绿
      ——返工触发（9 条）、维度覆盖反回归（12 条）、`degraded` 强断言（2 条），
      反证 C1/C2/D1/D2 全红后逐字节还原。**注意第三条原本会写成恒真断言**，
      见问题 24
- [x] 阶段 7 后端一半：导出（`core/report/export.py`）+ 报告接口
      （`api/routes_reports.py`：列表 / 详情 / 导出 / 批注），44 条新用例
      ——**两个缺陷都是本轮自己写出来的**：中文主题塞进 `Content-Disposition`
      让每一份报告导出都 500（HTTP 头只能 latin-1），批注行直接透传导致
      前端读到 `undefined` 而**什么都不显示**。反证 R1–R3、R5、R5b 全红；
      **R4 第一次报"漏掉"，查下去发现是我的断言被自己的说明文字满足了**，
      而 R5 上一版是**空操作突变**（只改了文字没改位置）。见问题 25
- [x] 「深化本节」：`POST /api/reports/{id}/refine`（批注驱动二次调研）
      ——44 条新用例（21 集成 + 23 纯函数单元）。反证 11 条突变全部抓住、
      逐字节还原。两个缺陷都是当场犯的：`except LookupError` 把
      `KeyError` 一起吞了（错的 sectionKey 返回 422，404 是死代码），
      以及一个只在人造例子上生效的切词器（按空格切中文 →
      英文词和中文粘在一起就丢）。见问题 26
- [x] 侧边栏外壳 + 五个导航页：工作台（`/`）、我的调研（`/library`）、
      知识库（`/knowledge`）、专家（`/experts`）、竞争情报中心（`/dashboard`）
      ——`AppShell`/`Sidebar`/`TopBar` + 路由分两组（侧边栏组 / 全屏沉浸组，
      分组依据是"这一页要待多久"，见 `App.tsx` docstring）。
      配色由深色整体改为暖色浅色，`@theme` 里的令牌全部重写并核对过
      **没有悬空工具类**。73 条新后端用例（证据库 9 / 队伍 8 / 任务清理 8 /
      证据接口 12 / 专家接口 16 / 仪表盘 13 / 身份同步 7），
      反证 29 条突变全部符合预期。见问题 27
- [x] 报告页（阶段 7 前端一半）：`/report/:reportId` 端到端渲染真报告
      ——三栏布局（目录 / 正文 / 证据栏）+ 全部结构化块，14 处 `ReportPanel`，
      TOC 锚点不变量、降级横幅、批注工具条都在用例里。6 条渲染用例用的是
      **真实 payload 裁出来的夹具**（`test/fixtures/realReport.ts`），
      第一条就跑出一个**必崩**的 `TypeError`（`metrics.rework.delta`
      不存在，真值在 `changes[key].delta`），紧接着是第二个
      （`reworkLog` 是对象数组、类型声明成了 `string[]`）。
      **夹具末尾那个 ` as ReportBody` 一去掉，`tsc` 又报出 6 处类型与现实不符**
      ——这 6 处此前穿过好几个窗口都没人发现。见问题 31
- [x] 报告页上三处"看不出来的缺失"
      ——目录里的**死锚点**（点数不动，读者会以为页面坏了）、
      被丢掉的 `yAxis` 量表（4.2 与 4.4 看着差一倍，而标题写着"评分"）、
      两处没有类型的降级披露（`siteFilterFolded` / `llmFallback`，
      降级过的报告和干净的报告看起来一样）。见问题 32
- [x] `GET /api/tasks/{id}/trace` 的口径修对并补上测试（阶段 8 的地基）
      ——这个接口在阶段 8 之前**一条测试都没有**，而它身上有三个
      "不报错、不崩、只是数不对"的地方：`durationMs` 是并发相加而非任务耗时
      （实测 183 秒的任务它是 789 秒，4.3 倍）、`failedCount` 把 24 条
      `degraded` 数成"24 次失败"、`span_tree` 的兜底分支**从没被真实数据走到**
      （1036 行里 `parentId` 全为空，全仓库没有一处嵌套的 `with span(...)`）。
      新增 19 条用例 + 后端反证 6 条（`42/42` → `48/48`）。见问题 33
- [ ] **阶段 8 动工前复查：回放不能靠"证据捕获时间"驱动**（设计约束，已量化）
      ——计划里那句"拖动回放滑杆，被引证据面板跟随"有一根支柱不成立。
      对真实数据量出来的两条：

      | 事实 | 数字 |
      |---|---|
      | `evidences.captured_at` 每个任务有几个不同取值 | **2 个** |
      | `TK-1d1ff1f7090b` 的分布 | 14:48:42 ×84，14:49:42 ×24 |
      | `TK-aea39ac70af5` 的分布 | 21:09:55 ×188，21:11:38 ×24 |

      也就是说 `captured_at` 是**按采集轮次盖的章**（第一轮 84 条、返工轮 24 条），
      不是逐条捕获时刻。按"游标时间 ≥ 捕获时间"来reveal 的话，
      滑杆走过 45% 的行程面板都是空的，然后**一次性弹出 84 条**——
      那不是回放，是三段跳。

      **改用的两根支柱**（都验过）：

      1. **滑杆走 span**：249 条 span 的 `startedAt`/`endedAt` 是毫秒级的，
         14:47:26 → 14:50:30 有 249 个真实台阶，这一段是真的。
      2. **span → 证据按域名连，不按时间连**：`fetch` span 的
         `detail.domain` 与证据 URL 的主机名 **100/100 全部对得上**
         （该任务 100 条 fetch、63 个主机、108 条证据）。
         所以"这一条 span 抓的是这些证据"是能算出来的。

      **于是证据面板的职责要改写**：它不是"随时间逐条冒出来"，
      而是"跟着当前 span 高亮/过滤"。这个差别必须在页面上说清楚，
      否则用户会以为滑杆坏了。同一份数据也不支持更细的粒度——
      `span.detail` 里**没有任何 evidence id**（`search` 只有 `{sites, hits}`，
      `fetch` 只有 `{domain, chars, boilerplate, degraded}`，
      `llm` 只有 `{tier}`），所以引用级联动只能落在报告那一侧
      （`citations` 给出 evidence↔编号），不能落在 span 这一侧。
- [ ] A4 全部页面 + API 端点
      ——路由现在有 8 条：`/`、`/library`、`/knowledge`、`/experts`、`/dashboard`、
      `/clarify/:taskId`、`/workspace/:taskId`、`/report/:reportId`。
      阶段 8 还差三条：知识图谱（`/graph/:reportId`）、Trace、专家详情
      （`/experts/:id`）。
      **Trace 的路由定为 `/trace/:taskId`（带可选 `?report=<id>`），
      不是 `/trace/:reportId`**：span 天然属于任务；报告要回放时先读报告
      拿 `taskId` 再过来，所以报告号只能是一个可选的覆盖层参数。
      相应地，`GET /api/reports/{id}/trace` 这个别名**决定不做**——
      报告详情已经返回 `taskId`，再开一条路径读同一批 span 就是
      同一份数据有两个入口。契约与 `types/domain.ts` 里指向它的注释已改写。
      「去看报告」原来指向不存在的路由那笔欠账**已清**——
      `App.tsx` 里那段说明（原文就是记这笔债的）也一并改写了
- [ ] B 类全部补齐（黄金集、评测、压测、脚本、文档）
- [x] 测试不再碰生产库
      ——`conftest.py` 的 `_never_touch_production_db`（autouse）默认把
      `DB_PATH` 指到临时目录；`test_health_api.py` 加一条守卫用例，
      `TestSeqAfterRestart` 补上它本来就需要的夹具。反证脚本第 30 条
      就是删掉那行 `DB_PATH`。见问题 28
- [x] 工作台不再对着一个跑完的任务无限重连（`调研中 / 重连中 / 14%`）
      ——两个缺陷：服务端发完历史正常关流、而 `EventSource` 把"正常关闭"
      当成"掉线"（实测 149 次请求）；以及 `scheduleFlush()` 只挂
      `requestAnimationFrame`，后台标签页里它是被**暂停**的，界面冻在
      切走那一刻的快照上。修法是把"这次断开是不是预期的"收敛到一个
      `settleIfTerminal()`，并给合帧加 250ms 兜底；`ConnectionState`
      新增 `ended`（与 `fatal` 相反、与 `idle` 不能合并）。
      **端到端用真 `EventSource` 打真后端验证过，含一次对照实验**：
      对照（关掉修复）14 秒内 5 次请求且用例红，修复后 1 次且用例绿。
      8 条新用例，反向突变 M1–M5 全红；M6–M9 证伪不了，已在注释里
      标明"不是被测试保护的"。见问题 29
- [x] A5 `git init` 完成，历史按模块拆成 8 个 commit（骨架与契约 / provider /
      数据层 / 流水线 / 名册 / API 与 SSE / 前端 / 评测与 CI 与文档），
      `.env` 确认没进任何 commit
      ——**2026-09-21 完成。** 做法是先 `git add -A` 再逐条 `git diff --cached --name-only`
      看清单（这一步确实挡下了东西：`.env` 挡住了，但
      `backend/data/reports/` 那 13 份运行时报告没挡住，是这一眼看见的）。
      另外补了 `.gitattributes`：本机 `core.autocrlf=true` 而仓库里没有它时，
      **入库的是 LF、检出的是 CRLF**，`scripts/dev.sh` 的 shebang 会变成
      `#!/usr/bin/env bash\\r`——**作者本机跑得通、克隆的人跑不通**。
- [x] A5 的下一步：**push 之前先"照陌生人的做法"验一遍**
      ——把仓库克隆到一个空目录再跑测试。这一步抓到了问题 55：
      **克隆里 `1 failed, 942 passed`**（本机 `943 passed`），
      因为有一条用例默默依赖了本机的 `backend/.env`。
      也就是说 **CI 的 pytest job 从第一次跑就会红**，
      而这件事在"克隆验证"之前不可见——`.env` 是 gitignore 的，
      那个差别只在别人的机器上出现。
      已修（[`问题记录.md`](问题记录.md) 问题 55），现在两边都是 `945 passed`。
      **这一条值得当成本项目的第一步常规动作，而不是一次性的：**
      CI 绿不绿是个读数，而"本机绿"不是它的证据。
      ——**这件事现在比之前更急**：真库已经被测试写脏过一轮
      （问题 28），而仓库还没有版本控制，任何删除都找不回来
- [ ] `bash scripts/dev.sh` 起得来，`/health` 返回当前 provider
- [ ] `pytest -q` + `npm run test` 全绿，零 API 花费
      ——后端已 `945 passed`（6 条 live 默认排除）；前端 `314 passed / 17 个文件`，
      门禁三条：`tsc -b --noEmit`、`oxlint src --deny-warnings`、`vitest run`
      （**三条都要跑**：`npm run build` 不含 lint，见问题 21）。
      两个反证脚本**分开跑**：后端 `python -m scripts._mutate_check` `49/49`，
      前端 `python scripts/falsify_frontend.py` `56/56`——两边都是
      **0 漏掉 / 0 锚点失效 / 0 仪器崩 / 逐字节还原**，
      而且**都要看退出码**：后端那个一直把"测试没抓住"算进退出码，
      前端那个到 2026-09-20 才补上（见问题 53——在那之前，
      一条真的漏会让它退出 0，长的就是"全绿"）。
      （前端那个改过目标文件之后必须 `--resnapshot`，否则它会拒绝开跑——
      判据是"与快照逐字节相同吗"，见该脚本 `main()`。
      另外它一轮要跑**四十多分钟**：56 条突变，每条都重跑 tsc + vitest。
      中途看进度：输出加 `-u` 再看那个重定向的文件，否则不到最后一行什么都看不到。）
- [x] CLI 跑 mock 出完整事件流 + 合规报告
      ——渲染器已修（D2），叙事输出不再是空白行
- [ ] 浏览器走完整一遍：澄清 → 工作台 → **中途断网 10 秒** → **中途刷新** →
      报告页 → 决策回放 → 图谱 → 仪表盘
      ——**怎么走、每一步看什么，见「附二」。**这一步不能靠"跑一遍看看"：
      界面不显示 seq，缺口在肉眼上看不出来，判据在 DevTools 的原始帧里
- [x] 反证脚本能认出**被强杀**留下的现场，并且能按字节还原
      ——`falsify_frontend.py --status` / `--restore` / `--resnapshot`，
      快照在 `REPO/.falsify-baseline/`（**不能放进 `frontend/`**，见问题 22）。
      实测过一次真强杀：只点名 `taskEvents.ts` 并显示到具体哪一行，还原字节一致
- [ ] `python -m eval.run_eval --gate` 绿；`--live --repeat 3` 的数字已写进文档
- [ ] `locust` 压测无 `database is locked`
- [ ] 陌生人照 README 能跑起来

---

## 附二：浏览器验收怎么走

这一节存在的理由：清单上"浏览器走完整一遍"是**唯一一条没有自动化**的验收，
而它同时又最容易被走成"看起来没问题"。界面不显示 `seq`，所以"无缺口"
在肉眼看是看不出来的——**判据在 DevTools 的原始帧里**，不在界面上。

### 0. 前置：让这次运行慢下来

mock 快到 0.2 秒跑完一整个 quick 任务。**不慢下来，断网 10 秒这件事根本来不及发生**，
刷新也刷不到"跑到一半"。

```bash
cd backend
MOCK_LATENCY_MS=4000 LLM_PROVIDER=mock SEARCH_PROVIDER=mock FETCH_PROVIDER=mock \
  PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8020
```

实测 4000ms 下一次 quick：**建任务 15 秒 + 流式 48 秒 ≈ 63 秒**，够用。

> 别把这个值写进 `.env`：集成测试会经由 registry 拿到同一个值。
> 实测 `MOCK_LATENCY_MS=300 pytest tests/integration` 从 10.9 秒变成 172.9 秒。

前端 `npm run dev` 起在 3500，打开 <http://localhost:3500>。

**确认这两个服务真的带着这个值在跑**，别靠记忆：

```bash
curl -s 127.0.0.1:8020/api/providers/health | python -m json.tool | grep latencyMs
```

三个 probe 都报 `4000` 左右才算就绪（`llm` / `search` / `fetch` 各一次调用，
这个接口本身就要 12 秒）。**`latencyMs` 是 `0` 就说明起服务时漏了环境变量**，
那时后面的断网步骤会变成"任务早跑完了，没什么可断的"。

> 同理，**别用 UI 判断后台进程还活着没有**。这套服务器在会话被拆掉之后
> 仍然监听着端口——`netstat -ano | grep -E ":8020|:3500"` 看到有 PID 才是真的在，
> 而"界面上那个任务好像停了"什么都不说明。反证脚本被这件事坑过一次，
> 记在 `问题记录.md` 问题 22：一个还在写盘的后台进程让同一棵树在不同时刻
> 给出互斥的读数。

### A. 断网 10 秒

1. 首页建任务（用含糊需求会先进澄清页；不想答就点「先不答，直接看它跑」）
2. 进工作台，等思维流开始滚动，右上角徽标是 **实时**
3. DevTools → Network → 过滤 `stream`，点开那条 SSE 请求 → **EventStream** 标签页
   （没有这个标签就退回 **Response**，同样能看到 `id:` 行），
   记下**最后一个 `id:` 的值**，假设是 `N`
4. Network 面板把 **Offline** 勾上（或直接拔网线）。徽标应变成 **重连中**，
   已经渲染出来的内容**不清空、不消失**
5. 等满 10 秒
6. 取消 Offline。徽标应回到 **实时**

**要看的证据（这一步的全部价值就在这里）：**

- Network 里会多出**第二条** stream 请求。它的 **URL 与第一条完全相同**——
  这是对的：浏览器自动重连用的是同一个 URL，它靠请求头而不是查询参数续传
- 点开第二条 → **Headers → Request Headers**，应看到 `Last-Event-ID: N`
- 点开第二条的 **Response**，它的**第一帧 `id:` 应该恰好是 `N + 1`**

三种结果的含义完全不同，所以要逐字对：

| 第二条的第一帧 | 含义 |
|---|---|
| `id: N+1` | 正确续传。缺口没有、重复也没有 |
| `id: 1`（或从很早开始） | 水位没生效，整段历史被重传——界面上会看到内容"重放" |
| `id: M`，且 `M > N+1` | **中间缺帧**。这是最危险的一种，因为界面看起来是正常的 |

"无重复"再看一眼界面上推：思维流与证据流里同一个内容不应出现两次。

### B. 跑到一半刷新

1. 趁还在跑，按 F5
2. 工作台应**从 journal 恢复**：DAG 上已完成的节点仍是完成态，思维流仍是满的，
   而不是从空白重新长出来
3. Network 里这次 stream 请求的 URL **应带 `?from_seq=<非 0>`**

这是刷新路径与自动重连路径的**关键区别**：刷新是新建连接，而
`EventSource` 发不了请求头，所以新连接只能靠查询参数续传。
URL 上 `from_seq` 是 0 或缺省 → 快照里的水位没喂进 store，
表现是整段历史被重传、界面闪着重放一遍。

### C. 这一遍**验不到**什么（别把它当成全都验过了）

- **精确的连续性**：几百帧无缺口无重复，界面上判不了。那件事由
  `backend/scripts/smoke_http.py` 覆盖——实测带 `Last-Event-ID` 重连后
  **317 帧、seq 无缺口无重复**。浏览器这一遍验的是**接线**
  （`EventSource` 的自动重连没被自己写坏、水位真的喂进去了），
  不是帧级的连续性
- **React 渲染本身没有自动化测试**（没有 Playwright，Node 24 也没有全局
  `EventSource`）。所以这一遍不是"补充验证"，它是**唯一**能证明
  "这套东西在浏览器里真的能跑"的东西。做完请把结论写回上面那张清单
- 决策回放 / 图谱 / Trace **还不存在**（阶段 8），工作台那一步之后
  能走的是报告页（`/report/:reportId`，已做）。仪表盘也已经在
  （竞争情报中心 `/dashboard`，只是数据来自真库、与 mock 流水线无关，
  所以不在这一遍的验收范围里）。清单上那半截要等阶段 8 做完再走

### D. 收尾

验收做完之后，按普通方式重启后端（不加 `MOCK_LATENCY_MS`），否则日常点一下要等一分钟。
