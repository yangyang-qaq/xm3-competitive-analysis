# 开发问题记录

> 项目：xm3 — 个人 AI 竞品分析系统
> 记录开发过程中真实撞上的问题，以及它们是怎么被定位、怎么被修的
>
> 每条按 **现象 → 定位过程 → 根因 → 修复 → 验证** 五段写。编号是事件序号，不是日记。
> 只收"真的发生过"的问题：**没写进这里的 bug 不代表没犯过，但写进来的都是真犯过的**。
>
> 本文件在阶段 3 建起来，其中前 9 条是从阶段 1–3 回溯补记的（代码与测试里留有当时的
> 注释），第 10 条是当时正在处理的。补记的日期精确到阶段，不编造具体某天。

---

## 问题 1：`--provider deepseek` 报"未注册的 search provider"

**日期**：2026-09（阶段 3，回溯补记）

**现象**：

```
KeyError: 未注册的 搜索 provider: 'deepseek'。可用：bocha, mock
```

而 `--provider mock` 一切正常。也就是说：这个参数**唯一能用的取值，恰好是那个什么都不改的**。

**定位过程**：

`--provider X` 的实现是把 `LLM_PROVIDER` / `SEARCH_PROVIDER` / `FETCH_PROVIDER` 三个环境变量
一起设成 X。对 `mock` 成立，因为 mock 在三个类别里都注册了；对 `deepseek` 不成立——
它只注册了 LLM 那一类。

**根因**：

把"这个 provider 叫什么"和"它属于哪一类"当成了同一件事。一个名字在 LLM 注册表里存在，
不代表它在搜索注册表里也存在。快捷方式写成了"设三个变量"，而语义应该是
"在这一家**注册了的**那些类别上用它"。

顺带暴露了第二个问题：`--provider deepseek` 真正该干的事是"LLM 换掉，搜索与抓取照 `.env`"，
而不是"三个都换"。第一版实现连意图都写错了。

**修复**：

`_apply_provider_env` 改成先查注册表，只在 `provider in registry` 的类别上设环境变量；
一个类别都没命中时直接报错退出，并列出可用名字——而不是留下一个"参数被静默忽略"的运行。

**验证**：

`--provider mock` 仍走通全流程（三类都命中）；`--provider deepseek` 只动 LLM，
搜索继续用 `.env` 里的博查。CLI 帮助文本同步改成
"把这一家用在该 provider 注册了的每个类别上（deepseek 只影响 LLM，mock 影响三类）"。

---

## 问题 2：抓取探测恒红，且报的是错的诊断

**日期**：2026-09（阶段 1，回溯补记）

**现象**：

`/api/providers/health` 上 HTTP 抓取器永远红着，报"正文抽取失败"。换任何网络环境都一样。

**定位过程**：

探测目标是 `PROBE_URL = "https://example.com"`。抓取器抽出正文后要求长度 ≥ 200 字，
而 example.com 那页只有 117 字。于是每次探测都判失败。

真正刺眼的地方在于**报错内容**：它说"抽取器坏了"，而实际是"这个页面本来就没多少字"。
一个恒红且诊断指向错误模块的探测，比没有探测更坏——它会让人去查抽取逻辑。

**根因**：拿一个正文过短的目标页去测一条"必须抽出正文"的通路。

**修复**：

`PROBE_URL` 换成 `https://www.iana.org/help/example-domains`（正文足够长、长期稳定、
无地区限制），并补两条断言把这个选择钉住：

- `test_the_probe_url_is_not_example_com` —— 免得有人觉得换回 example.com 更"标准"；
- `test_the_probe_url_is_not_a_url_the_mock_simulates_as_broken` —— `MockFetcher` 按 URL
  哈希模拟约 1/7 的抓取失败，而 `PROBE_URL` 是固定的，一旦落在那 1/7 里，mock 的探测
  就**恒红**，而且同样是假红。

**验证**：两条断言常驻，`pytest tests/unit/providers/test_probe.py` 绿。

**教训**：探测目标本身是一个需要测试的输入。固定常量 + 固定判据 = 一个可能恒假的结论。

---

## 问题 3："这家 provider 能不能用"有两套判据，结果它们打起架来

**日期**：2026-09（阶段 3，回溯补记）

**现象**：

`/api/providers` 上 mock provider 挂着一个"未配置"的红标签，`/api/providers/health`
还会把它跳过——而它跑得好好的，整套离线测试都靠它。

**定位过程**：

代码里有两个地方在回答"能不能用"：一处是 `credentials(name).configured`（有没有密钥），
另一处是构造得出来吗。对真实 provider，两者结论一致；**对 mock，它们必然打架**——
mock 没有也不需要密钥。

**根因**：

同一个问题有两个答案来源。这不是"漏了一个 if"，是概念重复：一旦允许"是否可用"在两个
地方被分别计算，就一定会存在某个 provider 让两边不一致。而且第二套判据还要维护一份
"哪些 provider 需要密钥"的知识，那份知识在构造函数里已经有一遍了。

**修复**：

判据收敛到构造这一处：`try_build()` 返回 `BuildOutcome(instance, error)`，
`ok` 就是"建出来了"。密钥缺失时构造函数自己抛 `ProviderNotConfigured`，结果一样是失败，
不需要在外面重复一遍谁需要密钥。

**验证**：mock 在 `/api/providers` 上不再挂红标签，健康探测也不再跳过它。

---

## 问题 4：流水线在第一个阶段就崩——信封保留键撞上了摊平的事件载荷

**日期**：2026-09（阶段 2，回溯补记）

**现象**：

`Tracer` 一绑上，整条流水线在第一个阶段抛 `ValueError` 退出，一条事件都没发出去。

**定位过程**：

报错来自 `EventJournal.publish` 的保留键检查。载荷里带了 `taskId`。

**根因**：

事件的最外层是信封（`seq` / `type` / `taskId` / `createdAt`），载荷直接摊平在同一层。
而 `Span` 的落库读取路径**必须**带 `taskId`（跨任务读取时用），决策回放要 `seq`——
两个都是保留键。摊平的话没有位置放它们。

**修复**：

携带领域对象的那六种事件（`thought` / `message` / `evidence` / `span` / `chart` / `image`）
把对象嵌在自己的键下面；剩下五种（`node_update` / `progress` / `report_ready` / `done` / `error`）
是"事件自身的事实"，保持摊平。这个划分写进了 `contracts/sse_events.json` 的说明里。

**验证**：`test_reserved_keys_are_only_checked_at_the_envelope_level` 同时断言
"对象内部的 `taskId` 不覆盖信封里那一份"和"载荷里出现 `taskId` 会被拒"。

**教训**：这一次崩溃是**好事**。同一个错误如果写成静默覆盖，表现会是前端某个字段
莫名其妙变成别的任务的值，而日志里什么都没有。

---

## 问题 5：缓存命中率恒为 0，因为计费函数把它吃掉了

**日期**：2026-09（阶段 3，回溯补记）

**现象**：

`TokenUsage` 上加了 `cached_prompt_tokens` 字段、模型也真的报了命中数，
但 traces 表里这一列读出来永远是 0（当时还没建这一列，是先在内存里发现的）。

**定位过程**：

读 `apply_pricing(usage, pricing)`——它按定价表算完 `cost_usd`，**重新构造了一个
`TokenUsage` 返回**，而构造时没带上新加的字段。

**根因**：新增字段只加在了 dataclass 上，没加在这条"重建对象"的路径上。

**修复**：`apply_pricing` 透传该字段，并加注释说明为什么这件事必须修：

> 丢掉它会让上报的命中率恒为 0。而"命中率 0"和"从来没命中过一次"长得一模一样，
> 于是没人会去查。

**验证**：`test_usage_as_dict_is_camel_case` 与 `test_cached_tokens_are_a_subset_not_an_extra_batch`
覆盖；端到端层面由 `pytest -m live` 里那条"连发两次相同长前缀请求，第二次必须报出
`cached_prompt_tokens > 0`"守着。

---

## 问题 6：`0` 和 `None` 的语义塌陷——已配置的"缓存免费"被当成"没配缓存价"

**日期**：2026-09（阶段 3，回溯补记）

**现象**：

我自己新写的那条测试红了：`assert 1.0 == 0.0`。

**定位过程**：

`ModelPricing.cost()` 里写的是 `cached_rate = self.cached_input_per_mtok_usd or self.input_per_mtok_usd`。
测试构造了一个"缓存价 = 0"的定价（缓存确实可能是免费的），期望成本为 0，
实际拿到的是按未命中价算出来的数。

**根因**：

`0.0 or X` 走的是 `X`。也就是说：**0 是一个合法的单价，却被 `or` 当成了"没配置"**。
两个不同的语义（"这个价是 0" 与 "这个价没配"）塌进了同一个值里。

**修复**：

字段类型改成 `float | None = None`，判据用显式的 `is None`：

```python
cached_rate = (
    self.input_per_mtok_usd
    if self.cached_input_per_mtok_usd is None
    else self.cached_input_per_mtok_usd
)
```

`Settings.pricing_for` 对应地接受 `[in, out]` 与 `[in, out, cached]` 两种长度，
省略第三项时返回 `None` 而不是 0。

**为什么缺省值取"按未命中价"**：这是**上界**。定价表没配缓存价时成本会算高一点——
而算高的成本会被人多看一眼，算低的不会。

**验证**：`test_deepseek_pricing.py` 里 `test_cached_tokens_are_a_subset_not_an_extra_batch`
与一条显式的"省略第三项 = 未配置"用例。

---

## 问题 7：定价表按模型名子串匹配，`reasoner` 被按 `flash` 价计费

**日期**：2026-09（阶段 3，回溯补记）

**现象**：

设计 `_priced_models()` 时写成了"已配置的模型 ∪ 公开的 flash 名单"。读回来发现两个方向都错：

1. `deepseek-reasoner` 会被算成 flash 的价——**成本报低了**；
2. 用户在 `.env` 里实际配的那个模型反而没被计价——成本直接是 0。

**定位过程**：

把"有哪些模型"和"哪些模型适用这张价目表"当成了同一个集合。前者是配置问题，
后者是定价表的适用性问题，两者只在"恰好一致"时才不出错。

**根因**：

价目表是按模型族给的（flash 一档价），判定依据只能是**模型名**。而"配置了哪些模型"
跟"该不该按这档价算"是两件事。取并集会把两个方向的错误都引进来。

**修复**：

改成只看**已配置的三档模型**里名字含 `flash` 的那些（`_FLASH_MARKER`），
并按实测的命名现实写清代价：

> 这个判据的代价是：将来官方把价目表按别的名字分档，这里要跟着改。
> 反过来（按"已配置的全集"取并集）的代价是**静默算错钱**——
> 一个会在账单上体现、在代码里看不出来的错。两者不对称，所以选前者。

**验证**：`test_a_reasoner_model_is_left_unpriced` —— 未定价的模型**不编一个价**，
`cost_usd` 保持 0 且 `pricing()` 里查不到它，而不是按某个近似的价目表硬算。

---

## 问题 8：没实测就把"成本偏高约四成"写进了三处文档

**日期**：2026-09（阶段 3，回溯补记）

**现象**：不是运行时报错，是**我自己的注释和测试在说一个我没测过的数**。

**定位过程**：

设计缓存感知计费时，我按"缓存价与未命中价差 50 倍"推断出"不区分缓存会让成本偏高约四成"，
并把这个说法写进了 `base.py`、`deepseek.py`、`test_deepseek_pricing.py` 三处的 docstring
和测试断言里。

后来真的跑了一次 quick 模式（报告 `RP-4583f574eab8`，13 次 LLM 调用、76332 prompt token），
实测命中率是 **0.8%**——13 次调用里只有最先两次命中。也就是说，按未命中价一刀切的话，
成本误差不到 1%，而不是四成。

**根因**：

**几个章节是并行扇出写的**——每个章节都是自己那份前缀的第一个写者，所以后写的那些
章节根本命中不了谁的前缀。这个结构性原因只有跑起来才看得见；靠"价差 50 倍"推断
只会推断出"影响很大"。

**修复**：把三处说法全部改成实测的 0.8%，并写明原因。

**为什么这条值得记**：这个项目的原则是"无证据不立论"，而这条注释违反了它——
还是被我自己写进去的。**代码里的数字是论据，不是修辞**：一个没测过的数字写在注释里，
比写在报告里更难被发现，因为它看起来像常识。

那还要不要算缓存价？要。**因为在测出来之前没人知道是 0.8% 还是 80%**——
这个判断本身就是靠测量才变成"不用管"的，不是靠推测。

**验证**：`RP-4583f574eab8` 的 trace 数据；`cacheHitRate` 现在与成本一起上报
（见问题 5 的注释：单看成本无法判断"便宜"是因为便宜还是因为算错了）。

---

## 问题 9：跑完之后再打开工作台，会重跑整条流水线

**日期**：2026-09（阶段 2/5，回溯补记）

**现象**：

任务跑完之后再打开一次工作台，**账单翻倍**、报告 id 悄悄换了一个。
证据数看起来正常。

**定位过程**：

`ensure_runner` 里的判据是 `if existing is not None and not existing.finished`。
"跑完了"于是被当成了"不存在"，于是新建一个 runner 并把整条流水线重跑一遍。

**根因**：

这个模块存在的**全部理由**就是堵住"重复订阅导致重跑"，却在另一个触发点上漏了——
不是"跑到一半重连"，而是"跑完之后重开"。

漏得还特别隐蔽：证据不会明显翻倍，因为 `evidence_id` 由 URL 摘要生成、跨轮次稳定，
`INSERT OR REPLACE` 会把它去重掉。能看见的只有账单和报告 id。

**修复**：

规则改成两条：
1. 内存里有 runner 就一律返回（跑完了也算有）；
2. 内存里没有、但任务行已经是终态，走 `recover()` **只回放历史、绝不启动流水线**。

**验证**：`recover()` 里 `_apply_event(event, persist=False)` 的那条路——
回放历史事件如果也写库，就变成"打开一个历史报告会改它的任务行"。
`test_pipeline_mock.py` 里"同一 `task_id` 两个并发订阅只跑一次流水线"（用 provider 调用计数断言）。

---

## 问题 10：SSE 流里凭空少一条事件（事件入队跑在了锁外）

**日期**：2026-09-15

**现象**：

全量测试偶发失败，报的是 seq 缺口：

```
tests/integration/test_pipeline_mock.py::test_event_seq_is_contiguous_from_one
At index 30 diff: 32 != 31
```

**单独跑 3 次全绿**，下一次全量又绿了。这类"自己会好的"失败最容易被跳过——
而它正是问题里唯一一个会**静默丢数据**的。

**定位过程**：

按三条线索依次排除：

1. `EventJournal.publish` —— seq 的自增与写入 `_events` 都在 `self._lock` 内，排除。
2. `subscribe()` —— 注册订阅者与快照历史在同一次加锁里完成，排除。
3. 订阅端的消费循环：

```python
while True:
    item = await queue.get()
    if item is None:
        return
    if item.seq <= yield_from:   # 单调水位去重
        continue
    yield_from = item.seq
    yield item
```

到这里对上了。去重用的是**一个单调水位**，它隐含假设"投递顺序是单调的"。
而 `publish` 里的扇出——

```python
for loop, queue in self._subscribers:
    loop.call_soon_threadsafe(queue.put_nowait, event)
```

——**在锁外**。于是两个线程可以各自拿到 seq 31 与 32，再按相反的顺序调
`call_soon_threadsafe`，队列里就成了 `[32, 31]`。订阅者收到 32、水位推到 32，
随后到达的 31 被判成"重复"，**静默丢掉**。

**为什么这个丢法特别坏**：它是**不可恢复**的。前端的水位已经推到 32，
重连时按 `Last-Event-ID: 32` 补发的是 `since(32)` —— 31 永远拿不回来。
而"跑到一半断网 10 秒，恢复后无缺口、无重复"正是这套东西的验收条件之一。

**修复**：

把扇出挪进锁内，与 seq 的分配一起完成（`call_soon_threadsafe` 只是往目标循环的
就绪队列里塞一项，不会阻塞，占着锁是安全的）。**消费者回调（sinks）留在锁外**——
那些是外部代码，可能很慢甚至阻塞，不能拿它们占着锁。

**验证**：

新增 `test_concurrent_publishers_never_punch_a_hole_in_the_stream`：8 个线程各发 25 条，
断言订阅者收到的 seq **一个都不少**。

**第一次证伪失败了，这一步才是关键。** 我把入队挪回锁外，跑 5 次——**全绿**。
排查发现两个原因：

1. 我那次"挪回去"改错了：`if True:` 包着的那份仍在锁内，等于只是把事件多投了一遍。
   是**注释改了、代码没改**——比不改更坏，因为它看起来已经验证过了。
2. 就算真挪出去也不一定红：CPython 默认 5ms 才切一次线程，
   而"释放锁"到"入队"之间只有两条语句——线程一口气就能发完 25 条，那个窗口压根轮不上被抢占。

改成把 `setswitchinterval` 压到 1 微秒（不是"为了让它红而调参"：跑在修好的实现上时，
这个加严只让锁竞争更激烈，而锁内的不变量在任何调度下都成立），再真正把入队挪出去：

- 锁外版本：**10/10 失败**，报 `订阅者少收了 5 条事件，前几个是 [6, 30, 129, 146, 190]`；
- 锁内版本：**10/10 通过**。

**一条抓不住 bug 的测试比没有测试更坏**——它会让人以为这块已经被守住了。
所以这条测试的成败判据不是"它绿了"，而是"**把它要防的 bug 放回去，它必须红**"。

顺带修正了一处恒真断言：原来还断言了 `seen == sorted(seen)`（收到的 seq 递增）。
这条**永远不可能失败**——水位去重会把任何回退的条目丢掉，所以"乱序"根本到不了
订阅者眼里。**"看不到乱序"恰恰是这个 bug 静默的原因**：乱序不表现成乱序，
表现成缺失。断言已删，理由写进了测试的 docstring。

**教训**：偶发失败不是噪音。
"单独跑就绿"的测试失败，往往指向一个只在并发下出现的静默数据丢失——
这类问题在演示时几乎必然发生一次。

---

## 问题 11：回放很忠实，除了三个数——而它们刚好全都会让门禁恒绿

**日期**：2026-09-15

**现象**：

不是故障，是**边界**。录完第一条 cassette（`notion-vs-obsidian`）之后，
把"那天真实跑的那次"和"刚才离线回放的那次"的报告逐指标比了一遍：

- **31 个指标逐位相同**：成本 `0.01847`、token `86151`、证据 48 条、
  独立域名 30、维度覆盖 1.0、论点 12 条（交叉验证 7 条）、幻觉引用率 0、
  质量门 `passed=true`，连评审结果的四个维度评语都一字不差。
- **三个不同**：

| 指标 | 录制 | 回放 |
|---|---|---|
| `durationMs` | 50859 | 139 |
| `firstEvidenceMs` | 25844 | 108 |
| `slowestCallMs` | 12905 | 0 |

**定位过程**：

回放时 span 的耗时是**回放进程自己的耗时**——它不发网络请求，所以是 0.1 秒级。
这不是 bug，这就是"离线"的定义。顺带发现 `reportId` 也不同（`RP-54cc2233094f`
vs `RP-af7606c3b356`）：它是 `assemble.py` 里的 `f"RP-{uuid.uuid4().hex[:12]}"`，
每次运行都是新的。

**为什么它危险**：

一个天真的门禁如果断言 `durationMs` 或 `slowestCallMs`，在回放下会拿 0 与 108 去比，
于是**恒绿**——而它守的恰恰是"性能有没有退化"，最需要守的东西之一。
`reportId` 同理：断言它等于把一条永远失败的用例写进 CI。

**修复**：

代码不用改（这不是缺陷）。要做的是**把边界写下来**，并且在门禁里划清：

- **回放能证明的是内容**：成本、token、证据、指标、正文、评审结论。
  一次 prompt 改动、一个 coercer 改动、一处维度覆盖逻辑的退化，都会在回放里显形。
- **回放不能证明的是时间**。耗时类指标只能在 `-m live` 里测——
  那也正是它该待的地方：性能问题的结论本来就该建立在真实网络与真实模型上。

若将来真需要在回放下断言耗时，正确做法不是"接受 0"，而是让 span 用**录制里已经存下来的**
`latency_ms`（`cassette._encode_llm` 一直在存它，只是报告用的是 span 的墙钟时间而不是它）。
那是一个有意义的改动，但它改的是"回放要不要模拟耗时"，与本次的结论不冲突。

**验证**：

上面两组数字来自 `data/golden.db`（`RP-54cc2233094f`，录制那次）与
`data/replay_check.db`（`RP-af7606c3b356`，无密钥离线回放那次）。
回放那一次**没有配任何 API Key**，整条流水线仍然跑完并出了完整事件流，
这同时也是"CI 零成本 + 零密钥"这个主张的实测证据。

---

## 问题 12：名册从 15 人扩到 48 人，把一个一直存在的漏洞从边角变成了常事

**日期**：2026-09-16（阶段 4）

**现象**：

阶段 4 把 `validate_team()` 接到 48 人名册上做冒烟，随手送了一份队伍：

```
lead: 'L2-007'
```

**它被接受了。** 而 `L2-007` 是战略层（品牌与传播分析师），组长要求 L3。
`validate_team()` 检查了"这个 id 在不在名册里"，但**从来没有检查"它在哪一层"**。

**定位过程**：

这个洞一直都在，只是名册只有 15 人时几乎踩不到——L2 只有 4 个人，
模型从 digest 里挑组长时，可选项本来就少，而 L3 的 3 个人很显眼。

扩到 48 人之后比例完全变了：

| 层 | 人数 | 被误当成组长的先验概率（均匀挑） |
|---|---|---|
| L3 | 3 | — |
| L2 | 9 | 9/48 ≈ 19% |
| L1 | 36 | **36/48 = 75%** |

**48 人里有 36 个是执行层。** "模型挑了个执行层来当组长"从一个边角情况
变成了一件常事。而三层分工是 xm3 对外的核心说法——它在数据上原本是不设防的。

**根因**：

把"合法性"压缩成了一个布尔判断。`expert_by_id()` 返回 `None` 就是非法，
返回对象就合法——但"这个人的层级配不配这个角色"是另一条独立的规则，
它没有地方可以表达。

坑在于**这两种失败在数据上长得一模一样**：一次层级错配的调度
和一次正常调度写进库里的东西完全相同，只是组长是个执行层的人。
没有任何地方会报错，报告读起来也很正常。

**修复**：

`_ROLE_SPECS` 里每个角色加上要求的层级，`validate_team()` 变成两道关：

```python
_ROLE_SPECS = (
    ("lead",        "L3", 1, ("lead", "lead_expert", "组长", "负责人")),
    ("strategists", "L2", 3, ("strategists", "strategist", "战略", "战略层")),
    ("executors",   "L1", 4, ("executors", "executor", "执行", "执行层")),
)
```

层级不符的 id 被挡掉、记进 `wrong_level`（带期望层级与实际层级），
该角色整类回退到默认队伍。

顺带把 `TeamOutcome` 拆成四个字段。原先只有一个 `dropped` 列表，
而 `describe()` 把它统一说成"N 个专家 id 不存在于名册"——四类问题里
**只有 `phantom` 一类真的不存在**。这就是实测那句"有 11 个专家 id 不存在于名册"
的来源，它听起来很确定，但数是错的。详见 `修补文档.md` D1。

**验证**：

- 改之前先确认**既有测试的 fixture 本来就是层级正确的**，所以加这道关
  没有破坏任何既有用例——否则就是"测试在迁就实现"，得先弄清楚谁对。
- `tests/unit/test_dispatch.py` 新增 20 条，其中 6 条是参数化的错层用例
  （lead←L2/L1、strategists←L3/L1、executors←L3/L2），每条都断言
  "被挡掉 + 整类回退 + `wrong_level` 记的是哪一层"。
- **守卫已反证**：把 `if expert.level != level:` 改成 `if False:`，
  **20 条里 8 条红**。改回来即绿。
- `test_the_default_team_satisfies_its_own_contract`：把兜底队伍自己喂给校验，
  必须零问题。它是一份硬编码的 id 列表，只在"调度已经失败"时才被用到，
  所以它坏了也没人会看见——除非在这里守一道。

---

## 问题 13：CLI 的整个叙事输出是空白的，而退出码是干净的

**日期**：2026-09-16（阶段 4）

**现象**：

阶段 4 的验收要跑一次完整流水线，输出长这样：

```
      · ：
      →  ⇒ ：
      [ ] EV-ab34f22fe0cc     ? 
      ⏱      0ms
```

六种事件全部是空行。而**退出码 0，没有任何报错**。

同时终端最下面两行是：

```
[报告] RP-7d7c7c78c446 章节=4 证据=216 质量门=未通过 完整度=0%
[完成] 耗时 0ms，返工 0 轮，质量门未通过
```

而同一段输出里，审计专家刚刚说过"质量门结论：通过"。**两行字互相矛盾，
而错的那一行看起来更权威**（它是个汇总）。

**定位过程**：

拿契约 `contracts/sse_events.json` 对着渲染器逐条比。契约里十一种事件分两种形状：

- **嵌套**（六种）：`thought` / `message` / `evidence` / `chart` / `trace` / `image`
  —— 载荷挂在一个子对象下面，`trace` 那个键还叫 **`span`** 而不是 `trace`。
- **扁平**（五种）：`node_update` / `progress` / `report_ready` / `done` / `error`。

渲染器**一律按扁平读**。`data.get("expertName")` 在 `{"thought": {...}}` 上
永远是空串，于是每一行都渲染成了"骨架 + 空值"——不报错，只是没有内容。

`report_ready` 和 `done` 是另一类问题，比读错键更严重：**它们读不到字段时
给了一个默认判断，而不是不说。**

| 事件 | 读的键 | 契约里实际在哪 | 读不到的后果 |
|---|---|---|---|
| `report_ready` | `quality` | **没有这个键** | 默认"未通过、完整度 0%" |
| `done` | 顶层 `elapsedMs` / `reworkRounds` | `metrics` 里 | 默认 0，看起来像测量结果 |

契约明确写了 `report_ready` 里**没有** `quality`（挂账到阶段 7），
也明确写了"elapsedMs / reworkRounds 不再单独带 —— 三者都在 metrics 里"。
所以这不是契约变了而渲染器没跟上，是渲染器**在一个没有该字段的地方
编了一个判断出来**。

**根因**：

三层，一层比一层难发现：

1. **直接原因**：没有 `NESTED_PAYLOADS` 这张表，渲染器不知道哪些事件是嵌套的。
2. **为什么没被发现**：CLI 的"验收"就是看一眼**有没有报错**。
   一个把整个叙事输出印成空白的 bug，在退出码上完全干净。
   `tests/contract/test_sse_contract.py` 的 docstring 恰好拿 `node`→`stage` 这个改名
   当过例子——但它防的是**前端**读 `e.node`。**CLI 是第三个消费者，没人查。**
3. **为什么它敢这么写**：函数 docstring 里写着"渲染逻辑单独成函数并被测试覆盖"。
   **当时没有任何 CLI 测试文件。** 一句话把上面全部缺陷盖住了——
   读到它的人（包括我自己）会跳过这个模块。

**修复**：

- 新增 `NESTED_PAYLOADS` 表 + `_inner()` 解包（缺失或类型不对时返回空 dict，
  渲染器照常产出那一行，不抛异常——半路崩掉会丢掉本来要看的信息）。
- `node_update` 改读 `stage`，并加返工轮次后缀（"联网采集（第 2 轮）"）。
- `done` 改从 `metrics` 读三个数。
- `report_ready` **不再渲染质量门**：那一条事件里没有这个信息，判定要等 `done`。
- 补 `chart` 分支。
- 新增 `tests/unit/test_cli_render.py`（32 条）。

**验证**：

真实输出（`--mode quick --provider mock`，零 API 花费）：

```
      · 沈衡：收到调研需求：「对比 Notion 与 Obsidian」。先确认调研对象和范围。
      · 池越：采集完成：216 条证据，33 个独立域名，其中 0 条正文降级。
      · 纪岚：审计完成：evidence 216 条、覆盖率 100%、blocker 0 / major 0 / minor 0。质量门结论：通过
      · 方叙：报告正文撰写完成：4 章，正文共引用 16 处证据，其中 0 章被标记降级。
  [报告] RP-d99370b2a871 章节=4 证据=216 无问题
  [完成] 耗时 172ms，返工 0 轮，质量门通过
  [>]  78.0% 质量审计
```

**关键是最后三行现在互相一致**：审计专家说"质量门结论：通过"，
而 `[报告]` 说"无问题"、`[完成]` 说"质量门通过"。
从前这里印的是"质量门=未通过 完整度=0%"，与同一次运行里的审计结论直接冲突。

（`reportId` 每次运行都不同——它是 `uuid4()`，见问题 11。
所以上面这个号只对应当前这一次运行，不构成可断言的期望值。）

守卫反证（`scripts/falsify_cli_render.py`）：把五个缺陷逐个放回去，
每条守卫都真的变红——嵌套按扁平读触发 5 条、`node` 键 3 条、
`done` 读顶层 2 条、`report_ready` 编造结论 3 条、`NESTED_PAYLOADS` 漂移 2 条。

最有价值的一条是 `test_nested_payload_map_matches_the_contract`：它拿契约的
`shape` / `required` 比对 `NESTED_PAYLOADS`。现在同一件事有**三份独立声明**
（契约、契约测试的 `CARRIED_KEY`、渲染器），三份必须对上。
**刻意不把契约读进来当唯一真相源**——读进来就失去了交叉验证。

**教训**：

"缺字段时给一个默认判断"比"缺字段时什么都不说"危险得多，
因为默认判断读起来像结论。"未通过"是一句有内容的话，
而报告缺一个键是常态。

判据可以写下来：**契约承诺过的键，缺了可以说；契约没承诺的键，缺了就不许说。**

---

## 问题 14：反证脚本把"全绿"误判成"全红"，差点让我去重写已经写对的测试

**日期**：2026-09-16（阶段 4）

**现象**：

写完 `test_cli_render.py` 之后，为了反证这些守卫真的抓得住 bug，
写了个变异脚本 `scripts/falsify_cli_render.py`：把每个缺陷逐个改回去，
跑测试，看是否变红。第一次运行：

```
基线（未改动）
  0 passed, 0 failed
  基线就是红的——先修好再谈反证。
```

而手动跑同一批测试明明是 `32 passed`。

**定位过程**：

直接看子进程的原始输出：

```python
subprocess.run([PY, "-m", "pytest", TESTS, "-q", ...])
```

stdout 只有一行点号和 `[100%]`，**没有汇总行**。而手动跑时是有的。

差别在 `pyproject.toml`：

```toml
addopts = "-q -m 'not live'"
```

它**已经有一个 `-q`**。我又传了一个，合成 `-qq`——而 `-qq` 会把汇总行整个
删掉。脚本读到 0 passed，于是判定基线是红的，拒绝继续。

改掉之后仍然报 0。第二个 bug 在解析里：

```python
if part.endswith("passed"):        # 汇总行是 `===== 32 passed in 0.14s =====`
```

那一行**以等号结尾**，`endswith("passed")` 恒不成立。换成正则才读到 32。

**根因**：

一个自检脚本，本身没有任何测试。而且它**读的是人给人看的输出**——
pytest 的汇总行是给眼睛看的排版，不是接口，加一个 `-q` 就会消失。
拿它当数据源，等于把"我的参数恰好没改变输出格式"当成了隐含前提。

两个 bug 叠在一起，第一个把第二个藏住了：只要汇总行还在，
`endswith` 那个 bug 就会继续静默地报 0——而那时报告的会是
"0 passed, 0 failed"，同样触发拒绝继续。**如果它的默认判定反过来
（把解析失败当成"没有失败"），它会报告"五条守卫全部漏掉"，
而我大概会去给已经写对的测试再加一层断言。**

**修复**：

- `-o addopts=` 清掉 pyproject 里的 addopts，`not live` 自己带上
  （清掉是为了不受它的 `-q` 影响；自己带是因为不清掉它，
  `-m live` 的用例会真的去联网花钱）。
- 解析改成正则 `(\d+) passed`。
- **加了失败可见性**：解析不出汇总行时直接打印原始输出尾部，
  而不是静默返回 0。
- 基线守卫保留：基线不绿就拒绝反证。**它这次真的挡住了**——
  如果没写这个守卫，脚本会继续跑完五次变异，每次都因解析失败报
  "0 failed"，然后输出一张"五条守卫全部漏掉"的表。

**验证**：

```
基线（未改动）          32 passed, 0 failed
M1 嵌套载荷按顶层读      27 passed, 5 failed -> 抓住
M2 node_update 读 node   29 passed, 3 failed -> 抓住
M3 done 读顶层           30 passed, 2 failed -> 抓住
M4 report_ready 编造结论  29 passed, 3 failed -> 抓住
M5 NESTED_PAYLOADS 漂移  30 passed, 2 failed -> 抓住
已还原：True
```

最后一个 `已还原：True` 是脚本自己断言的：五次变异跑完，
源文件必须逐字节回到原样。一个会改源码的脚本必须证明它改回去了。

**教训**：

**"抓不住 bug 的测试比没有测试更坏"——这条规则对反证脚本自己也成立。**
它检查的是别的测试，所以它错了没人会查它。这次是靠"基线守卫"
偶然挡住的，而不是靠设计。

顺带一个通用结论：**解析工具的输出时，先清掉工具自己的配置。**
`addopts` 这种"项目级默认参数"会和你传的参数叠加，
而叠加的结果（`-q` + `-q` = `-qq` 静默删掉汇总行）
比任何一边单独都更难预料。

---

## 问题 15：`POST /api/tasks` 要等整份报告写完才响应（注释说的和代码做的不是一回事）

**日期**：2026-09-16（阶段 6）

**现象**：

阶段 6 的建任务接口写完之后，第一版端到端冒烟（真起服务走 HTTP）里，
`POST /api/tasks` 一直不返回。mock provider 下等了两秒多才回来。
而它的现象是**没有现象**——请求最后还是成功了，报告也写出来了，
只是"点下开始调研"之后按钮要转很久。

真实配置下这不是"很久"，是**几分钟**：deep 档要跑 6 个品牌 × 6 个维度，
再加上十来章的撰写。而工作台本该在这几分钟里显示它在想什么。

**定位过程**：

接口里等的是 `runner.begin()`，而它的 docstring 写得很清楚：

> 启动，并**等需求理解结束**再交回控制权。
> …需求理解正常结束时协程并不会结束（后面还有六个阶段），
> 所以能等的只有"那一步完成了"这件事本身。

注释准确描述了**应该**发生什么。代码是：

```python
async def _run(self) -> None:
    try:
        ...
        self._result = await run_pipeline(...)   # 七个阶段全在这里面
    finally:
        self._intake_done = True
        self._intake_ready.set()                 # ← 唯一的 set()
```

`_intake_ready` 全项目只有一处 `set()`，在 `_run` 的 `finally` 里，
而那时 `run_pipeline` 已经返回——**七个阶段全跑完了**。
于是 `begin()` 等的是"整条流水线结束"，和注释说的差着六个阶段。

**根因**：

写这段的时候，"需求理解结束"这件事**没有可等的对象**。
`run_pipeline` 是一条完整的协程，它内部跑到哪一步外面看不见。
当时的选择是"用一个 Event 近似一下"，然后把 set 放进了 `finally`——
那个位置在"跑完之后一定要做点收尾"的语境下看起来完全合理，
而它恰好是这条 Event 唯一不该在的位置。

**测试为什么没抓到**：

两层，都是这个项目里反复出现的老问题：

1. **mock provider 跑完整条流水线只要几毫秒**，"等 intake"和
   "等全部"在时间上分不出来。这不是"测试写得不好"，是
   **进程内测试对耗时不敏感**——一个只有在真实耗时下才成立的缺陷，
   在把耗时抹掉的测试里就是不可见的。
2. 断言写的是 `assert body["status"] in ("running", "done")`。
   这条断言的本意是"别管跑到哪一步了"，而它的实际作用是
   让"等 intake"和"等全部"两种行为**都能通过**——
   缺陷恰好落在被放行的那一支里（`done`）。

第 2 条是更值得记的那条：**一条为了"不那么严格"而放宽的断言，
放行的正是缺陷。** 断言里每个 `in (...)` 都该问一句
"我到底在放行什么"。

**修复**：

给 `run_pipeline` 加一个回调：

```python
async def run_pipeline(..., on_intake_settled: Callable[[bool], None] | None = None)
```

编排器在需求理解有结论的那一刻调用它（放在 `finally` 里，
这样需求理解**失败**时等待方也能醒——否则它会一直等一条
永远不会到来的流水线）。runner 侧：

```python
def _intake_settled(self, paused: bool) -> None:
    if paused:
        self._pause()
    self._intake_ready.set()
```

顺带删掉了 `_run` 里那句 `if self._result.paused: self._pause()`——
暂停动作现在只在 `_intake_settled` 里发生。留着也能跑（它是幂等的），
但那意味着"什么情况算暂停"有两个判断点，将来只改一处
就会出现"状态说停了、界面还在跑"。

**关于顺序**：`_pause()` 在 `set()` 之前，但**不是因为有竞态**。
`_pause()` 是同步的，两者之间没有 `await`，所以反过来写也一样对——
asyncio 是协作式的，被唤醒的协程要等本协程让出控制权才会跑。
按这个顺序写是因为 `_pause()` 会写库、将来完全可能变成 `async def`
（那时中间就多了一个 `await`），而"公布了就必须已经是最终状态"
本身该是这条函数的契约，不该靠调度器的实现细节兑现。

**这一点是写注释时专门核对过的**：第一版注释里我写的是
"反过来会有一个很窄的竞态"，核对之后发现那个竞态今天不存在，
改成了现在的说法。**注释里写一个代码不可能产生的 bug，
和漏写一个真实存在的 bug 一样坏。**

**验证**：

`test_post_returns_while_the_pipeline_is_still_running`：把采集阶段
换成一个卡在 `threading.Event` 上的函数，然后断言建任务的响应
**在流水线还卡着的时候**就回来了，且 `status == "running"`。
缺陷在的时候它返回 `done`。

反证 M9（把回调改成 `None`，唤醒退回 `finally`）：**抓住**，
9 条用例变红。

同时把上面那条放宽的断言收紧了：

```
- assert body["status"] in ("running", "done")
+ assert body["status"] == "running"
```

**教训**：

**注释描述的是意图，不是行为。** 这条 docstring 写得越详细，
越容易让人相信它——"能等的只有那一步完成了这件事本身"
读起来像是一个已经解决的技术难点的说明，
而实际上它描述的那件事**从来没被实现**。

一个可操作的判据：**写"为什么只能这样做"的注释时，
回头确认那个做法真的被做出来了。** 这条注释的措辞
（"所以能等的只有…"）正是在为一个当时并不存在的机制辩护。

---

## 问题 16：应用启动时不建表，第一个读库的请求 500

**日期**：2026-09-16（阶段 6）

**现象**：

真起一个服务（不是 `TestClient`），配上一个干净的库，然后建任务：

```
File "app/api/routes_tasks.py", line 160, in create_task
    tasks_repo.create(
sqlite3.OperationalError: no such table: tasks
```

**而 `/health` 是好的**，启动日志一行错误都没有。

**定位过程**：

`main.py` 的 `lifespan` 里只做了两件事：配日志、打印 `settings.describe()`。
**迁移从来没被调用过。** 计划里写的是"lifespan（跑迁移、预热专家表）"，
实现的时候只写了后半句提到的那半。

顺带发现同一个病因的另一处表现，比 500 更坏：

```python
def _today_cost() -> dict:
    """今日成本。DB 还没建表时返回零值而不是报错——
    /api/providers 是启动后最先被打开的页面之一，它不该因为"还没跑过任何任务"
    而返回 500。
    """
    try:
        return traces_repo.cost_summary(since=today)
    except Exception as exc:  # noqa: BLE001 - 尚未迁移 / 空库
        log.warning("读取今日成本失败，按 0 返回：%s", exc)
```

这段的 docstring 把**两件不同的事**说成了一件：
"库里还没有任务"和"库里根本没有表"。前者是正常状态，
`cost_summary` 自己就会返回零值，**根本不会抛异常**；
后者才是异常。所以这个 `except` 只可能被后面那种情况触发——
它是**在开发过程中真的撞到过 `no such table`、
然后把它就地归因成"空库"**留下的。

于是 `POST /api/tasks` 报 500（看得见），
而成本面板永远显示"今天 $0"（看不见）。后者更危险：
一个显示着"0 美元"的成本面板，看起来是"还没花钱"，
不是"读不到数据"。

**测试为什么没抓到**：

夹具自己建了表：

```python
@pytest.fixture
def mock_pipeline_db(tmp_path, monkeypatch):
    ...
    migrate(get_conn())      # ← 这一句
```

531 条用例全绿，包括所有跑完整流水线的集成测试。
**夹具做的那一步，正是应用启动时该做而没做的那一步**——
测试替被测对象把活干了，于是这条路径在测试里根本不存在。

这不是"夹具写错了"。夹具需要建表，否则它自己跑不了。
问题是**没有任何一条用例走"应用自己启动"这条路**。

**修复**：

`lifespan` 里补上迁移，并顺手把另外两件事一起做了：

- **预热专家名册**（`roster_size()`）。它是个 `lru_cache`，
  第一次调用要读 YAML、构建 48 个对象。不预热的话，
  "第一个建任务的用户"要多等这一下；更重要的是，
  **名册文件坏了会变成启动即失败**，而不是等流水线跑到组队那一步
  才炸——那时用户已经等了两分钟。
- **`close_all()` 放进 `finally`**。线程局部连接不会自己关，
  不关的话进程退出后 SQLite 文件仍被占用，
  而报错信息指向文件而不是连接。

**验证**：

新增 `TestStartupMigratesTheDatabase`，用一个**自己不建表**的夹具
（`virgin_db`）——这正是它存在的理由。先断言前置条件：

```python
assert "tasks" not in _tables(virgin_db), "夹具自己不建表才有意义"
```

不写这一句的话，将来有人往夹具里补一句 `migrate()`，
这条用例会因为"表已经有了"而永远变绿，
而它守的那个缺陷又回来了。**一条只会绿、不会红的守卫比没有更坏。**

反证 M8（`applied = migrate(get_conn())` 改成 `applied = []`）：
**抓住**，两条用例变红。

**另外做了一件事**：加了 `scripts/smoke_http.py`，真起一个服务、
走一遍 HTTP（建任务 → 澄清 → 按水位续传 → 跑完）。
这个缺陷就是它发现的。它存在的理由是**进程内测试结构性地看不见三件事**：
启动路径、序列化（中文请求体经不经得起一次编码往返）、
以及 SSE 的真实分帧（`id:` 行、空行分隔、心跳注释帧）。
它跑在 mock provider 上，零花费。

它自己也有一个值得一提的断言：

```python
seqs == list(range(1, len(seqs) + 1))
```

只看"两边都有帧"会漏掉重复（两边都从 1 开始）和缺口（水位算错了），
而这两种在界面上都表现为"看着差不多"。

**教训**：

**夹具越能干，应用自己的启动路径就越没人走。**
凡是"应用启动时应该发生的事"，都需要一条**让应用真的启动一次**的用例。
在这个项目里，那条用例的成本恰好很低——`TestClient` 的上下文管理器
就会触发 `lifespan`，需要的只是一个不抢着建表的夹具。

还有一条更小的：**`except Exception` 加一句 `log.warning`，
是在给一个还没查清的病因做局部麻醉。** 那段代码的 docstring
把"空库"和"没建表"混为一谈，而修复只需要让应用自己把表建上——
之后那段 `except` 就再也用不着了。

---

## 问题 17：去重只作用于「批的开头那一下」——续传重叠时同一条思维出现两遍

**日期**：2026-09-16（阶段 6）

**现象**：

还没有现象。这一条是**写测试的时候**发现的，不是线上撞到的。
`taskEvents.ts` 的 `reduceEvents` 注释里写着：

> 去重是断线续传的必要条件，不是优化……没有去重，
> 那一段思维会在界面上出现两遍——而用户分不出是重复还是真的发生了两次。

注释说得没错，**但代码做的不是这件事**：

```ts
  const fresh = events
    .filter((event) => event.seq > watermark)     // ← 用的是批处理"开始时"的水位
    .sort((a, b) => a.seq - b.seq)

  const next: TaskViewState = { ...state }
  for (const event of fresh) {
    applyOne(next, event)
    watermark = Math.max(watermark, event.seq)
  }
```

`filter` 在循环**之前**对整批做一次判定，用的是**批开始时**的水位。
于是同一批里出现两个 `seq = 7` 时，两条都大于初始水位、两条都通过，
**两条都会被应用**。

而"同一批里出现两个相同的 seq"恰恰是续传会产生的形状：
`?from_seq=` 的补发段与订阅的实时段接在一起，
同一帧既在 journal 的补发里、又在推送里。
水位全程只看批开始时那一个值，所以它挡不住这件事。

**为什么注释会走在代码前面**：

写注释的时候想的是"这里要去重"，写代码的时候写成了"这里要过滤掉已经看过的"，
而这两句话在**单条事件**的尺度上确实是一回事——
所以自审读到这里的时候，注释和代码看起来是一致的。
它们只在"一批里有重复"这个尺度上分开，而那个尺度没有被想到。

**修复**：

把判定挪进循环：

```ts
  for (const event of fresh) {
    if (event.seq <= watermark) continue
    watermark = event.seq
    applyOne(next, event)
  }
```

排过序之后 `watermark` 单调递增，所以是赋值而不是 `Math.max`。

**验证**：

`taskEvents.test.ts` 里新增一条：同一批里两条 `seq = 7`，断言思维流里只有一条。
把 `continue` 那行改成 `if (false) continue`（反证 F1 的突变）→ **抓住**。

反证这个动作在这里是必需的：这个缺陷**只在有测试的前提下才存在**，
所以"修好了"这件事本身没有任何外部信号能确认。

**教训**：

**一批里的不变量和一条里的不变量不是同一个不变量。**
去重、排序、上限这三件事都有这个形状：
写成"对整批做一次判定"往往看起来更整齐，而它恰好漏掉批内的情况。
`sort` 那一条更隐蔽——见问题 18。

---

## 问题 18：反证抓出了两条**测试自己**是假的

**日期**：2026-09-16（阶段 6）

**现象**：

前端的守卫以前是**手工**反证的（改一行、跑一遍、改回来），
没有任何脚本留下痕迹。所以这一轮把后端 `falsify_*.py` 的做法
搬到前端，写了 `frontend/scripts/falsify_frontend.py`：
22 条突变，每条声明"该由谁抓住"——
`test`（必须有一条指定名字的用例红）或 `tsc`（必须编译不过）。

第一次跑，**两条突变没被抓住**：

```
**漏掉**   F2 去掉按 seq 排序（批内乱序时进度条退回去）
**漏掉**   F13 reset 不清 scheduled（之后所有事件都不再渲染）
```

两条都不是"守卫写少了"，是**守卫写假了**。

**F12 / F2：测试测的不是它声称的那件事**

F12 的突变是把 `Math.max(0, options.fromSeq ?? 0)` 里的夹取去掉。
这条守卫当时断言的是 URL：

```ts
openTaskStream('TK-1', h, { fromSeq: -5 })
expect(FakeEventSource.last.url).toBe('/api/tasks/TK-1/stream')
```

而 URL 那一边是靠 `fromSeq > 0 ? ... : ''` 挡住的——
`Math.max` 在不在，`-5 > 0` 都是假，URL 都一样。
**这个断言和它要守的那行代码没有任何关系。**

夹取真正保护的是**初始水位**（`let watermark = fromSeq`），
它会成为 `onDropped()` 报出去的那个数。改断言成
`expect(stream.watermark()).toBe(0)` 之后，突变被抓住了。

F2 更值得记。它的突变是去掉 `.sort(...)`，而原测试断言
"乱序到达时进度不倒退"，理由是注释里写的"按到达顺序处理会让进度条退回去"。
**去掉排序之后这条断言照样通过**——而且原因很有意思：

```
乱序到达 0.8(seq3)、0.2(seq1)、0.5(seq2)
循环里：seq3 先应用，水位推到 3；seq1、seq2 都 ≤ 3 → 被去重守卫丢掉
→ progress 停在 0.8，断言通过
```

真正坏掉的是**丢事件**：那两条是被当成"已经看过"丢掉的，
而水位同时被推到了最大值，所以之后每次重连都从最大值之后补发，
那几条思维再也回不来。是同一批里另一条测试
（`乱序时思维流也按 seq 排`）抓住了这个突变。

所以 `sort` 的作用**不是**"让进度更好看"，是**不丢数据**——
这个理由比原来那个强得多，而原来那个理由是错的。
注释也一并改了。

**F13：我给 `reset()` 写的注释描述了一个这段代码不可能产生的 bug**

突变是把 `reset()` 里的 `scheduled = false` 删掉，原测试断言
"reset 之后重建连接，事件照样会渲染"。它**没红**，因为：

```
emit 一帧 → scheduleFlush 排了一个 rAF 回调，scheduled = true
reset()   → 那个回调还在队列里
emit 一帧 → scheduleFlush 看到 scheduled 是 true，提前返回（不排新帧）
flush     → 跑的是**上一个**回调，它照样会 flush，也照样把 scheduled 抹回 false
```

`reset()` 留下的那个 `true` 会在下一个 rAF 回调里自己恢复。
**所以它根本不是一个线上 bug**，而我在 `reset()` 里写的注释是：

> 于是**此后所有事件都不再渲染**，页面上一条思维都不出现，日志干净。

这句话是假的。它和问题 15 里那条是同一个毛病
（"注释里写一个代码不可能产生的 bug，和漏写一个真实存在的 bug 一样坏"），
只是这次是在**刚写完的注释**里犯的。

改法有两步：
- **注释改成实话**：那一行不是修 bug，是让 `reset()` 真的回到初始状态；
  并写明它从"卫生"变成"必需"只需要一个看着像优化的改动——
  有人给 `reset()` 加一句 `cancelAnimationFrame`，那一刻起
  没有任何东西会再清那个标志，而后果正如原注释所说。
- **测试换成真的能红的**：`reset()` 丢掉缓冲里没落地的帧
  （突变：删掉 `pending = []`，抓住）。

**教训**：

前面几条问题记的都是"代码有 bug、测试没抓到"。这一条记的是第三种情况，
比前两种都难发现：**测试和注释都在，也都读得通，只是它们守的不是同一件事。**

反证之所以必须成脚本、必须每次都跑，原因就在这里：
手工反证只能证明"我把这行改坏了会红"，
而它会逼着人**逐条去问"这条断言和那行代码之间到底有没有关系"**。
F12 那个断言单独看毫无破绽——它通过、它有意义、它测了一个真实的行为
（负数不带查询参数），**只是那个行为不是它声称要守的那一行产生的。**

一句话：**一条测试通过，说明不了它在测什么。**

---

## 问题 19：一个删掉也没人发现的参数，和一条"缺陷不可能出现"的测试

**日期**：2026-09-17（阶段 6）

**现象**：

给 `useTaskStream`（"先取快照、再连流、离开时断开"）补了 16 条用例，
把突变扩到 31 条再跑一遍。**又是两条漏掉**：

```
**漏掉**   F24 connect 时丢掉快照给的水位（刷新页面之后从头重传整个 journal）
**漏掉**   F26 丢掉 cancelled 守卫（旧任务的快照回来把新任务盖掉）
```

问题 18 的教训是"测试可能是假的"。这次的两条是**另外两种**：
一条是**被测代码里有一段删掉也不会怎样的冗余**，
一条是**测试的回放顺序让那个缺陷根本不可能出现**。

**F24：传了一个删掉也没人发现的参数**

当时的调用是 `store.connect(taskId, snapshot.lastSeq)`。
突变把这半个参数删掉，门禁**全绿**。原因是：

```
hydrate(snapshot)   → lastSeq = max(0, snapshot.lastSeq) = 137
connect(taskId)     → resumeFrom = max(fromSeq ?? 0, get().lastSeq)
                    → max(0, 137) = 137
```

`hydrate` 已经把水位写进 store 了，而 `connect` 本来就会取
`max(fromSeq, 手上水位)`——**第三份拷贝**。
断言里的 `?from_seq=137` 是靠前两句撑起来的，和那半个参数毫无关系。

注意这和问题 18 里 F12 的形状一样（断言守的不是它声称的那行代码），
但**结论相反**：F12 是测试写错了、代码是对的；这里代码也不对——
**那半个参数是装饰，不是守卫**。它读起来像一层保险，
而反证证明它一层都没保（真要丢了 `hydrate` 那一句，它才起作用，
可那时候 F24 的突变形态就完全变了）。

修法是**把冗余删掉**，而不是给它补一条测试：

```ts
store.reset()
store.hydrate(snapshot)
// 水位不在这里传……
store.connect(taskId)
```

这样水位只有一个来源（store），于是"先 hydrate 再 connect"
从一个**建议的顺序**变成**必需的顺序**——反过来水位就是 0。
删掉一个删得掉的参数，比给一个删不掉的参数写测试更值。

**F26：测试自己的回放顺序让缺陷不可能出现**

原用例是这么写的：

```ts
const first = await requestAt(0)        // TK-1 的请求（不发回）
rerender({ taskId: 'TK-2' })
await act(async () => { first.ok(TK-1 的快照) })   // ← 先回旧的
await act(async () => { (await requestAt(1)).ok(TK-2 的快照) })  // ← 后回新的
expect(useTaskStore.getState().subject).toBe('新的')
```

**先回旧的、后回新的**，于是"后到的覆盖先到的"——
缺了 `cancelled` 守卫，最后落地的仍然是新任务，断言全绿。
要抓这个缺陷，慢的那条必须**最后**到：

```ts
const stale = await requestAt(0)
rerender({ taskId: 'TK-2' })
await act(async () => { (await requestAt(1)).ok(TK-2 的快照) })  // 新的先回
await act(async () => { stale.ok(TK-1 的快照) })                 // 旧的后到
expect(useTaskStore.getState().subject).toBe('新的')
```

顺带把这条用例能断言的东西加满：少了守卫时，那个迟到的 `.then` 不只是
把内容盖掉，它还会**为 TK-1 再开一条流**——于是两条流并存，
都往同一个 store 里灌。断言 URL 指向 TK-2 的流即可。

**根因**：

两条都不是"忘了写守卫"。一条是**写了个不起作用的守卫**，
一条是**用能通过的顺序去测一个只在另一种顺序下才出现的缺陷**。
它们有一个共同点：**光看代码看不出来**。
`connect(taskId, snapshot.lastSeq)` 读起来比 `connect(taskId)` 更稳妥，
先回旧快照读起来比后回旧快照更自然——两处都在"更稳妥/更自然"的方向上错。

**验证**：

`python scripts/falsify_frontend.py` → 31 条突变，**31 条抓住**，`已还原：True`。
前端门禁：`tsc -b --noEmit` 绿、`oxlint src` 零告警、
`vitest run` 147 条全过（含新增 `useTaskStream` 16 条）。

**教训**：

反证到现在抓出了三类东西，一类比一类难发现：

1. **代码有 bug、测试没抓到**（问题 1～17 里的多数）——最容易发现，写个测试就行。
2. **测试是假的**（问题 18）——测试读得通、能通过，但断言和它声称守的
   那行代码没关系。
3. **测试和代码都"看起来更稳妥"，于是缺陷被它自己的写法排除掉了**（本条）。

第 3 类的危险在于：它不会留下任何痕迹。缺陷是被**测试自己的构造顺序**
挡住的，所以就算把守卫删掉，也**永远不会有红灯**。
要发现它，唯一的办法是**逐条突变、逐条问"这个缺陷真的有机会发生吗"**——
而不是问"这个断言真的在跑吗"。

**附：连我为反证脚本新写的那道守卫，自己也假了一次**

被强杀之后我加了一道"现场不干净"的检测：开跑前扫一遍目标文件，
如果有文件还停在上一轮突变的状态，就点名它并停，而不是丢一句
"tsc 红了"让人去翻源码。加完第一次跑，它报了：

```
现场不干净——下面这些文件还停在**上一轮突变**的状态：
  frontend\src\hooks\useTaskStream.ts  ←  F24 拿到快照却不喂给 store 就接流……
  frontend\src\hooks\useTaskStream.ts  ←  F26 丢掉 cancelled 守卫……
  frontend\src\hooks\useTaskStream.ts  ←  F30、F31 ……
```

而那时候树是**干净的**（`tsc -b --noEmit` 刚过）。

根因和 F24 是同一件事：判断写成了 `new in text`，而 F24 的 `new`
（`store.reset()`）**是 `old`（`store.reset()` + `store.hydrate(snapshot)`）
的前缀**——干净的树上当然也含它。于是"替换后的文本在不在"这个判据，
对"把 A 换成 A 的一部分"这类突变恒真。

改成两个条件都成立才算残留：**原文不在了，而替换后的文本在。**

改完又反过来验了一遍这道守卫本身（在内存里逐条把突变放回去，不动磁盘）：
31 条里 30 条能点名，漏掉的是 F25——它是**插入式**的（原文还在，只是多了一段），
`old not in text` 不成立。这条缺口不打算补：残留最终由**基线门禁**兜住
（基线一红就停，不会往下跑），这道检测的职责只是"让现场更容易认"。
把这句话写进了它的 docstring——**一个只有 30/31 命中的守卫，
必须写明它不覆盖什么**，否则下一个人会当成 31/31 来信任。

两轮的教训合起来是一句：**"在不在"这种判据太弱，撑不起任何一条守卫。**
F24 用它判"参数有没有用"，假的；F26 用它（"后到的赢"）判"覆盖有没有发生"，假的；
这道检测用它判"现场干不干净"，也假的。三次都是**"能通过"不等于"在测那件事"**。

---

## 问题 20：一条做不到的验收，和一个"让行为可观测"的开关

**日期**：2026-09-18（阶段 6）

**现象**：

阶段 6 的验收里有这么一条：

> 浏览器跑完整一次；**跑到一半断网 10 秒，恢复后无缺口、无重复**；
> 跑到一半刷新页面，工作台从 journal 恢复。

这条在 mock 下**物理上做不到**。mock 快到一整个 quick 任务 0.2 秒跑完，
"跑到一半"这个时刻在人类反应过来之前就结束了，断网 10 秒更无从谈起。
于是这条验收一直停在清单上，写法是"浏览器走一遍"——而走不了。

比"验收做不了"更值得写下来的是它的**后果**：这条路径（浏览器
`EventSource` 的自动重连、`Last-Event-ID` 续传、刷新后从 journal 恢复水位）
**从来没有在真实浏览器里被触发过**。测试里覆盖它的是我自己写的
`FakeEventSource`，而"我的替身和真浏览器一样吗"这件事本身是个假设——
问题 18 已经教过一次：替身写得太顺手，就会把被测代码护在缺陷之外。

**这不是 bug，是能力缺口**

零成本的端到端跑得快，本身是优点；但它同时是**所有跟时间有关的行为的盲区**。
同一类盲区还有 SSE 心跳（15 秒一次）、并发订阅的窗口、压测里的真实并发度。

**修法：`MOCK_LATENCY_MS`**

mock 的每次 provider 调用后人为等待 N 毫秒，默认 0。三个调用点各一处：
`MockLLMProvider.chat` / `MockSearchProvider.search` / `MockFetcher.fetch`。

两个刻意的选择：

1. **等待放在失败判断之前。** 真实 provider 的一轮失败（429、超时）同样
   花掉一个往返。放到后面的话，mock 一调慢就成了"成功慢、失败瞬回"，
   而返工与降级恰恰是被失败驱动的两种分支——测出来的时序是假的。
2. **默认必须是 0。** 一次 quick 运行的 provider 调用数以百计，默认值只要
   不是 0，全部测试（单元、集成、e2e）都要陪着等，而症状只是"测试变慢了"，
   没人会因此去查 mock。

**代价（实测，不是估计）**

写成 `.env` 里的值对**测试**同样生效：集成测试经由 registry 用
`get_settings()` 构造 provider。于是

```
pytest tests/integration          10.89s
MOCK_LATENCY_MS=300 pytest ...   172.89s   （16 倍）
```

所以演示时必须把它作为**一次性环境变量**传给进程，而不是写进 `.env`。
这句话连同实测数字一起写进了 `.env.example`——只说"注意别写进 .env"
是没有用的，得让人看见代价是多少。

调多大：实测 4000ms 下一次 quick 是**建任务 15 秒 + 流式 48 秒 ≈ 63 秒**，
够你打开 DevTools、勾上 Offline、等满 10 秒、再取消。

**反证（6 条）**

新增 `scripts/falsify_mock_latency.py`。这个开关尤其需要反证：它是一条
**行为**开关，而"开关没接上"的表现与"接上了但没人设这个值"**完全一样**——
任何现有的测试都不会红，唯一的证据就是把它拆掉看红不红。

```
抓住  M1 删掉 search 里的等待（搜索不再受开关控制）
抓住  M2 等待挪到 search 的失败判断之后（失败路径瞬回）
抓住  M3 删掉 LLM 的等待（补全不再受开关控制）
抓住  M4 等待挪到 LLM 的失败判断之后
抓住  M5 删掉 fetch 里的等待（抓取不再受开关控制）
抓住  M6 默认值改成 100（整个测试套件陪着等）
已还原：True
```

六条各自由**点名的**那条测试抓住（`test_搜索走这个开关`、
`test_搜索失败的那次调用也一样等待`……），不是"反正红了"。

**附：反证脚本自己的一次"假抓住"，比上面六条都值得记**

第一版用字符串拼接删调用点：

```python
pieces = orig.split(CALL)
mutated = CALL.join(pieces[:1] + pieces[2:])   # 想删第一处调用
```

它报"抓住"了。但它是假的：`pieces[1]` 是**两处调用之间的 88 行代码**，
这一次拼接把那段整个删掉了，于是 `app.providers.mock` 里不再有
`MockFetcher`，测试报的是收集期 `ImportError`：

```
ImportError: cannot import name 'MockFetcher' from 'app.providers.mock'
```

任何一个 import 这个模块的测试都会这样红，和被测的那条守卫没有半点关系。
它不是"抓住了缺陷"，它是**把程序弄坏了**。

三条判据补进脚本，缺一条这个坑就会再踩：

1. 突变**只许删它该删的那几行**（脚本现在打印每次编辑的行数变化）
2. 出红的必须是**点名的**那条测试——这条 `falsify_cli_render.py` 里早就有，
   我这个临时脚本没有，所以才被自己骗过去
3. **收集期错误一律不算抓住**（`ERROR ` 开头的行），宁可报"漏掉"再去查

第 2 条尤其值得记：**同一份仓库里已经有一条写对了的规矩，我在它旁边
临时写了个脚本，就顺手把这条规矩丢了。** 规矩不是知道就行，是要长在
脚本里——它没有长在我这次的位置上。

**验证**

- `scripts/falsify_mock_latency.py` → 6/6 抓住，`已还原：True`，退出 0
- `pytest` 全量 → `549 passed, 6 deselected`（新增 7 条）
- 后端以 `MOCK_LATENCY_MS=4000` 起，实跑一次 quick：**建任务 15.0 秒 +
  流式 48.2 秒 = 63.2 秒**走完，任务 `TK-e2489dda0d56`，报告 `RP-a0ae0f3fc89b`

---

## 问题 21：lint 这条守卫不会红

**日期**：2026-09-18（阶段 6）

**现象**：

跑完前端门禁，`oxlint src` **一行输出都没有、退出码 0**。

"零告警"和"什么都没扫"在输出上**完全一样**。这类"静默成功"正是这一整轮
在查的东西，所以我没有把它当成绿灯，而是塞了一个故意违规的文件进去：

```tsx
export function Probe() {
  const unused = 1
  if (true) { return null }
  return <div />
}
```

它确实报了：

```
src/__lint_probe.tsx:2:9: warning eslint(no-unused-vars): ...
src/__lint_probe.tsx:3:7: warning eslint(no-constant-condition): ...
```

**而退出码是 0。**

**根因**

`oxlint` 的退出码只反映**错误**，与**告警**无关。而 `package.json` 里写的是：

```json
"lint": "oxlint"
```

于是这条命令的真实语义是"把告警打印到屏幕上"。任何只看退出码的地方
——`&&` 串起来的脚本、CI、我自己敲的那条"跑完门禁"——**带着任意多条
告警都会通过**。这条守卫不会红。

它比"一条抓不住 bug 的测试"还要低一档：测试至少还有人断言过；
而"一条没人必须读的输出"，连断言都不存在。

**修法**

```json
"lint": "oxlint src --deny-warnings"
```

两处改动，各有理由：

- `--deny-warnings`：告警也产生非零退出码。**判据从"有没有人看"
  变成"退出码是多少"**——门禁唯一能被脚本读懂的语言就是退出码。
- 补上 `src`：原来的 `oxlint` 不带路径，扫的是当前工作目录。开发时
  站在 `frontend/` 下跑也没什么问题，但它的范围是**碰巧对**的，
  而范围该由写的人指定。顺带把 `node_modules` 那类噪音排除在意图之外。

**验证（两个方向都测）**

```
干净树          npm run lint  →  exit 0
塞一个未使用变量  npm run lint  →  exit 1（并打印那条 warning）
删掉探针文件      npm run lint  →  exit 0
```

只测"干净时是绿的"等于没测——问题 18 的教训。**守卫要能红，
才谈得上绿。**

**留下的问题**

`npm run build` 目前是 `tsc -b && vite build`，**不含 lint**。这是有意的：
两者分开，红的时候能一眼看出是类型问题还是风格问题。但这也意味着
"构建通过"**不等于**"lint 通过"——写进阶段 9 的 CI 时要记得两条都跑，
别只跑 build。这一点已经写进 `修补文档.md` 的验收清单。

---

## 问题 22：一个"已经停掉"的后台进程，和一份会误诊的现场报告

**日期**：2026-09-18（阶段 6）

这一条记的是**工具链**的错，不是产品的错，而且它比前面那些更值得写下来：
前面每一条的后果是"某段代码错了"，这一条的后果是**"我不知道哪段代码错了"**——
它损坏的是我用来判断对错的仪器本身。

### 22.1 现象：同一棵树，两次读数互斥

46 条突变的整套反证跑完之后，我在"确认现场干净"这一步上拿到了两组
互相矛盾的结果：

```
第一次：tsc 绿、oxlint 绿、vitest 168 passed / 0 failed
紧接着：vitest 红，失败的正是 F10 的证人用例
```

间隔不到一分钟，同一棵树，测试结论翻转。第一反应是"有回归"，
但两组读数之间**我没有改任何代码**。所以更可能的是：这棵树不是我以为的那棵。

### 22.2 定位：脚本"跑完了"，进程还活着

反证脚本会改源码再还原。它的收尾靠 `finally`——异常能盖住，
但进程**没结束**的话 `finally` 也不会跑。而我在 UI 里看到任务已经"停止"，
就据此认为树是静止的。

证据是直接把文件哈希打出来、隔 25 秒采样两次：

```
src/store/taskStore.ts   a3f1…  →  7c92…     # 文件在自己变
```

文件在自己变，说明**那个后台进程还活着**，正拿着突变后的内容往盘上写。
前面那次"全绿"只是我在它两次写入之间撞上的一个瞬间；后面那次"红"
是它刚好把 F10 的突变写了进去。

两个后果，一个比一个坏：

- **假绿**：撞上写入间隙 → 报"全绿"，而树上有残留。
- **假红**：撞上突变在位 → 报"回归了"，而回归根本不存在。
  假红比假绿更糟：它**长得和真回归一模一样**，我会照着它去翻源码，
  找一个不存在的类型错误。

### 22.3 第一版修法，以及它为什么更坏

被强杀的现场需要有人告诉后来者"哪些文件还停在突变状态"。第一版用的是
**锚点启发式**——对每条突变，检查"原文不在了、替换后的文本在"：

```python
if new in text and old not in text:
    found.append(name)
```

它在真实现场上给了一份**点名报告**，而且点错了。现场里真正残留的是 F25
（把 `store.connect(taskId)` **插**在 `store.hydrate(snapshot)` 前面）：

```
实际文件：store.reset()  →  store.connect(taskId)  →  store.hydrate(snapshot)
脚本报的：F23、F24
```

原因是**突变之间会互相遮挡**。F23 的锚点是 `store.reset()
store.hydrate(...)`，
F24 的锚点是它的前缀——F25 插进去的那一行正好把这两条锚点**切断**，
于是"原文不在了"成立、"替换后的文本还在"也成立，两条同时命中。
而 F25 自己（插入式：原文**还在**，只是多了一段）反而一条都不满足。

它连一句"我不知道"都不会说：它报的是两个错误的名字，**语气却是确定的**。
按它去还原，我会留下真正的残留，再往文件里塞一句多余的 `store.reset()`
——比不还原更坏。

同一个现场，我在验收脚本里把两种判据并排跑了一次：

```
旧启发式点名的突变：['F23', 'F24']
```

真话（F25）不在里面。这就把"换掉它"从洁癖变成了有据可依。

### 22.4 修法：把判断题变成比对题

判断"现场干不干净"不该去**解释**现场，只该**描述**现场。所以改成开跑前
把每个会被改的文件按字节存一份快照（`REPO/.falsify-baseline/`），
之后所有判断都退化成哈希比对：

| 能力 | 旧（锚点启发式） | 新（字节快照） |
|---|---|---|
| 认出"改 A 为 B"式残留 | 能 | 能 |
| 认出"插入式"残留（F25） | **不能** | 能 |
| 会不会张冠李戴 | **会**（F25 现场报成 F23/F24） | 不会 |
| 能不能修 | 不能 | 能（`--restore`，按字节） |
| 干净树上会不会误报 | 取决于锚点是否互相包含 | 不会 |

另外补了两件事：

- **`RUNNING` 标记**：开跑时写下、正常结束时删掉。它**不参与判断**
  （判断只有"字节一样吗"这一条），只用来**解释**——让报错能说清
  "这是上一轮被强杀留下的"，而不是干巴巴一句"文件变了"。
- **脏了就拒绝开跑**：不干净时直接退出，而不是"顺手往下跑"。
  因为快照一旦被已经坏掉的内容覆盖，之后所有"已还原：True"都会变成假的
  ——**一个被污染的快照会让这个脚本永远说好话**。要接受现状得显式
  `--resnapshot`。

### 22.5 修的过程中又踩到两个坑

**一、快照目录自己被测试收走了。**

快照第一次放在 `frontend/.falsify-baseline/` 下。`vitest` 没配 `include`，
用的是默认 glob `**/*.{test,spec}.?(c|m)[jt]s?(x)`，而默认 exclude 里
**没有**这个名字。于是 9 份备份里那份 `task.contract.test.ts` 的副本
**真的被当成测试收走了**，实测：

```
测试文件数：10（9 个真的 + 1 个副本）
副本：status = failed，assertionResults = 0
      message: Failed to resolve import "./domain"
success: false   numFailedTests: 0        ← 注意这一对
```

`success: false` 配 `numFailedTests: 0`，因为副本从
`.falsify-baseline/files/` 出发解析不了相对 import，**一条用例都没跑**。
而当时的 `run_gate()` 只看失败数、不看 vitest 自己的退出码——也就是说
**我用来判断别人有没有说谎的仪器，自己会把"没跑起来"读成"全过"**。

这个坑有两层，分开堵：把快照挪到 `REPO/.falsify-baseline/`
（vitest 的 root 是 `frontend/`，够不着），备份名统一加 `.bak`
（即使 glob 以后被放宽，`*.test.ts.bak` 也匹配不上）。位置和名字是
两道独立的锁。

**二、`label()` 是相对仓库根的，解析时却拼了 `FRONTEND`。**

有一处突变在 `contracts/task_states.json`，它在 `frontend/` 之外，
所以 `label()` 返回的是**相对仓库根**的路径。新写的还原代码顺手写成
`(FRONTEND / rel)`，于是每个文件都解析到 `frontend/frontend/...` 去，
查出来一律"文件不存在"，`--restore` 一个都还原不了。这条是验收脚本
自己抓出来的（`D5`/`D6`/`D7` 三条 FAIL），不是我读出来的——
能渲染出来的错误比能推理出来的错误可靠。收口成一个 `_resolve()`。

顺带在同一处把 `vitest_ok`（退出码与 `success` 都判）加进了 `run_gate()`，
并让"收集失败"在两种判定里分别说话：

- 基线时：vitest 自身没成功 → 直接拒绝开跑（仪器坏了不能用它量东西）；
- 判证人时：**单独标注"门禁自己崩了，这条结论不算数"**，不计入"漏掉"。
  因为两者的下一步动作完全相反：一个去补测试，一个去修现场。

### 22.6 验证

反证脚本自己也要被反证。写了 17 条断言验收新判据（`xm3_detector_check.py`），
关键几条：

| # | 断言 | 结果 |
|---|---|---|
| A | 干净树上没有差异（否则它自己就是噪声） | PASS |
| B1/B2 | 手工放进 F25 后**只报一个文件**，且是它 | PASS |
| B3 | 报错里显示第一处差异：`基线 'store.hydrate(snapshot)' ≠ 当前 'store.connect(taskId)'` | PASS |
| C1/C2 | 同一现场，旧启发式点出 F23/F24，**且没点出 F25** | PASS |
| D1–D3 | 脏现场**拒绝开跑**（exit 1），一条突变都不跑 | PASS |
| D4 | `RUNNING` 在时报"上一轮没有正常结束" | PASS |
| D5–D7 | `--restore` 按字节还原（`read_bytes() == 原备份`），还原后再查是干净的 | PASS |
| E1–E3 | 快照目录在 `frontend` 之外；vitest 只收 9 个文件；`success=True / 168 passed` | PASS |

`C` 那两条是这一条记录的核心：**同一个现场，旧判据给了一份自信的错答案。**
只测"新判据能认出 F25"是不够的——那证明不了旧判据错在哪，
也就留不下"为什么必须换"的依据。

### 22.7 换完判据之后，整套反证重跑了一遍

工具修好之后，46 条突变从头跑了一遍（这是它第一次在干净的仪器上跑完）：

```
46 抓住 / 0 漏掉 / 0 无效 / 0 门禁自己崩了
已还原：True        exit=0
```

随后 `--status` 报告：

```
快照里有 9 个文件，最后开跑前记录于 manifest.json。
现场与快照逐字节一致。
```

**"逐字节一致"这句话本身也被反证了一次。** 它同样是一句可能恒真的读数——
如果 `_resolve()` 又把路径拼错（见 22.5 第二条），每个文件都查不到，
输出会是什么？一个诚实的检查会报"文件不存在"，但也可能报"一致"。
所以手工往 `taskStore.ts` 追加了一行再问一次：

```
现场与快照不同：
  frontend\src\store\taskStore.ts：前 215 行相同，当前多了 1 行
```

它认得出来，而且报到了行号。**"干净"这个读数因此是有内容的**，
不是"什么都没检查"的另一种说法。

顺带确认了一件本来会让人误判的事：`HomePage.tsx` 与 `lib/api.ts`
**不在**这 9 个文件里（快照只覆盖突变目标），所以对这两个文件的改动
不会让快照变脏。当时正好改了它们，看到"一致"的第一反应是
"检查没生效"——查过清单才确认是真的不一致才怪。

**教训**

1. **"进程停了"和"进程还活着"在 UI 上长得一样。** 验证必须以
   **进程自己给出的完成标记**为准（这里是收尾打印的"已还原：True"），
   不能以"界面看起来停了"为准。
2. **同一个现场被反复误诊时，换判据，别修判据。** 锚点启发式在"插入式"
   残留上无解——这不是实现不够好，是方法不对。
3. **不要问"哪条突变还在"，要问"字节一样吗"。** 前一个问题需要解释现场，
   后一个只需要描述现场。**能描述的就不要解释。**
4. **仪器要连自己一起量。** 一个用来判断"测试是不是真的会红"的脚本，
   本身也必须有能红的守卫——这一条里它红了两次（D5–D7、E2–E3），
   两次都是真实缺陷。

---

## 问题 23：返工永远搜不到东西——一个恒为 0 的指标，和一条从没被执行过的路径

### 23.1 现象

`metrics` 里有一个指标叫 **返工提升 Δ**（`metrics_after - metrics_before`），
写在四条铁律的第三条里，是被当成"系统真的会自我修正"的证据的。
它一直是 0。

0 这个数不叫唤。它的读法是"这一轮没触发返工"或者"触发了但没补到东西"，
两种读法都很正常，都不会让任何东西变红。真正让它变成问题的是
另外一件事：**把返工逼出来之后，它还是 0。**

### 23.2 定位：先证明这条路径**从来没有被执行过**

在动手改之前先量一件事：现有测试里，返工到底跑起来过没有？

答案是没有。`decide_rework` 在三个档位上跑完整流水线，返回的都是：

```
没有达到返工门槛的问题（minor 只记录）
```

也就是说 mock 数据下审计一次都不判返工。整条返工路径
（`collect.run(rework=True)` → `_rework_queries` → 合并新证据 → `metrics_after`）
**从来没有被任何一条测试走过**。它是一个只存在于代码里的分支。

这一条比缺陷本身更重要，因为它解释了缺陷为什么能活下来：
**A2 不是"测试没测出来"，而是"这段代码在测试里等于不存在"。**

顺带发现同一类的第二处：`build_queries` 的 docstring 写着

> 纯函数（只读 ctx），可单测。预算逻辑有明确对错，不该和网络调用混在一起测。

而全仓库**没有任何一个测试文件 import 过它**。这句话承诺了可测性，
但从没被兑现——所以预算算错了也没有任何地方会知道。

### 23.3 两个缺陷（它们是两个，不是一个）

**其一：返工和首轮共用一本账。**

```python
budget = max(0, ctx.mode.max_search_calls - ctx.search_calls_made)   # 旧
```

首轮的查询计划是 `planned[:budget]` —— **按剩余预算截断**。
所以首轮结束时这个池子几乎必然是 0。返工再问它要额度，拿到的永远是 0：

```
首轮预算已用尽（24/24），本轮没有发起任何搜索
```

于是返工"什么都没补到"。而这句话在报告里的措辞是关于**搜索源**的，
读起来像是 provider 不好用；真相是这个程序**一次请求都没有发出去**。
报错指错了旋钮——它会让人去换搜索源，而该动的是预算。

**其二：就算给了返工独立的额度，它跑的仍是首轮那批查询。**

返工轮曾经复跑第 1~3 轮（`品牌 维度`、`品牌 角度`、平台定向）。
这些查询文本和首轮一模一样，返回的是**同一批 URL**，
经过 `evidence_id` 去重之后，一条新证据都进不来。
它会白吃掉这个本来就小的池子，还会让"Δ 是哪来的"彻底说不清。

第二条单独看是"浪费"，和第一条合起来才是致命的：
**共用的池子保证它搜不出去，复跑的查询保证它搜出去也没用。**
两个缺陷症状完全相同，都是 `Δ = 0`。

### 23.4 修法

**给返工一本自己的账**（`app/core/modes.py`）：

| 档位 | `max_search_calls`（首轮） | `rework_search_calls`（返工，所有轮次加起来） |
|---|---|---|
| quick | 24 | 8 |
| deep | 64 | 24 |
| expert | 160 | 48 |

下界不是拍出来的：**至少要够跑完一轮完整目标**（`max_dimensions × 2`），
否则"补上缺口"这件事在结构上做不到——补到一半池子就没了，
剩下半个缺口和完全没补在指标上长得一样。这条被写成了断言
（`test_返工池至少够跑一轮完整目标`），而不是写在注释里。

**返工轮只跑指定目标**（`app/core/pipeline/collect.py`）：

```python
if rework:
    budget = max(0, ctx.mode.rework_search_calls - ctx.rework_search_calls_made)
    if budget <= 0:
        return []
    # 返工轮**只**跑指定目标。不复跑第 1~3 轮：那些查询文本和首轮
    # 一模一样，会返回同一批 URL，去重之后一条新证据都进不来。
    return _rework_queries(targets)[:budget]
```

`_rework_queries` 从 `build_queries` 里被抽成一个独立函数，
两个分支共用**同一份**目标翻译逻辑——分开写两份的话，
"目标的优先级高于普通查询"这件事会在其中一份里悄悄失效。

**记账收在一个点上**（`app/core/pipeline/calls.py`）：

```python
search_sync(ctx, query, *, rework=False)   # 唯一的收口
```

`CallStats.rework_search_calls` 是 `search_calls` 的**子集**而不是并列项：
返工的开销也要算进总成本（不然报告少算钱），
但归因时要能分出"哪部分是首轮、哪部分是返工"——
"Δ 是花了多少换来的"正是这个指标存在的意义。
所以 `to_dict()` 里两者并存，而 API payload 明确注释了
**不能**把它并进 `maxSearchCalls`。

**降级文案要说清是哪个池子**：

```
返工搜索预算已用尽（8/8），本轮没有发起任何搜索
```

"首轮"和"返工"两个词是这句话的全部价值——含混的一句
"预算已用尽"会让人去加大 `max_search_calls`，而返工花的是另一个池子，
改那个数一点用都没有。

### 23.5 夹具：怎么把返工逼出来

改完了还得能测。mock 数据下审计不判返工，所以不能靠"跑一遍看会不会返工"。

办法是让首轮**真的失败一个维度**：`MockSearchProvider.fail_queries`
让 `Notion 定价` 这条 query 抛 `Transient`——等同于真实 provider 的
429/超时，是返工存在的原始理由。那个维度于是没有任何证据，
审计给出 blocker、返工目标指向它。补采前把失败清掉，
于是"首轮失败 → 补采成功"这条路径走通了。

这比"构造一个假的返工目标"更硬：目标是从真实的审计里长出来的，
所以**测的是整条链**，不是链的最后一环。

### 23.6 过程中踩到的坑

**一、反证脚本假设"只有一个目标文件"。**

返工这条链跨两个模块：预算在 `collect.py`，记账在 `calls.py`。
第一版脚本只写了 `TARGET`，M3 于是在 `collect.py` 里找 `calls.py` 的
那三行，锚点数 0，脚本自己抛 `AssertionError` 炸了。

比炸更糟的是**它炸掉了前面的结论**：断言在打印之前，
所以 M1/M2 的结果一起丢了。改了两处——每条突变自带目标文件，
以及**逐条打印**，后面炸了也不影响前面已经得出的判断。

还加了第三条：锚点匹配数不等于 1 时，报"这条突变根本没生效，
下面的结论无效"，而不是让它混进"抓住"里。**反证工具自己也需要反证。**

**二、三条我写错的期望值（都是"记错了"而不是"代码错了"）。**

| 我写的 | 实测 | 真相 |
|---|---|---|
| 首轮一定花光池子，所以返工拿到 0 | 拿到 8 | 首轮只有**计划超过预算**时才截断 |
| 加目标不改变首轮查询数 | 26 → 28 | 只在截断时成立；不截断时它就是不一样 |
| 一轮返工花光返工池 | 只花 2/8 | 一轮目标数由审计决定（1 维度 × 2 关键词） |

第三条尤其值得记：**"见底"是个跨轮次的状态**。
返工池是"所有返工轮加起来"的额度，不是每轮一份——
所以测试要循环到池子见底才能断言上限，而不是跑一轮就断言。
第一版测试跑一轮就断言 `== rework_search_calls`，当然红。

### 23.7 验证

**测试**（新增 25 条，全绿；写这一条时后端合计 **574 passed, 6 deselected**，
加上问题 24 的 14 条之后是 **588**）：

| 文件 | 条数 | 守卫的是什么 |
|---|---|---|
| `tests/unit/test_collect_budget.py` | 16 | 算术：两个池子分开、各自按剩余额度截断、首轮按剩余预算截断、每档返工池都小于首轮池、返工池至少够跑一轮完整目标 |
| `tests/integration/test_rework_loop.py` | 9 | 闭环：审计拎出坏维度 → 返工搜得出去 → 维度覆盖真的变好 → 花光自己的池子后停下 → 降级文案指对池子 → 三档都走得通 |

核心那两条：

```python
def test_首轮池见底时返工照样搜得出去() -> None:
    ...
    assert planned, "首轮池见底就搜不出返工查询——两个池子又被合回去了"
```

```python
assert after["dimensionCoverage"] > before["dimensionCoverage"]   # 铁律三
```

**反证**（把缺陷放回去，守卫必须红）：

| 脚本 | 突变 | 结果 |
|---|---|---|
| `xm3_falsify_a2.py`（单元） | M1 两池合一 / M2 复跑通用查询 | 抓住，逐字节还原 |
| `xm3_falsify_a2b.py`（端到端） | M1 / M2 / M3 漏记账 | 抓住，逐字节还原 |

M3 的证人是**集成测试**（`test_首轮池见底后返工仍然搜得出去`、
`test_返工花光自己的池子后就停下`）而不是单元测试——
因为"记账漏记"这件事在纯算术层看不出问题，
它的表现是"返工池永远用不完"，只有跑起来才看得见。

```
M1 ... 抓住 test_首轮池见底时返工照样搜得出去（本轮共 11 条失败）
M1 ... 抓住 test_首轮池见底后返工仍然搜得出去（本轮共 11 条失败）
M2 ... 抓住 test_返工轮不复跑首轮的通用查询（本轮共 5 条失败）
M3 ... 抓住 test_首轮池见底后返工仍然搜得出去（本轮共 3 条失败）
M3 ... 抓住 test_返工花光自己的池子后就停下（本轮共 3 条失败）
已还原（逐字节）：True
```

**教训**

1. **一个恒为 0 的指标要当成红灯读。** "返工提升 Δ 恒为 0"不叫"没触发返工"，
   叫"这个功能可能从来没工作过"。设计文档里写下的每一条量化承诺，
   都该有一条测试去问它："你到底有没有可能不是 0？"
2. **先量"这段代码被执行过吗"，再去读它。** A2 能活下来的原因不是它藏得深，
   是它在测试里等于不存在。**没被走过的分支，读多少遍都是纸上谈兵。**
3. **症状相同不等于缺陷相同。** 两个缺陷都表现为 `Δ = 0`，
   但一个是"发不出请求"、一个是"发出去也白费"。
   只修一个的话，另一个仍然会以完全一样的症状站在原地。
4. **docstring 里那句"可单测"是一句承诺。** 写下它的时候没兑现，
   就等于给自己留了一个"这里已经测过了"的错觉。
   `build_queries` 的零调用持续了很久，就因为它头上顶着一句看起来已经测过的话。
5. **反证工具本身也要能被反证。** 锚点找不到时它必须说"这次反证无效"，
   而不是抛异常——抛异常会连前面已经成立的结论一起带走。

---

## 问题 24：一条"强断言"原本会是恒真的——以及三条自己不会红的守卫

### 24.1 起因

A3 是阶段 5 留下的三条验收缺口，其中一条写的是：

> `degraded` 强断言 —— `problems` 非空时应断言
> `body["degraded"] == ctx.degraded_blocks`；弱断言会让"降级丢了"静默通过。

这句话诊断得对，开出的方子却是错的。直接照它写下去，会得到一条
**缺陷不可能出现**的测试——因为干净跑完一条流水线时，
两边**都是空的**：

```
ctx.degraded_blocks     = []
report['degraded']      = []
段落级降级              = []
```

`[] == []` 在装配把整份清单丢掉时同样成立。这条断言越"强"
（用了 `==` 而不是子集判断），越容易让人以为它守住了什么。

方子要改的地方只有一处：**用一个必然降级的需求去跑**。含糊的需求会触发
澄清，`auto_clarify=True` 于是记下"自动采用了推荐选项（未人工确认）"——
一条报告读者有权知道的假设。实测：

```
ctx.degraded_blocks = ['需求澄清：自动采用了推荐选项（未人工确认），共 3 项']
report['degraded']  = ['需求澄清：自动采用了推荐选项（未人工确认），共 3 项']
```

先断言清单非空，再比对。**"先确认这条测试有可能失败"是写断言前的动作，
不是写完之后的检查。**

### 24.2 顺带发现：那段零覆盖的代码

查 `degraded` 什么时候会非空，把 `ctx.degrade(...)` 的十七个调用点过了一遍，
于是看到 `assemble.py` 的这一段：

```python
if problems:
    ctx.degrade("报告合规校验", f"{len(problems)} 处问题：{'；'.join(problems[:3])}")
    ...
    # **把刚登记的这条降级补回正文。** `assemble()` 里 `body["degraded"]`
    # 是校验之前抄的一份快照，于是"这份报告没通过自己的出库校验"——
    # 最该被读到的一条降级——恰恰不在报告的降级清单里。
    body["degraded"] = list(ctx.degraded_blocks)
```

注释自己写着这是一次修复，而**整段 `if problems:` 没有任何测试**。
注释里那句"最该被读到的一条降级"说得很准：报告没通过自己的校验，
这件事被漏掉之后，读者看到的是一份看起来完整、实则不合格的报告。

注意这里的顺序问题是有一般性的：`assemble()` 在**校验之前**抄了
`body["degraded"]` 的快照，校验之后才登记的那条自然不在里面。
同一个模式在文件里还有一处（`report_ready` 事件刻意从 `ctx` 现读
而不是从 `body` 抄，注释也写着原因）。**"抄一份快照"和"后面还会追加"
这两件事放在一起，就是一类缺陷的配方。**

修法不是改代码——代码是对的。是把它钉住：把 `validate_report` 换掉，
逼出那条分支，断言报告里必须出现"报告合规校验"这一条。
校验器本身有自己的测试，这里测的是"发现问题之后做了什么"。

### 24.3 三条守卫，都得先证明它们会红

这一轮新加的守卫有个共同点：**它们的绿看起来都很正常**。
一条永远通过的守卫和一条正确的守卫在报告里长得一模一样。
所以四条突变全部放回去试过：

| 突变 | 改了什么 | 结果 |
|---|---|---|
| C1 | 覆盖率改成数"模型填过几个维度" | 抓住 |
| C2 | 忽略每条维度的证据门槛（有就算覆盖） | 抓住 |
| D1 | 装配时把降级清单抄成空的 | 抓住 |
| D2 | 删掉校验之后补回正文的那一行 | 抓住 |

```
C1 ... 抓住 test_覆盖率的判据是有没有证据而不是模型有没有填（本轮共 8 条失败）
C2 ... 抓住 test_覆盖率的判据是有没有证据而不是模型有没有填（本轮共 4 条失败）
C2 ... 抓住 test_门槛来自档位而不是写死的常数（本轮共 4 条失败）
D1 ... 抓住 test_the_report_carries_every_degradation_the_run_recorded（本轮共 1 条失败）
D2 ... 抓住 test_a_report_that_failed_its_own_validation_says_so（本轮共 1 条失败）
已还原（逐字节）：True
```

### 24.4 覆盖率那条：一次钉住两个错答案

维度覆盖率是全系统最容易被算成 100% 的指标。两种算法都通得过 code review：

- **甲**：`len(模型返回的维度) / len(计划维度)` —— 模型总是会把字段填上，
  于是恒为 100%。参考实现就是这么算的（`Evidence.matched_dimensions`
  的注释里记着这件事）。
- **乙**：`len(有证据的维度) / len(计划维度)`，但只看"有没有"不看"够不够"。

甲的危险不在于数字好看，而在于**它会让质量门永远通过**——
一个永远通过的质量门等于没有质量门。

所以核心断言的失败信息里把两个错答案都写出来了：

```python
assert coverage == 0.25, (
    f"覆盖率算成了 {coverage}。"
    f"1.0 = 它数的是『模型填过几个维度』，那个判据恒为 100%，质量门永远不会拦下任何东西；"
    f"0.5 = 它忽略了每条维度至少 {threshold} 条证据的门槛。"
)
```

只报"数字不对"的话，后人看到的是一次算术失败；报出两条路，
他看到的是**自己走的是哪一条**。

同一批证据在三个档位上算出三个不同的覆盖率（`min_evidence_per_dimension`
是 3/4/6），这才是"门槛真的被读了"的证据——写死常数的话三个数会一样。

### 24.5 一个我写错、而代码是对的断言

`test_计划外的维度既不进分子也不进分母` 我原本还断言了
"计划外的维度不该出现在 `dimensionCoverageDetail` 里"，红了。

想了一下，**代码是对的，我的期望是错的**。明细是"证据锚到了哪些维度"的
原始计数，把计划外的删掉就等于丢掉了"这次调研发现了计划外的维度"
这个信息——而发现漏掉的维度本来就是这系统该干的事。

两件事答的不是同一个问题：标量答"计划完成得怎么样"，明细答"证据落在哪儿"。
所以改成钉住**两条**不变量，并把代价写进测试：

```python
# 明细 = 计划内（哪怕是 0）+ 计划外（有证据的）。
assert set(metrics["dimensionCoverageDetail"]) == {*DIMENSIONS, OFF_PLAN}
assert metrics["dimensionCoverageDetail"][OFF_PLAN] == 10
# 明细的合计**不**是覆盖率的分母：分母是 `dimensionsPlanned`。
```

代价记在 docstring 里：将来报告页照着明细渲染时，计划外的那些要单独说，
不能混在计划的维度里显示成一个并列条目。

**教训**

1. **"强断言"不等于"会更红的断言"。** `==` 比 `>=` 强，但两边都是空集时
   两者一样恒真。写断言之前先确认**它有可能失败**——用真实数据跑一次，
   把那个值打出来看一眼，比推演靠得住。
2. **注释里写着"这是修复"的代码，往往是零覆盖的地方。** 修一个 bug 时
   注意力全在"让它对"上，很少顺手加测试。这批代码值得专门搜一遍
   （`assemble.py` 这一处就是这么找到的）。
3. **"抄一份快照"和"后面还会追加"放在一起，就是一类缺陷的配方。**
   看见了就要问一句：追加的那条，抄快照的地方知道吗？
4. **失败信息要报出"你走的是哪条错路"，而不只是"你错了"。** 覆盖率算成
   1.0 和算成 0.5 是两条完全不同的错路，修的地方也不同。
5. **测试自己的期望值也会错，而这和代码有 bug 是两件事。** 分清楚：
   红的两次里，一次是代码错了（`label()` 拼错路径），一次是我记错了
   （计划外的维度该不该在明细里）。**分不清这两者的话，会一直改代码去
   迁就自己记错的期望。**

---

## 问题 25：导出与报告接口——两个只在"中文报告"和"驼峰"上才犯的错，以及两条抓不住东西的反证

阶段 7 的后端一半：`core/report/export.py`（Markdown / JSON 导出）与
`app/api/routes_reports.py`（列表 / 详情 / 导出 / 批注）。44 条新用例
（22 导出 + 22 接口）。这条记录里有两部分是**我自己的错**，
而且第二部分是"我用来证明自己没错的工具本身没生效"。

### 25.1 现象一：每一份中文报告导出都 500

导出接口第一次跑通时是好的——因为我用 `curl` 试的，`curl` 不关心头部编码。
接进测试客户端之后：

```
UnicodeEncodeError: 'latin-1' codec can't encode characters in position 21-24
```

定位几乎不用花时间：`Content-Disposition` 里拼了报告主题，
而**报告主题必然是中文**（这个系统就是做中文竞品分析的）。
`starlette` 写响应头时对值做 `v.encode("latin-1")`，HTTP 头本身不允许非
latin-1 字节。

根因是"HTTP 头的编码"和"业务字段的编码"被当成了同一件事。

修复按 RFC 6266 给两个名字：

```python
stem = "".join(ch for ch in subject if ch.isascii() and (ch.isalnum() or ch in " -_")).strip()
fallback = f"{stem or report_id}.{suffix}"          # 老客户端
encoded = quote(f"{subject or report_id}.{suffix}", safe="")
return f'inline; filename="{fallback}"; filename*=UTF-8\'\'{encoded}'
```

三个细节都不是装饰：

1. **纯中文主题的 ASCII 兜底名是空的**，所以退回 `report_id`——
   一个所有下载都叫 `.md` 的名字比 ID 更糟。
2. **百分号编码顺带挡住了引号和换行。** 主题来自模型输出，
   不能假定它不含 `"`——一个引号就能截断整个头部。
   `test_导出文件名挡掉引号与换行` 专门把主题改成 `对比 "A" 与\nB`。
3. **`filename*` 里要是完整主题**，所以测试断言
   `unquote(encoded) == f"{subject}.md"`，而不只是"存在 `filename*`"。

**教训：只在某一条路径上出现的错误，恰恰是最不会被顺手点到的那条路径。**
"导出"这个按钮没人天天点，所以它可以坏很久。

### 25.2 现象二：批注字段是蛇形的

`repo.list_feedback()` 返回数据库的行（`feedback_id` / `section_key`），
接口层是驼峰（`reportId` / `generatedAt`）。我把它直接透传了，
前端读 `feedbackId` 拿到 `undefined`，然后**什么都不显示**——
不报错、不告警，界面上只是少了一块。

这类"看起来没接上"的缺陷在测试里也难发现，因为**少一块不会让任何断言变红**。
现在的守卫是一条集合相等断言，把整个键集钉住：

```python
assert {"feedbackId", "reportId", "sectionKey", "kind", "content",
        "author", "createdAt"} == set(item)
```

在边界上一次性转掉（`_feedback_out`），而不是让前端兼容两种命名。

### 25.3 一条结构性的决定：列表不返回报告正文

`_summary()` **刻意不含 `data`**。这不是优化，是正确性：一份 quick 报告
正文里有 216 条证据、每条带 `fullText`，实测整份 JSON 有几 MB；
列表页显示 50 份就是几百 MB。而这个数字随使用时间**自然增长**——
它不是某一天写错的代码，是"用久了才会出现"的那种问题。

所以断言落在"`data` 根本不在响应里"，而不是"响应小了一点"。
反证 R3 把 `record.data` 塞回摘要，这条立刻红。

配套：`MAX_LIMIT = 200` + `Query(le=...)`。没有上限的话
`?limit=100000` 就是一个免费的拒绝服务入口。

### 25.4 导出说"通过"，同一屏又说"不能发布"

第一次把质量门导出来时，读者看到的是：

```
结论：**通过**
可发布：否
```

这两句并排读起来是自相矛盾。实际的语义是：`passed` 数的是**证据与覆盖门槛**
（够不够支撑结论），`publishable` 数的是**整块内容在不在**
（快速模式缺舆情标注 → 不可发布）。两件事都对，但**理由没印出来**。

修法是补两样：完整度分数（`78%`——0 分和"没算"在 markdown 里长得一样，
所以分数要有单位）和"不能发布，因为下面这些整块内容缺失："
的具体清单。一句"否"变成一条可执行的信息（"补上舆情标注再发"）。

### 25.5 本轮真正的教训：两条反证，一条是假的断言，一条是空操作

写完这批代码后跑反证脚本 `xm3_falsify_r1.py`，六条突变里两条报"漏掉"：

```
R4 不存在的证据静默跳过（角标无声消失）      **漏掉**  （本轮共 0 条失败）
R5 降级横幅挪到文末（等于没披露）            **漏掉**  （本轮共 0 条失败）
```

**R4：断言是恒真的，因为它被自己的说明文字满足了。**

被怀疑的断言是 `assert "[?]" in markdown`。手工把 `return "[?]"` 改成
`return ""`（确认锚点命中 1 次、突变确实生效），单独跑那条见证用例——
**它照样通过**。

原因是"引用异常"那一段的说明文字里写着：

> 正文引用了 1 个不在证据表里的编号，已在正文中标记为 `[?]`：

这句话里**就有 `[?]` 这三个字符**。所以只要存在引用异常，
全篇搜索 `"[?]"` 必然命中——无论正文里到底标没标。
断言分不出"标记渲染了"和"我在旁边提了一句这个标记"。

修法是把搜索范围切到正文那一段：

```python
article = markdown.split("## 引用异常")[0]
assert "[?]" in article, "不存在的证据必须看得见"
```

这是第 3 类缺陷（**测试自己的构造让缺陷不可能被抓住**）的一个新变体：
前几次是"断言两边都是空集"，这次是**断言的目标字符串出现在断言之外的
地方**。共同点：断言与它声称要守的东西之间没有因果关系。

**R5：突变本身是空操作。**

上一版的 R5 把横幅标题从

```
## ⚠️ 本次运行有 3 处降级
```

改成

```
## 附录 D：本次运行有 3 处降级
```

只改了**文字**，位置一字未动。而被守卫的不变量是
`markdown.index("处降级") < markdown.index("## 目录")`——**换了个名字，
顺序没变，断言当然还成立**。（"处降级"三个字在新旧文本里都在。）

这是第 4 类缺陷的镜像：不是测试坏了，是**突变没生效**，
而"没生效的突变"读起来和"测试抓住了"完全一样——两者都不报错。
所以反证脚本现在对每条突变都先数锚点命中数，0 次就报
"这条突变根本没生效，结论无效"。

修好之后 R5 改成**真的把两段换位**（目录在前、横幅在后），
另加 R5b 把整段披露删掉。两条都被 `test_降级横幅在目录之前` 抓住。

**教训**

1. **"红"和"绿"之间还有第三种状态：什么都没发生。** 改完代码跑测试，
   全绿不代表测试守住了——可能是改动压根没生效。反证脚本必须
   自己确认"突变确实落到了文件里"。
2. **找一条字符串之前，先问它在文档里出现过几次。** 断言里的目标串
   如果也出现在解释性文字里，全篇搜索就是在测那段文字。
   范围要收到**该出现的地方**。
3. **并排展示两个结论时，必须同时给出区分它们的那个理由。**
   "通过 / 否"读者只会读成自相矛盾；补上完整度和缺失清单之后，
   它变成可执行的信息。
4. **只在冷路径上出现的缺陷可以活很久。** 导出按钮没人天天点，
   所以中文主题 500 和引号截断头部都没人会发现。
   冷路径的守卫要靠测试补，不能指望用户点到。

---

## 问题 26：一个吞掉 KeyError 的 except，和一个只会在"人造例子"上生效的切词器

「深化本节」（`POST /api/reports/{id}/refine`）的后端：批注驱动的
二次调研 + 重写一章。新增 21 条集成用例 + 23 条纯函数单元用例，
反证 11 条突变全部抓住。这条记录里的两个缺陷都是**写代码时当场犯的**，
而且都属于"看起来能用"的那一类。

### 26.1 现象：错的 `sectionKey` 返回 422，而 404 那一支是死代码

`_section_of` 有两种失败：**指定的章节不存在**（请求指错了地方）和
**报告里一章都没有**（数据有问题）。前者该是 404，后者该是 422。

代码是这么写的：

```python
try:
    index, section = _section_of(data, section_key)
except LookupError as exc:          # ← 想接住"一章都没有"
    raise ValueError(str(exc)) from exc
```

而 `_section_of` 里"章节不存在"抛的是 `KeyError`。**`KeyError` 是
`LookupError` 的子类** —— 于是这一支把两种失败一起接住了，
后面路由上那个 `except KeyError → 404` 永远不会执行。

测试立刻抓住了（`assert 422 == 404`）。修法是把 `KeyError` 写在前面
并且原样重抛：

```python
except KeyError:
    raise                      # 章节不存在 → 404
except LookupError as exc:     # 报告一章都没有 → 422
    raise ValueError(str(exc)) from exc
```

**教训：`except` 一个基类会把它所有子类一起接住，而"我没写那个子类"这件事
从代码上看不出来。** 顺序在这里是有语义的，所以它值得一条注释
（现在那条注释就写着"必须在 LookupError 之前接住"）。

### 26.2 现象：一条"看起来能用"的切词器

「深化」要把批注变成检索词。第一版按空格切：

```python
tokens = annotation.replace("\n", " ").split(" ")
```

实测：

```
"这部分太笼统了 我想知道他们到底怎么收费的"
  → ['这部分太笼统了']          # 7 个字的半句话
```

中文没有词边界，**按空格切出来的从来不是词**。这半句话会变成一个搜索查询，
换回一堆无关页面，而用户会以为搜索源不行。

第二版改成"只留 ASCII token"，看起来对多了。但实测又栽了一次：

```
"看看 API，还有 SaaS 的价格"  →  ['SaaS']
```

因为切出来的 token 是 `"API，还有"` —— `。`、`，` 不是 ASCII，
`isascii()` 为假，整个词被丢掉。**而中文批注里的英文词通常就和中文粘在一起**，
中间没有空格。只有"刚好被空格夹在中间"的英文词才活得下来 ——
那是一种只在人造例子上能用的实现。

第三版不切了，用正则**捞**：

```python
_TERM = re.compile(r"[A-Za-z][A-Za-z0-9._+-]*")
```

`API，还有`、`看看API`、`SaaS。` 全都能捞出 `API` / `SaaS`。
首字符限定成字母顺带解决了纯数字（`2024` 匹配不上，而要"最新的"
由搜索的 `freshness` 参数负责，不该塞进查询文本）。

另外有个小坑：`rstrip("._+-")` 想收干净句末符号，结果把 `C++` 收成了 `C`，
然后因为太短被丢掉。**只收句点**就够了：

```python
word = match.group(0).rstrip(".")
```

**教训：判据里出现的字符串，和它判断的对象，必须是同一个值。**
第二版里 `isascii()` 判的是**没剥过标点**的 token，而返回的是剥过的那个 ——
差一个变量，整套过滤就形同虚设。这和问题 25 的 R4 是同一类病：
**断言 / 判据与它声称要处理的东西之间没有因果关系。**

### 26.3 检索词的主力是章节标题，不是批注

既然中文批注切不出词，那检索词用什么？答案是**章节标题**：
"定价分析"、"功能对比" —— 它本来就是这一章的主题，一定存在、
一定与这次深化相关。批注负责的是**重写**那一半（整句传给模型，不经过切词）。

这条分工值得写进面试：**当一个转换（自然语言 → 检索词）没有可靠的
通用解法时，正确的做法不是硬造一个，而是换一个一定可靠的输入源。**
用假的切词器换来的那点精度，代价是用户以为自己说了不算。

### 26.4 一次深化花钱的上限，是一条**成本**守卫

`MAX_DEEPEN_QUERIES = 2`。批注是自然语言，不是审计给的靶子，
每多发一条查询都是拿用户的钱赌他批注里的词恰好是好查询。

这条上限同时也是**走返工那条路**的守卫，而这一点不容易看出来：
`build_queries(rework=False)` 会把维度轮、角度轮、平台轮全排出来
（quick 档上限 24 条），`rework=True` 只跑批注生成的那几条目标。
误用首轮计划的话：

- `addedEvidences` 只会**变多**，看起来"补得更充分了"
- 界面上、报告里，没有任何一处异常

只有"一共发了几条查询"能抓住它。所以断言落在
`len(search.calls) - before <= 2`，而不是"够不够用"。

**教训：花钱这类指标要单独钉一条守卫。"结果更好"和"过程更对"
是两件事，而前者会把后者的错盖住。**

### 26.5 反证 11 条，全部抓住

| # | 突变 | 抓住它的用例 |
|---|---|---|
| S1 | 补到的证据只写库、不并进正文 | 深化后导出没有引用异常 |
| S2 | 误用首轮查询计划 | 一次深化最多发几条查询 |
| S3 | 指标不重算 | 深化后指标跟着走 |
| S4 | 质量门不重判 | 深化后质量门跟着重判 |
| S5 | 重判时 `ctx.claims` 留空 | 深化不凭空造出没有论点的结论 |
| S6 | `KeyError` 被 `LookupError` 吞掉 | 深化不存在的章节是404 |
| S7 | 深化记录覆盖而不是追加 | 深化两次会留下两条记录 |
| S8 | 说了不搜还是去搜 | 不搜索时只重写 |
| S9 | 深化不留记录、不留批注 | 深化留下记录和一条refine批注 |
| S10 | 章节 key 直接当维度名 | 章节key不是维度名时不带维度 |
| S11 | 按空格切中文 | 跟着中文的英文词也能捞出来 |

S1 那一条**先量后断**：动手之前不确定 mock 重写出来的正文里到底有没有
引用新证据（没有的话，`citation_problems` 那条断言就是恒真的）。
手工把突变打进去跑了一遍，确认它真的红——`[?]` 是因为章节真的引用了
一个正文里没有的 id 才出现的。

---

## 问题 27：一个"两种口径恰好相等"的 bug，和三条抓不住东西的守卫

竞争情报中心（`/api/dashboard`）与知识库（`/api/evidences/facets`）都要回答
同一个问题——"这个系统一共沉淀了多少条证据、被引用了多少次"。做仪表盘时
发现：这两页**读的是两个不同的口径**，而今天它们恰好给出同一个数。

### 27.1 现象：注释写的和代码做的不是一件事

`coverage.mentions` 上方的注释写着：

```
#: 行数（含同一来源被多次调研采到）。报告里的"本次用了 N 条证据"
#: 是这个口径，两者不能混。
```

而实现是 `mentions = int(_sum(reports, "evidences"))` ——
**把每份报告自报的 `metrics.evidences` 加起来**。那不是行数，
是"各报告自己声称用了多少条"。

### 27.2 为什么它今天不报错：两个定义恰好给出同一个数

实测这台机器：`mentions = 2426`、`sources = 266`。把实现换成真正的
行数（`SELECT COUNT(*) FROM evidences`）——**还是 2426**。

原因：目前每一行证据的 `report_id` 都非空。在一行里验一下：

```sql
SELECT COUNT(*) FROM evidences WHERE report_id IS NULL OR report_id = ''
```

返回 0。所以两个定义今天没有分叉。

**这不是"没事"。** 分叉的条件是具体且已经发生过的：一个任务采到了证据、
然后失败了或被中断（本机累计 114 行 `failed`），那些行计入行数、
不计入"各报告自报之和"。到那天两个页面会给出两个数，
而两页的 hint 写的是**同一句话**——用户没有任何办法判断该信哪个，
我们也没有任何地方会报错。

一个"当前数据下恰好相等"的实现错误，比一个当场就错的更隐蔽：
**它连一次红灯都没有。**

### 27.3 修法：先从定义，再从实现

第一步把 `mentions` 改成读证据表。但只做到这一步是半个修法——
仪表盘读的是证据表，知识库的筛选面读的是**它自己内联写的一句
`SELECT COUNT(*)`**。定义一致了，实现还是两份，而"两份 SQL 改一处
漏一处"正是这一页 docstring 自己警告过的事。

所以第二步把它们收成一个函数：

```python
def count_rows(...) -> int:            # 按行数。与 count_library() 是一对
    where, params = _filters(...)
    row = get_conn().execute(f"SELECT COUNT(*) AS n FROM evidences {where}", params).fetchone()
```

仪表盘的 `mentions` 与知识库筛选面的 `mentions` **都调它**。
现在是"同一个实现"，不只是"同一个定义"。

顺带删掉了一个 `search()`：它是 `count_rows` 的配对（按行的筛选查询），
接口改用去重的 `library()` 之后就没有调用方了——全仓库（app / tests /
scripts / eval）零引用。`test_evidences_api.py` 里那条用例的 docstring
记的就是"接口绕开 `library()` 直接用 `search()`"这个 bug，
所以它的唯一有据可查的用法是被修掉的那个缺陷。

`facets()`（按行的筛选面，与 `search()` 同一对）**留着没删**，
docstring 里写明了"当前没有调用方"。原因：它里面的 `degraded` 分面
别处没有，而**仓库还没有 git**（`git init` 尚未执行）——删掉找不回来。
阶段 8 做知识库页面时一并定。

### 27.4 修完之后：夹具本身处在"库里不可能出现的状态"

改完实现，仪表盘的测试立刻红了——但红得不合理：
`avgEvidencesPerReport` 变成 0.0，因为夹具造的报告"自报用了 10 条证据"，
却**一行都没写**。

那个夹具描述的是一个**在库里不可能存在**的状态：报告指标是流水线
写完正文之后落库的，它说用了 10 条，就一定有 10 行。所以修的不是
测试的期望值，是夹具——默认把行写进去：

```python
if write_rows:
    ev_repo.save_many(task_id, [_ev(...) for _ in range(evidences)], report_id=report_id)
```

要造"自报数与行数不一致"这种分叉时，显式传 `write_rows=False`。

**顺序上的教训**：夹具之所以能一直绿，是因为旧实现读的正是夹具
"自报"的那个数——**一个有缺陷的实现会挑出配合它的夹具**。
它绿了很多轮，没有人看出来。

### 27.5 两条"守不住"的守卫——以及它们必须被验证过

反证脚本加进这一轮的代码之后，有两条突变**活了下来**：

| 突变 | 位置 |
|---|---|
| `COUNT(DISTINCT task_id)` → `COUNT(*)` | `repo/evidences.py` |
| 翻页排序去掉 `evidence_id` tiebreaker | `repo/evidences.py` |

查下去，两条都是**在当前 schema 下无法被证伪**的：

1. `evidences` 的主键是 `PRIMARY KEY (task_id, evidence_id)`。
   "同一任务同一来源两行"这个状态**不可能存在**，所以
   `DISTINCT` 与不 `DISTINCT` 等价。留着它是防御，不是一条测试
   能守住的东西。
2. `GROUP BY evidence_id` 的输出本来就按分组键有序。最后那个
   `evidence_id` 只是把排序写全，不是必需的。

原来的测试**声称**守住了这两条。那是假话——它们永远绿。
处理方式是三份，缺一不可：

- 测试里删掉那个声称，换成一条**真能红**的断言；
- 源码那个位置写明"这里是防御性的，本 schema 下无法被证伪，
  **不要为它写测试**"；
- 反证脚本加第三个判定 `defensive`：**期望它照样绿**，
  它红了才说明注释写错了。

第三步是关键。"反正是防御性的、测不出来"这句话听起来永远合理，
所以它必须和别的突变一样跑一遍——`defensive` 红了就是失败。

### 27.6 反证 29 条：活下来的还有一条，是我的夹具放它跑的

`mentions` 那条突变（改回"各报告自报之和"）**第一次是活的**。

原因在我自己：那个用例的夹具是 `_report("RP-1", evidences=2)` 并写了 2 行
——两个定义又相等了。**我拿一个"两个答案一样"的夹具，去测"实现选的是
哪个答案"**，它当然测不出来。

改成自报 99、实写 2：

```python
_report("RP-1", evidences=99, write_rows=False)
ev_repo.save_many("TK-RP-1", [两条], report_id="RP-1")
```

差得越远，读错口径的实现越藏不住。这一条现在红了。

另一条活的突变在 `repo/tasks.py`，原因不同——**锚点 0 次命中**，
即突变压根没落进文件（写锚点时漏了一个换行）。脚本已经会报"结论无效"。

这条突变还有一个**主动的选择**：原本想把
`WHERE status IN (...)` 换成更直白的 `WHERE status <> 'done'`。
没那么写——新 SQL 只有 2 个 `?` 而参数传了 3 个，`sqlite3` 直接抛
绑定数不匹配。测试确实红了，但红的原因是参数个数，不是"终态被改写了"。
**一条抓不住目标 bug 的突变，读起来和抓住了完全一样。**
所以改成 `WHERE 1=1 OR status IN (...)`：占位符个数不变，
红的原因只能是目标行为。

### 27.7 教训

1. **"两个口径今天恰好相等"是 bug 的藏身处，不是它不存在的证据。**
   发现时要去量**它们什么时候会分叉**，而不是只量"现在等不等"。
   这里的分叉条件（证据存在于失败任务上）在本机已经发生过 114 次。
2. **定义一致不等于实现一致。** 只统一口径、留下两份 SQL，
   下次分叉只是换了个地方发生。同一个数在全系统只能有一个函数。
3. **一个有缺陷的实现会挑出配合它的夹具。** 夹具跟着实现的错一起绿，
   是最难发现的一类。判据是"这个夹具描述的状态，库里可能出现吗"，
   而不是"断言过没过"。自报 10 条却零行的报告，库里不可能有。
4. **"这条守不住"也必须被验证。** 把不可证伪性写成注释很容易，
   所以它得和别的突变一样跑一遍——`defensive` 红了就是注释在撒谎。
5. **突变没生效时，红和绿都说明不了任何事。** 先数命中次数，再谈结论。
6. **突变要在参数个数上保持等价。** 否则测试因 SQL 报错而红，
   那条红与目标行为无关。

---

## 问题 28：跑一次测试，把用户正在跑的任务全部杀掉

这条是**在演示过程中撞上的**，不是设计时想到的。危害是活的：
仪表盘上那个 `running` 计数每隔一分钟归零一次，而用户的查询
一个都没跑完。

### 28.1 现象：任务凭空失败，而进程没有重启

真库里的任务接二连三变成 `failed`，`error` 写的是：

> 后端进程重启时这个任务还没跑完，它已被中断。进程内的流水线不跨重启存活，重新发起即可。

这句话是 `tasks_repo.INTERRUPTED_REASON`，只在 `interrupt_unfinished()`
里写。但**后端没有重启**：`netstat` 里 8020 的 PID 从头到尾是 22592，
而 "被中断" 的任务创建于这个 PID 启动**之后**。

一个"进程重启"的失败原因，出现在一个从未重启的进程上。

### 28.2 定位：唯一的调用方是 lifespan

`interrupt_unfinished()` 全仓库只有一个调用点——`app/main.py` 的 lifespan。
所以问题不是"谁调了它"，而是**谁进入了这个应用的 lifespan**。

```
$ grep -rn "TestClient" tests/
tests/integration/test_health_api.py:10:    with TestClient(create_app()) as client:
```

`test_health_api.py` 是这个目录里**唯一一个没有挂 `mock_pipeline_db`**
的文件——它刻意如此，因为它要测的是"把应用装起来"，挂上夹具就变成
"测夹具装出来的应用"了。而它一进 `with` 就跑 lifespan。

`interrupt_unfinished()` 写的是 `get_settings().db_file`，那个默认值是：

```python
return BACKEND_DIR / "data" / "xm3.db"      # 生产库
```

**所以每跑一次这个文件，真实库里所有非终态任务全被判死。**
这个文件里正好**两条**测试——真库里那批成对出现的行就是这么来的。

### 28.3 金丝雀实验：不推理，直接插一条看着

"看起来是这样"不够。往真库插一条 `running` 的假任务，跑一次那个文件：

| | 金丝雀状态 |
|---|---|
| 植入后 | `running` |
| 跑一次 `pytest tests/integration/test_health_api.py` | **`failed`**，error 就是那句"后端进程重启…" |

干净利落。跑完把金丝雀删掉。

### 28.4 顺带撞出来的第二件事：那一百多条"僵尸任务"是测试造的

同一轮排查里去看 `TestSeqAfterRestart`（`tests/integration/test_tasks_api.py`）
为什么在修完之后红了——它**一个夹具都没有**：

```python
class TestSeqAfterRestart:
    def test_the_journal_seq_continues_instead_of_restarting_at_one(self):
        task_id = _make_row(status="running")     # ← 直接走 tasks_repo
```

`_make_row()` 用 `new_task_id()` 生成 `TK-` + 12 位十六进制，
query 用的是模块常量 `CLEAR_QUERY`。对上了：

```
TK-8ec89f875666 failed 2026-09-18T12:15:57
TK-cd636e8d7ce8 failed 2026-09-18T12:15:57   ← 同一秒，成对
TK-1383bd8f5d53 failed 2026-09-18T12:12:26
TK-57bf4bf5f122 failed 2026-09-18T12:12:26
```

**真库里那 100 多条僵尸任务，一大半不是谁点出来的，是这两条用例插的**
（一问一对，所以永远成对出现）。它们插进去的行，又被下一次
跑测试的 lifespan 判死——`创建于同一秒、失败于同一秒`的成对行，
就是这两件事合起来的样子。

我一开始的判断是反的：看见两条同秒创建的任务，我以为是用户在
双击提交。**差一点就去修前端了**——而真正的原因在我自己的测试里。

### 28.5 修法：默认安全，而不是记得加夹具

给 `test_health_api.py` 单独补一个夹具就够了吗？不够。那是一句
"要记得加"的约定——而这个仓库已经栽过一次同样形状的跟头：
`reset_mock_state()` 的 docstring 写着"测试之间必须调用"，
而在有 autouse 夹具之前**没有任何地方调用它**（`conftest.py` 开头就写着这件事）。

所以堵在根上，`conftest.py` 加一条 autouse：

```python
@pytest.fixture(autouse=True)
def _never_touch_production_db(tmp_path, monkeypatch):
    monkeypatch.setenv("DB_PATH", str(tmp_path / "xm3-default.db"))
    get_settings.cache_clear()
    yield
    get_settings.cache_clear()
```

**谁都够不到生产库**，除非它显式把 `DB_PATH` 指过去。
`TestSeqAfterRestart` 另外显式挂了 `mock_pipeline_db`——它**确实
需要一张建好表的库**（`events_repo.append` 要 `task_events` 表），
这是它的真实依赖，不是防御。

### 28.6 守卫，以及它得是能红的

`test_health_api.py` 里加了一条**不为测 `/health` 而存在**的用例：

```python
def test_测试里构造的应用永远不指向生产库(tmp_path) -> None:
    with TestClient(create_app()) as client:
        assert client.get("/health").status_code == 200
    assert Path(get_settings().db_file).parent == tmp_path
```

断言写成"父目录必须是本用例的 `tmp_path`"，而不是"不等于生产路径"：
后者会被一个恰好指向别处的环境变量蒙混过去。

三次实验，缺一不可：

| 实验 | 结果 |
|---|---|
| 夹具在，跑一次 `test_health_api.py` | 金丝雀**活着**（`running`） |
| 夹具在，跑完整套（752 条） | 真库**一行没多**，仍停在修复前那 130 行 |
| **把 conftest 那行 `DB_PATH` 删掉** | **恰好 1 条红**（就是这条守卫），且金丝雀同时变 `failed` |

第三条同时证明了两件事：守卫是能红的，危害是真的。
这条突变也进了反证脚本（`30/30`），脚本里那段注释写着
**"有任务正在跑的时候别跑这个脚本"**——因为它真的会写进真库。

### 28.7 教训

1. **测试默认值就是生产资源时，"记得加夹具"不是一道防线。**
   默认值必须是安全的那个：夹具负责把它指开，而不是靠每个
   用例作者记得加。这个仓库在 `reset_mock_state` 上已经学过一遍，
   这次是第二个位置。
2. **一个只在某个调用路径上出现的失败原因，会把人指向错误的系统。**
   "进程重启"这句话让排查方向变成了"谁重启了后端"，
   而真凶是"谁进入了 lifespan"。**失败原因写的是现象，不是原因。**
3. **看见两个同秒创建的实体，先怀疑机器，再怀疑用户。**
   我第一反应是"用户双击了提交按钮"，差一点去修前端。
   同秒 + 成对 + 每小时固定出现，这三条合起来是程序的特征，不是人的。
4. **危害和守卫要分别证明。** 金丝雀证明危害，删掉那行夹具证明
   守卫能红。只做前者会留下一条永远绿的守卫；只做后者则不知道
   自己在守什么。
5. **测试可以既是受害者也是加害者。** 这一轮里，同一批测试
   既写脏了真库（28.4），又被真库里的残留状态影响（28.4 的红色
   只有在夹具补上之后才暴露出来）。

---

## 问题 29：一个已经跑完的任务，浏览器还在打它 149 次

这条是从用户的一句界面文案开始的：

> 调研中
> 重连中
> 14%
> 联网采集

四个词看着像一件事（"任务卡住了"），实际是**两个互不相干的缺陷**
叠在同一个页面上，而且它们分处两个文件。

### 29.1 现象：日志里躺着 151 条一模一样的请求

后端访问日志里，同一个任务被反复请求：

```
GET /api/tasks/TK-1d1ff1f7090b/stream?from_seq=16   200 OK   （149 条）
GET /api/tasks/TK-1d1ff1f7090b                       200 OK   （2 条）
```

而这个任务在库里是 **`done`**、`progress=1.0`、`journal` 里 520 条事件、
报告 `RP-b1e819bfd098` 也在。一个已经结束 20 分钟的任务，
浏览器每 4.8 秒来问一次"有新东西吗"。

### 29.2 定位一：那 149 次是谁发的

有两个候选，而它们的修法完全不同：

- **A**：`EventSource` 自带的自动重连，复用它构造时拼死的那个 URL；
- **B**：React 反复调 `connect()`，每次新建一条流。

面板上区分不了——两种情况的 URL 一模一样。但**客户端代码的形状**能区分：
`useTaskStream` 每次 `connect()` 之前都必然先发一次快照请求。

```
$ grep -c 'GET /api/tasks/TK-1d1ff1f7090b ' bdzn3tmka.output     # 不带 /stream
2
$ grep -c "stream" bdzn3tmka.output
151
```

**2 次快照 vs 151 次流。** `connect()` 只被调了两次，剩下 149 次是
`EventSource` 自己发的。这也解释了 `from_seq=16` 为什么从头到尾没变过——
**URL 在构造时就拼死了**，重连只是重新请求同一个地址。

### 29.3 定位二：14% 是另一个缺陷

先把嫌疑最大的一环排除掉：把库里那 520 条真实事件逐条喂给
`reduceEvents`，一路走到 `progress=1 / status=done`。**reducer 是干净的。**

那 14% 是哪来的？事件流里有一条：

```
seq=13  node_update  →  progress=0.14  stage=orchestrator
```

而 `from_seq=16` 说明页面打开那一刻快照里的水位就是 16。
两者对得上：**store 停在打开页面那一帧的快照，之后一条事件都没落地。**

为什么没落地——`scheduleFlush()` 当时只挂在 `requestAnimationFrame` 上：

```ts
if (typeof requestAnimationFrame === 'function') requestAnimationFrame(run)
else setTimeout(run, 16)
```

**后台标签页里 `requestAnimationFrame` 根本不回调。** Chrome 是暂停它，
不是让它变慢。expert 模式要跑好几分钟，用户切走去干别的——这期间事件
一直在到、`pending` 一直在涨（无上限），而界面一帧都不刷新。
切回来看到的就是一个冻在 14% 的进度条。

### 29.4 根因 A：服务端"正常关流"在客户端眼里就是"掉线"

`routes_tasks.py` 里那段 docstring 写得明明白白，而且是对的：

> 任务跑完之后 journal 是关着的，`subscribe` 会把历史发完然后正常结束，
> 于是这条连接会自己关掉——而不是永远挂着一条不再有内容的流。

问题出在客户端。`EventSource` **没有"流已结束"这个概念**——SSE 协议里
也没有。它只能看到"socket 关了"，而它对这个事实只有一种解释：
掉线了，退避 3 秒重连。

curl 实测，三种姿势服务端**全部都是发完就关**：

| 请求 | 返回 |
|---|---|
| `?from_seq=16` | 1,494,305 字节，然后关闭 |
| `Last-Event-ID: 16` | 1,494,305 字节，然后关闭 |
| `Last-Event-ID: 520` | 0 字节，然后关闭 |

所以"任务跑完"和"网络断了"在客户端是同一个输入，而客户端把它们
当成了后者——于是永远重连。**服务端的行为没错，缺的是客户端那一句
"这次断开是预期的"。**

### 29.5 修法：把"这次断开是不是预期的"收敛到一个判断点

```ts
function settleIfTerminal(note: string): boolean {
  flush()                                    // ← 先落地
  if (!isTerminal(useTaskStore.getState().task.status)) return false
  endStream(note)
  return true
}
```

三处细节都是有理由的：

1. **必须先 `flush`。** 服务端推完历史立刻关流，而那一刻 rAF 很可能还没跑过——
   最后那批事件（含那条 `done`）还躺在 `pending` 里。不 flush 就判断的话
   看到的状态是 `running`，于是一个**已经跑完**的任务被判成掉线。
   这一行就是那 149 次请求的直接成因。
2. **`onDropped`（会重连）和 `onFatal`（不会重连）都要问它。**
   浏览器把"服务端正常关流"归到哪个分支不在我们的控制里，
   只在一处判断的话，修复成不成全看浏览器当天怎么归类。
3. **`ConnectionState` 加一个 `ended`。** 与 `fatal` 是相反的两件事
   （"该收的都收齐了" vs "该收到的没收到"），与 `idle` 也不能合并，
   否则界面分不出"跑完了"和"从没连上"。

另外，`flush` 里发现批内有 `done`/`error` 时**主动关流**，不等服务端关。
敢这么写是因为查过：全后端 `journal.publish` 只有 `core/pipeline/context.py`
一处入口，`core/report/refine.py`（"深化本节"）一个事件都不发——
它改的是报告，不往这条流里推东西。

缺陷 B 的修法是给合帧加一个 250ms 的兜底定时器，与 rAF 并存：
可见的标签页里永远是 rAF 先跑（16ms vs 250ms），它只在 rAF 被暂停时生效。

### 29.6 验证：能红吗

**单元层**，8 条新用例，逐条反向突变：

| 突变 | 结果 |
|---|---|
| `onDropped` 不再先 flush | **红**（"done 还躺在缓冲里时流就断了"） |
| `flush` 里不再主动关流 | **红**（2 条） |
| 去掉 `setTimeout` 兜底 | **红**（2 条） |
| `onDropped` 不判终态 | **红**（2 条） |
| **反过来：断线一律关流** | **红**（3 条，含 2 条原有的重连用例） |

最后一条是"修复写得太宽"的反面：写成"断线就关"，一次网络抖动会让
工作台永久停更，而界面上写着"已结束"——那比 149 次请求难查得多。

**但突变 M6–M9 全是绿的**，包括"两条一起改坏"。那些是 `clearScheduled()`
里的 `clearTimeout`/`cancelAnimationFrame` 和 `run()` 里那句
`if (!scheduled) return`：两层兜底叠在一起，观察不到差别。
按 `_mutate_check.py` 的判定这叫 `defensive`，于是**在代码注释里写明
"这几行不是被测试保护的，改它们不会有任何东西报警"**，
而不是给它配一条永远绿的用例。

**端到端层**，用**真的 `EventSource`**（`node --experimental-eventsource`，
undici 的规范实现）打**真的后端**，把浏览器当初那个 URL 原样复刻：

| 用例 | 14 秒窗口内服务端收到的请求 |
|---|---|
| `from_seq=16`（复刻线上那一刻） | 修复后 **1 次** |
| `from_seq=520`（服务端一条不发就关） | 修复后 **1 次** |

只有"1 次"是不够的——如果 node 里的 `EventSource` 本来就从不重连，
这个数在修复前后都是 1。所以做了一次**对照**：把两条关流路径都改坏，
用同一套量法再跑一遍。

| | `from_seq=16` | `from_seq=520` | 用例 |
|---|---|---|---|
| 修复关掉（对照） | 14 秒内 **5 次** | 14 秒内 **5 次** | **红** |
| 修复打开 | 14 秒内 **1 次** | 14 秒内 **1 次** | 绿 |

14 秒 5 次 ≈ 每 2.8 秒一次，正是 `EventSource` 默认的重连节奏。
**对照证明量法有效，修复把 5 降到了 1。**

这一轮里还差点吃下一次假绿：第一次端到端是在 jsdom 环境跑的，
它和 undici 抢 `Event` 的域，`dispatchEvent` 抛
`ERR_INVALID_ARG_TYPE`，而那个异常**打断了 EventSource 内部的重连状态机**。
那一轮服务端也确实只收到 1 次请求，用例也确实"失败"了——
两个信号都不对，因为**环境本身是坏的**。换到 `--environment node`
（无 jsdom，`Event` 只有一个域）之后才是干净的三轮一致。

### 29.7 教训

1. **"绿"之前先问：这套环境还红得起来吗。**
   端到端第一轮同时给出了"1 次请求"和"用例失败"两个信号，
   而两个信号都是坏的。**先证明量法能测出问题，再相信它测不出问题。**
2. **一个页面上的两个症状，可以是两个缺陷。**
   "重连中"和"14%"看着像同一个卡住，实际一个是服务端正常关流
   而客户端当成掉线，一个是 rAF 在后台标签页不回调。它们分处
   `onDropped` 和 `scheduleFlush`，改一个不会让另一个好。
3. **协议里没有的那句话，只能由领域知识补上。**
   SSE 没有"流已结束"这一帧，`EventSource` 分不出"正常收完"和"掉线"。
   能分出来的只有我们——因为只有我们知道"`done` 之后不会再有事件"。
   而这个判断值得被**验证一次**（查 `journal.publish` 的调用点），
   因为它决定了主动关流安不安全。
4. **证伪不了的行要写下来，而不是配一条永远绿的用例。**
   M6–M9 的绿是"测不出来"，不是"没问题"。把这句话写进注释，
   下一次改这几行的人才知道没有东西会拦他。
5. **先看日志里的计数，再看代码。**
   149 和 2 这两个数一摆出来，"谁在重连"就不用猜了。
   我一开始的假设是"React 在反复重连"，而那个数直接把它否掉了。

---

## 问题 30：三条"看起来在守、实际没有任何信号"的东西

**日期**：2026-09-19（阶段 7）

这三件事互不相干，共同点是**全都没有信号**：测试全绿、`tsc` 全绿、
界面上也看不出任何异常。它们不是被测试抓出来的，是**读代码 + 看真实数据**看出来的。

### 30.1 深化重写了一章正文，而库里存的引用编号还是旧的

**现象**：没有现象。这正是问题所在。

**定位过程**：追 `body["citations"]` 的读写两侧。
`assemble.py` 在装配时算一次并存进报告（问题 25 之后的改动），
报告页与导出都读那一份。而"按批注深化"（`core/report/refine.py`）
会**重写某一章正文**——新写的这一章可能引到一条刚刚才采到的证据，
存下来的那份编号里自然没有它。

**根因**：`refine` 改了正文却没改编号。两侧的后果不对称：

| 读的人 | 它怎么做 | 结果 |
|---|---|---|
| 导出 | `_CitationIndex(seeded=citations)` 接着种子发新号 | 新证据拿到 `[3]`，看起来正常 |
| 报告页 | `citations` 非空就**不**现算 | 那条引用显示成 `[?]` |

**两边都不报错**，而 `citation_problems` 比对的是"正文里的 `[N]`"与
"附录里的 `[N]`"——导出自己前后一致，所以它一路绿灯。
**这个缺陷没有任何一道检查会碰到。**

**修复**：`refine` 结尾调 `_renumber(data)`，按重写后的正文整体重发。
整体重发而不是"给新证据补一个号"：编号的定义是**首次出现的顺序**，
补号会让 `[1]` 不再是第一次出现的那条。

**验证**：`test_深化后正文编号跟着重发`（`tests/integration/test_report_refine.py:150`）。
反证突变"深化之后不重发正文编号"（`_renumber(data)` → `pass`）→ **红**。
这条用例还带一句额外的断言：**正文里如果一条新证据都没引到，两份编号本来就相同**，
那样这个测试就是空转的——所以它同时断言"被正文引用到的证据 id 集合"与编号集合相等。

### 30.2 转置矩阵的翻正逻辑写对了，但从没在真实数据上跑过

**现象**：库里 15 份报告，有 2 份的 `matrix.scores` 是**转置**着存的：

```
RP-b1e819bfd098  dims=7 brands=5 rows=7 widths=[5]   ← 行是维度、列是品牌
RP-4583f574eab8  dims=4 brands=2 rows=4 widths=[2]
```

转置的矩阵**画出来完全正常**，只是每个数字都挂在错的东西上：
"定价"这一行印的其实是"甲品牌"那一列。它不报错、不空白、不缺数据。

**定位过程**：`analysis/matrix.py` 里 `orientation()` / `orient()` 写得很完整
（方阵先判品牌优先、行宽不一致判 UNKNOWN），单测也有。
但**真实数据里那两个报告是修复之前生成的**——也就是说这段逻辑当时是新写的，
没有一份真实报告被它处理过。

**根因**：不是"逻辑写错了"，是"逻辑没被真数据验证过"。
一个只有单测、没有真实样本的转换函数，和一个只有单测、没有真实样本的类型是同一类东西
（见 30.3）：它对不对，只有拿真东西试过才知道。

那两份历史报告**没有被回填**——它们记录的是一次真实的运行，
改库就是改历史。修复只对新报告生效，而 `coercion` 里那条
"对比矩阵是转置着给的……已按品牌优先翻回来"会**当场披露**这件事，
所以新旧报告在页面上的区别是看得见的（旧的那 2 份没有这条降级说明）。

**修复**：`analyze._analyze_comparison` 里 `orient()` 的返回值**真的接住**
（原先只记降级、不翻回来），并把朝向结论写进 `coercion`。

**验证**：`tests/integration/test_matrix_ingest.py` 4 条（转置被翻回、
记了降级、翻完之后图仍然是好的、正常矩阵不留降级）。
反证突变"识别出转置却不翻回来（只记降级）"→ **红**。

### 30.3 一个声明着错误形状、又没有任何调用方的类型

**现象**：同样没有现象。

**定位过程**：在报告页要写审计面板，去找"审计问题"的类型，
在 `types/domain.ts` 里找到一个 `Issue`：`{issueId, kind, severity,
target, message, suggestion}`，`kind` 有五个取值。

而服务端的 `Issue.to_dict()` 发的是 `{kind, kindLabel, severity, dimension,
brand, detail, suggestion, targets, evidenceIds, resolved}`，
`kind` 有**九个**取值（`models.ISSUE_KINDS`）。

**两边只有"有个问题"这件事是一致的。**

**根因**：它错了很久而**没有任何东西报错**，因为没有任何代码读它——
唯一提到它的 `TeamPreview` 自己也是死代码（后端从不发那个事件）。
类型检查、测试、界面全都碰不到那一条链。

**这比"多个没人用的类型"严重。** 一个声明着错误形状、又没有任何调用方的类型，
下一个人会**照着它写读取代码**：他会看到 `issue.target` 和 `issue.message`，
写出来，然后在前端拿到 `undefined`——和问题 25 里那个批注行透传
是同一种失败，只不过这次的源头在前端自己的类型文件里。

**修复**：`Issue` 与 `TeamPreview` 一起删掉，换成 `types/report.ts` 的
`ReportAuditIssue`（照着 `Issue.to_dict()` 写，有报告页在用）。
`kind` 声明成 `string` 而不是联合类型：真值是后端那张表，
在前端抄一遍就是第二份真相源；标签直接用服务端发来的 `kindLabel`，
**前端不建 kind → 标签的映射表**——建了就会在加一种问题时静默显示一个英文 key。

**验证**：靠 `tsc`。删掉之后报告页的审计面板照样过编译，
说明新的那份确实覆盖了原来的用法；而旧的那份如果再出现，`ReportAuditIssue`
会立刻报出字段名对不上。

### 30.4 教训

1. **"没有信号"和"没有问题"是两件事。** 30.1 和 30.3 都是"绿得看不出来"，
   而它们各自的后果都不小（一个让页面与导出各说各话，一个会诱导下一个人写出 bug）。
   能抓到它们的只有两样：**读两侧的读写路径**（30.1），
   和**拿真实 payload 对一遍类型**（30.3）。
2. **一段只有单测的转换逻辑，等于一段没验证过的逻辑。**
   30.2 的单测是对的、逻辑也是对的，但"真实数据里是什么形状"这个问题，
   单测答不了——因为单测的数据就是照着自己的假设造的。
3. **一个没人用的类型不是"待清理"，是"待误导"。** 删它的理由不是整洁。

---

## 问题 31：报告页第一次拿真实报告渲染，当场炸出两个必崩的缺陷

**日期**：2026-09-19（阶段 7 前端一半）

### 31.1 现象：`TypeError: Cannot read properties of undefined (reading 'dimensionsCovered')`

报告页写完了，`tsc` 过、`oxlint` 过，我先拿它渲染了一份**真报告**
（`RP-8b4e09569307`，从 `GET /api/reports/...` 直接抓下来，未做任何修改）。

第一次渲染就崩在 `ReportMetrics.tsx:246`。

### 31.2 定位一：代码和类型是照着同一个错误猜测写的

出问题的那一行读的是：

```ts
const delta = metrics.rework?.delta?.[key]      // ← 猜的
```

而服务端 `compute_metrics` 发的是：

```jsonc
"rework": {
  "before":  {"dimensionsCovered": 5, "evidences": 188, ...},
  "after":   {"dimensionsCovered": 6, ...},
  "changes": {"dimensionsCovered": {"before":5,"after":6,"delta":1}, ...},  // ← 逐项对比在这里
  "costDelta": 0.0312, "improved": true
}
```

**没有 `delta` 这个键**，逐项差值在 `changes[key].delta` 里。

关键在于：**组件和它的类型是同时写错的。** `types/report.ts` 里
`ReportMetrics.rework` 也声明成了 `{before, after, delta}`，
所以 `metrics.rework?.delta?.[key]` 在编译期完全合法——
`tsc` 一个字都不会说。两处错误互相印证，于是谁都不报。

**它会打中大多数报告。** 只要 `rework` 非空（也就是任何触发了返工的运行，
而这恰恰是正常路径），`Object.keys(metrics.rework.after).map(...)`
就会去读不存在的 `delta`，整页白屏。

### 31.3 定位二：紧跟其后的第二个必崩

修完第一个，我继续对着真数据读那一段，发现了排在后面的第二颗雷：

```ts
{metrics.reworkLog.map((entry) => <li>{entry}</li>)}     // 类型声明的是 string[]
```

而真实数据是：

```jsonc
"reworkLog": [
  {"round": 1, "targets": 12, "added": 24, "reason": "发现 5 个达到返工门槛的问题…", "qualityPassed": true},
  {"round": 2, "targets": 10, "added": 0,  "stopped": "no-new-evidence"}
]
```

React 会抛 `Objects are not valid as a React child`。
两个崩溃是**前后脚**的：修掉第一个，第二个立刻显形。

还有一处只有把两条日志放在一起看才知道的：**提前结束的那一轮没有 `reason`，
只有 `stopped`**（上面第 2 轮）。按原来那版渲染，它会显示成"补采 10 个目标、
新增 0 条证据"——读起来像"补采失败"，而真实原因恰恰相反：
**补了也不会有新证据，所以停**，那是一条正面结论。
于是给 `stopped` 配了一张只有一条的映射表
（`'no-new-evidence'` → "补采没有新增证据，再跑也不会有新结论"），
并且保留 `?? entry.stopped` 的兜底——将来多一个原因码，显示那个码本身，
而不是一片空白。

### 31.4 根因

**我从后端"应该发什么"猜了一个形状，然后照着这个猜测同时写了类型和组件。**
两者一致，所以没有任何一道门禁能发现。

这不是 `tsc` 的问题——`tsc` 只能保证"代码和声明一致"，
不能保证"声明和现实一致"。**`as` 断言、手写 fixture、凭印象写的 interface，
三者对 `tsc` 来说长得一模一样。**

### 31.5 修复

1. `types/report.ts` 的 `rework` 改成 `{before, after, changes, costDelta?, improved?}`，
   `reworkLog` 改成对象数组。两处都带上"这是照着真实 payload 改的"注释。
2. `ReportMetrics.tsx` 读 `changes[key].delta`；`reworkLog` 按对象渲染
   （第 N 轮 / 补采 X 个目标 · 新增 Y 条证据 / 质检通过 / 提前结束）。
3. 顺带发现同一张表里**两个是比率、四个是条数**（`crossValidationRate`、
   `dimensionCoverage` 是 0–1，其余是条数）。不格式化的话同一列里
   会出现 `188` 和 `0.1667`，而读者只会看到"从 0.1667 变成了 0.25"。
   比率名单照抄 `metrics.rework_delta()` 的 `keys` 元组——
   是耦合，所以写死在代码里，而不是用"小于 1 就当比率"这种启发式
   （那会把恰好等于 1 的条数也吞掉，而 `dimensionCoverage` 返工后正好是 1）。

### 31.6 验证：把真实 payload 做成夹具，并且**不加 `as`**

新增 `frontend/src/test/fixtures/realReport.ts`——85 KB，
从 `GET /api/reports/RP-8b4e09569307` 抓下来裁剪而成
（正文 2 节、论点 2 条、证据 4 条、份额 2 条、每个目录 1 条、
术语 3 条、图集 2 条、审计问题 2 条、质检维度 2 个、coercion 2 条……），
类型标注写成 `export const REAL_REPORT_BODY: ReportBody = {...}`。

**一开始我在末尾写了 ` as ReportBody`。** 那一句让 `tsc` 对整份夹具闭嘴——
而这份夹具存在的唯一理由就是让它说话。
去掉 `as` 之后，`tsc` 一次报出 **6 处**不一致：

| # | 字段 | 我声明的 | 真实的 |
|---|---|---|---|
| 1 | `BarSpec.yAxis` | 不存在 | `{min, max, name}`，矩阵那根柱子有 |
| 2 | `audit.review` | 不存在 | 质检维度数组 |
| 3 | `metrics.rework.changes` | 不存在（写成 `delta`） | 逐项 `{before,after,delta}` |
| 4 | `metrics.reworkLog` | `string[]` | 对象数组 |
| 5 | `metrics.siteFilterFolded` | 不存在 | 能力协商的账 |
| 6 | `metrics.llmFallback` | 不存在 | 主模型故障切换 |

第 6 条是**修完前 5 条之后才冒出来的**——去掉 `as` 之后编译器只报第一个
卡住它的地方。这正好说明为什么"一次修完"不可靠：真相是一条一条露出来的。

**`ReportPage.test.tsx` 的 6 条用例**，其中第一条就跑通了整页渲染。
那次运行里它第一次红，就是 31.1 那个 `TypeError`。

### 31.7 教训

1. **一个 `as T` 断言会消灭这份夹具的全部价值。** 夹具的意义是
   "让编译器拿真实数据审查我的类型"；`as` 恰好把这件事关掉了。
   写夹具时那句"先让它编过"是本能的，而它把要测的东西藏了**好几个窗口**。
2. **手写 fixture 是照着自己的假设造的，真实 payload 不是。** 31.2 和 31.3
   两个崩溃都是"拿真数据渲一次"发现的，不是"再补一条用例"发现的。
   在写测试之前**先拿真数据跑一遍**，比先写测试更省事。
3. **组件和类型一起写错时，两者会互相背书。** 唯一的破法是从**外部**
   引一个形状进来：真实 payload、后端源码、或者 `to_dict()` 的输出。

---

## 问题 32：报告页上三处"看不出来的缺失"

**日期**：2026-09-19（阶段 7 前端一半）

这一条里的三件事都属于同一类：**页面上少了点东西，而少掉的样子
和"本来就没有"长得一模一样**，读者不会去猜是数据缺了。

### 32.1 目录上的死锚点

**现象**：报告页左边是目录。目录由两部分拼成：正文章节（`sections`，
有就有、没有就没有）和"其他"块（指标、矩阵、舆情、图集、参与专家……）。

后者是页面**列出来**的，而它们各自在数据缺席时返回 `null`——
`ReportMatrix` 没数据就整块不渲染，`SentimentPanel` 同理。
于是一份没有舆情的报告，目录上仍然有一条"舆情分析"，点下去**纹丝不动**。

**根因**：目录那份列表是**无条件拼出来的**（17 条写死的 `{key, label}`），
而它对应的元素是**有条件渲染**的。两个列表各写各的，谁也不看谁。

**这个错误在截图上看不出来**：一条灰字目录项，和一条好的一模一样。
读者得到的信息不是"这份报告没有舆情"，而是"这个页面是坏的"。

**修复**：把那 17 条改成 `useMemo`，**逐条镜像每一个子组件的早退条件**。
`ReportPage.tsx` 里那段注释写明了这是真实耦合，并提醒：
**子组件的早退条件改了而这里没改，不会有任何东西报错。**

**验证**：`ReportPage.test.tsx` 把"每个锚点都对应一个真的存在的元素"
当成不变量来断言（`data-anchor` 属性是为此加的测试接缝——
`jump()` 是 JS，按钮文字是给人看的，两者都不适合当断言目标）：

```ts
const dead = anchorReport(container).filter((a) => !a.exists).map((a) => a.id)
expect(dead, `这些目录项点了没反应：${dead.join('、')}`).toEqual([])
```

前面还有一句 `expect(anchors.length).toBeGreaterThan(10)`——
不写它的话，目录整个空掉时这条断言也是绿的。
另有一条用例**删掉 `matrix` 和 `sentiment`**，断言两块既不在页面上、
也不在目录里，而整条不变量仍然成立。

### 32.2 后端的量表被丢掉了

**现象**：能力矩阵那根柱状图的标题写着"评分"，
而 4.2 分和 4.4 分的柱子看起来**差了一倍**。

**根因**：`charts.py` 在那根柱子上带了 `yAxis: {min: 0, max: 5, name: "评分"}`
——后端在说"这是 1–5 分的量表"。而前端 `barOption()` 一概自适应，
把这个键**丢了**。ECharts 自适应时会把 0.2 的差距铺满整个绘图区。

**这是个"图是对的、读法是错的"缺陷**：数据一个没错，柱子一根没少，
但读者从图上得到的结论是错的。

**修复**：`barOption()` 读 `spec.yAxis`，后端给了就用后端的
（`min` 缺省仍补 0——不从零起的柱状图是另一类误读）；
`types/report.ts` 补上这个可选字段。

**验证**：`chartOptions.test.ts` 里三条用例是**一个决策的三个面**：
不给量表时不设 `max` 但 `min` 是 0；给了量表就照用；
只给一半时另一半走默认。拆成三条是因为它们会分别被别人改坏。

### 32.3 两处降级披露没有类型，于是没有界面

后端在指标里发两个键，前端 `types/report.ts` 里都没有：

- `siteFilterFolded` —— 搜索源不支持按站点过滤时，平台约束被折进查询词，
  结果通常更宽。这个数非零表示有几次检索的精度打了折。
- `llmFallback` —— 主模型熔断、改走备用模型时的那本账
  （`{primary, secondary, latched, fallbackCalls, degraded, events}`）。

**两者的后果是同一种**：一份**降级过的**报告和一份干净的看起来完全一样。
读者不知道这次的成本和延迟里有一截是故障切换造成的，
也不知道有几次检索的精度和平时不同。

**修复**：两个都补进类型并渲染。`llmFallback` 那行**只在 `degraded` 为真时出现**
——那一行如果恒在、写着"0 次切换"，一份正常的报告就会看起来像出过事。
类型上六个字段全是可选的，因为正常值是 `{}`（没配 fallback provider 时
`calls.py::llm_status` 就返回空 dict）。

顺带一句：`audit` 这个类型也被**加宽**了（补上 `review` / `quality` /
`reworkTargets` / `reworkReason`）。**声明一个键的含义是"后端会发这个"，
不是"前端在用这个"**——把这句话写进注释，是因为不写的话
下一个人会以为这些字段是多余的、可以删。

### 32.4 顺带修掉的一处：六张卡片各写了一遍外壳，其中一张漏了偏移量

报告页的正文卡片原先各自写了一遍外壳，`scroll-mt-[88px]` 是手抄的。
抄的过程中 `ReportMatrix` 漏了——它有两个 `return`（正常表 / 形状对不上时的提示），
两个 `id="matrix"` 都没带。后果是**点目录里那一条，落点和别的每一条都不一样**：
别的是离上沿 88px，它是贴着上沿。

这件事本身很小，但它暴露的是"七个地方手抄同一行类名"这件事本身。
修法是收敛到一个 `ReportPanel`（`components/report/ReportPanel.tsx`），
现在报告页有 14 处用它；`ReportMatrix` 因为有两个 `return` 没并进去，
两处单独补。

> **顺带纠正一句当时写错的注释。** 那些 `scroll-mt` 旁边原本写着
> "抵消吸顶页头的高度"——**页头不是吸顶的**。它是 `h-screen flex-col`
> 的第一行 `shrink-0`，滚动发生在下面那个 `<main className="overflow-y-auto">`
> 里，所以 `scrollIntoView` 根本不会把标题藏到页头后面。
> 这个 88px 真实的作用是"跳转落点留出上边距"（贴着滚动区上沿落下会显得很挤）。
> 同一套说法还出现在 `ReportSection.tsx`、`ReportPanel.tsx`、
> `ReportToc.tsx` 的 `rootMargin` 注释里，一并按实际机制改过了。
>
> 记这一笔是因为它和 32.1 是同一类毛病：**注释描述了一个不存在的行为，
> 而没有任何东西会反驳它**。区别在于 32.1 的后果是死锚点，
> 这个的后果只是把下一个改代码的人引到错误的地方去。

> 它**没有**和 `primitives/Panel` 合并：那个是工作台的面板（紧凑、带标题条、`flat`），
> 这个是报告卡片（宽松、`p-4`、`shadow-card`、标题在正文里）。
> 合并要往一个已经有六个工作台面板在用的原语上加三个开关——
> 那是拿一个原语去伺候两种设计，不是复用。

### 32.5 教训

1. **目录和正文是两个列表时，必须有一条断言把它们绑在一起。**
   靠"记得同步改"是守不住的，因为漏改的样子和正确**一模一样**。
2. **"后端发了、前端没接"和"后端没发"在界面上是同一种空白。**
   前者是缺陷，后者是现状。要分清它们，只能拿真实 payload 对一遍类型
   ——这正是 31 那条的同一个手法。
3. **图对不等于读数对。** 32.2 里没有一个数字是错的，
   错的是读者从图上得到的结论。这类缺陷没有任何自动检查会碰它，
   除非你把"后端给的量表要照用"写成一条断言。
4. **注释也会"看起来在守、实际没守"。** 32.4 里那句"抵消吸顶页头"
   写得很具体、很有把握，而页头根本不是吸顶的。
   一个描述错误的注释比没有注释更坏——它会拦住下一个真去查的人
   （"代码里都写了，就是这个原因"）。**写机制的时候要顺手验一下那个机制。**

---

## 问题 33：trace 接口三个数各自在说另一件事，以及"span 树"其实是一片森林

阶段 8 要建决策回放页，它的数据源是 `GET /api/tasks/{id}/trace`。
动手前先把这个接口在**真实数据**上对了一遍——它此前一条测试都没有。
三个问题，都是"不报错、不崩、只是数不对"。

### 33.1 `durationMs` 是并发相加，不是任务耗时

汇总里原来有一个 `durationMs`，是**所有 span 的耗时相加**。

拿库里最新那次真跑量一下（`TK-1d1ff1f7090b`）：

| 数 | 值 |
|---|---|
| 所有 span 耗时相加 | **788.6 秒** |
| 最早开始 → 最晚结束 | 183.2 秒 |
| 比值 | **4.30** |

**那个 788 秒从没在墙上发生过。** 采集阶段是并发扇出的：118 条 search
与 100 条 fetch 同时在跑，把它们的耗时加起来自然超过墙钟。

原来那个字段名叫 `durationMs`，而这个名字会被任何人读成"这次任务花了多久"。
回放页的成本面板一旦把它接上"耗时"，用户看到的就是一个**大四倍的数字**，
而它不报错、不空、不恶心——只是错。

`done.metrics.durationMs` 才是任务耗时（它从任务开始算到报告落库，
包含出题、调度、写作、落库，这些 span 全都不覆盖）。所以在 trace 汇总里
再算一个等于给同一件事造第二个会不一致的答案。

> **这一段的字段名我一开始写错了，而且错法和 32.4 一模一样。**
> 第一版写的是"`done.metrics.elapsedMs`"，查下去发现 `metrics` 里
> **根本没有** `elapsedMs` 这个键（36 个键里没有它）；真正的字段叫
> `durationMs`。`elapsedMs` 是**另一个东西**：它在 `node_update` 事件上、
> 以及 `runner.snapshot()` 里，表示"进行中"的已用时间。
> 而 `done` 顶层的 `elapsedMs` 是被**刻意忽略**的——
> `tests/unit/test_cli_render.py` 里正好有一条用例拿 `999999` 当诱饵验证这件事。
>
> 也就是说：我在写一条"注释要描述真实存在的机制"的教训（32.4）之后，
> 立刻写了一条描述不存在的字段的注释。四处副本全部改正
> （`routes_tasks.py`、`api.ts`、本文件、以及测试文件顶部）。
> **写机制的时候要顺手去 grep 一下那个机制**——这次是因为顺手查了
> `metrics` 的键集合才发现的，不是因为"我怀疑自己写错了"。

**修复**：拆成两个名字都不像"任务耗时"的字段。

| 字段 | 含义 |
|---|---|
| `spanDurationMs` | 所有 span 耗时**相加**（= 总占用） |
| `spanWindowMs` | span 覆盖的墙钟窗口（最早开始 → 最晚结束），**也不是任务耗时** |
| `concurrency` | 前两者相除。≈1 是串行链，4.3 是同时开了四路 |

第三个不是装饰：**"任务变慢了"与"任务变成串行了"是两种病**，
只看端到端耗时它们长得一样，而这两个数分得开。

### 33.2 `failedCount` 把 24 条降级数成了 24 次失败

同一个汇总里 `failedCount` 的定义是 `status not in ("", "ok")`。
而 span 的 `status` 只有三个取值：`ok | error | degraded`。

真实数据（1036 行）：`ok` 997、`degraded` 39、`error` **0**。

所以那份 249 条 span 的报告会印出"**24 次失败**"，而真相是
"24 个页面抓到了但正文太薄"——那是网页采集的**正常形态**，
不是故障。读者会由此认为这个系统不稳。

`Tracer.metrics()` 一直是分开数的（`errors` 与 `degraded` 两个计数）。
同一个库里两套口径的另一处不一致。

**修复**：`failedCount` 拆成 `errorCount` / `degradedCount`，与 `metrics()` 同口径。

### 33.3 "span 树"是一片森林，而树代码从没被真实数据走到

`span_tree()` 把扁平 span 按 `parentId` 组装成树，代码里还有一句兜底：
**父 span 不在（被环形上限截掉、或跨进程）就当成根节点**，
注释写明"丢掉它会让整棵子树消失"。

一量：

- mock 流水线一次跑出 49 条 span，`parentId` **全是空串**；
- 真库里 1036 行，`parent_id != ''` 的 **0 行**。

原因不神秘：全仓库**没有一处 `with span(...)` 嵌在另一处里面**
（静态扫过，0 处），所以 `current_span_id()` 永远取不到东西。
`span_tree()` 在当前数据上是个**恒等函数**，`children` 永远是 `[]`。
那句兜底、以及"组装成树"这件事本身，**一次都没被走到**。

### 33.4 我写的第一版测试因此是假绿的——这一条比上面三条都重要

发现 33.3 的过程是这样的：我给这个接口写了 16 条测试，
其中一条断言"树把每一条 span 都装进去了"，跑过，全绿。

然后按惯例做突变校验，把兜底那一句改成"父不在就丢弃"——
结果**测试还是全绿**。

回去看：因为 mock 数据的 `parentId` 全是空串，**每一条 span 都是根**，
兜底那条分支根本没进过。那条断言是 `49 == 49`，它证明的是
"`_flatten` 会遍历根节点"，不是"树装对了"。

改法是**不让测试依赖退化数据**：直接把带 `parentId` 的 span 写进库，
造出一个三层的根→子→孙，再断言 `children` 的形状。
同时补两条：父不在库里（孤儿要自己当根，不能消失）、
`parent_id == span_id`（脏数据不能让折叠无限递归）。

再跑突变，`test_父亲不在库里时它自己当根_而不是消失` 变红。这才是真的守住了。

**教训**：**"真数据跑通了"不等于"这段代码被走到了"。**
数据是退化的（全空外键、全零耗时、单一取值）时，
一条绿色测试守住的可能只是"循环会转"，而不是任何业务规则。
分辨这两种情况的唯一办法就是突变——把那段代码改坏，看它红不红。
这一次它没红，差值就是我的整个判断错误。

### 33.5 顺带修掉的工具缺陷：突变脚本会把文件行尾悄悄改写

`scripts/_mutate_check.py` 原来是 `path.read_text()` → 改 → `write_text()`。
`read_text` 走 universal newlines 把 `\r\n` 归一成 `\n`，
`write_text` 在 Windows 上又把它转回 `\r\n`。

- 对**全是 CRLF** 的文件：恒等变换，看不出问题；
- 对**行尾混用**的文件：那几行裸 `LF` 被静默改成 `CRLF`，
  于是"突变跑完了、文件已还原"而**字节其实变了**。

本仓库两种行尾确实都有（`db/repo/events.py` 是 LF，`db/repo/traces.py` 是 CRLF，
来自不同时期的编辑）。这次就撞上了一次：`traces.py` 在跑完一轮突变后
字节与运行前不一致，而我当时的校验脚本只报了一句"被改坏了"，
直到回头数行尾才确认**只是行尾被归一，没有丢内容**。

**修复**：改成 `read_bytes` / `write_bytes`，并先判断文件原本是不是全 CRLF，
按原样写回；每次运行把判定结果（`行尾 CRLF` / `行尾 LF/混用`）打在标签行上。

顺带改准一处报错信息：突变让代码抛异常（而不是断言失败）时，
**顺序上最先碰到那行代码的用例**会变红，那不一定是我写来断言这件事的那条。
`byKind` 那条突变就是这个情况——它先炸出一个 `KeyError`，
于是证人是碰巧第一个请求接口的 `test_父子关系真的被装成树`。
崩溃也算红（"caught"成立），但把证人写准才知道该信哪条断言。
所以输出改成"最先变红：…"，并把那一条突变直接指到具体用例上。

> 有一点**没有**改：那个字段名在 `setdefault` 的默认字典与下面的自增语句里
> 各出现一次，改一处会抛 `KeyError`。**这个保留**——一个立刻抛异常的
> 失败比一个悄悄少算的失败好得多，而"两个字符串必须一致"这件事
> 在这里没有第二种写法值得为它换来一个静默路径。

### 33.6 教训

1. **字段名是一个接口。** `durationMs` 这个名字本身就是缺陷的一部分：
   数字（并发相加）不算错，会被接错的是名字。
   同理 `failedCount` 把降级算进去——两个字段合一个桶的时候，
   问一句"它们合成的那个词，是不是同时描述了两种东西"。
2. **派生值要连口径一起给。** 只给端到端耗时看不出一条链是变慢了还是变串行了。
   把窗口与并发度一起给出来，同一个数据才能回答"该优化哪里"。
3. **真数据能跑过 ≠ 这段代码被走到了。** 见 33.4。
4. **工具本身的行尾/编码假设也要验。** 一个"还原了"的声明，
   在文件行尾混用时是假的。校验脚本说"字节一致"才是真的验过。

## 问题 34：一个真数被接到另一个问题上（113 与 117）

写回放页的 `firstFetchIndex` 时，注释里我写了：

> 实测 `TK-1d1ff1f7090b` 的 249 步里有 **113 步**发生在第一条证据被捕获之前
> （需求解析、调度、以及最早那批搜索），滑杆的前 45% 都属于这一段。

写别的文档时顺手把这句话验了一遍，量出来是 **117**，不是 113。

两个数都是真的——它们只是**在回答两个不同的问题**：

| 问的问题 | 答案 |
|---|---|
| 第一条件为真的 `fetch` span 排在第几位（= `firstFetchIndex` 要的） | **117** |
| 有多少条 span 在第一轮证据被盖章之前**结束** | **113** |

113 是我之前为了论证"证据捕获时间是按轮次盖章的"而量出来的数，
写到 `firstFetchIndex` 的注释里时**接错了问题**。而那句话说出口之后
自己就自相矛盾了——括号里解释的是"需求解析、调度、最早的搜索"，
那是**非 fetch 的 span**，与"证据盖章之前"根本不是一回事。
**两半各自都像真的，所以整句读起来是通的。**

113 与 117 只差 4，这也是它没被察觉的原因：量级、位置、用途全都对得上，
读者（包括写的人）不会觉得这个数可疑。**一个偏了 1 个数量级的错数反而安全**，
因为它一眼就不对；差 3% 的错数是最难发现的那一类。

### 34.1 顺手量到一件更强的证据：`capturedAt` 盖在轮次的**开头**

排一下那一刻的 span 就看清了：

```
[116] search  14:48:41.850  Nuclino 评价
[117] fetch   14:48:42.466  https://www.informat.cn/qa/169310   ← 第一个 fetch
```

而证据的 `capturedAt` 是 **14:48:42**（整秒）。也就是说：
**盖章发生在第一条 fetch 甚至还没开始的时候，早了 0.47 秒。**

于是"按 `capturedAt` 驱动回放"这件事又少了一种可能的辩护——
它不是在"抓完一条记一条"时盖的，而是**在一轮采集开始时盖一次章**，
盖完之后这一轮的证据才会陆续产生。用它当时间轴，
等于把"这一轮开始的时刻"当成了"这些证据到手的时刻"，
而且整轮共用同一个时刻。

这也是 113 的来历：它量的是"第一轮开始之前已经结束的 span"，
一个**有意义、但属于另一个问题**的数。现在它回到它该在的地方——
论证 `capturedAt` 没有逐条分辨率。而 `firstFetchIndex` 用它自己的数 117。

### 34.2 教训

**量出来的数和写下来的问题要对得上。** 33.4 的失效是"测试守的是别的东西"，
这一条的失效是"数字答的是别的问题"——同一个病根的两种长相：
**被验证的对象和声称被验证的对象不是同一个。**
防法和 33.4 一样：不要凭"这个数我量过"就用它，
要在**用它回答的那个问题下**把它再量一遍。这一次是多花了一条 SQL 才发现的。

## 问题 35：前端自己发明了一套"第几步"，与后端那套在结构上可以不一致

写 `replay.ts` 的 `flattenSpans` 时，我的注释是这么说的：

> 回放要的是"第几步"这个概念，而后端 `Tracer.snapshot()` 定下的规矩是
> **按开始顺序**（那里的注释写明了理由）。两边必须是同一条规矩。

这句话是对的。但我**实现的不是它**。

### 35.1 后端说的"开始顺序"是 `spanId` 编号，不是时间戳

`Tracer.spans()` 的排序键是 `_sequence(span_id)`——也就是 `SP-00041` 里的 41：

```python
ordered = sorted(self._spans, key=lambda sp: self._sequence(sp.span_id))
```

而它的 docstring 写明"按**开始顺序**返回，与 `span_id` 的编号一致"。
**这个等式成立，是因为编号是在 span 开始时发的**：

```python
def _next_span_id(self) -> str:
    with self._lock:
        self._counter += 1
        return _new_span_id(self._counter)
```

所以后端内部**根本没有用时间戳排序**——它用的是编号，而编号"就是"开始顺序。

我写的前端却用 `startedAt` 排。两套规矩，平时结果一样。

### 35.2 它们可以不一样，而且机制就在源码里

看 `span()` 里这两行的**相对位置**：

```python
span_id=self._next_span_id(),     # 193 行：拿锁、自增、放锁
...
started_at=_now_iso(),            # 200 行：**锁已经放掉了**
```

**编号是在锁里发的，时间戳是在锁外取的。** 于是这个交错是合法的：

| 时刻 | 线程 A | 线程 B |
|---|---|---|
| t1 | 拿锁 → 编号 41 → **放锁** | |
| t2 | *（被调度走了）* | 拿锁 → 编号 42 → 放锁 |
| t3 | | `_now_iso()` → **T₂** |
| t4 | `_now_iso()` → **T₁**（T₁ > T₂） | |

结果：**编号 41 的 `startedAt` 晚于编号 42**。按编号排是 `41, 42`，
按时间排是 `42, 41`。

这不是理论上的可能——这段代码**确实在多线程里被并发调用**：

- `gather_search` 用 `asyncio.gather` 并发跑一批搜索，每一个都进
  `asyncio.to_thread(search_sync, ...)`（`calls.py:262`）；
- 而 `search_sync` **在 worker 线程里**开 span——它的 docstring 自己写着
  "线程里的 `span()` 能看见 tracer"；
- 抓取那条路更并发：`fetch_many` 进去之后再开一层自己的线程池。

所以"两个线程同时在 `span()` 里"是常态，不是边界情况。

### 35.3 但它在真实数据上没有发作——0/15

拿库里 15 个有 span 的任务，两种排法逐个比：

```
任务总数 15，两种排序**结果不同**的任务数：0
```

**这一条要如实说清楚：它是一个潜伏缺陷，不是一个正在错的行为。**
我没有找到任何一份真实数据上两种顺序不一致的例子。

那为什么还要改？因为它不是"概率问题"，是**两个定义**的问题：

- 接口给前端的数组已经按编号排好了（`span_tree` 的输入就是 `spans()`）；
- trace 页、`metrics()`、按 kind 的分组，全都用编号那套；
- 只有回放页用时间戳那套。

平时两个定义给出一样的结果，所以**没有任何信号**告诉你有两套。
等到某次并发恰好交错，回放页的"第 137 步"就与 trace 页的第 137 条不是同一条——
而那时没有人会想到去怀疑排序。

**一个只在压力下才现形的分歧，比一个稳定错的分歧更难查。**

### 35.4 修复方向：以后端那套为准，前端不要另立一套

改成按 `spanSequence` 排序，`startedAt` 降级为**并列时的兜底**：
编号取不出数字时（游离 span，`_sequence` 返回 0）会有大量并列，
那时才用时间戳分。

这和 35.1 里 `_sequence` 的注释"取不出数字的（游离 span）排在最前"是同一套规则。

被改的地方：`replay.ts::flattenSpans` 的排序键，以及 `replay.test.ts` 里
两条跟着它走的用例：

| 原用例 | 改成 |
|---|---|
| `三层嵌套按**开始时间**排，而不是按遍历顺序` | `三层嵌套按 **spanId 编号** 排，而不是按遍历顺序` |
| `时间戳坏掉时退回 spanId 编号` | `编号取不出数字时退回开始时间` |

**注意"重新排序"这件事本身没有错**：树是深度优先遍历出来的，
而遍历顺序确实不是开始顺序（父 span 一定晚于子 span *完成*）。
错的是排序键选了时间戳。

> **后端那边还有一处可以收紧，本次没做。** 把 `_now_iso()` 移进
> `_next_span_id` 的锁里，编号与时间戳就成了一个原子事实，
> 35.2 那张表里的交错在结构上消失（只剩"墙钟本身回拨"这一种可能）。
>
> 没做的理由是**它无法被确定性地验证**：要让它变红，得让某条线程
> 恰好在"放锁之后、取时间之前"被调度走，这是测不出来的。
> 一个改不出见证用例的改动，与这个仓库现有的验证纪律（每条突变都要有
> 一条会变红的用例）不合，所以记在这里而不是顺手改掉。
>
> 前端按编号排之后，这个分歧已经没有任何消费者了——它是"更正确"，
> 不是"更安全"。等真有人按 `startedAt` 排序时再做。



### 35.5 后端那条守卫靠的是"时间戳比 span 创建还粗"——顺手加的突变发现的

后端**已经**有一条测试在守这件事
（`tests/unit/test_trace.py::test_spans_come_back_in_start_order_even_when_nested`），
于是我去补了一条对应的突变：把 `spans()` 的排序键从

```python
sorted(self._spans, key=lambda sp: self._sequence(sp.span_id))
```

换成 `key=lambda sp: sp.started_at`。**结果它被抓了**——用例变红：

```
E  assert ['SP-00002', 'SP-00001'] == ['SP-00001', 'SP-00002']
```

但**抓它的理由不是"时间戳排错了"**，值得写清楚：

`_now_iso()` 只到**毫秒**（`timespec="milliseconds"`），而那条用例里两个 span
是**同一毫秒**内开出来的。于是 `started_at` **并列**，Python 的 `sorted` 稳定，
退回 `_spans` 的原始顺序——也就是**完成**顺序——正好是错的，
用例因此变红。

**也就是说：这条契约的守卫依赖"时间戳粒度比 span 创建速度还粗"。**
哪天把 `_now_iso()` 改成微秒精度（一个看起来很自然的"提高精度"改动），
两个 span 就会拿到严格递增的时间戳、排序结果与编号一致，
**这条突变会变成绿色的**——而那时没有任何别的东西在守
"编号 = 开始顺序"，35.2 的并发交错也就重新获得了进入的资格。

**已补上一条与精度无关的守卫**：把巧合换成断言。
新用例 `test_spans_order_follows_span_id_even_when_timestamps_contradict_it`
**直接构造矛盾**——先开两个 span，再把第一条的 `started_at` 改成晚于第二条，
也就是 35.2 那张表里真会发生的那种交错——然后断言顺序仍然是编号顺序。
再证伪一次：这条突变现在让**三条**用例变红，其中这一条与毫秒精度无关。

剩下的那个更好的修法（把编号与时间戳收进同一个临界区）仍然没做，
理由见 35.4 的引用块。

### 35.6 教训

1. **"后端是这么定的"这句话，要去读后端那几行。** 我在注释里正确地
   陈述了后端的规矩，然后按自己的理解写了一版。注释与实现各说各话，
   而两边都像是对的——**同一份文件里的注释不能给自己的实现作证**。
2. **同一个概念有两处定义时，选"已经流出来的那一份"。** 前端拿到的
   数组本来就是后端排好的序，再排一次就是在制造第二个定义。
   要重排就该用**同一个键**，而不是一个"通常等价"的键。
3. **锁内取的东西和锁外取的东西，不构成一个原子事实。** 这里的编号与
   时间戳目标是同一件事（span 的开始），却取自两个不同的临界区，
   中间那道缝就是 35.2 那张表。
4. **一条突变变红，要看清它"为什么"变红。** 35.5 里那条突变是红的，
   但红的原因是**毫秒并列**，不是排序键错了。把"变红了"当成
   "这件事被守住了"，就会错过"改个时间精度守卫就没了"。
   **突变的结论要连机制一起记，否则它只是一次观测。**

## 问题 36：前端反证脚本里，3 条突变早就死了，而它照报成功

改完 `replay.ts` 跑前端反证脚本，汇总里出现三行从没见过的字：

```
  锚点失效  F13 reset 不清缓冲（重置之后上一轮的帧还会渲染进来）
  锚点失效  F16 disconnect 把 fatal 抹成 idle（界面说空闲，其实永远不会再来了）
  锚点失效  F17 disconnect 把缓冲里的帧一起 flush（断开反而多渲染一次）
```

而**这一轮退出码是 0**。47 条里实际只有 44 条在验证，脚本报的是成功。

### 36.1 三条锚点是怎么死的

全部来自两次**看起来完全无害的重构**：

| 重构 | 被杀掉的锚点 |
|---|---|
| 把 `scheduled = false` 抽成 `clearScheduled()`（顺带取消了 rAF 与兜底定时器） | F13、F17 |
| 把 `state.connection === 'fatal' ? ... : 'idle'` 抽成 `isSettled(...)` | F16 |

两处重构都让代码**更好**（取消逻辑集中到一处、终态判定成了一个有名字的概念），
而它们各自顺手带走了一到两条突变的锚点。

### 36.2 为什么脚本没报警——这才是要修的地方

```python
count = original.count(old)
if count != 1:
    print(f"  !! 锚点在 {rel} 里出现了 {count} 次，无法定向替换")
    verdicts.append(f"  锚点失效  {name}")
    continue                      # ← 只跳过了这一条
...
return 0 if not dirty and not broken else 1   # ← dead 不在里面
```

锚点失效的名字进了 `verdicts`（会打印），但**没有进 `broken`**，
而退出码只看 `dirty` 和 `broken`。于是：

- 三条突变什么都没验证；
- 汇总里它们与"抓住"并排，只在那一列字上不同；
- **退出码 0**——CI 或任何看退出码的人都会认为这一轮是干净的。

顺带一处措辞也误导：`count` 为 0 时打印"出现了 **0** 次"，
读起来像"出现过、只是次数不对"。而 0 次和 >1 次要做的事**完全不同**：
0 次是"这段代码被重构过，去改锚点"，>1 次是"锚点太宽，去把它写窄"。
这和脚本自己在别处写的"这两者要采取的行动完全不同"是同一条道理，
只是没贯彻到这里。

> 这和**问题 30**（"三条'看起来在守、实际没有任何信号'的东西"）
> 是同一个病，只是长在了验证工具自己身上：
> **一条不再对应任何代码的断言，比一条失败的断言危险得多。**

### 36.3 修复：三件事

1. **锚点失效进 `dead` 列表，并让退出码变红。** 它既不是"漏掉"
   （测试没抓住）也不是"仪器崩了"（测试没跑起来），是第三种失败，
   要有自己的说法和自己的退出码。
2. **0 次与 >1 次分开报。**
3. **加锚点预检。** 在一轮开跑**之前**把所有锚点扫一遍：
   这是唯一一种**开跑前就能知道**的失败（只取决于源码文本，
   与测试跑得怎么样无关）。预检不过就直接返回，**一个文件都不改**。
   不做预检的话，一条被重构带走的锚点要等一个多小时之后才在汇总里露面——
   而这一轮里它什么都没验证。预检放在快照建立之后、而不是更早，
   因为判据必须与突变循环用**同一份文本**（那是快照，不是磁盘现场），
   拿现场判会在工作区脏时报出一批假失败。

### 36.4 重新锚定：注意"删一半"是抓不住的

把 F13/F17 的锚点改成现在的代码时发现一件事，值得单独写：

它们原本的语义是"不清缓冲"，而现在的代码里清缓冲是**两行**——
`pending = []` 和 `clearScheduled()`。只删 `pending = []` 的话，
`clearScheduled()` 会把那个已排好的回调取消掉，`flush()` 再也跑不到，
**缓冲里那条帧没有任何人看得见**，于是用例照样是绿的。
（实测确认：单独删 `pending = []`，`reset 丢掉缓冲里还没落地的帧` 不变红。）

所以锚点必须两行一起删。这不是为了让突变"能变红"而凑的——
"不清缓冲"本来指的就是这两行都不动。

这也说明 `pending = []` 与 `clearScheduled()` **各自单独都证伪不了**，
只有合起来才可观测。代码里那段注释早就写了"都**不是在修一个线上 bug**…
证伪不了"，与实测一致。**注释里的这句判断是对的，而它值得被验证过一次。**

### 36.5 教训

1. **退出码是这套纪律的最后一环。** 一份报告里有三行"锚点失效"、
   而退出码是 0，等于这三行不存在——**看退出码的人比读报告的人多**。
   任何"发现了问题但不改变退出码"的分支都要问一句：那谁会知道。
2. **重构的代价要算上"谁的锚点会死"。** 这次两处重构都是改良，
   代价却记在了一个完全不相干的文件上（`scripts/falsify_frontend.py`）。
   预检把这个代价**从"一小时后"提前到"一秒内"**。
3. **`0 次`与`N 次`不是同一件事。** 报错信息里的数字要能区分
   "找不到"和"找到太多"——它们指向两种修法。

---

## 问题 37：知识图谱上"一个品牌有几条证据"有两个答案，而画出来的是第三个

知识图谱这一页（`lib/graph.ts` + `pages/GraphPage.tsx`）写完之后，照例拿**真实报告**
跑一遍再算做完。四份真报告里，前三份各暴露出一层不同的问题，且**每一层都是"我
自己写下的规律其实是错的"**，不是代码写错。三份都跑通之后，第四份才干净。

这一条记的是这次的过程，因为三层的**发现方式完全一样**：夹具全绿，真数据一跑就分叉。

### 37.1 第一层：我声明的第一条不变量是假的，而测试是绿的

`lib/graph.ts` 的文件头原本写着：

> 1. **品牌 / 维度节点的 `weight` 等于它所有边的权重之和。**

测试也照着它写，而且**过了**。真报告一跑：

```
RP-8b4e09569307  brand:星星充电: expected 46 to be 45
```

品牌 45 条证据，六条边加起来 46。

为什么夹具是绿的：那条测试用的是默认夹具，**每条证据只命中一个维度**，
于是"证据条数"与"边权之和"恰好相等——两个不同的定义在这份数据上给出同一个数。
这与问题 35.5 是同一类缺陷：**守卫的力量来自一个巧合，而巧合不是每天都成立。**

查清楚之后，真实关系是**不对称的两条**：

| | weight 的含义 | 与边权之和 |
|---|---|---|
| 维度 | 命中该维度的证据条数 | **严格相等** |
| 品牌 | 该品牌进图的证据条数 | 边权之和**可能更大** |

差额的来源：一条证据可以同时命中多个维度，那么它在**每个**维度上各记一笔，
而按"证据条数"只算一条。维度那一侧之所以不受影响，是因为一条证据不会
命中同一个维度两次。

### 37.2 第二层：差多少，我也说错了

第一版的差额文案写的是"其中 N 条同时命中了多个维度"，`multiDimensionHits()`
的注释也这么写。第三份报告（Notion）又对不上：

```
RP-b1e819bfd098 Notion 差额: expected 4 to be 3
```

4 是代码算的，3 是我在探针里的期望。原因是这两种说法**本来就不是一个数**：

- 多维度证据的**条数**：一条命中 3 个维度的证据记 **1**
- 多记的**笔数**：同一条证据记 **2**（每多命中一个维度就多一笔）

函数返回的是后者，`Σ(命中维度数 − 1)`。两种说法在"每条多维度证据都只命中
两个维度"时才相等——又是一次巧合。已在函数注释、文件头、页面文案三处
把"笔数"这个词写死，并在测试里**造出一条命中 3 个维度的证据**把这个分叉钉住。

### 37.3 第三层：这才是真正的 bug —— "进图"的判据被写了两遍

第二层修完，第一份报告报出：

```
RP-a9466ff0aa56 示例调研对象 证据条数: expected 72 to be 40
```

**品牌节点上写着 40（或 45），点开却列出 72 行。** 这一条与上面两层不同，
它确实是代码错误，而且是本项目反复出现的那一类：**同一个概念有两份定义。**

- `buildGraph` 判"进图"：品牌在名单里 **且** 命中至少一个计划维度。
- `pickEvidences` 判"取出来"：只按调用方传进来的字段过滤。

前者紧、后者松，于是图上 45、列表 72，多出来的 27 条是没命中任何计划维度、
压根不在图上的证据。**两个数字各自都能自圆其说**，合起来就是"这张图上的数
别信"。这和问题 32 的"量表被丢掉"、问题 33 的"三个数各说各话"是同一个病。

修复不是把 `pickEvidences` 的过滤条件改紧一点，而是**把那条判据抽成一份**：
`plannedMatches(evidence, planned)`，两边都调它。并且 `pickEvidences` 的入参
**必须**带上 `brands` 与 `dimensions`（这份报告的计划名单）——想绕过这条判据，
就得先显式地把名单传进来，做不到了。类型系统在这里当了守卫：改完之后
两处调用点立刻编译不过。

### 37.4 顺带把"丢了多少条"拆成三个数

修完第三层，第四份报告又是另一种形态：

```
RP-26be47598e10 | 证据 216 / 进图 0 / 丢 216
```

图上一条实线都没有。而页面上原来只有一句话：

> 另有 216 条证据没能进图：品牌不在名单里、或者一条计划维度都没命中。

这句话**没错，但没有用**——读者最想知道的恰恰是哪一种。查了正文：
这份报告的 `dimensions` 字段是 `null`（**根本没有维度这一层**），而它的证据
却都带着 `matchedDimensions`（"功能覆盖"“定价策略”…）。于是：

| 数 | 含义 | 读者要做的 |
|---|---|---|
| `droppedNoBrand` | 品牌不在名单里 | 看这次调研的口径 |
| `droppedNoDimension` | 一条计划维度都没命中 | 看证据归类 |
| `droppedUnknownDimension` | 有标注、但名字不在计划维度里 | **两份数据对不上**（前两者的子集） |

前两个互斥、加起来正好是总数（品牌先判，避免一条同时缺两样的证据被数两遍），
第三个是第二个的子集。页面按这三种情况说三句不同的话；`dimensions` 为空时
还会说成"这份报告根本没有计划维度"，与"两套词对不上"分开。

### 37.5 教训

1. **夹具全绿只能证明"代码在夹具上对"。** 这一页的三层问题**全部**由真实报告
   发现，没有一层是单元测试先抓到的。原因是每一次分叉都要求数据里出现
   一个夹具没有的形态：一证多维、一证三维、无计划维度。**补夹具比补断言重要**——
   三十七节里造出的那三条夹具（一条命中两维、一条命中三维、一条品牌维度
   同时不合格）才是这次的真正产出。
2. **"不变量"要先在真数据上验一遍，再写进文档。** 37.1 那条不变量写得斩钉截铁，
   注释里还解释了"为什么必须这样"，而它是假的。**写进注释的自信不构成证据**；
   一份真报告五秒钟就能证伪。
3. **同一件事有两个数字时，先问"哪个是我自己写出来的"。** 37.3 里图和列表
   都是代码算的，我最初的反应是去查渲染；实际是两边各算了一遍。
4. **能解释的差可以接受，不能解释的差会让人放弃全部。** 45 与 46 本身不致命，
   致命的是页面上说不出这 1 从哪来。所以差额不是"藏起来"而是"印出来"：
   品牌详情里会写"一共多记了 N 笔，所以边上的数字加起来是 M"。

---

## 问题 38：48 人动态组队有一半在悄悄失效——校验器把模型抄回来的整行当成了"编造"

这是**评测体系建成之后抓到的第一个真实缺陷**，而且抓到它的方式值得单独记一笔：
不是单测先红，是回放的降级横幅上多了一行以前没见过的话。

### 38.1 现象：一句听起来很有把握、但完全不对的话

对 5 条真实 query 跑回放，其中 3 条的降级区里出现：

```
专家调度：8 个 id 不在名册（L3-002 首席分析官、L2-002 定价与商业模式分析师、L2-004 技术架构分析师…）；
组长、战略层、执行层整类退回默认队伍
```

`phantom` 这个字段的语义是**"模型在编 id"**——它是对模型的一项指控。
所以第一反应是去名册里查这几个号存不存在：

```
roster size 48
L3-001 True / L3-002 True / L3-003 True / L2-001 True / L2-002 True
L2-004 True / L2-006 True / L2-009 True / L1-001 True / L1-036 True
```

**全都在。** 校验器把真实存在的人报成了不存在，而报告里那句话读起来像是模型的错。

### 38.2 根因：名册教一种格式，校验器要求另一种

去读录下来的**原始响应**（不是夹具），第一个 dispatch 录制就是：

```json
{"lead": "L3-002 首席分析官",
 "strategists": ["L2-002 定价与商业模式分析师", ...],
 "executors": ["L1-002 知识管理行业专家", ...]}
```

再看喂给模型的提示词：名册是以 `- L3-002 首席分析官` 这个形状拼进去的
（`roster_digest()` 里就是 `f"- {expert_id} {role_title}"`），
而输出格式示例写的是 `{"lead": "", "strategists": [], ...}`——
**一个空串，什么形状都没说明。** 于是模型把名册整行抄了回来，这很正常。

而校验器做的是：

```python
expert_id = as_str(item)          # "L3-002 首席分析官"
expert = expert_by_id(expert_id)  # 字典查找 → None
if expert is None:
    outcome.phantom.append(expert_id)   # 记成"编造"
```

于是三个角色全空，按"任何一类为空就整类退回默认"的规则，**三类全部失效**。

顺带解释了为什么它一直没被发现：`DEFAULT_TEAM` 恰好与模型想选的人大量重叠
（`L2-002` / `L2-003` / `L1-002` / `L1-025` / `L1-026` 都在里面），
兜底队伍跑出来的报告**看起来完全正常**。**守卫的力量来自一个巧合。**

6 条录制里 3 条是长格式——**一半**。

### 38.3 为什么所有单测都是绿的

`tests/unit/test_dispatch.py` 有 21 条测试，喂的 id 全是裸的：

```python
def test_phantom_ids_are_dropped_and_reported() -> None:
    outcome = validate_team(_payload(executors=["L1-001", "L1-099"]))
```

**"模型会写裸 id"是所有测试共同依赖的假设，而它从来没被量过。**
这和问题 37 是同一个病：夹具不会犯错，模型会。

### 38.4 修复：形状识别 + 提示词，两处都改

校验层（**这是保证**）加 `_extract_id()`：精确命中优先（行为与改前一致），
否则取第一个 `L[123]-\d{3}` 片段。**只认形状，不做模糊匹配**——
放宽到编辑距离那一类，"模型编的号"会被匹配成"名册里某个号"，
`phantom` 就永远为空了，而它是这套机制唯一的报警器。
所以 `L2-099 财务分析师` 仍然记 phantom，只是记 `L2-099` 而不是整行。

提示词（**这是请求**）加一句"id 只写编号本身，不要把岗位名抄进来"。
它有效——但**它只是请求，不是保证**，所以两层都要有。

### 38.5 验证：对着真实录制跑，而不是对着夹具

新增的测试里有一条直接读 `tests/fixtures/cassettes/llm.deepseek.jsonl`，
取出全部 11 条真实 dispatch 响应，断言：

> 任何一段里如果含有一个**名册里真实存在**的 id，那它绝不能被报成 phantom。

判据是 `phantom` 的语义本身（它是对模型的指控，必须只包含真的编造的号），
而不是"输出等于某个快照"。

反证（把 `_extract_id` 退回旧行为）：

```
旧行为 → phantom: ['L3-002 首席分析官', 'L2-002 定价与商业模式分析师', ...]
旧行为 → 退回默认: ['组长', '战略层', '执行层']
旧行为 → 横幅: 5 个 id 不在名册（L3-002 首席分析官…）；组长、战略层、执行层整类退回默认队伍
旧行为下真实录制 11 条里有 3 条报出 phantom
```

**复现出的横幅与线上观测到的那句一字不差**——根因判断成立。

### 38.6 改提示词的代价：一个量化结果

提示词改了，cassette 的 key（由请求内容哈希而来）就变了，那 6 条 dispatch 录制
全部失效，必须补录。补录是**续录**：`RecordingProvider` 对已有的 key 直接复用，
只有缺的那几条联网。

补录后重新量了一遍 11 条真实响应（6 条旧的 + 5 条新的）：

| 提示词 | 样本 | 写成长格式的条数 |
|---|---|---|
| 改之前 | 6 | **3**（一半） |
| 改之后 | 5 | **0** |

复现命令：回放这 5 条 query，`专家调度` 那行降级从 3/5 变成 0/5。

### 38.7 教训

1. **对外的每一个字段都是一句断言，它必须是真的。** `phantom` 说的是"模型在编"，
   那么它里面就一个真的编造的号都不能多。观测通道说假话的时候，
   "全程可观测"就只剩一个好看的界面——这条在问题 24、34 已经写过两次，
   这是第三次，而这次代价最大：**一个主打能力有一半在悄悄失效。**
2. **名册教什么格式，校验器就得认什么格式。** 提示词里展示 `L3-002 首席分析官`，
   却在别处要求 `L3-002`——这个矛盾是我自己造出来的，而模型只是照着抄。
3. **兜底路径会掩盖主路径的失败。** `DEFAULT_TEAM` 与模型的偏好高度重叠，
   于是"整类退回默认"产出的报告看起来很正常。**兜底越像正确答案，
   失败就越难被发现**；所以兜底必须留痕（这次是那句横幅），
   而横幅本身必须说真话。

---

## 问题 39：闸门绿着，而夹具早就失效了——一个 `required=False` 把"夹具坏了"伪装成"这次没成功"

问题 38 改完提示词之后，回放报了 `CassetteMiss`。但**在我修好之前，它报的是"12 项契约全绿"。**
这一条记的是那个假绿，它比 38 本身更危险。

### 39.1 现象：一个不可能同时成立的两件事

改了调度提示词 → 那条 dispatch 录制失效（key 由请求内容决定）→ 回放层抛
`CassetteMiss`。但那次 `--replay` 的输出是：

```
跑了 1 条 · 总成本 $0.0254 · 档位 quick

降级过 1 条：
  notion-vs-obsidian：5 处
    · 专家调度：组长、战略层、执行层整类退回默认队伍
    · 正文抓取：3/12 条正文抓取失败，这些证据只有搜索摘要

闸门通过：1 条，12 项契约全绿。
```

**闸门绿着，而动态组队整个没生效。** 注意那行降级说"整类退回默认队伍"，
却**不说为什么**——因为这一次没有任何 phantom，`validate_team` 拿到的是空 payload。

### 39.2 根因：`required=False` 分不清两种失败

链子是这样的：

```
CassetteMiss 失效
  → dispatch 那处调用是 required=False
  → chat_text 的 except ProviderError 吞掉它、返回空串
  → chat_json 返回 {}
  → validate_team({}) 三类全空 → 整类退回默认队伍
  → 报告照常产出
```

`required=False` 的语义是"**这次调用失败也能继续**"，而这在当初写下时是**对的**：
生产里不该因为一个可选步骤偶发限流就毁掉整份报告。问题在于它把两种完全不同的
失败当成了同一种：

| | 说的是 | 正确反应 |
|---|---|---|
| `RateLimited` / `Transient` | 这次调用没成功，**但系统是好的** | 降级继续 |
| `CassetteMiss` | **这次运行依赖的东西本身是坏的** | 停下来 |

而 `CassetteMiss` 当时直接继承 `ProviderError`，从类型上和限流没有任何区别。

### 39.3 修复：让"夹具错误"在类型上就与运行时故障分开

`errors.py` 加 `HarnessError(ProviderError)`，`CassetteMiss` 改继承它；
再加一个**唯一**的判定函数：

```python
def should_degrade(exc, *, required=False) -> bool:
    if required:
        return False
    return not isinstance(exc, HarnessError)
```

两个条件都满足才降级。收成一个函数而不是在每个 `except` 里各写一遍，
理由与 `build_charts` 出口那道收口相同：**一个口子比 N 处各判一次更不容易漏。**

接线的地方有四处于是有区别了：
`chat_text`（观测到的那处）、`search_sync`（搜索总是降级，但夹具错误仍要穿透）、
`evidence/fetch.py`（`except Exception`，夹具缺失会被记成"只有摘要"，
看起来像抓取失败）、`providers/fallback.py`（**夹具问题不是"主 provider 挂了"，
不能因此换一家**——回放时静默换 provider，等于把这次评测换成另一个模型跑的结果，
而两份数字还会被放在一起比较）。

### 39.4 验证

- 单元：`should_degrade` 四个方向各钉一条，其中"夹具缺失即使可选也必须抛出"
  是那次假绿的直接回归测试。
- 集成（`tests/integration/test_harness_error_escapes.py`）：走**真实的**
  `chat_json` → provider → 异常 这条路径，断言 `required=False` 时
  `CassetteMiss` 必须逃出来。守的是**接线**——规则写对了但某个 `except`
  分支没调用它，那串连锁反应会原样重演。
- 反证（把 `should_degrade` 换回"只判 required"）：
  `test_可选调用遇到夹具缺失时抛出而不是返回空` → `DID NOT RAISE CassetteMiss`。

反证之后，同一个回放命令的输出从 "12 项契约全绿" 变成：

```
[ 1/1] notion-vs-obsidian · quick  失败：流水线失败——
       CassetteMiss: [deepseek] 回放缺失：chat(purpose='dispatch', tier=fast)
一条都没跑成。
```

**它现在会指名道姓地说出是哪一次调用、哪个 purpose、哪一层。**
闸门从"绿着撒谎"变成"红着说清楚"，这就是它该有的样子。

### 39.5 顺带修好的第二句话：录制脚本把"用量"印成了"扣费"

补录时发现脚本印的是 `$0.13261`，而这次**只补了 5 条 dispatch**，
真实扣费不到一分钱。查文件 mtime：`search.bocha.jsonl` 与 `fetch.http.jsonl`
根本没被写过，只有 `llm.deepseek.jsonl` 追加了 5 行。

原因不是算错，是**那个数旁边没有任何东西说明它是哪个意思**：
`costUsd` 累加的是响应里的 usage，而**回放的响应照样带着当初录下的 token 数**。
它回答的是"这条流水线跑一遍值多少钱"，不是"这次从你账上扣了多少"。

修复：脚本结尾比较各 cassette 文件的新增行数，明确印出
"本次联网新录 N 条（llm.deepseek.jsonl +5）"或
"本次全部命中已有录制：**未联网、未产生任何费用**"，并把结果写进
`MANIFEST.json` 的 `freshRecords`。顺带发现 `queries` / `costUsd` 描述的是
**这一次运行**，而 `--only` 续录之后 manifest 会写着 `queries: 1`，
而录着 5 条 query 的文件就在旁边——所以另加 `cassetteTotals` 描述
"cassette 里现在合计有什么"，两个数各标明在说哪件事。

而这个"新增/复用计数"**在脚本 docstring 里已经写了很久了**，只是从没实现。
一条"文档说会报、实际不报"的说明比没有说明更坏：它会让人以为自己已经看过那个数。

### 39.6 教训

1. **闸门必须能发现自己的夹具失效了。** 一个只会说"和上次一样吗"的闸门，
   在夹具本身坏掉的时候会给出最强的绿灯——因为它比较的两边一起坏了。
   夹具缺失必须是**硬失败**，不能是一条降级说明。
2. **"可选"描述的是失败的性质，不是失败的来源。** `required=False` 当初写对了
   语义、用错了范围：它该容的是"这次调用运气不好"，不该容"你依赖的东西不存在"。
3. **降级说明必须说得出原因。** 那句"整类退回默认队伍"本身没错，
   但它下面没有第二句——于是读者无法区分"模型给了一队编的号"和
   "我们根本没拿到模型的回答"。**一句解释不了自己的降级，等于没有降级说明。**
4. **凡是会被读成金额的数字，旁边必须有它是什么的说明。**
   `$0.13261` 在那次补录里是"整条流水线的用量"，而它就印在一次真实扣费
   不到一分钱的操作末尾。

## 问题 40：评测结果的自述有两处与事实不符——一处印错了条件，一处比错了东西

这两个都是**在修完 39 之后、把那条命令再跑一遍读输出**时发现的，不是搜代码搜出来的。
顺序很关键：39 修完之后 `--replay` 的输出从"17 条失败"变成"闸门通过"，
那是一个**看起来终于对了**的输出——而它里面还藏着两句不成立的话。

### 40.1 表头印的 provider 是命令行缺省值，不是这次真用的那个

回放跑完，表头是这么一行：

```
回放：22 条 · provider=mock · 临时库
```

而每一份报告的每一个数字（证据数、独立信源、token、成本）都是 **DeepSeek 的录制**
跑出来的。那句 `mock` 没有一个字是真的。

查 `run_gate`：

```python
print(f"{where}：{len(golden)} 条 · provider={args.provider} · 临时库")
```

`args.provider` 是 argparse 的缺省：

```python
args.provider = "deepseek" if args.kind == "live" else "mock"
```

只有 `--gate` 和 `--live` 会走 `_set_provider_env()` 把它写进环境变量；
**`--replay` 刻意不覆盖 provider**（回放要走的正是真适配器那条路径，
覆盖成 mock 的话就变成"用 mock 跑一遍"，什么都证明不了——这个决定本身是对的）。
于是那个缺省值谁也没去改，就那么被印了出来。

**根因**：表头取的是"我要求了什么"，而不是"我实际用了什么"。这两者在
`--gate` / `--live` 下恰好相等，所以这个错只有在 `--replay` 下才暴露——
而 `--replay` 正是**新加的**那条路，它的表头从来没被人读过。

**修复**：新增 `_runtime_providers()`，从 Settings 现读三家 provider 的名字
（Settings 是缓存单例，且 `main()` 里所有 env 改写都发生在它构造之前，
所以读到的一定是本次运行的值）。同名时合并显示，回放模式下额外写明
"真实适配器 + cassette 回放"：

```
回放：22 条 · 真实适配器 deepseek/bocha/http + cassette 回放 · 临时库
```

同一句谎还写在**落盘的 JSON** 里（`"provider": args.provider`），而那个文件是
基线，会被当成"上次是在什么条件下测的"存档。改成三个字段各说各的：

```json
"kind": "replay",
"requestedProvider": "mock",
"runtimeProviders": {"llm": "deepseek", "search": "bocha", "fetch": "http"},
"cassetteMode": "replay"
```

### 40.2 漂移比对把"这台机器当时忙不忙"当成了"系统退步"

基线重新生成后，立刻拿它比了一次自己：

```
与基线相比变差（容忍 15%）：
  · durationMs：max 266 → 405（+52.3%）
  · firstEvidenceMs：max 94 → 125（+33.0%）
```

**同一个命令、同一份录制、同一份代码、同一台机器，连跑两遍。**
变差 52% 的只有一件事：当时有没有别的进程在抢 CPU。

而回放模式下这两个数**连网络时间都不含**——它量的是本地进程调度，
本来就不是"端到端耗时"。拿它做漂移检测的后果是：每次运行都亮两盏黄灯。
常亮的警告等于没有警告，它还会把真正该看的那几条（独立信源数、已验证论点、
幻觉率）淹在噪音里。

**根因**：`diff_baseline` 遍历的是聚合里的**全部**指标，而聚合里混着两类东西——
一类描述被测系统（引用、覆盖率、成本），一类描述运行环境（墙钟耗时）。
后者的方差在回放/CI 环境下远大于任何真实退步的幅度。

**修复**：加 `DRIFT_EXCLUDED = {durationMs, firstEvidenceMs}`，并在输出里
**说出来**（静默排除会被读成"耗时一直很稳"，而它其实抖得最厉害）：

```
（durationMs、firstEvidenceMs 不参与漂移比对，理由见 DRIFT_EXCLUDED）
与基线相比没有变差（容忍 15%）。
```

真要量耗时的变化，用 `--live` 或 `--from-db`：那里有网络、有模型延迟，
抖动相对量级小得多，也才是用户感知的那个数。

### 40.3 顺带补上的是"评测层自己的测试"

这两个 bug 都在 `eval/run_eval.py` 里，而它**此前一条测试都没有**。
它是整个项目里唯一决定"算不算合格"的地方，却只被人肉读过。

- 把 `pythonpath` 从 `["."]` 放宽到 `[".", ".."]`（`"."` 必须在前，
  否则 `scripts` 会解析到仓库根那个而不是 `backend/scripts`）；
- 新增 `tests/unit/test_eval_drift.py`（8 条），钉住漂移比对器的三个方向：
  该报的报、不该报的不报、**排除项必须真的存在**。

最后一条是专门为这个 bug 写的：

```python
def test_排除项必须是真实存在的指标名() -> None:
    unknown = DRIFT_EXCLUDED - set(NUMERIC_KEYS)
    assert not unknown, f"DRIFT_EXCLUDED 里有不存在的指标名：{sorted(unknown)}"
```

拼错的排除项会**静默失效**——名字对不上，那条指标照比不误，黄字照报，
而写排除的那个人以为自己已经处理过了。指标改名时这条会红，
而不是让排除悄悄变成一句注释。

### 40.4 验证：反证

把 `if key in DRIFT_EXCLUDED: continue` 删掉再跑：

```
E  AssertionError: assert ['durationMs：...300（+219.1%）'] == []
E     Left contains 2 more items, first extra item: 'durationMs：max 266 → 800（+200.8%）'
FAILED tests/unit/test_eval_drift.py::test_计时项不参与漂移比对
```

删掉排除，测试立刻拿**真实的那个数**（266）报错——这条测试不是对着假数据写的。

回放路径在**没有 `backend/.env`、没有 API key** 的环境下也单独验过一次
（把 `.env` 挪走、`env -u` 两个 key 变量，退出码 0），因为 CI 里没有那个文件——
这条现在是 `ci.yml` 里的一步显式护栏。

后端：`849 passed, 6 deselected`（此前 841），`ruff check app tests scripts ../eval` 全过。

### 40.5 教训

1. **自述也是输出。** 一个数被人引用时，它旁边那句"这是什么条件下测的"
   会一起被引用。表头印错条件，和数字算错，是同一等级的错。
2. **"我要求了什么"和"实际用了什么"要分开存。** 两者在大多数分支下恰好相等，
   所以只写一个字段时，错的那个会长期隐身——直到出现一条它们不等的分支。
3. **比一个量之前，先问它变了会不会是因为别的什么。** 墙钟耗时在 CI 里
   主要由机器负载决定；把它放进漂移比对，等于给每次运行点一盏常亮的灯。
4. **排除要出声。** 静默过滤会让人把"没报"读成"没问题"，
   而这两件事在这里正好相反。

## 问题 41：报告页一张图都不显示，而两个"有人用"的假象把它盖住了

这条是**新写的 Playwright 冒烟测试**第一次运行就发现的，不是读代码读出来的。
它的价值因此不只在"修了一个 bug"，更在于说明**哪一层测试能看见哪一类缺陷**。

### 41.1 现象

`ReportChart` 组件写得相当完整：按需引入 ECharts（附一段"漏写一行 `use`
不会报错、只是不画"的说明）、`ResizeObserver` 跟着面板宽度走、
画不出来时给一句说明而不是一张空图、连它自己修过的那个折线图 bug 都记在文件头。
一百多行。

**而它没有任何地方引用。** 报告页没有 import 它，全站只有它自己提过自己。

于是报告页上四张图一张都没有。真浏览器里数出来的数：

```
figures: 2      ← 都是「图集」里的图片，与图表无关
canvases: 0
svgs: 0
```

而 `GET /api/reports/{id}` 的响应里明明有 4 张：

```
chart-matrix-bar/bar/功能维度评分对比
chart-matrix-radar/radar/能力雷达图
chart-market-share/pie/市场份额
chart-source-mix/pie/证据来源分布
```

### 41.2 为什么没有测试发现

三层测试，每一层都恰好看不见这件事：

| 层 | 为什么看不见 |
|---|---|
| `ReportPage.test.tsx`（jsdom） | 文件里有一句 `vi.mock('../components/report/ReportChart', ...)`，**mock 的是一个页面根本没引用的模块**，而且没有任何断言去查那个假组件渲染出来的东西。 |
| `vitest` 整体 | jsdom 没有 canvas。就算页面引用了 `ReportChart`，`echarts.init` 在那一层也画不出东西——**这一类缺陷在单测层面结构上不可见**。 |
| 我自己（读代码） | `ReportChart.tsx` 打开一看就是"做完了"的样子。它是全项目里注释最密、最像深思熟虑过的组件之一。 |

那个 mock 值得单独说：它带来一个**双向的假象**。
对读者，"页面 mock 了图表组件"说明图表接过了；
对组件，"有测试 mock 我"说明有人在用我。
两边都不成立，而两边都不需要对方成立就能看起来成立。
**一个空转的 mock 比没有 mock 更坏**——没有 mock 至少不会发出信号。

### 41.3 一个更早的征兆，当时被读过去了

`completeness.blocks` 里有一项叫 `charts`：

```json
{ "matrix": "filled", "charts": "degraded", "sentiment": "missing" }
```

报告页上那张"模块完整度"的表会把它列出来。也就是说：
**页面先告诉读者有"图表"这么一块、还标着 `degraded`，然后一个图都不给。**
读者看到 `degraded` 会以为是"图生成得不好"，而真相是"图根本没往页面上挂"。

这句我在做阶段 7 时见过，当时读成了"后端这次图表降级了"。

### 41.4 修复

1. 新增 `src/components/report/ReportCharts.tsx`：用 `ReportPanel` 包一块
   `id="charts"`，网格排四张图，空数组时整块不渲染（连 `id` 都不给，
   与 `ReportMatrix` 的约定一致）。
2. 接进报告页，位置在 `ReportStructured` 之后——那四张图就是矩阵和份额的
   图形化，紧挨着它们的数据来源放，读者能对上"这张图和上面那张表是同一批数"。
3. 目录补一条 `图表` 锚点，条件**照抄** `ReportCharts` 的早退条件。
4. `ReportPage.test.tsx` 里那个 mock **留下来了**，但配上了它一直缺的断言：
   每一张图的 `chartId` 都按顺序传到了组件上，且目录锚点真的指向渲染出来的 `id`。
   mock 不再是摆设。

### 41.5 验证

- 单元：`报告页把每一张图都挂到页面上，且目录里有对应锚点` —— 断言的是
  **数量和顺序**，不是"至少有一张"。少一张的话，页面上只是"这块图比别的报告少"，
  肉眼看不出来。
- 浏览器：`e2e/report.spec.ts` 断言**没有一张图落在"这张图没画出来"那条分支上**、
  四张 canvas 的宽高都大于 50px、且打开过程中控制台无报错。
- 截图：`e2e/.artifacts/report-charts.png` 存了一张图表区的图——四张都画出来了，
  每张底下带着自己的证据条数。

后端 `849 passed`，前端 `311 passed`（+1），`tsc` / `oxlint` 全过。

### 41.6 两个附带发现

**（一）`npx playwright test` 会拿上一次构建的 `dist/` 跑。**

反证的时候踩到了：摘掉 `<ReportCharts>`、直接跑 `npx playwright test`，
那条"四张图必须都画出来"的测试**照样通过**——因为 `vite preview` 服务的是
`dist/`，而它是更早那次 `npm run test:e2e` 构建出来的（图表还在的那一版）。
**反证失败了，而失败的样子看起来像成功。**

修法不是"记得先 build"（那是要靠人记住的约定，迟早失效），
而是加 `globalSetup` 每次都构建。放在 `globalSetup` 而不是
`webServer.command`：后者的那句带着 `reuseExistingServer`，
本地已经在跑 preview 时**整条命令都不执行**，构建跟着一起没了。

**（二）`fullPage: true` 在一个 `h-screen` 页面上什么也没多做。**

报告页是三栏定高、中间那栏自己滚，文档本身没有溢出，
所以 `fullPage` 截出来和视口一模一样。
一个看起来在截整页、实际只截了首屏的参数，比不写它更容易误导人。
改成：首屏一张、`#charts` 那一块单独一张。

### 41.7 教训

1. **"有没有人用"不能靠读那个东西本身。** 组件写得好、注释写得深，
   与它被挂上去了是两件独立的事，而前者会强烈地暗示后者。
   要问的是"谁 import 它"，那是一个 `grep` 能回答的问题。
2. **空转的 mock 会双向说谎。** 它同时对读者和组件发出"这边有人"的信号。
   加 mock 的时候顺手加一条查它的断言——否则它只是让覆盖率好看了一点。
3. **有一类缺陷只能在真浏览器里看见。** jsdom 没有 canvas、没有布局，
   所以"图画不出来"、"横向溢出"这两类在这套单测里**结构上不可见**。
   这不是"单测写得不够"，是那一层测不了——所以那条 e2e 不是锦上添花，
   它是这些缺陷唯一可能的出口。
4. **反证也要反证。** 摘掉代码之后测试仍然绿，第一反应必须是
   "测试根本没在看这段代码"，而不是"这段代码不影响这个行为"。

### 41.8 接上图表之后，首屏多了 572 kB——顺手拆掉

把 `ReportCharts` 静态 import 进报告页之后，构建输出变了：

| | 首屏 chunk | gzip |
|---|---|---|
| 图表块未接（ECharts 被 tree-shake 掉） | 437 kB | 132 kB |
| 图表块静态 import | **1010 kB** | **327 kB** |

多出来的 572 kB 是 ECharts，而它是**全站唯一碰 ECharts 的地方**
（`ReportChart` 的按需引入已经做过了，省不掉的部分就是这个量级）。
问题不在于它大，在于它进的是**首屏那个 chunk**：
`App.tsx` 里所有路由都是静态 import，没有做懒加载，
所以首页、工作台、仪表盘、专家页**全都要为一张它们没有的图付一次**。

修法是把图表网格单独拆出去懒加载。拆的时候有个细节：
**面板本身不能跟着一起懒**。

```
ReportCharts（同步）   ← ReportPanel，带 id="charts"
  └ Suspense
      └ ReportChartGrid（lazy，570 kB）   ← 唯一 import echarts 的文件
```

如果 `lazy()` 包的是整块面板，那么"目录里已经列出「图表」这一项、
而面板还没到"的那一小段时间里，点目录会没反应——而报告页那段注释
（`anchors` 上方）明确说了这件事要避免。把面板留在同步那一侧，
这个窗口就不存在了，`id` 从第一帧起就在。

拆完：首屏回到 **438 kB / gzip 132 kB**（比原来只多 0.8 kB，
就是那个面板壳），ECharts 那 571 kB 只在报告页真正需要时才拉。

`GridFallback` 按两张图的高度给占位，不是给一行 spinner：
后者会让下面的内容在 chunk 到达时整体跳一下，而读者正在读那里。

---

## 问题 42：修好了，却没有东西拦得住它再发生——加闸门时又撞上一个自己造的编译错误

### 42.1 现象

问题 41 的修法是把 `ReportCharts` 接进去。但**接进去之后，那个缺陷仍然是可达的**：
再有人写一个组件、写完忘了挂，情况和 `ReportChart` 一模一样——
文件完整、注释详尽、测试里可能还有一句 mock，而页面上什么都没有。

41 的教训写的是"要问的是谁 import 它，那是一个 `grep` 能回答的问题"。
既然是 `grep` 能回答的，就没有理由是**人**去 grep。

### 42.2 第一版：直接红在编译上

按最直觉的写法用 `node:fs` 遍历 `src/`，`tsc -b` 立刻报三条：

```
src/test/moduleGraph.test.ts(28,43): error TS2591: Cannot find name 'node:fs'.
src/test/moduleGraph.test.ts(29,51): error TS2591: Cannot find name 'node:path'.
src/test/moduleGraph.test.ts(38,21): error TS2591: Cannot find name 'process'.
```

根因在 `tsconfig.app.json`：

```json
"types": ["vite/client"],   // 只开了这一个
"include": ["src"]          // 而测试文件就在 src 底下
```

**这个 `types` 是刻意的**，不是漏配。它让"在浏览器代码里写了 `process.env`"
变成一个**编译错误**——否则那句会一路过到运行时，在浏览器里炸成
`process is not defined`，而那是部署之后才发现的错误。

于是三条路：

| 方案 | 代价 |
|---|---|
| `"types": ["vite/client", "node"]` | 给**全部业务代码**放行 Node 全局，为一条测试拆掉一整类编译期保护 |
| 测试文件拆到自己的 tsconfig | 可行，但要再加一个配置文件；且以后每个用 Node API 的测试都得记得放对位置 |
| **用 `import.meta.glob`** | 零 Node API，零新配置 |

选第三条。`import.meta.glob` 是 Vite 自己的能力，构建时就把源码文本收进来
（`{ query: '?raw', eager: true }`），类型声明在 `vite/client` 里——
那条测试要读文件，本来只是为了拿到**文本**，没必要真的去碰文件系统。

```ts
const SOURCES = import.meta.glob('/src/**/*.{ts,tsx}', {
  query: '?raw', import: 'default', eager: true,
}) as Record<string, string>
```

### 42.3 一个类型错误能让整层 e2e 一起消失

这条值得单独记：`tsc` 红了之后，`npx playwright test` **也红了**，
报的是 `at globalSetup (e2e/global-setup.ts:40:3)`。

原因是 41.6 加的 `globalSetup` 里跑的是 `npm run build`，而它是
`tsc -b && vite build`。也就是说——

> 一个测试文件里的类型错误，会让**浏览器冒烟测试整层不执行**。

这本身不算错（构建不过确实不该跑 e2e），但它的**报错位置具有误导性**：
`playwright test` 的输出指向 `global-setup.ts`，看起来像 Playwright 配置坏了，
而真正的原因在 `src/test/moduleGraph.test.ts` 的第 28 行。
排查时先看那行 `build` 的完整输出，不要只看 Playwright 给的那一行调用栈。

### 42.4 反证

把报告页里 `import { ReportCharts }` 那一行和 `<ReportCharts />` 拿掉，重跑：

```
- []
+ [ "components/report/ReportCharts.tsx" ]
```

红的，而且指名的正是那个文件。

顺带验到一件想验的事：报告页里有**两处注释**提到了 `ReportCharts`
（`anchors` 那段和 `#charts` 那块上方的说明）。这两处**没有被算成引用**——
引用形式判的是引号里的字面量路径，注释里的裸名字不算。
这是想要的：如果注释能算引用，那么"写个注释就能让孤儿检查变绿"，
这道闸门就废了。

### 42.5 为什么只在前端立这条

后端也扫过一遍（83 个源文件，0 个孤儿），但**没有把它变成一条常驻测试**。
后端有注册表、装饰器这类"约定式引用"——文件本身不出现 import，
却被注册表按名字找到。这种误报在这个代码库里很容易出现，
而**会误报的闸门很快就会被绕过**（第一反应是加豁免，
加到最后没人再看它）。

前端的引用全是引号里的字面量路径（含 `import('./X')` 这种懒加载），
判断是确定的，所以这条只放在前端。

顺带一提，后端那次扫描本身也反证过：先写了个探针文件确认它会红，才相信那个 0。
一个从没红过的检查工具，报 0 和报不了是同一件事。

### 42.6 防它自己空转

```ts
expect(files.length, '没扫到任何源文件，这条测试失去意义').toBeGreaterThan(30)
```

glob 写错、目录挪走，`files` 会是空的——而空集合上跑"有没有孤儿"永远通过。
**一条什么都不检查的测试比没有测试更容易骗人**，因为它还占着"已覆盖"的位置。

### 42.7 验证

```
tsc -b --noEmit --force     exit 0
oxlint src --deny-warnings  干净
vitest run                  312 passed / 17 files
playwright test             1 passed
pytest -q                   849 passed / 6 deselected
```

### 42.8 教训

1. **修完一个缺陷，要问"它还能不能再发生"。** 41 修的是结果，42 修的是原因。
   前者让图上显示出来，后者让同类的事在提交前就红。
2. **一条检查的写法可以被它要守的东西带偏。** 为了读文件而开 `node` 类型，
   等于为了这条测试把整个应用的编译期保护降一格——收益和代价完全不成比例。
   换个工具（`import.meta.glob`）比换个配置干净得多。
3. **闸门的报错位置会骗人。** 类型错误报在 `global-setup.ts`，
   真正的位置在一个八竿子打不着的测试文件里。跨层的闸门尤其要看它调用的那层。

---

## 问题 43：压测脚本写完了、跑一次就"绿"了，而它测的不是它自己声称的那个东西

**日期**：2026-09（阶段 10）

### 43.1 现象

`loadtest/locustfile.py` 写完之后跑第一次，结果是**全绿**：
20 个用户、4 个场景、0 失败、`database is locked` 0 次。按计划这就是阶段 10 的验收条件。

但绿得不对劲。四个场景里的三个，`Requests/s` 低到不像在压一个 mock 服务，
而 `--tags` 那条命令行读起来明明是"只跑 stream 场景"。于是去数了一下
**真正发出去的请求长什么样**：

```
[压测] 场景 'read'，用户类：ReadUser, StreamUser, CreateUser
```

命令行说的是 read，实际跑的是三个类一起。而 20 秒里刷了 **796 行错误**。
也就是说：**如果只看"0 失败"这个结论，这次压测已经把三个缺陷全部放过。**

### 43.2 定位过程

三层，逐层往下。

**第一层：汇总只有标题，没有内容。**

```
================================================================
压测收尾
================================================================
```

四次运行，四次都只印出这两条线。不是"没有数据可印"——上面那张表里
明明有 892 个请求。是**印的那段代码抛异常了**，而异常被吞掉了。

去读 Locust 源码（`.venv/Lib/site-packages/locust/event.py`）：

```python
def fire(self, **kwargs):
    for handler in self._handlers:
        try:
            handler(**kwargs)
        except Exception:
            logging.error("Uncaught exception in event handler", exc_info=True)
```

`EventHook.fire` **吞掉 handler 的所有异常**。
然后是真凶：我写的是 `entry.request_type`，而 `StatsEntry` 那个字段叫 `method`
（`request_type` 是**事件的 kwarg 名**，不是 `StatsEntry` 的属性）：

```
AttributeError: 'StatsEntry' object has no attribute 'request_type'
```

两个错误的组合效果是：**标题照印，正文消失，退出码 0**。
一个"看起来跑完了"的压测。

**第二层：`--tags` 过滤的不是类。**

原以为 `--tags` 能把用户类筛掉，于是命令行写的是 `--tags stream`。
读 `locust/user/task.py` 才知道它过滤的是 **task**，不是类；
被过滤空了的类只会收到一句 warning，**照样被实例化**。
所以 20 个用户里只有约 7 个在做 stream 那件事，其余在跑它们自己的 task——
而它们需要的种子数据这个场景根本没准备，于是 796 行错误。

**第三层：一个变量名泄漏，让一个场景直接崩，另两个侥幸通过。**

改成 `LOADTEST_SCENARIO` 环境变量 + `abstract = True` 之后，
`read` 和 `stream` 都好了，`mixed` 报：

```
ValueError: ... same class name: ... ReadUser, ..., ReadUser
```

Locust 发现用户类的方式是扫 `vars(module)`（`locust/util/load_locustfile.py`
里的 `is_user_class`），**模块里任何一个绑到 `User` 子类的名字都会被收走**。
而我写的是：

```python
for _cls in {c for group in SCENARIOS.values() for c in group}:
    if _cls not in chosen:
        _cls.abstract = True
```

循环变量 `_cls` 是**模块级**的，循环结束后它指向一个用户类——
于是这个类被发现了两次。

`read` 和 `stream` 之所以通过，是因为它们剩下的那个重复项恰好是 `abstract` 的。
**不是设计对了，是碰巧。** 修法是把循环挪进函数里（`_select()`），
局部变量不进模块命名空间。

### 43.3 根因

三条根因，共同点是**"读代码看不出来"**：

1. **一个属性名写错，代价是整段输出消失而不是一条报错。** 因为
   `EventHook.fire` 吞异常，写错的地方离表现的地方隔着一整个框架。
2. **命令行参数的语义与直觉不符，而且不匹配时不报错。** `--tags` 长得像
   "选择要跑什么"，实际是"选择要跑哪些 task"；选空了只 warning。
3. **框架的发现机制是"扫模块里所有的名字"。** 这种机制下，
   一个多余的名字就是一个多余的被发现的类，而它们**长得一模一样**。

### 43.4 修复

| 问题 | 修法 |
|---|---|
| 汇总打印不出东西 | 字段改回 `entry.method`；并把"算正文"与"印标题"合成一个单元——**正文算不出来就印异常**，绝不留下一个光秃秃的标题 |
| `--tags` 语义不符 | 换成显式的 `LOADTEST_SCENARIO` 环境变量 + 用 `abstract = True` 真正停用没被选中的类；场景名不认识就直接 `SystemExit` |
| 变量名泄漏 | 选择逻辑挪进 `_select()`，模块命名空间里不留循环变量 |

第一条的修法值得单独看——它不是"加了 try"，而是**改了打印的顺序**：

```python
try:
    body = _summary_lines(environment)
except Exception:  # noqa: BLE001 - 汇总自己坏了也必须说出来，不能只留个标题
    body = ["汇总本身出错了：", traceback.format_exc()]
print("\n".join(["", "=" * 64, "压测收尾", "=" * 64, *body, "=" * 64, ""]))
```

先算完正文，再印整体。**"标题印出来了"这件事不构成"跑完了"的证据。**

### 43.5 护栏的证伪

`FORCE_MOCK_PROVIDER=1` 是这套压测唯一挡在"20 个并发用户打到真实 provider"
前面的东西，而那个事故花的是真钱。所以它自己也验了一次：

对着一个**真实的**（非 mock）后端跑，脚本必须先拒绝发压。实测：

```
[压测] 前置条件不满足，**不发压**：
http://127.0.0.1:8021 上的 provider 不是 mock：llm=mock search=mock fetch=http
```

退出码 1，**一个请求都没发出去**。这一条不是"读了一遍代码觉得它应该拦得住"，
是把护栏对着它要防的那个东西试了一次。

护栏本身也补了测试（`tests/unit/test_config.py` 的"压测护栏"一节）：
`force_mock_provider` 覆盖三家 provider、清掉 `llm_fallback`
（它**是会真的被调用的**——主 provider 出错时降级，漏掉它就是护栏自己开了个洞）、
默认必须是关的、`/health` 三家都要报（压测拿那份自述当准入条件，
缺一家就检查不全）。

### 43.6 教训

1. **"跑通了"和"测到了"是两件事。** 这次四个场景全绿、0 失败、
   0 次 `database is locked`，而它同时在跑错场景、吞掉汇总、
   把三倍的无效请求算进延迟里。**绿色的读数不构成"这次压测有效"的证据**——
   要单独问一句"它到底发了什么请求"。
2. **框架吞异常的地方，错误的表现形式是"一切正常"。** 埋在事件回调里、
   后台线程里、`except: pass` 后面的报错都属这一类。加打印前先确认
   那段打印真的执行了：一个只有标题的汇总不是排版问题。

---

## 问题 44：三个性能问题，同一个动作在其中一处快 38 倍、另一处慢 1.8 倍

**日期**：2026-09（阶段 10）

### 44.1 现象

问题 43 修完之后，压测才第一次报出可信的读数。而那几个读数里，
有三个端点明显不对（`data/loadtest.db`，48,384 行证据 / 233 份报告）：

| 端点 | 单线程实测 | 说明 |
|---|---|---|
| `GET /api/evidences` | 630 ms | 知识库列表 |
| `GET /api/evidences/facets` | 950 ms | 知识库页首屏的筛选面 |
| `GET /api/experts` | 1200 ms | 名册页，**侧边栏点一次就有一次** |

三个都不是"以后会慢"，是**当时就慢**；而且 `team_usage()` 的 docstring
早就写明了它的天花板在哪、该怎么修（"规模上去之后正确的做法是加一张
`report_team(report_id, expert_id)` 关联表"）。压测只是把那个天花板量了出来。

### 44.2 第一处：`/api/evidences` —— 一条 SQL 读整张表只为取 30 组

知识库列表是"一条来源一行"（去重），按被引用次数排序。原来是一条 SQL：

```sql
SELECT {_LIBRARY_COLUMNS} FROM evidences {where}
GROUP BY evidence_id
ORDER BY task_count DESC, credibility DESC, evidence_id LIMIT ? OFFSET ?
```

它能跑对，但 `_LIBRARY_COLUMNS` 里有 `MIN(full_text)` 和 `MIN(images)`——
于是 48,384 行的**全部正文**被读进来做了聚合，而最后只要 30 组。

改成两步：先只用三个窄列把 216 组排出来，再回表取那 30 组。

```
一条 SQL     338 ms / 535 ms（两次）
两步         187 ms / 331 ms
```

**代价是结果可能不一致，所以改写必须被守住。** 加了一条测试
（`test_两步改写与改写前那条SQL逐列一致`），拿改写前那条 SQL 当参照物，
在 14 组筛选/翻页参数下**逐行逐列**比对整行字典（只比 id 的话，
第二条查询取错列也发现不了）。

这条测试自己也证伪过一次：第一次的突变改坏了绑定参数的个数，
报的是 `sqlite3.ProgrammingError` ——**红得没意义**。换成语义合法的突变
（把第二步的 `WHERE evidence_id IN (...)` 去掉）之后，它在
`{'min_credibility': 53.0}` 这一组上正确地红了：那正是筛选条件会把
一个来源的若干次出现**切掉一部分**的情形，也就是两步之间唯一可能分歧的地方。

### 44.3 一个反直觉的结果：给 `evidence_id` 加索引会更慢

看到 `GROUP BY evidence_id` 没有索引，直觉是加一个。实测相反：

```
加索引前   357 ms   SCAN evidences + USE TEMP B-TREE FOR GROUP BY
加索引后   648 ms   SCAN evidences USING INDEX idx_evidences_eid
```

因为那条查询要的是**整行**（含 `full_text`）。走索引扫描之后，
每一行都得**回表随机读一次**，而顺序扫一遍是纯顺序 I/O。
省掉那个 GROUP BY 临时 B 树，远不值这些随机读。（那条实验索引已经删掉了。）

### 44.4 同一个动作在另一处快 38 倍

筛选面是同一张表上的另外两条查询。它们**本来就有索引**——
`idx_evidences_source(source_type)` 从 v1 就在。而查询计划也确实写着
`USING INDEX idx_evidences_source`，看上去一切正常。实测 303 ms。

原因：查询里除了分组列还要 `COUNT(DISTINCT evidence_id)`，
而索引里没有 `evidence_id`，**每扫一行都要回表取一次**。
把 `evidence_id` 补进索引（`(source_type, evidence_id)`）之后：

```
加索引前  SCAN evidences USING INDEX idx_evidences_source        | USE TEMP B-TREE ...
加索引后  SCAN evidences USING COVERING INDEX idx_evidences_source_eid | USE TEMP B-TREE ...
```

两行都是"走了索引"，差别只在**回不回表**：

| | 加索引前 | 加索引后 |
|---|---|---|
| `by_source_type` | 303 ms | **8 ms** |
| `by_brand` | 97 ms | **9 ms** |
| `total` | 20 ms | 9 ms |

**"有索引"不是一条性能结论，"索引里有查询要的全部列"才是。**
这也是 44.3 与 44.4 能同时成立的原因——同一个"加索引"的动作，
一次慢 1.8 倍、一次快 38 倍，区别只在查询要不要回表。

顺带确认了列表**没被拖慢**：`_library_rows` 第一步 151 ms → 151 ms，
查询计划仍是 `SCAN evidences`（它还要 `task_id` 和 `credibility`，
这两条索引里没有）。加了索引不等于所有查询都改走索引。

守住这一点的测试断言的是查询计划里的 `COVERING` 字样，
**不是索引在不在**：把 `evidence_id` 从索引里去掉，索引照样存在、
只查名字的测试照样绿，而性能悄悄退回 38 倍之前。证伪过——
去掉 `evidence_id` 后计划变成 `USING INDEX`，测试报
"筛选面没有走覆盖索引 idx_evidences_source_eid"。

### 44.5 第三处：`/api/experts` 每请求读 41 MB JSON

`/api/experts` 是 1.2 s，其中 `team_usage()` 占 950 ms，`load_experts()` 只占 0.3 ms。
`team_usage()` 数的是"每位专家被派进过多少次调研"，原来要
`SELECT data FROM reports` **把每份报告的正文整个读出来解析**：

```
data/loadtest.db   233 份报告 / 41 MB 正文   team_usage()  950 ms
data/xm3.db         16 份报告 / 3.4 MB        team_usage()   48 ms
```

它的 docstring 早就写好了解法，所以这次是**照着自己写下的结论做**：
加 `report_team(report_id, expert_id)` 关联表（迁移 v4），
`save()` 通过 `_sync_team()` 维护，`team_usage()` 变成一次 `GROUP BY`。

名册详情页还有一个更离谱的：`list_recent(limit=200)` 之后逐份读正文再过滤——
**为了回答"这个人参与过哪几次"，读了 200 份报告的全文**。
换成关联表上一次索引查询（`reports_of_expert()`），
并且那条查询**只取元数据、不取 `data`**。

实测（同一个库）：

| | 修前 | 修后 |
|---|---|---|
| `GET /api/experts` | 1200 ms | **6~8 ms** |
| `GET /api/experts/L3-001` | ~1200 ms + 读 233 份正文 | **5~10 ms** |

回填是这条迁移的另一半：只建表不回填，**已经存在的报告参与度会全部变成 0**——
界面照样打得开，数字只是安静地错了。233 份真实报告回填出 1398 个
(报告, 专家) 对，与另一段代码独立解析 JSON 数出来的结果**完全一致**。

### 44.6 最意外的一条：最慢的那个端点不只慢它自己

四个场景全部重跑（20 用户，read/stream/create 各 60 s，mixed 120 s）：

| 场景 | 请求数 | 失败 | req/s | 中位 | p95 | p99 |
|---|---|---|---|---|---|---|
| read | 1346 | 0 | 22.43 | 110 ms | 1200 ms | 1700 ms |
| stream | 1774 | 0 | 29.76 | 83 ms | 1000 ms | 1200 ms |
| create | 319 | 0 | 5.36 | 1500 ms | 12000 ms | 12000 ms |
| mixed | 1926 | 0 | 16.09 | 280 ms | 3000 ms | 4000 ms |

`database is locked`：**四个场景都是 0**。

而在修 44.5 之前，同样的 `read` 场景、同一个库，连跑三次是
**425 / 493 / 381 个请求，聚合 p95 7400 / 11000 / 13000 ms**。
改完之后 **1346 个请求、p95 1200 ms**。

也就是说 `/api/experts` 那 1.2 秒不只让它自己慢：它是**单进程 uvicorn 的
GIL 上堵着的那一块**，把队列里每一个请求都一起拖慢了。这是这次压测里
最有价值的一条——**修最慢的那个端点，收益不止在那个端点上**。

`stream` 场景是另一条独立结论：887 次订阅的"首事件" p95 是 **27 ms**，
而它们抢的是**同一批 3 个任务**。这是 `ensure_runner`（N 个订阅者共用一次
流水线执行）那句设计要的证据——如果它失效，首事件时延会塌成单用户的 N 倍。

### 44.7 教训

1. **压测的价值在于它会报出你没在找的那个数。** 这三处没有一处是
   "读代码读出来的"；三处的代码都跑得对，只是慢。
2. **同一个动作在不同查询上可以完全相反。** "给证据表加索引"，
   一次慢 1.8 倍、一次快 38 倍。所以性能结论只对**那条查询**成立，
   换一条就得重量——写进注释时必须带上限定条件。
3. **改写性能代码，先写等价性测试。** 快不是价值，"快且结果一个不差"才是。
   而那条测试本身也得证伪：第一次的突变红得没意义，换成语义合法的突变
   才真的验到了东西。
4. **最慢的那个端点可能不止拖慢自己。** 一个 1.2 秒的端点堵在事件循环上，
   会让整台服务的吞吐减半——**要看的不是单端点延迟，是它的延迟乘上它的频率**。

---

## 问题 45：为新代码写的第一版测试，一条是空的、一条会让应用起不来

**日期**：2026-09（阶段 10）

### 45.1 现象

问题 44.5 的修法是加 `report_team` 关联表。加表就要加两样东西：
回填（老报告也要有参与度）和等价性测试（回填的结果必须与原来从正文数的一致）。

两个新写的测试都绿了。绿得可疑的是**第一条只花了 0.15 秒**——
它要建一个库、插 9 行报告、跑一次迁移。于是去做了突变校验。

### 45.2 空的那一条：两边都是空字典，断言成立

等价性测试的语料我按 `_save()` 的入参形状写了九行：

```python
("RP-1", {"lead": ["L3-001"], "strategists": ["L2-001", "L2-002"]}),
("RP-2", {"lead": ["L3-001"], "executors": ["L1-001"]}),
...
```

而 `_save()` 那个形状是**被包进 `{"team": ...}` 之后才落库的**。
直接往表里塞 report 行时少了那一层，`$.team` 根本不存在。
于是：回填什么都没插，参照物那侧（`team_ids()`）按同样的错数据也算不出人，
**两边都是空字典，`assert got == want` 成立。**

九行语料里五行在验一个空集。它看起来是绿的。

抓出来的方式不是"读了一遍觉得不对"，是**突变校验**：
把 `INSERT OR IGNORE` 改成 `INSERT`。
那份语料里唯一会产生重复 `(report_id, expert_id)` 的行是 RP-4
（`{"lead": ["L3-001"], "strategists": ["L3-001"]}`，一人挂两角），
而 RP-4 正好是那五行之一。**改了应该红，结果绿。**

修法两条，缺一不可：

1. 语料全部改成落库后的正文形状（`{"team": {...}}`）。
2. 末尾加**反空洞下限**——先钉住这份语料确实产出若干个人和几个具体数字：

```python
assert want, "语料没有产出任何参与关系，这条等价性断言等于没测"
assert want == {"L3-001": 4, "L2-001": 1, ..., "7": 1, "9": 1}, f"语料本身的预期就不对：{want}"
assert got == want, f"回填与 team_ids 数出来的不一致：回填 {got}，参照 {want}"
```

改完重跑那三个突变，全部正确变红：

| 突变 | 结果 |
|---|---|
| `INSERT OR IGNORE` → `INSERT` | `IntegrityError: UNIQUE constraint failed` |
| `je2.type IN ('text','integer','real')` → `je2.type = 'text'` | 回填少了 `'7'` / `'9'` |
| `WHERE je1.type = 'array'` → `WHERE 1=1` | 回填多出 `'5'` |

第三条**在第一次跑的时候没有变红**，而修法是**加一行语料**而不是删一行断言：
`{"team": {"lead": ["L3-001"], "budget": 5, "note": "临时凑的"}}`。
原来的语料里，所有"成员不是数组"的形状（`"L3-001"`、`["L3-001"]`）
都在第二层被 `json_valid` 兜掉了，**根本走不到类型判断那一步**——
所以那一行过滤写不写测试都一样。加进 RP-10 之后它才是一个能被证伪的断言。

### 45.3 会让应用起不来的那一条：`json_each` 与 `json_type` 会解析它们的参数

回填是纯 SQL，用 `json_each` 把 `$.team` 拆开。第一版写的是：

```sql
  FROM (SELECT report_id, data FROM reports WHERE json_valid(data)) AS r
  JOIN json_each(r.data, '$.team') AS je1
  JOIN json_each(je1.value)        AS je2
 WHERE json_type(je1.value) = 'array'
   AND json_type(je2.value) IN ('text', 'integer', 'real');
```

跑起来直接失败：

```
sqlite3.OperationalError: malformed JSON
```

根因是 SQLite JSON 函数一个不明显的性质：

> `json_each(X)` 与**单参数的** `json_type(X)` 都会把 X **当成一份 JSON
> 文档去解析**。X 是一个普通字符串时，它们不返回"这不是 JSON"，
> 而是直接抛 `malformed JSON`。

而队伍里到处都是普通字符串：`"team": {"lead": "L3-001"}` 时 `je1.value`
就是 `L3-001`（`json_each` 给的是**解码后的值**），成员本身也是 `L3-001`。
于是 `json_type(je1.value)`、`json_type(je2.value)`、`json_each(je1.value)`
三处全是地雷。

**这件事的严重性不在迁移失败，在于它是启动时跑的**——
一份形状不对的老报告能让整个应用起不来。

第三处尤其阴：`json_type(je2.value)` 在**最常见的形状**下就会炸。
`{"team": {"lead": ["L3-001"]}}` 的成员是字符串 `L3-001`，
`json_type('L3-001')` 就是 `malformed JSON`。也就是说那条 `IN ('text', ...)`
写在最正常的输入上根本执行不到。

修法三条，缺一不可：

1. `WHERE json_valid(data)` 放在**子查询**里——放外层 WHERE 不保证先于 JOIN 求值。
2. 第二层的参数用 `CASE WHEN json_valid(je1.value) THEN je1.value ELSE '[]' END` 兜住：
   参数本身永远得是合法 JSON。
3. **判类型一律用 `json_each` 虚拟表自带的 `.type` 列**，不用 `json_type()`。
   `.type` 是列，取它不会重新解析，给什么都不会抛。

```sql
 WHERE je1.type = 'array'
   AND je2.type IN ('text', 'integer', 'real');
```

### 45.4 教训

1. **一条断言能通过，不代表它验到了东西。** 反空洞下限不是形式主义：
   `assert got == want` 在 `got == want == {}` 时同样成立，
   而"两边都是空"正是这条用例第一版的样子。
   **突变校验是唯一能发现这件事的手段**——前提是突变要选在
   "语料里真的存在的那种形状"上。第三条突变第一次没红，
   原因就是语料里缺那个形状。
2. **一次突变没红，有两种可能：断言是空的，或者语料是空的。**
   两者的修法相反——前者要改断言，后者要加语料。要分辨清楚再动手。
3. **框架函数的"友好失败"设计会骗人。** `json_each` 接受"文档或路径"两种入参，
   于是收到一个普通字符串时它不会说"这不是 JSON"，而是按文档去解析然后炸。
   用这类"能同时接受两种东西"的 API，**参数必须由调用方保证类型**，
   不能指望它自己判断。

---

## 问题 46：`d3-force` 被 import 了，而 `package.json` 里没有它——五种检查全绿

**日期**：2026-09（阶段 10）

### 46.1 现象

写 `技术栈.md` 要逐条交代依赖。写到前端的图谱那一行时，先把依赖表对了一遍：

```
$ grep -rn "d3" src/ --include=*.ts --include=*.tsx | grep import
src/lib/graph.ts:53:import { forceCenter, forceCollide, forceLink, forceManyBody, forceSimulation } from 'd3-force'
```

整个 `src/` 里只有**这一处** d3 相关的 import，用的是 `d3-force`。
而 `package.json` 的 `dependencies` 里写的是 `"d3": "^7.9.0"`——**元包**，
不是被引用的那个。

`d3-force` 在 `package.json` 里**一次都没出现过**。

### 46.2 定位过程

第一反应是"那它怎么跑起来的"。查了一下：

```
$ ls node_modules/d3-force/package.json   → 在
$ npm view d3@7.9.0 dependencies          → 30 个 d3-* 子包，其中就有 d3-force
```

`d3@7` 是一个元包，依赖 30 个子包（`d3-array` / `d3-scale` / `d3-force` …），
而 npm 默认把依赖**提升**到 `node_modules` 顶层。
于是 `import ... from 'd3-force'` 能找到——**因为另一个包的依赖树把它带了进来。**

接着确认它没被任何检查拦住。把 `import` 改成 `'d3-force-undeclared'` 反而
立刻炸（找不到模块），说明**能被解析不等于被声明**。而在原样下：

| 检查 | 结果 |
|---|---|
| `tsc -b --noEmit` | 过（`node_modules/d3-force` 确实在） |
| `oxlint src --deny-warnings` | 过 |
| `vitest run`（312 条） | 过 |
| `vite build` | 过（704 modules transformed） |
| CI 五个 job | 全绿 |

**没有任何一层会报"这个包你没声明"。** 它不是一个能被现有工具看见的缺陷类型。

### 46.3 根因

`package.json` 里声明的是"我用的东西的**容器**"，而不是"我用的东西"。

这个错误有一个具体的诱因：装依赖时想的是"我要画力图 → 装 d3"。
`d3` 是那个名字更好记、更好搜的包，而真正需要的是它的一个子包。
元包让依赖表看起来更整洁（一行，而不是先查清楚要哪几个子包），
**同时把真实的依赖关系藏起来了**。

真正让这件事成为缺陷的，是它**只在特定条件下才会断**，而断的时候报的是
"找不到模块"这种看起来像手滑的错：

1. 换 pnpm，或 `npm ci --install-strategy=nested`（不做提升）→ 立刻断；
2. `d3@8` 改了依赖列表，不再包含 `d3-force` → 某次 `npm update` 之后断；
3. **有人把 `d3` 删掉** → 断。

第 3 条不是假设：**本次要修的就是它。**（见下）

### 46.4 修复

修的方向有两个：把 `d3-force` 补上声明，或者把 import 改成 `from 'd3'`。
选前者，并且**顺手把 `d3` 与 `@types/d3` 删掉**：

```
$ npm install d3-force@^3.0.0
$ npm install -D @types/d3-force
$ npm uninstall d3 @types/d3
```

依赖表与"依赖保持最小"这条原则一致：**实际用到的只有 5 个函数**，
没有理由为了它们装 30 个子包。

| | 修复前 | 修复后 |
|---|---|---|
| `package.json` 里 d3 相关 | `d3` + `@types/d3` | `d3-force` + `@types/d3-force` |
| `node_modules` 里的 d3\* 包 | **31 个**（元包 + 30 个子包） | **4 个**（`d3-force` + 它的 3 个依赖 `d3-dispatch` / `d3-quadtree` / `d3-timer`） |

有意思的是：**第 3 条失效路径正是我这次要做的事。**
如果先删 `d3` 再发现这件事，症状会是"构建突然找不到 `d3-force`"，
而那时最自然的反应是"把 d3 装回来"——**把缺陷恢复原状，并且以为修好了**。

### 46.5 加一道闸门

改完是绿的了，但"绿"和上一次没有区别：**没有任何东西拦得住它再发生。**
所以加一条测试，放在已有的 `src/test/moduleGraph.test.ts` 里——
那个文件守的是"模块图里的边"，而这是一个**同一形状**的缺陷：
*图里有一条边，而该知道它的地方不知道*。上一次是"文件在、没人引"（问题 41），
这一次是"包在用、没声明"。

判据：扫 `src/` 全部源码，取出**裸**说明符，取包名
（`@scope/name` 取前两段，其余取第一段），断言它在四个依赖字段里出现过。

两个刻意的设计：

- **不做版本检查。** 那要一套 semver 实现，而这条要防的是"压根没声明"，
  不是"声明的范围松了"。
- **注释必须先剥掉。** 不剥的话，写一句 `// import { c } from '没声明的包'`
  就能让它变绿——和 `moduleGraph.test.ts` 第一条 `it` 里
  "注释不算引用"是同一条原则。

**先做了突变校验，再确认它是绿的**：

| 突变 | 结果 |
|---|---|
| 把 `graph.ts` 的 import 改成 `'d3-force-undeclared'` | **正确变红**，只红 1 条，另 2 条照绿，报错点名 `d3-force-undeclared` |

另 2 条照绿这一点也是要看的：它证明这个扫描器**没有宽到把什么都说成未声明**。
另外还有一条自证 `it`，喂进去已声明 / 未声明 / 相对路径 / `import.meta.glob` /
注释里五种形态，断言只交出裸的那几个。

写这段扫描器时踩到的一个细节：`import.meta.glob('/src/**/*.ts', { query: '?raw' })`
**同时含 `import` 和含引号的参数**，是一条现成的假阳性来源。
四条正则各靠一处避开它——`(?!\.meta)`、不允许 import 与 `from` 之间出现引号、
动态 import 要求 `import` 紧跟 `(`。它既在测试里被断言，也在这次的源码里真实存在。

### 46.6 验证

| 检查 | 结果 |
|---|---|
| `npm run typecheck` | 过 |
| `npm run lint` | 过 |
| `npm test` | **312 条全过**（+2 条新用例 = 314） |
| `npm run build` | 过，704 modules，`index-ChWRUziJ.js` 438 kB |
| 新测试的突变校验 | 正确变红，指名未声明的包 |

### 46.7 教训

1. **一个能跑的 import，不等于一个被声明的依赖。** 编译器、linter、
   测试、打包器、CI 全都会放过它——它们的共同假设是"能解析就是对的"。
   而这五层里没有一层问过"这是从哪儿来的"。
2. **"删掉那个恰好把它带进来的包"是唯一会立刻暴露它的动作**，
   而它同时也是最容易被误诊的动作——症状看起来像自己删错了东西。
   发现顺序如果是反的，这件事会变成一个"莫名其妙修好又复发"的幽灵。
3. **依赖要声明"我用的那个"，不是"我用的那个所属的大包"。**
   元包让依赖表显得整洁，代价是把真实依赖藏起来。整洁在这里是负资产——
   `package.json` 不是给人读的目录，是给工具读的声明。
4. **写文档时逐条核对依赖表，是有产出的。** 这条缺陷不是靠测试、
   也不是靠 code review 发现的，是靠"给每一行依赖写一句为什么"发现的——
   写不出来就得去查，一查就发现有一行其实没人在用，而真正在用的那行不在。

---

## 问题 47：`--live` 从来没跑通过——它不花钱是因为它连第一个请求都没发出去

**日期**：2026-09（阶段 10）

### 47.1 现象

写 [`面试问答集.md`](面试问答集.md) 时要把「引用忠实度」填进技术统计表。
这个数只有 `eval --live` 加 `--judge` 跑得出来，所以先按计划里写的
跑一次 5 条子集：

```
$ python -m eval.run_eval --live --repeat 3 --judge \
    --only dewu-vs-shihuo,taobao-vs-pinduoduo,weread-vs-zhangyue,\
notion-vs-obsidian,mihoyo-vs-netease-games
真跑：5 条 · provider=deepseek · 临时库
==============================================================================
[ 1/15] notion-vs-obsidian · quick #1      失败："未注册的 搜索 provider：'deepseek'。可用：bocha, mock, tavily"
[ 2/15] notion-vs-obsidian · quick #2      失败："未注册的 搜索 provider：'deepseek'。可用：bocha, mock, tavily"
...
[15/15] mihoyo-vs-netease-games · quick #3 失败："未注册的 搜索 provider：'deepseek'。可用：bocha, mock, tavily"

一条都没跑成。
```

**15 次运行、一条都没跑成、退出码 0、总花费 $0。**

### 47.2 定位过程

表头那一行已经把线索印出来了，只是当时没读出来：

```
真跑：5 条 · provider=deepseek · 临时库
```

`provider=` 后面是 `_runtime_providers()` 的结果——它**从 Settings 现读**，
不是 argparse 的缺省值。三家同名时它只印一次（`dict.fromkeys` 去重）。
所以这行字的意思是：

> **LLM、搜索、抓取，三家 provider 全都被设成了 `deepseek`。**

而搜索不是 "deepseek 家不支持"，是**根本没有这个名字**。注册表里只有
`bocha / mock / tavily`。往下追到 `main()`：

```python
else:                          # ← --live 走这一条
    _set_provider_env(args.provider)

def _set_provider_env(provider: str) -> None:
    """三家 provider 一起定。只定 LLM 不定 search 是个隐蔽的坑：
    想跑 mock 却只覆盖了 LLM，搜索还会去打真 API——评测号称零成本，实际在花钱。"""
    for key in ("LLM_PROVIDER", "SEARCH_PROVIDER", "FETCH_PROVIDER"):
        os.environ[key] = provider
```

`args.provider` 的缺省值是 `"deepseek" if args.kind == "live" else "mock"`。
于是 `--live` 把 `deepseek` 填进了三个格子，其中两个格子不接受它。

### 47.3 根因

**一个标量被用来代表三个不同的角色，而它只在其中一个取值上成立。**

`--provider` 的取值里，**恰好只有 `mock` 同时是三家的合法名字**：
mock 有 LLM 版、搜索版、抓取版，所以"三家一起填"在 `--gate` 下是对的、
也是被真实跑过的（CI 每次都在跑）。而 `--live` 用的 `deepseek`
是 LLM 专有的名字。

这件事能活到阶段 10，是三件事叠在一起的结果：

1. **`--gate` 一直在跑，所以那个抽象看起来是被验证过的。**
   它验证的其实是"当值等于 mock 时这个抽象成立"——一个只在退化情形下
   成立的抽象，与"这个抽象成立"是两回事。
2. **`--live` 在计划里被排到了最后**（"写文档前刻意跑一次"），
   在它之前没有任何东西依赖它，于是它从来没有被执行过一次。
3. **失败是响亮的，但当时没人在跑它。**
   这一点值得说清楚：这个缺陷**没有**伪装成正常——它把 15 次失败
   一条条印了出来。它之所以长期存在，纯粹是因为最后一个阶段之前
   没有人需要它。**响亮地失败，也仍然可以被搁置十个月。**

还有一处附带发现：`_set_provider_env` 的 docstring 本身是对的——
"只定 LLM 不定 search 会让评测号称零成本却在花钱"确实是个真问题。
**注释写对了一件事，不代表那段代码在另一种参数下也是对的**；
那段 docstring 描述的是 `--gate` 的处境，却被写成了对函数的普遍陈述。

### 47.4 修复

不去加一家叫 `deepseek` 的搜索适配器——那没有意义，`--provider` 的语义
本来就该是"LLM 的快捷方式"。改的是把这个语义写清楚，外加一个特例：

```python
#: `--provider` 的取值里，只有这一个代表"整条链路都用假实现"。
_ALL_MOCK = "mock"

def _set_provider_env(provider: str) -> None:
    os.environ["LLM_PROVIDER"] = provider
    if provider == _ALL_MOCK:          # ← 只有 mock 是"三家通用"的特例
        os.environ["SEARCH_PROVIDER"] = provider
        os.environ["FETCH_PROVIDER"] = provider
```

`--live` 于是不再覆盖 search / fetch，让它们按 `.env` 来（`bocha`）。

顺带把 `--live` 的另一处收口：`CASSETTE_MODE` 显式钉成 `off`。
`.env` 里现在是 `off`，但若有人留着 `replay`，这次运行会**一边查表
一边把结果标成"真跑"**——表头、写出的 JSON、最终进文档的数字会一起错。
在"花了钱"这条路径上，宁可在这里钉死。

### 47.5 修复后的验证

```
$ python -m eval.run_eval --live --only notion-vs-obsidian --repeat 1
真跑：1 条 · provider=bocha/deepseek · 临时库
...
跑了 1 条 · 总成本 $0.0232 · 档位 quick
闸门通过：1 条，12 项契约全绿。
```

表头从 `provider=deepseek` 变成了 `provider=bocha/deepseek`——
**同一个去重逻辑，如实报出了两个不同的值。**

回归测试 [`backend/tests/unit/test_eval_provider_env.py`](backend/tests/unit/test_eval_provider_env.py)
（6 条）。它问的不是"deepseek 在不在搜索表里"，而是**注册表**：
"你填进某个角色的名字，在那个角色自己的注册表里存在吗？"

写成查注册表而不是查一张手抄名单，是因为手抄名单的两个结局都不好：
将来真加一个叫 `deepseek` 的搜索源，它会开始**阻止一件正确的事**；
而漏抄一个新增的角色，它就**悄悄少检查一块**。

**突变校验**（把 `_set_provider_env` 改回修复前的写法）：

```
$ python -m pytest tests/unit/test_eval_provider_env.py
3 failed, 3 passed

E  AssertionError: --provider deepseek 让某个角色拿到了它不认识的名字：
   ["SEARCH_PROVIDER=deepseek（search 的可用值：['bocha', 'mock', 'tavily']）",
    "FETCH_PROVIDER=deepseek（fetch 的可用值：['http', 'mock']）"]
```

正确变红，并且**指名了是哪两个角色、可用值是哪些**。

### 47.6 教训

1. **一个抽象可能只在它退化情形上成立，而那个情形恰好是被测过的那个。**
   `--provider` 的"三家共用一个值"在 `mock` 下成立、被 CI 天天跑；
   在 `deepseek` 下不成立、一次都没跑过。**"这条路径被测过"必须带上
   "用哪个参数测的"**——否则覆盖率是会骗人的。
2. **"最后再跑一次"的东西，就是永远不会跑的东西。**
   计划把 `--live` 排到阶段 10，于是它从阶段 3 写好到阶段 10 之前
   没有被执行过一次。**一个从未被执行过的入口，与一个不存在的入口
   在可靠性上没有区别。**
3. **响亮的失败并不会自动被看见。** 这个缺陷的表现是最好的那种——
   它把 15 次失败逐条印出来，还写了"一条都没跑成"。
   但它仍然活到了最后，因为**没有人在那个时候需要它工作**。
   "报错很明确"降低的是**发现之后的**成本，不是发现本身的概率。
4. **注释写对了一件事，不等于那段代码在另一种参数下也对。**
   `_set_provider_env` 的 docstring 讲的坑是真的，但它描述的是
   `--gate` 的处境，却写成了对函数的普遍陈述。
   **把"在某一条调用路径上成立"写成普遍陈述，是注释最容易被接受的错法**——
   因为它读起来像是在讲道理。

---

## 问题 48：同一份"规模"表在三个文档里有三个版本，而没有任何工具会发现

**日期**：2026-09（阶段 10）

### 48.1 现象

写 [`项目总结.md`](项目总结.md) 的技术统计表时，要求每个数字都是**实测**的，
于是逐目录重新数了一遍。数完和另外两处已有的表对了一下：

| 位置 | 前端 `src/` | 后端 `tests/` | 报的是什么 |
|---|---|---|---|
| `README.md` 的「规模」 | 20,072 | 13,625 | 物理行（括号里是"代码行"） |
| `技术栈.md` 的分层图 | 20,072 | — | 物理行（括号里是"代码行"） |
| `项目总结.md` 的技术统计表 | **20,233** | **13,827** | 物理行 + 注释行 + 文件数 |

**同一个仓库、同一份代码、三张表、两个版本。**

### 48.2 定位过程

先要回答的是"哪个是对的"。查下来结论是：**两个都不是错的。**

- `README.md` / `技术栈.md` 的那组数是**文档阶段开始时**数的；
- `项目总结.md` 的那组是**写完四份文档之后**重数的，其间
  `frontend/src/test/moduleGraph.test.ts` 涨了一截（问题 46 加的扫描器），
  后端也加了用例。

所以差的那 161 行是**真实的代码变动**，不是笔误。问题不在数字算错了，
在于**它们各自都是"当时正确"，却同时挂在仓库里，且都读作"现在"**。

顺着这条往下发现了第二层，这一层才是真正的缺陷：

```
$ grep -rn "46 条" README.md 项目总结.md 开发文档.md 技术栈.md
技术栈.md:173:  所以名册有 46 条测试钉住结构（恰好 48 人、层级分布、id 格式、生成幂等）。
开发文档.md:411: 名册由 **46 条用例**守护：恰好 48 人……
README.md:164:   | [问题记录.md](./问题记录.md) | 46 条按「现象→定位→根因→修复→验证」…… |
项目总结.md:180: | 问题记录 | **46 条** |
```

**`46` 在这里指两件完全不同的事**：一处是"名册测试有 46 条用例"，
另一处是"问题记录有 46 条"。它们恰好相等，纯属巧合。
这意味着**将来任何一次"把 46 改成 47"，如果只改后者，两者的碰撞就会
让 grep 出来的一堆 46 里混着不该改的**——而改错的那一条（名册测试数）
不会有任何东西报错，它只是从此变成了一个假的数。

（本次就撞上了：加完问题 47 之后，`46 条` 在两个语义下需要分别变成
`47 条` 和保持 `46 条`。区分它们只能靠逐条读上下文。）

### 48.3 根因

**同一个事实被写在多个地方，而没有一条规则说哪个是源。**

这跟这份 README 曾经链向五个不存在的文件（[`test_docs_links.py`](backend/tests/unit/test_docs_links.py) 守的那件事）
是同一个结构，只是那次是**链接**重复，这次是**数字**重复。

而数字这一类比链接更难发现：

- 链接坏了，点一下就知道；
- **数字没有任何形态上的异常。** `20,072` 和 `20,233` 都长得像真的，
  读的人无从判断哪个是当前值，只能信自己先看到的那个——
  而"先看到哪个"取决于他先打开哪份文档。

顺带一提，这一条之所以在**写文档时**而不是在**跑测试时**暴露：
这个仓库里没有任何检查会读中文散文里的数字，
而三张表全是 markdown 表格里的中文散文。

### 48.4 修复

**不是"把三处改成一样"，那是把问题推迟到下一次。** 改的是让事实只有一处：

1. **`项目总结.md` 的技术统计表是唯一的源。** 逐目录的行数、注释行数、
   文件数，只在那里出现。
2. **`README.md` 只留两个量级数**（后端合计、前端合计），
   并明确写出"逐目录的数只在 `项目总结.md` 一处"。
3. **`技术栈.md` 的分层图不再写行数。** 那张图的职责是画清层与依赖方向；
   行数是另一样东西，摆在图里就是第二个版本。删掉时把理由留在了原位。
4. **"46 条"的两个语义各自写清楚。** 改动时逐条读上下文，
   不按 `grep` 的结果批量替换。

### 48.5 验证

```
$ grep -rn "46 条\|47 条" README.md 项目总结.md 开发文档.md 技术栈.md
技术栈.md:173:  名册有 46 条测试钉住结构      ← 名册测试，保持 46
开发文档.md:411: 名册由 46 条用例守护          ← 名册测试，保持 46
开发文档.md:460: test_experts.py      # 46 条  ← 名册测试，保持 46
开发文档.md:467: 46 条全绿                    ← 名册测试，保持 46
README.md:164:   47 条按「现象→定位→根因→修复→验证」  ← 问题记录，改 47
项目总结.md:180: 问题记录 **47 条**            ← 问题记录，改 47
开发文档.md:9:   （47 条）                    ← 问题记录，改 47
开发文档.md:1010: 47 条真实踩坑               ← 问题记录，改 47
```

四个改、四个不改，**全部靠读上下文判断**——这正好说明了为什么
"把 46 改成 47"不能是一次 `sed`。

### 48.6 教训

1. **一个数字写在两个地方，就会有两个版本。** 这不是"要记得同步"，
   而是**结构上必然会分叉**：每次代码一变，两处就各自欠一次更新，
   而没有任何机制会提醒。唯一可靠的做法是让它只出现一次。
2. **过期的数字不会报错，也不会看起来可疑。** `20,072` 与 `20,233`
   都是"看起来很精确"的数字——**精确感与正确性无关**，
   一个四位精度的过期值和它的正确值在形态上完全一样。
3. **数字重复是链接重复的近亲，而且更难查。** 链接检查器能自动扫，
   而"两份中文散文里的同一个数应该相等"这件事，
   要写成检查就得让它去读散文——那正是本项目里反复出现的那句话：
   **能被工具抓住的缺陷会被抓住，抓不住的只能靠人去比对。**
   所以这里的修法不是加检查，是**减少需要比对的东西**。
4. **写文档时逐条核对数字，和逐条核对依赖表一样，是有产出的**
   （问题 46 的教训 4 是同一条）。两次都是"为了写清楚某一段，
   不得不去查一个本来不会去查的事实"。

### 48.7 补记：这个数后来**又漂了三次**，然后才上了守卫

上面教训 3 的结论是"减少需要比对的东西"，而不是加检查。
那个结论只在"**需要人来判断语义**"时成立——`46 条名册测试` 与
`47 条问题记录` 长得一样，只有读上下文才知道哪个该改。**那种数确实守不住。**

但**测试条数不一样**：它有一个机器可读的真相源
（`pytest --collect-only`），所以它守得住。它漂过的每一轮都在下面这张表里：

| 时间点 | 后端条数 | 四份文档是否同步 |
|---|---|---|
| 写 §48 时 | 917 | 是 |
| 加 4 条 lint 测试（问题 50） | 921 | **否**——漂了整整一轮才发现 |
| 此后每加一组守卫（本条的 3 条 + 问题 52、53 各 1 条） | 924 → 925 → **926** | **三次都是先红后绿**，红的是它自己引入的差 |
| 问题 54 那批守卫（17 条）与两次规模数复核 | 939 → 941 → **943** | **又是三次先红后绿**（守卫自己让总数变了两次，另一次是规模表那两条） |

后两行本来是六行，各自合成一行是因为**它们是同一件事发生了三次、又发生了三次**：
每加一组守卫，那组守卫第一次跑都是红的，而红的原因是**它自己让总数多了一条**——
它一跑就报"文档写着 924，实测 925"。如果它当时是绿的，那说明它问的不是真问题。
三次形状完全一样：**守卫的价值由它第一次跑的那一下证明，
而那一下必然是红的。** 一条新守卫第一次就是绿的，值得怀疑：
它多半在数一个不会变的东西。

（问题 54 那三次里有一次**红在守卫自己身上**：新加的规模表守卫第一次跑是红的，
原因是它的模式用了 `^` 而我没给 `re.finditer` 加 `re.MULTILINE`——
它一行都没匹配到。这与上面那三次不同：红的是**仪器**，不是数据。
**它同样是有价值的红**——如果当时它是绿的，我会以为那五条模式真的锚住了行首。）

所以修法是
[`backend/tests/unit/test_docs_counts.py`](backend/tests/unit/test_docs_counts.py)：
逐文件、逐句断言那四份文档里写了条数的那句话等于实测值。
**它故意不覆盖 [`问题记录.md`](问题记录.md)**——这篇文档里的 `917` 是历史数，
它当时就是对的，一个全文搜索的守卫会把正确的历史判成错误，
然后被人加一行豁免绕过去，**那时候这个守卫就什么都不守了**。

它的能力边界写在那个文件的模块 docstring 里，也写在这里：
**它能抓到"改了 README 忘了改技术栈"，抓不到"改了代码、四份文档全忘了改"。**
后者只有真跑一次 `npm test` 才知道——前端那 314 条因此在守卫里
只做"四份文档彼此一致"的检查。**把边界写下来，比把它糊过去有用。**

---

## 问题 49：判官少吐一个右括号，5 份报告被判成「可用性 0.00/5」

**日期**：2026-09（阶段 10）

### 49.1 现象

`eval/run_eval.py --live --judge` 跑完 15 份报告（5 个查询 × repeat 3），
12 项契约全绿、闸门通过。判官那一段长这样：

```
判官指标（15 份 · core 档 · 温度 0.0 · 不参与闸门）
  notion-vs-obsidian               忠实度 0.96（判 12/12 条） · 可用性 0.00/5
  notion-vs-obsidian               忠实度 1.00（判 12/12 条） · 可用性 0.00/5
  notion-vs-obsidian               忠实度 0.92（判 12/12 条） · 可用性 0.00/5
  taobao-vs-pinduoduo              忠实度 1.00（判 12/12 条） · 可用性 0.00/5
  taobao-vs-pinduoduo              忠实度 1.00（判 12/12 条） · 可用性 0.00/5
  …（其余 10 份在 3.00–3.40 之间）
```

5 份 `0.00/5`，而且**恰好是正文最长、最有的可说的那几篇**：
一个查询的 3 次 repeat 全军覆没，另外 2 份来自另一个查询。

值得记一笔的是它为什么能安静地活到汇总表里：`--judge` **不参与闸门**
（这是有意的，见 `docs/EVAL.md`），所以那 12 项契约该绿还是绿。
**闸门绿与这个数是不是真的，是两件事。**

### 49.2 定位过程

#### 第一次假设，错了

第一反应是"800 的 `max_tokens` 截断了输出"，而且当时的证据看着很支持它：

- 失败的恰好是最长的那几篇（长报告 → 更长的判词 → 更容易超）；
- 5 个维度各要一句 reason，实测每句 90–150 字，`5 × 150 = 750` 个汉字
  加上 JSON 骨架确实贴着 800。

于是把 `max_tokens` 从 800 提到 2000，**重跑，5 份照样失败**。

假设被证伪。但真正的代价不是多花了那点钱，是**这个假设已经被写进
`rubric.py` 的注释里了**，语气是"证据："——如果当时不去重跑，
它会以"事实"的身份留在代码里，被下一个读这段的人当结论用。
（这条教训在第 6 节。）

#### 捞原文

失败路径那时只返回一个空列表，什么也没留下。加上 `raw` 字段
（`finish_reason` + 字数 + 原文前 1200 字）之后重跑，捞到的第一条是：

```
finish_reason=stop len=874
```

**`stop`**——模型认为**自己答完了**，不是被砍断的。
那 `len=874` 的一整段 JSON，为什么 `json.loads` 会抛错？

数括号：

```
花括号计数: { = 6  } = 5
补一个 } 之后 loads OK，维度: {'structure': 4, 'argument': 4, 'actionable': 2,
                              'differentiation': 4, 'honesty': 5}
```

五个维度对象**都闭合了**，只有**根对象的右括号没吐出来**。
一次采样少一个字符。

#### 但同一个 body 有三种形态

补丁写完之后，把三次探针里捕获到的失败原文都拿出来过了一遍，
结果比"少一个右括号"复杂：

| 探针 | 结束原因 | 长度 | `{`/`}` | 形态 | 补括号能救吗 |
|---|---|---|---|---|---|
| probe1 | （无记录） | — | 3/1 | 停在字符串中间（`…CL-51d8b1c09d Notion A`） | 不能 |
| probe2 | `stop` | 731 | 3/2 | 停在 `"actionable": ` 后面，句子断在半截 | 不能 |
| probe3 | `stop` | 874 | 6/5 | 答完了，根对象少一个右括号 | **能** |

三份全部来自 `notion-vs-obsidian`，而且**这一份报告累计问了 6 次、
6 次都没读出来**——不是采样噪声，是这份 body 稳定地把判官逼成废输出。

### 49.3 根因

两层，各自独立，**都要修**。

**第一层在解析（数据被丢了）。** `_parse` 用

```python
raw.find("{"), raw.rfind("}")          # 切出 raw[start:end+1]
```

找结尾。`rfind` 找的是**最后一个**右括号，不是**配对的那一个**。
少一个字符，切出来的片段就少一层收口，`json.loads` 抛错，
函数返回空列表——**一整段答对的判词被整段丢掉**。

**第二层在报告（丢掉的被显示成 0 分）。**

```python
class RubricResult:
    @property
    def mean(self) -> float:
        return sum(...) / len(self.scores) if self.scores else 0.0
```

`0.0` 这个默认值**在对象层面是对的**（"没有分数"的均值不该是别的），
**在报告层面是灾难**：`0.00/5` 与"裁判真的打了最低分"在数字上
完全一样。聚合时又把它们当 0 一起平均，得到
`可用性均分 2.08/5`——一个既不是质量（它偏低）、也不是失败率
（它没说是失败）的**不存在的量**。

**这两层是同一个东西的两面**：一个"没测出来"被显示成了"测出来是 0"。

顺带核对了 [`faithfulness.py`](eval/judges/faithfulness.py) 的同类解析：
它的 `_parse` 在解析失败时返回 **`部分支持`** 而不是 `不支持`，
注释里写着理由（"裁判没答上来不等于证据不支持论点"）。
**同一个伤害在那里落在安全方向上**——所以忠实度那 15 个数
（均值 0.971）不受这个问题影响。这也说明这个坑当时是**被想到过的**，
只是没想到它会以"少一个括号"的形态出现在另一个裁判身上。

### 49.4 修复

五处，按"数据 → 仪器 → 报告"的顺序：

**1. 补括号（`_close_unbalanced`）。** 扫描一遍，把**没闭合**的括号补上。
边界写死了三条：

- **只补不猜。** 一个字都不改、一个维度都不编，唯一做的事是把写完的内容收口；
- **字符串里的花括号不算结构**（`reason` 里写一句 `用 {a,b} 表示` 不会算错层数）；
- **对不上不猜、超过 3 层不补**——缺 4 个以上说明坏的不止是收尾，
  这时"补好再读"更可能读出一份**看着没问题**的错分数。

**2. 有界重试（`_MAX_ATTEMPTS = 2`）。** 三种形态里只有一种能靠补括号救，
probe1/probe2 那种"没答完就停"的，补什么都得靠编——**修法是再问一次**。
上限 2 是因为：重试要钱，而这里要的是一个**能报出去的失败率**，
不是把失败藏到零。

**3. 把"这个分是怎么来的"变成字段。** `RubricResult` 多了两个：

- `repaired`：补了括号才读出来的；
- `attempts`：问了几次才拿到能解析的输出。

理由很直接：**补出来的 4 分和一次答对的 4 分，在数字上完全一样，
可信度不是一回事。**

**4. 失败时留下判据。** 原文带上 `finish_reason`、字数、`{`/`}` 计数、
以及**每次尝试各一条**（因为同一个 body 的失败形态并不稳定）：

```
[finish_reason=length len=42 braces=2/0] {"structure": {"score": 4, "reason": "砍在这里 | …
```

**5. 聚合排除未解析的，并把失败率印出来。**

```
可用性均分：3.80/5 · 离散 0.00  （解析成功 1/1 份）
判官输出要修才读得出来：补括号 1/1 份 · 重问 0/1 份
  这两项**不是**质量问题，是判官这个测量仪器自身的可靠性。
```

### 49.5 验证

**同一份报告，修复前后**（这是这次修复的全部目的）：

| | 修复前 | 修复后 |
|---|---|---|
| `notion-vs-obsidian` 的可用性 | `0.00/5` | **`3.80/5`** |

**逐字捕获的原文进了测试。** `backend/tests/unit/test_eval_rubric_parse.py`
里 `_CAPTURED` 是那 874 字**原样**（含模型自己写的中文引号与省略号），
断言的是"这条真实输入现在读不读得出分数"，不是"补括号函数写得对不对"。

**自我证伪。** 有一条用例把**修复前的三行**原样写出来，断言它必须抛
`JSONDecodeError`——否则这份 fixture 量的就不是它声称的那个缺陷：

```python
raw = _CAPTURED.strip()
start, end = raw.find("{"), raw.rfind("}")
with pytest.raises(json.JSONDecodeError):
    json.loads(raw[start : end + 1])
```

14 条用例全绿，覆盖：还原 5 个维度、补完的文本**等于原文加一个 `}`**
（证明"只补不猜"）、理由没被改写、截在字符串中间不补、括号对不上不猜、
缺太深不补、正常输出不受影响、理由里带花括号不被打乱、
`as_dict` 标出 `repairedBraces`、重试后成功记 `attempts=2`、
补括号成功就不重试、两次都失败时两次原文都留着。

### 49.6 教训

1. **"0 分"和"没测出来"必须长得不一样。** 这是本项目里反复出现的同一件事
   （问题 47 是"绿不等于跑过"，这条是"0 不等于差"）。
   一个默认值 `0.0` 在对象层面完全正确，到了报告层面就成了一个
   **看起来像测量结果的缺省值**——而它的形态和真测量结果一模一样。
2. **我的第一个假设很合理、很符合证据，而且是错的。** 更值得记的是它的
   下半段：那个假设**已经以"证据："的语气写进代码注释了**。
   不去重跑，它就会以事实的身份留下来。
   **假设写进注释之前要先被证伪一次**，否则注释就是下一个人的路标，
   而它指向错的方向。
3. **"找到了一个原因"不等于"找到了原因"。** 补括号修好了 probe3，
   而同一个 body 还有另外两种形态。**如果一个坏输入只有一条证据，
   就只能声称修了那一条。** 这次是因为把三次探针的原文都翻出来比对了，
   才没停在"少一个右括号"上。
4. **同一个伤害落在哪个方向，取决于代码怎么写默认值。** 忠实度判官的
   同类失败记成"部分支持"（中间值），所以那 15 个数能用；
   可用性判官记成 0（地板），所以这 5 个数不能用。
   **默认值不是风格问题，它决定了坏数据往哪个方向污染结果。**
5. **测量仪器本身也要能被质疑。** 判官算不算数，不只取决于它测得准不准，
   还取决于**它自己有多可靠**——所以 `repaired` / `attempts` / 失败率
   都得报出来。一个"问两次才有的分"和一个"一次就有的分"混在一张表里，
   表就不可信了。
6. **修报告层和修解析层是两件事，而且修前者不能代替后者。**
   只把 `0.00/5` 改成 `—`，数据还是丢的；只补括号，聚合里那个 0
   还是会污染均值。**一个掩盖、一个找回，两个都要。**

---

## 问题 50：CI 的第一道关在本机是红的，而 CI 从未执行过

**日期**：2026-09（阶段 10）

### 50.1 现象

给判官的修复补测试时，顺手跑了一遍 CI 里那条命令（想确认新写的测试文件
自己也过 lint），结果：

```
$ cd backend && ruff check app tests scripts
app\db\repo\reports.py:19:48: F401 `app.db.codec.load_json` imported but unused
tests\unit\test_eval_provider_env.py:29:1: I001 Import block is un-sorted or un-formatted
tests\unit\test_eval_rubric_parse.py:34:1: I001 Import block is un-sorted or un-formatted
tests\unit\test_team_usage.py:29:8: F401 `pytest` imported but unused
tests\unit\test_team_usage.py:32:43: F401 `app.db.migrations.migrate` imported but unused
Found 5 errors.
```

只有最后一条里的 `test_eval_rubric_parse.py` 是这次刚写的，**其余 4 条都不是今天产生的**。

### 50.2 定位过程

#### 第一步：这为什么一直没被发现

`.github/workflows/ci.yml` 的 backend job 是这么排的：

```yaml
      - name: ruff
        working-directory: backend
        run: ruff check app tests scripts     # ← 没有 || true

      - name: pytest（排除 live）
        working-directory: backend
        run: pytest -q
```

ruff 在 pytest **之前**，而且没有 `|| true`。所以这 5 条错误意味着
**CI 会停在 ruff 那一步，917 条测试一条都不会开始跑**
（917 是**当时**的数；修完加进 4 条 lint 测试之后是 921，见 §50.5）。

而这 5 条错误本身：3 条是"import 了没用到的名字"，2 条是"import 块少一个空行"。
**没有一条涉及行为**——所以 `pytest` 全绿、`vitest` 全绿、
文档里写的 917 + 314 也全绿。**没有任何一条测试有可能发现它们**，
因为"代码的行为对不对"和"这段代码过不过 lint"是两个不相干的问题。

#### 第二步：CI 跑过吗

按上面的推理，CI 应该早就红了。于是去看它有没有真的跑过：

```
$ ls -d .git
ls: cannot access '.git': No such file or directory
```

**仓库还没有 `git init`。** 也就是说 `.github/workflows/ci.yml` 从写下来到现在
**一次都没有执行过**——不是"跑绿了"，是**没有跑**。

这一下把问题的性质变了。文档里那句话是：

> | CI 五个 job | 全绿，**且没有任何一步联网调 API** |

"全绿"描述的是一个**从未发生的事件**。它不是从运行结果里读出来的，
是从"配置文件写完了"推出来的——**而这两件事中间隔着一整次执行**。

### 50.3 根因

两层。

**表层：同一条事实有两个检查入口，而只有一个入口在问。**
"这份代码 lint 干净吗"这个问题，CI 会问（pytest 不管），
本机没有人问（大家跑的是 `pytest`）。
于是这条检查**存在，但没有任何东西在触发它**。

**根上：一个检查如果没有执行入口，它等于不存在。**
CI 是这个检查的执行入口——而它依赖的东西（`git init`、远端、runner）
一个都还没有。配置写得再完整，在没有入口的情况下，
它提供的保证是**零**，而它给人的印象是"有一条 CI 在守着"。

这和问题 47 是同一句话的两格：

| | 问题 47 | 问题 50 |
|---|---|---|
| 那句话 | "`--live` 跑过了" | "CI 五个 job 全绿" |
| 实际情况 | 它一次都没跑成 | 它一次都没跑 |
| 谁会发现 | 只有去跑的人 | 只有去跑的人 |

**"绿了不等于跑过"，而这条更靠前：连"绿"都没有，只是它看起来会绿。**

### 50.4 修复

**1. 把 5 条错误修掉。** 3 条 F401 逐条确认后再删——
`F401` 是静态判断，删掉是行为改动，不能照着告警批量删：

| 文件 | 名字 | 怎么确认它真的没用 |
|---|---|---|
| `app/db/repo/reports.py` | `load_json` | `grep -c load_json` 只命中 import 那一行 |
| `tests/unit/test_team_usage.py` | `pytest` | `grep -n 'pytest\.'` 无命中 |
| `tests/unit/test_team_usage.py` | `migrate` | `grep -n migrate` 只有 import 行（同行的 `MIGRATIONS` 用了 4 次，**不能整行删**） |

最后一行值得单独说：`from app.db.migrations import MIGRATIONS, migrate`
要删的是**一个名字**，不是那一行。照着告警删整行会让 4 处引用直接崩。

2 条 I001 交给 `ruff check --fix`，改的是空行位置。

**2. 加一条本机守卫**（[`backend/tests/unit/test_lint_clean.py`](backend/tests/unit/test_lint_clean.py)），
问的就是 CI 问的那个问题：

```python
_CI_ARGS = ["check", "app", "tests", "scripts"]      # 与 ci.yml 逐字相同
...
assert result.returncode == 0, "ruff 有问题（CI 会停在这一步）"
```

4 条用例，其中两条是**自我证伪**：

- `test_跑的是同一个目录集合`：断言命令本身是 `app / tests / scripts`，
  并去读 `ci.yml` 确认里面还是那一行。
  **否则将来有人觉得 `scripts` 不重要而把它去掉，这条测试会继续全绿，
  却少检查一整块**——那正是它要防的事。
- `test_改坏一处_ruff_就该红`：在临时目录造一个明确的 F401，
  断言 ruff 对它返回非零。**证明上面那条 `returncode == 0` 是有意义的**，
  不是"怎么跑都绿"。

用临时目录而不是 `tests/` 下的探针文件，是为了不让探针留在仓库里
影响别的用例。

### 50.5 验证

```
$ cd backend && ruff check app tests scripts
All checks passed!

$ pytest tests/unit/test_lint_clean.py tests/unit/test_team_usage.py
20 passed

$ pytest            # 全量
921 passed, 6 deselected, 1 warning in 92.88s
```

> **917 / 921 / 924 / 925 / 926 五个数出现在同一篇文档里，这是问题 48 记的那件事。**
> 它们各自都对，差的只是**什么时候数的**：
> `917` 是修之前（那时这 4 条 lint 测试还不存在），
> `921` 是本节这次验证跑出来的，
> `924` 是后来加了 3 条文档计数守卫之后（见 §48.7），
> `925`、`926` 是先后再补两组守卫之后——一组守"问题记录有多少条"、
> 一组守"反证突变条数两处一致"（见问题 52、问题 53）。
> **只有标出"哪个是哪个时候的"，它们才都对**——所以这几处都标了。
> 当前值现在由 [`test_docs_counts.py`](backend/tests/unit/test_docs_counts.py)
> 钉在四份文档里，但那篇守卫**故意不管本文件**：这里存的是历史。

### 50.6 教训

1. **"配置写好了"和"跑过了"之间隔着一整次执行。** 文档里那句"CI 五个 job 全绿"
   是**推出来的**，不是读出来的。凡是要写"通过了"，先问一句：
   **在哪里通过的？** 答不上来的，就只能写"配置已就绪，尚未执行"。
2. **一个检查如果没有执行入口，它提供的保证是零。** CI 是 ruff 的执行入口，
   而 CI 自己还没有入口（没有 `git init`）。链条上任何一环没落地，
   后面所有"有检查守着"的话都不成立——**而它们读起来一模一样**。
3. **lint 失败与测试失败是两类东西，一套机制表达不了。** 那 5 条错误
   没有一条涉及行为，所以 917 条测试不可能发现它们。
   **只有"跑那条命令"能发现"那条命令会失败"**——这也是为什么修法是一条新测试
   而不是"以后记得跑 lint"。
4. **告警指向的是名字，不是行。** `MIGRATIONS, migrate` 那一行里，
   `MIGRATIONS` 用了 4 次。照着告警删整行，等于用一个 lint 警告换来 4 处崩溃。
5. **CI 里各步的顺序有意义。** ruff 排在 pytest 前面，所以 lint 挂了测试根本不跑，
   看到的是"第一步红"；反过来把 ruff 放最后，会得到"测试绿、lint 红"的报告，
   那时候更容易误判成"只有格式问题"。**顺序决定了你第一眼看到什么。**

---

## 问题 51：录过 5 条，回放得出来 4 条——差的那条是唯一走澄清路径的

**日期**：2026-09（阶段 10，写文档时发现）

### 51.1 现象

文档里到处写着"黄金集 22 条里录了 5 条"（`docs/EVAL.md`、`技术栈.md`、
`项目总结.md`、`修补文档.md` 各一处）。写 `修补文档.md` 的复核表时顺手
跑了一遍 `--replay` 数证据，输出是：

```
[ 1/22] notion-vs-obsidian · quick         覆盖 1.00 · 证据 64 · 信源 41 · $0.0256 · 5.0s
[ 2/22] figma-vs-sketch · quick            没录制，跳过
...
[22/22] vague-coffee-market（歧义） · quick    没录制，跳过
闸门通过：4 条，12 项契约全绿。
```

**4，不是 5。** 而这一轮是**首次真正去数这个数**——上面那四份文档里的"5"
全部来自同一个地方（下一节），没有一处是数出来的。

### 51.2 定位过程

**第一步：这个 5 是从哪儿来的。**

`MANIFEST.json` 里 `measured.queries = 5`、`ok = 5`。
脚本自己的账说 5 条全成功，所以文档抄它是有来源的——**问题是这份账和事实
现在对不上了**，而没有任何东西会去核。

顺带发现我之前对这份文件的一处误判：我以为它"只记条数不记名单"，
理由是 `list(m)[:8]` 里没看到 `queries` 键。**那是我把 `[:8]` 的截断
当成了文件的全貌**——`queries` 就在第 9 个键上，里面完整列着 5 条的名字。

**第二步：是哪一条。**

```
  notion-vs-obsidian       ok=True  mode=quick  llm=19  $0.025591
  dji-mini4-vs-autel-evo   ok=True  mode=quick  llm=13  $0.018250
  youzan-vs-weimob         ok=True  mode=quick  llm=18  $0.026436
  dewu-vs-shihuo           ok=True  mode=quick  llm=20  $0.029157
  vague-coffee-market      ok=True  mode=quick  llm=20  $0.033180   ← 只有它回放不出来
```

单跑一次确认：

```
$ python -m eval.run_eval --replay --only vague-coffee-market
[ 1/1] vague-coffee-market（歧义） · quick    没录制，跳过

1 条没有录制物（vague-coffee-market）。录制：python -m scripts.record_cassettes --only vague-coffee-market --yes
```

**第三步：为什么是它。**

它是这 5 条里**唯一一条走澄清路径的**——黄金集里它的标签就带着「歧义」，
别名对照组里的其他四条都是直接进采集。而澄清是流水线最前面的一步：
提示词改一个字，clarify 那次调用的 payload 就变了，`_digest` 算出来的 key
跟着变，录制里的那条记录**再也匹配不上**。

这正是 `CassetteMiss` 的 docstring 里写的那件事
（"改了 prompt 导致 key 变化是常事"），只是它这次落在了一条**录过的** query 上。
所以症状不是"回放失败"，而是**"这条没录过"**——两句完全不同的话，
在输出里长得一模一样。

### 51.3 根因

两层，而且第二层比第一层严重。

**第一层：一份录制品在一次提示词改动后静默作废了。**

录制与回放之间隔着一个 `_digest(payload)`。payload 的任何一个字节变了，
key 就变了，而**旧记录仍然安静地躺在 cassette 文件里**——
文件没有变小，`cassetteTotals` 还是 98/143/68，账面上一切正常。

**第二层：`--replay` 把"没录过"和"录过但作废了"说成了同一句话。**

```python
if args.kind == "replay" and isinstance(exc, _harness_error()):
    unrecorded.append(g.name)
    print(f"... 没录制，跳过")
```

判据是**类型**（`HarnessError`），而 `CassetteMiss` 不带 key、不带断点位置。
所以这一层能告诉你"这条读不出来"，**不能告诉你"错在第几次请求"**。
5 条里只有 1 条出问题的时候，这个区别不大；等到 10 条里有 3 条时，
"哪一步的 payload 变了"是唯一能指向原因的信息，而它现在没有被传出来。

**这两层合起来的意思是**：一次提示词改动可以让若干条录制**同时**作废，
而系统的反应是这几条从"有录制物"变成"没录制物"——
**看起来像是从来就没录过，而不是像是坏了。**

### 51.4 修复

改动很小，因为**能修的地方和不能修的地方要分清楚**。

1. **四份文档里的 `5` 改成 `4`**，并写明差的是哪一条、为什么。
   这是这次改动的主体：**错的是文档，不是代码。**
2. **`docs/EVAL.md` 与 `技术栈.md` 补上"能指出是哪一条，指不出是哪一次请求"**，
   把上面第二层的限制写进正文，而不是留在这里。
3. **不改 `--replay` 的判断逻辑。** 把 `CassetteMiss` 改成带上 key
   是能做且应该做的（它现在什么都不带），但那属于"下一次真要用到时再改"：
   现在没有任何地方消费那个 key，加了也只是多一个没人读的字段。

### 51.5 验证

```
$ python -m eval.run_eval --replay
闸门通过：4 条，12 项契约全绿。

$ python -m eval.run_eval --replay --only vague-coffee-market
1 条没有录制物（vague-coffee-market）。

$ python -m eval.run_eval --replay --only notion-vs-obsidian
[ 1/1] notion-vs-obsidian · quick   覆盖 1.00 · 证据 64 · 信源 41 · $0.0256
```

四份文档里现在是 `4` 且都带上了那条 query 的名字。

### 51.6 教训

1. **"录了 5 条"是一个数字，而"哪 5 条"才是那份凭据。**
   `MANIFEST.json` 两样都记了，但文档只抄了数字——
   **抄数字比抄名单省事，而数字失效时你连去哪儿找都不知道。**
2. **一个数从账上抄来、从没数过，就只是一句话。**
   那四份文档里的 `5` 有来源（脚本的账），没有一处是跑一遍数出来的。
   **有来源不等于核实过**——这次是隔了很久之后第一次真的去数。
3. **我自己也犯了同一类错：把 `list(m)[:8]` 的截断当成了文件的全貌。**
   由此得出"这份文件不记名单"的结论，并差点写进文档。
   **"我看过了"和"我看完了"不是一回事**，而截断的输出长得就像完整的输出。
4. **两类不同的事被同一个分支说成同一句话，是排查成本的主要来源。**
   "没录过"与"录过但作废了"需要的是两个完全不同的动作
   （去录 vs. 去查哪一步变了），而它们在输出里一模一样。
5. **录制品是有保鲜期的，而它不会自己标过期。**
   磁盘上躺着一条永远匹配不上的记录，`cassetteTotals` 照样把它数进去。
   **判断一份录制品还有没有用，唯一的办法是拿它回放一次。**


---

## 问题 52：一条守卫从没红过——因为它绿在**另一道闸**上

**日期**：2026-09-20（阶段 10，写文档复核验收清单时发现）

### 52.1 现象

写文档时要把"反证脚本"的结果抄进验收清单，于是先跑一遍：

```
$ python -m scripts._mutate_check
46/49 条突变符合预期
有 3 条不符预期，见上面标 ✗ 的
```

三条里有两条是这样的：

```
突变：翻页的 LIMIT/OFFSET 参数传反（第一页变空）
  ✗ 命中 0 次（需要恰好 1 次）—— 这个突变没生效或锚点不唯一，结论无效

突变：taskCount 去掉 DISTINCT
  ✗ 命中 2 次（需要恰好 1 次）—— 这个突变没生效或锚点不唯一，结论无效
```

**这两条我一眼能认出来**：它们说的是"这次反证没验成"，不是"守卫漏了"。
第一条锚的是 `return get_conn().execute(sql, ...)` 这种一次成型的写法，
而 `_library_rows()` 现在是**两步**（先排 216 组、再回表取 30 组，
理由见该函数的 docstring），那句话在文件里已经不存在了；
第二条的字符串在文件里出现两次（宽列版与窄列版各一份）。

**第三条不一样**：

```
突变：份额闸永远放行（2022% 也照画）
  ✗ 测试仍然全绿 —— 这条测试抓不住这个 bug
```

这句说的是"**有一条守卫根本不存在**"。而它在文档里的写法是
`后端反证脚本 48/48`——**一句已经写进验收清单的、绿的读数。**

### 52.2 定位过程

**第一步：先确认这不是脚本自己的毛病。**

反证脚本会改源码再还原，所以"测试全绿"也可能是"突变压根没落到文件里"。
手工把那行改掉、确认文件真的变了，再单独跑那条见证文件：

```python
# app/core/analysis/charts.py
-  if any(not 0 < row["share"] <= 100 for row in rows):
+  if False:
```

```
$ python -m pytest tests/integration/test_analyze_guards.py -q
..........                                                               [100%]
```

**十项全绿。** 洞是真的。

**第二步：饼图为什么没出来。**

按测试的写法，那张饼图**本来就该出得来**：`share=2022.0` 越界，
份额闸返回 `[]`。闸拿掉之后它应该出现——除非**还有另一道闸也在拦它**。

沿着 `build_charts()` 往下读，出口处有这么一段：

```python
backed = [chart for chart in charts if chart.get("evidenceIds")]
dropped = [chart for chart in charts if not chart.get("evidenceIds")]
```

而那条用例的夹具写的是：

```python
{"brand": "示例品牌", "share": 2022.0, "basis": "...", "evidenceIds": []}
```

**份额行没带证据 id，于是饼图先被"没有证据链的图不生成"这道收口拦掉了。**
断言 `_pie(body) is None` 因为这道闸而成立——份额闸在不在都一样。

打一行探针看得更清楚（`degraded` 里那一句就是证据）：

```
CHART_IDS= ['chart-matrix-bar', 'chart-matrix-radar', 'chart-trends', 'chart-source-mix']
DEGRADED= ['图表：1/5 张图的数据没有证据链，已不生成（市场份额）']
```

`chart-market-share` 不在图里，而**报告给的理由是"没有证据链"**，
不是"份额越界"。两条不同的原因，一个结果。

### 52.3 根因

**断言只能分辨它点名的那个机制，而"饼图不在"没有点名任何机制。**

`assert _pie(body) is None` 问的是"结果对不对"。它读起来像是
"越界的份额被拦住了"，实际上它能被**任何**一道上游闸满足：
证据链收口、形状闸、数据为空、图被改写……份额闸只是其中一条。
所以当份额闸被删掉时，这句话仍然成立——**它从来没有在测份额闸。**

而这件事**读代码是读不出来的**。那条用例的 docstring 写着
"饼图会把它画成 `示例品牌：2022%`"，夹具里那个 `2022.0` 也确实
有出处（真实报告里的值），整个用例从名字到注释都在讲份额闸。
唯一发现它的办法是**把份额闸改坏，看它红不红**——而它没红。

### 52.4 修复

**一、让那张饼图"本来该出得来"。** 加一个 `_share_rows()` 辅助，
给份额行挂上**真实存在**的证据 id（与默认夹具同一个来源
`mock._pick_evidence()`）。这样份额行不再是"没有证据链"的，
唯一拦得住饼图的就是份额闸。

**二、断言缺席的原因。** 两条越界用例各加一句：

```python
assert not _dropped_for_missing_evidence(body), (
    "饼图是缺证据链被收口拦掉的，不是被份额闸拦掉的——这条断言没在测份额闸"
)
```

这一句是给"夹具又被人改成空证据"准备的：那时它会红，并说出原因。

**三、修两条失效的锚点，并让它们**不可能**再静默失效。**

- LIMIT/OFFSET 那条改锚在第一步取数上（`(*params, limit, offset)`）。
- `taskCount` 那条带上收尾的 `)`，因为裸字符串在文件里有两处。

两条的注释里都写明了**为什么**要锚成这样——锚点失效的样子是
"结论无效"，与"守卫漏了"并列显示，没人会专门去查。

**四、突变条目自己带上这段历史。** `_mutate_check.py` 里份额闸那条
现在写着"这条**曾经是绿的**"以及修法，免得下一个人以为它从来就有用。

### 52.5 验证

**① 突变放进去，两条用例变红，且红在正确的那句断言上：**

```
FAILED tests/integration/test_analyze_guards.py::test_份额填成用户规模时不出饼图
FAILED tests/integration/test_analyze_guards.py::test_只有一个品牌越界就整张不出

>       assert _pie(body) is None
E       AssertionError: assert {'chartId': 'chart-market-share', 'kind': 'pie',
E       'title': '市场份额', 'evidenceIds': ['EV-d28541ffca9d', ...]} is None
```

失败信息里那张饼图**带着证据 id 回来了**——正是它本该有的样子，
也正是修之前测不出来的东西。

**② 还原之后十条全绿**（空行数是 10 个点）：

```
$ python -m pytest tests/integration/test_analyze_guards.py -q
..........
```

**③ 整套反证：**

```
$ python -m scripts._mutate_check
49/49 条突变符合预期
```

三条各自的结局（`（最先变红：…）`是脚本自己报的证人）：

```
翻页的 LIMIT/OFFSET 参数传反   ✓ 测试变红（test_四次写入三次调研只沉淀三条来源）
taskCount 去掉 DISTINCT        ✓ 如注释所述：本 schema 下无法证伪，测试照样绿
份额闸永远放行（2022% 也照画）  ✓ 测试变红（test_份额填成用户规模时不出饼图）
```

最后一条值得停一下：**它现在是被"我为它写的那条用例"抓住的**，
名字对得上。这不是必然的——脚本带 `-x`，一个崩溃型突变会让
**顺序上最先碰到那行代码**的用例变红，而那不是写来断言这件事的那条。

**④ 修这几处守卫本身没有动测试条数：**全量 `pytest -q` 绿；`ruff check` 干净；
`test_docs_counts.py` 仍绿（**这一批只改了断言与锚点，没加用例，所以 924 没变**）。

**⑤ 但紧接着加的下一组守卫把它变成了 925。**
写这条记录的时候顺手给 `test_docs_counts.py` 补了一组同类断言：
**"问题记录.md 有多少条"在六份文档里的写法**（README / 修补文档 /
开发文档两处 / 项目总结两处）。理由是本条自己踩的坑——写完问题 52，
那六处一处都没想起来改。

它**又一次**是先红后绿的，而且红的就是它自己引入的那一条差：

```
AssertionError: [README.md] 的开头那句简介写着 924 条，实测是 925 条（含 live 共 931）
```

这和 §48.7 里那条守卫的第一次跑是同一个形状（见该节表格第四行）。
另外顺手证伪了一次：把 README 里那个数改成 `99`，它报出

```
AssertionError: [README.md] 的文档索引表写着 99 条，`问题记录.md` 里实际是 52 条
```

——**指名道姓到"哪一句"，而不只是"有一个数不对"**，
这才是上面教训 2 要的那种断言。

### 52.6 教训

1. **测试绿，要问"是哪个机制让它绿的"。**
   这是问题 35 那条第 4 点的镜像：那边是"一条突变变红，要看清它
   **为什么**变红"，这边是"一条用例变绿，要看清它**为什么**变绿"。
   两者是同一种错——**把结果当成了机制。**
2. **断言"结果不对"，抓不住"原因不对"。**
   要守住某道闸，就得断言"是它拦的"，而不是"东西没出来"。
   `assert _pie(body) is None` 与"份额闸守住了"之间隔着一整条流水线。
3. **"我读了一遍，它是对的"在守卫问题上不是一种验证。**
   这条用例有名字、有 docstring、有真实数据、有出处，读起来毫无破绽。
   **突变校验是唯一能让它开口说话的手段**，而它一开口就是"我从没红过"。
4. **反证脚本的汇总行把两件事说成了同一句话。**
   `有 3 条不符预期` 里混着"锚点失效（结论无效）"和"守卫真的漏了"，
   而这两者要的动作完全相反（改锚点 vs. 补守卫）。
   明细行分得清，**但没人会去看明细——大家看的是最后那行数字。**
   （与问题 51 第 4 条同一个形状，只是这次换成了反证工具的汇总。）
5. **一个数字被写进验收清单之后，它就脱离了产生它的那个版本。**
   清单上写的是 `48/48`，而脚本现在有 49 条突变——
   说明那句话写下来之后**一次都没重跑过**——多出来那条就是
   问题 35.5 里为 `spans()` 排序键补的突变，它是在那句话之后加的。
   这次去跑，跑出来是 46/49。
   **没跑过的绿，和没验过的红一样，都不算读数。**
   这也正是问题 51 第 2 条的同一个形状：**有来源不等于核实过。**


---

## 问题 53：前端反证脚本里，唯一一种"退出码为 0 的失败"恰恰是最严重的那种

**日期**：2026-09-20（阶段 10，与问题 52 同一次复核）

### 53.1 现象

为了把两个反证脚本的读数抄进验收清单，前端那个也整个跑了一遍：

```
$ python scripts/falsify_frontend.py
...
================================================================
汇总
  抓住       F1 ...
  ...
  抓住       G6 初始位置改用 Math.random（两次布局不再一致）

已还原：True
```

**56 条突变，56 条抓住，0 漏掉，0 锚点失效，0 门禁崩，退出码 0。**
这一轮没问题。

问题出在**读它怎么得出那个退出码**的时候。三种失败在打印上分得很清楚，
而退出码只认其中三种：

```python
return 0 if not dirty and not broken and not dead else 1
```

| 败法 | 汇总里长什么样 | 进退出码 |
|---|---|---|
| 锚点失效（被重构带走） | `锚点失效  F13` | 是（`dead`） |
| 门禁自己崩了（vitest 收集失败） | `**漏掉** … ← 门禁自己崩了，这条结论不算数` | 是（`broken`） |
| 没还原 | `已还原：False` | 是（`dirty`） |
| **测试没抓住** | `**漏掉**  F24` | **不是** |

**最后一行就是那个洞。** `caught = False` 只影响打印出来的那行字，
不影响任何别的判断——**测试没抓住的突变，脚本退出 0。**

### 53.2 定位过程

**第一步：先确认这是个洞，而不是我读漏了一处。**

全文搜 `missed` / `漏掉` 附近的赋值：只有 `verdicts.append(f"  {mark:8s} ...")`
一处，没有任何列表收集它，也没有任何地方把它算进返回值。（上面那张表是
一行一行读 `main()` 读出来的。）

**第二步：看看这个脚本自己是怎么说"哪种失败要进退出码"的。**

`if dead:` 上面那段注释写着：

> 锚点失效是三种失败里**最安静**的一种，所以它必须让退出码变红。

这个理由**对"漏掉"更成立**：

- 锚点失效 = **工具**没验成（那段代码被重构走了）；
- 门禁崩了 = **现场**有问题（证人没机会出场）；
- **漏掉 = 守卫真的没了。** 这是这个脚本存在的全部理由。

一个"测试抓不住这个 bug"的读数，是这三种里唯一一种**必须有人去补测试**的。
而它恰好是唯一一种跑完是绿的。

**第三步：做一次对照实验。**

不看代码，直接制造一次漏：把一条突变的**证人字串**换成一句在任何用例名里
都不可能出现的字（`ZZZ-不可能出现的证人-ZZZ`）。这样 mutant 会被写进去、
测试照跑、判定照做，只是脚本再也认不出证人——`caught` 为假，
走的正是"漏掉"这一支。**这样做一次只跑一条突变，四十秒，而不是一小时。**

同一份探针，只改退出条件那一行：

| 退出条件 | 结果 |
|---|---|
| `... and not dead else 1`（改之前） | **`EXIT=0`** |
| `... and not dead and not missed else 1`（改之后） | **`EXIT=1`** |

改之前那一次，输出是：

```
汇总
  **漏掉**   F1 批内不再去重（续传补发与实时推送重叠时，同一条思维出现两遍）

已还原：True
```

——**屏幕上最后一句话是"已还原：True"，退出码是 0。**
两句话都不含"有条守卫没了"这件事。

### 53.3 根因

**同一种失败在"打印"和"退出码"上走了两套判断，于是两者之间必然出现不一致。**

打印那条路径是按 `mark` 算的（`"抓住" if caught else "**漏掉**"`），
而退出码那条路径是手写的一串 `and not ...`——两处各写一遍"什么算失败"，
就会有一处漏掉某个刚加进来的败法。**这正是这个仓库反复记的那件事**：
`dead` 是后补的（问题 36），补的时候加了列表、加了打印、加了退出码；
而 `漏掉` 从一开始就只活在打印里，因为**它当时没有被当成一种"失败"**——
它只是判定结果的一种取值。

而这个洞恰好落在这两个脚本分工的缝上：后端那个（`_mutate_check.py`）
是 `if passed: failures += 1`，**"没抓住"一直在退出码里**；
前端那个不是。两个脚本在这件事上从来没有对齐过。

### 53.4 修复

三处，都在 `frontend/scripts/falsify_frontend.py`：

1. **加 `missed` 列表**，在判定分支里收集：

   ```python
   if kind != "tsc" and not gate["vitest_ok"]:
       # 仪器崩了**不算漏掉**——证人都没机会出场，补测试是白补。
       tag = "  ← 门禁自己崩了，这条结论不算数"
       broken.append(name)
   elif not caught:
       missed.append(name)
   ```

   写成 `elif` 而不是两个独立的 `if`：**两者互斥**，
   而并列的话"仪器崩了"会同时进两个列表，同一个东西被报两遍。

2. **汇总里单列一段**，因为它要采取的行动与另外两种都不同：
   改锚点、修现场、**补测试**。

3. **退出码加上它**，并把"四种都要红"写在返回值旁边：

   ```python
   return 0 if not dirty and not broken and not dead and not missed else 1
   ```

**没有动判定逻辑本身**：`caught` 怎么算、证人怎么比，一行没改。
这条改的是"判定结果怎么传到退出码"，不是"怎么判定"。

### 53.5 验证

**① 对照实验**（上面那张表）：同一份探针，只换退出条件那一行，
`EXIT` 从 `0` 变成 `1`。改之后的完整输出：

```
汇总
  **漏掉**   F1 批内不再去重（续传补发与实时推送重叠时，同一条思维出现两遍）

已还原：True
有 1 条突变**测试没抓住**——这是真的漏，不是仪器的问题：
  F1 批内不再去重（续传补发与实时推送重叠时，同一条思维出现两遍）
  两种修法，选哪种都要写下来：补一条能抓住它的用例；
  或者删掉这条突变，并在注释里写明它为什么证伪不了。
```

**② 探针用完即删**，并确认工具没被它带歪：

```
$ python scripts/falsify_frontend.py --status
快照里有 12 个文件，最后开跑前记录于 manifest.json。
现场与快照逐字节一致。
```

> 探针会把基线快照重写成"只有一个文件"的样子（`snapshot_baseline()`
> 每次开跑都写盘，而探针只留了一条突变）。所以跑探针之前先备份
> `.falsify-baseline/`，跑完再放回去——**否则 `--restore` 就只剩一个文件可还原。**

**③ 完整的 56 条那一次**：`0 漏掉`，所以这个洞**没有在真跑里发作过**。
它是读代码读出来的，verification 靠的是上面那个一次性的对照实验。
**这一点必须说清楚：修复不是由一次真实故障推动的，是由一次阅读推动的。**

**④ 顺手补的那组守卫，又一次先红后绿。**
写本条时发现反证脚本的那两个数（后端、前端各一个）在
[`修补文档.md`](修补文档.md) 与 [`项目总结.md`](项目总结.md) 里各写了一遍，
而**那两份当时写的是 `48/48` 和 `47/47`**——那是加第 49 条、第 56 条突变
**之前**数出来的，加完没人回头重数。**一个数被抄进验收清单，就离开了产生它的那个版本。**
于是给 [`test_docs_counts.py`](backend/tests/unit/test_docs_counts.py)
补了第四条断言：两份文档里那句话必须相等。

判据写成 `(\d+)/\1` 而不是 `(\d+)/(\d+)`：**它顺带要求那句话写成 N/N**，
而 `46/49` 这种"总条数与抓住条数不等"的形状会当场匹配不上、
报"模式没匹配到"——不至于被一个看起来合理的 `46/49` 蒙混过去。

它也一样是**先红后绿**的，红的还是它自己引入的那条差：
新加的用例让收集总数从 925 变成 926，于是它一跑就报

```
AssertionError: [README.md] 的开头那句简介写着 925 条，实测是 926 条（含 live 共 932）
```

三次了（见 §48.7 那张表最后一行）。这条守卫的边界也写进了它的 docstring：
**它抓得到"改了一处忘了另一处"，抓不到"两处都没重跑"**——
真值只有花四十分钟跑一遍脚本才知道，而那是守卫本身扛不住的重量。

### 53.6 教训

1. **一个工具被自动读的只有退出码。** 人看汇总表，CI 看退出码；
   而"只印在表里、不进退出码"的失败，在自动化里等于不存在。
   这两个脚本今天都还没接进 CI（`.github/workflows/ci.yml` 里没有它们），
   所以眼下的危害是"人扫一眼最后几行就以为通过了"——
   **而那几行里最像结论的那句是 `已还原：True`，它只说文件还原了。**
2. **同一个判断写两遍，就会有一遍漏掉新分支。** "什么算失败"应该只有一处：
   汇总表和退出码都从同一批列表里派生。这里是两处手写的，
   于是 `dead` 补进去的时候 `missed` 没被想起来。
3. **"加了个列表、加了打印、忘记加退出码"是一种很难自查的改动**——
   它在正常情况下完全看不出来（今天就是 0 漏掉），
   只有在真的漏一次的时候才暴露，而那时候人已经在看别的地方了。
4. **要从"我读了一遍"变成"我试了一次"。** 这个洞是读出来的，
   但我没有停在读上：把证人字串换掉、跑一条突变、看退出码——
   四十秒就拿到了一次真正的对照实验。**能构造的最小实验，比最长的那段推理值钱。**
5. **又一次踩到"截断的输出看起来像完整的输出"。** 第一次跑这个脚本时我用了
   `| tail -50`，拿到的 48 行判定全是 `抓住`，我就差点照着它写文档——
   而那 50 行**丢掉了汇总表头和 F1–F8 八条判定**。
   这次发现得早（问题 51 第 3 条刚记过同一件事），
   代价是重跑一遍四十分钟。**结论：跑这种脚本时不要在第一层管道上截断。**

---

## 问题 54：同一个品牌两棵树，另一个品牌一棵都没有——而两个仪器都问不出这个问题

**日期**：2026-09-20（阶段 10，补文档时的最后一次复核）

### 54.1 现象

为截图脚本挑一份"数据好看的报告"时，逐份打开库里的真报告看，看到这一份：

```
RP-8b4e09569307  「国内新能源汽车充电桩运营商的竞争格局」

featureTrees  特来电 0.7969 · 特来电 0.8065 · 国家电网 0.8889     ← 特来电两棵，星星充电没有
pricingModels 特来电 · 特来电 · 国家电网
personaSets   特来电 · 特来电 · 国家电网
marketShare   特来电 41.0 · 特来电 27.4 · 星星充电 19.1 · 国家电网 17.8
brands        [国内新能源汽车充电桩运营商, 特来电, 星星充电, 国家电网]
degraded      [正文抓取：12/40 条…, 正文抓取：12/40 条…, 结构化分析：星星充电 未产出…,
               正文抓取：12/40 条…, 返工补采：…, 返工补采：…]      ← 同一句话三遍
```

**两棵 `特来电` 的覆盖率不一样（0.7969 / 0.8065）**——所以它们不是同一条数据被存了两遍，
是**两份不同的内容**都挂在了这个品牌名下。而 `星星充电` 有降级说明（被问了、没答上来），
`特来电` 没有。

这不是孤例。同一批里还有一份：

```
RP-b1e819bfd098  「对比 Notion 与 Obsidian 在团队协作场景下的差异」
featureTrees  Notion 0.6852 · Notion 0.6622 · Obsidian 0.5 · Coda 0.0 · Slite 0.4219 · Nuclino 0.0
```

前端的后果是**卡被顶掉**：`ReportCatalog.tsx` 三块都用品牌名当 React key，
React 遇到重复 key 时的行为是"可能重复渲染、也可能整个丢掉一条"，而且**只在控制台说一句**。
页面上看不到任何异常——少一张卡读起来像"这个品牌没采到"。

### 54.2 定位过程

**第一步：这不是渲染问题。** 先把前端排除掉——`body.featureTrees` 里本来就是两条。

**第二步：为什么会有两条？** 打了一次带 `purpose` 的日志，看到四个品牌都被问了：

```
analyze_structured:国内新能源汽车充电桩运营商
analyze_structured:特来电
analyze_structured:星星充电
analyze_structured:国家电网
```

`ctx.brands[0]` 是**调研对象**，而它经常不是品牌——这一份里它是"国内新能源汽车充电桩运营商"，
隔壁那份里是"Notion 与 Obsidian 在团队协作场景下的差异"。它被当成品牌问了功能树。

**第三步：那它为什么会以 `特来电` 的身份留下？** 三个 schema parser 都是这么写的：

```python
resolved_brand = as_str(pick(payload, "brand", "品牌", "product", "产品"), default=brand)
```

`payload` 里给了 `brand` 就用 `payload` 的。于是那句短语那一问的返回——模型按证据里
最响的那个品牌（特来电）作答——**被改名收下**，占据了"特来电"的一个槽位。
真正的 `特来电` 那一问也产出一棵，于是同一个品牌两棵。
`星星充电` 那一问没答上来，记了降级——**所以第三个槽位空了，而报告里看不出空的是谁**。

**第四步：为什么这件事以前没被发现？三个仪器全问不出这个问题。**

| 仪器 | 为什么问不出 |
|---|---|
| `pytest` 926 条 | 一条都没观察到——改完这五处之后全量重跑，**原有的 926 条没有一条变红**（红的只有我新加的那几条） |
| mock provider | 它对**每个**品牌都回同一份载荷，里面写着 `示例品牌`。于是"一个品牌两棵树"在 mock 下是**常态**，"问 A 答 B"根本不可见 |
| 前端 e2e 夹具 | 它正是从这份报告裁下来的，而裁剪规则是"每个数组只留前 N 条"，`featureTrees` 留了 N=1——**重复的那一条恰好在被留下的那条之外**，删掉了 |

最后一行值得单独说。那份夹具从**有缺陷的那份报告**里裁出来，
裁完却把这个缺陷裁掉了；而它保留的 `marketShare` 是 N=4，
**两份 `特来电`（41.0 / 27.4）都在**——于是饼图上有两片"特来电"，
ECharts 不报错，控制台一句话也没有，Playwright 的断言是"没有控制台错误"，
**绿**。（今天那份夹具里的 `chart-market-share` 还是没有证据链的老形状，
`evidenceIds: []`——后端现在会在出口把这种图丢掉，夹具早于那道闸。）

**仪器藏起了它本该测的那个缺陷。** 这与问题 52 是同一个形状：
一条从没红过的守卫，多半在数一个不会变的东西。

### 54.3 根因

三件事叠在一起，各自单独看都"说得通"：

1. **调研对象被当成品牌。** `ctx.brands[0]` 是"要调研什么"，不是"要对比谁"。
   用户问的那句话本身就是它，而它常常是一句短语而不是一个品牌名。
2. **回答的品牌名覆盖了请求的品牌名。** 对"同一个品牌的两种写法"（`国家电网` /
   `国家电网有限公司`）这是对的；对"答的是另一个品牌"就变成**把 B 的内容挂在 A 名下**——
   读者没有任何办法看出来。
3. **重复到达下游时，没有一处去重或披露。** `degrade()` 会把同一句话记三遍；
   饼图会把同一个品牌画两片；前端 key 会顶掉一条。

### 54.4 修复

**仪器先修，因为它是漏测的原因之一。**

- `mock._rebrand()`：结构化夹具里那个 `示例品牌` 是**占位符**，不是某个品牌的名字。
  现在按"这次问的是谁"填进去。只换占位符、不换别的名字——测试要能拿它伪造"问 A 答 B"。

**根因（第 1 条）**

- `analyze._structured_brands()`：调研对象不在候选品牌里时，不拿它去问功能树/定价/画像，
  并且**说出来**（思维流里一句"「X」是调研对象而不是竞品"）。跳过的理由不说，
  读者只能猜是漏了还是故意的。

**防御（第 2 条）**

- `analyze._reject_if_relabelled()`：解析出的产物，brand 和请求的对不上就**丢弃并记账**。
  丢弃的后果是这一块缺失，而缺失是明说的（`completeness` 判 `missing`、`isPublishable` 为假）。
- `analyze._same_brand()`：判"是不是同一个品牌"的规则。**这条规则被用例逼着改了三次**：
  1. 一开始用"包含"——它把 `Notion` 判成
     `Notion 与 Obsidian 在团队协作场景下的差异` 的同一个品牌（后者包含前者）。
  2. 改成"前缀"——只堵住了一半。上一例里被问的那句更长，前缀照样成立。
     上面那份真报告 `RP-b1e819bfd098` 就是这一例，不是假想。
  3. 最后的判据是**"多出来的那截里有没有连接词"**。后缀（`有限公司`、`Labs`、
     `（万帮数字能源）`）是名字的一部分；连接词（`与`）说明后面还有**另一个实体**，
     整个字符串是一句话，不是名字。
     不用"尾巴长度"卡阈值，是因为 `星星充电（万帮数字能源）` 的尾巴长 8 个字，
     比 `Notion Labs` 长得多——按长度卡会误伤它。

**披露（第 3 条）**

- `context.degrade()` 去重：同一句话只记一次（那三行"12/40 条正文抓取失败"）。
- `charts._market_share_chart()`：**同一品牌两条份额时不出饼图**（两片"特来电"
  会被读成"集中度很高"）；口径冲突本身由 `analyze._analyze_comparison` 记进改写记录，
  因为它才是知道每行 `basis` 的地方。
- `assemble._flag_duplicate_brands()`：出口上的**绊线**。这三块共用同一个前端 key 空间
  这件事，只有装配的时候看得见，所以钉在出口而不是三个产出点。
  重复真漏出来时报告里多一句话，而不是页面上悄悄少一张卡。
- `ReportCatalog.tsx`：三处 key 带上下标。后端已经改成不产出重复了，
  这是第二道——前端不该因为后端某天又出一个重复就静默少显示一份数据。

### 54.5 验证

**新增 17 条测试，`926 → 943`，全在这里：**

| 文件 | 新增 | 守什么 |
|---|---|---|
| `tests/unit/test_brand_attribution.py` | 7（新文件） | `_same_brand` 的判据、该问哪些品牌 |
| `tests/integration/test_analyze_guards.py` | 8 | 归属错位丢弃、口头披露、出口绊线 |
| `tests/unit/test_docs_counts.py` | 2 | 规模表自己算得对、两处规模数一致 |

**每一组都先红后绿，红的是它自己引入的差：**

| 守卫 | 第一次跑的结果 |
|---|---|
| `_same_brand` 那组 | **红**——`Notion 与 Obsidian…的差异` / `Notion` 判成了同一个品牌（"包含"和"前缀"两次都是） |
| 归属丢弃那两条 | **红**——`_rebrand` 把注入的品牌也改掉了，注入根本没到流水线 |
| 出口绊线那两条 | **红**——把 `_flag_duplicate_brands(ctx)` 摘掉，`coercion.repairs` 是空的 |
| 文档条数那条 | **红**——`README.md 的开头那句简介写着 926 条，实测是 939 条` |

**全量：`943 passed, 6 deselected`**（`unit 674 / integration 242 / contract 27`，
逐目录 collect 数出来的，不是读的汇总行）。ruff 全库干净。

**最该记的一句：原有的 926 条一条都没红。** 也就是说，这五处行为改变
（不再对调研对象问结构化、归属错位就丢弃、降级去重、饼图加闸、出口绊线）
**没有一条被现有的 926 条测试观察到**——包括那条专门守"引用与证据"的、
那条专守"图表要有证据链"的。绿不是证据。

### 54.6 教训

1. **一次全绿的回归，证明的是"没有一条测试观察到这次改动"，不是"这次改动没影响"。**
   926 → 943 之间那 17 条差，全是新加的守卫；老的一条没动。这句话在问题 47
   记过一次（`eval --live` 从没执行过，于是"契约全绿"是推出来的），这里是它的第二个实例。
2. **仪器会藏起它该测的那个缺陷。** mock 对每个品牌回同一份载荷，
   于是"一个品牌两棵树"在 mock 下是常态；e2e 夹具"每块留前 N 条"，
   把重复的那条裁掉了。**两个仪器都在结构上把这个缺陷盖住了**——
   而这比"没有测试"更危险：有测试的地方会让人不再去看。
3. **判据不是想出来的，是被用例逼出来的。** `_same_brand` 改了三次，
   每次都是"跑一下、发现它放过了（或误伤了）一个真实的例子"。
   第一版写完之后我"觉得"它是对的。
4. **一个数被复制到 checklist，就离开了产生它的那个版本。** 这条问题里的
   `marketShare 41.0 / 27.4` 一开始被我当成"两份重复数据"，写进笔记之后才发现
   `basis` 各写着各的出处——**它们不是重复，是两个口径**，报告用表格保留它们是对的，
   错的只有"合成一张饼图"。判据不同，修法就不同。
5. **代码规模那两个数在同一轮里也过期了。** README 写着"约 32,500 行"，
   而实测 33,191；`项目总结.md` 的测试分层表（`unit 663`）也一样。
   和问题 48 是同一件事：数被复制，只有一处会被顺手改。
   这次的处理与测试条数**故意不同**：行数**不加**"等于真值"的守卫——
   它每次改代码都会变，一条天天红的守卫最后一定会被人绕过去。
   改成两条自己就能验的关系：**两处文档说的是同一个数**、
   **`项目总结.md` 那张表的合计等于它上面几行加起来**（后一条不需要任何外部真值）。
   并在表下写明"这张表是一个快照"，用旁边那个**有守卫的**数（测试条数）当锚点。

---

## 问题 55：第一次真跑"克隆下来能不能跑"，CI 就红了

**日期**：2026-09-21（首次发布前的验证）

### 55.1 现象

发布前做的最后一件事是"照陌生人的做法验一遍"：把仓库克隆到一个空目录，
在一个**没有 `backend/.env`** 的地方跑测试。

```
本机（有 .env）    ： 943 passed, 6 deselected
克隆（没有 .env）  ： 1 failed, 942 passed, 6 deselected
```

唯一失败的是 `tests/unit/providers/test_registry.py::test_both_configured_builds_a_fallback`：

```
AssertionError: assert 'mock' == 'mock>deepseek'
WARNING 备用 LLM deepseek 未配置，降级能力不可用：[deepseek] 未配置 DEEPSEEK_API_KEY
```

这条用例要问的是"配了两个 provider 会不会把降级链组起来"，
而它实际上问的是"这台机器的环境里恰不巧有一把钥匙"。

### 55.2 为什么它一直没被发现

三个条件同时成立，缺任何一个都会早发现：

1. **本机有 `.env`。** [`config.py`](backend/app/core/config.py) 在模块导入时做
   `load_dotenv(backend/.env)`，于是开发机上跑测试时进程环境里**一直有两把真钥匙**，
   这条用例于是一直绿。
2. **`.env` 是 gitignore 的，克隆出来没有。** 所以这个差别**只在别人的机器上出现**——
   而写它的人怎么试都试不出来。
3. **CI 从来没有执行过。** 这个仓库直到发布前才 `git init`（见
   [`修补文档.md`](修补文档.md) A5）。`.github/workflows/ci.yml` 里那五个 job
   写得很完整，而它上面的每一句话都是推出来的。

第 3 条是关键：**只要 CI 跑过一次，这个问题在第一次就会暴露。**
所以这不是"一条测试写错了"，而是"一整层自动化保证从来没有被读过一次"。

### 55.3 根因

**测试套件依赖了本机的密钥文件。**

具体路径：`config.py` 的 `load_dotenv(ENV_FILE, override=False)` 把 `backend/.env`
灌进 `os.environ`；而 provider 凭据**不是 `Settings` 的字段**，是按
`<PROVIDER>_API_KEY` 的命名约定从 `os.environ` 动态查的。

两条设计单独看都对——后者正是"接一家新 provider 不用改 `config.py`"的实现。
合起来的效果却是：**测试的结果取决于跑测试的那台机器上有没有那个文件。**

### 55.4 修复

**修的是那一类，不是那一条。**

只给失败的那条补一个 `monkeypatch.setenv` 也行，但那只修了一条，
剩下九百多条仍然各自与环境有关，而这类故障同样是"只在别人机器上红"。

所以做成默认干净：[`tests/conftest.py`](backend/tests/conftest.py) 加一条 autouse 夹具
`_no_ambient_credentials`，把 `*_API_KEY` 一律从进程环境里摘掉。
凭据必须被用例显式设进来，否则就是没有。这与 `_never_touch_production_db`
是同一条理由——**"要记得加"等于没有**。

用例那一侧也补了：`test_both_configured_builds_a_fallback` 现在自己
`setenv` 一把假钥匙，它才真的只在问"两个 provider 会不会组链"。

### 55.5 验证

| | 本机（有 `.env`） | 克隆到空目录（没有 `.env`） |
|---|---|---|
| 修之前 | 943 passed | **1 failed, 942 passed** |
| 修之后 | **945 passed** | **945 passed** |

两边同一个数，这才是"与环境无关"的判据。新增 2 条测试
（[`tests/unit/test_env_isolation.py`](backend/tests/unit/test_env_isolation.py)），`943 → 945`。

**这两条守卫的证伪是不对称的，值得单独写下来：**

- **本机**：删掉那条 autouse 夹具，`test_凭据不会从本机的_env_漏进测试` 就红——
  它确实在挡东西。
- **CI**：环境里本来就没有凭据，**删掉夹具它照样绿**。

所以这是本仓库**唯一一条"只能在本机证伪"的守卫**。而"CI 全绿"这句话很容易
被读成"所有守卫都在工作"——这条不在那个集合里，它绿在"本来就没有东西可漏"上。
第二条第守卫（`test_环境里有本机的_env_文件时这条守卫才有意义`）做的就是把
这件事断言出来，免得这条守卫哪天悄悄变成一句空话。

### 55.6 教训

1. **"CI 全绿"必须是一次读数，不能是一句推论。** 这与问题 50 是同一件事
   （那次是 ruff 命令在本机就红，而文档写着全绿）。区别在于问题 50 至少
   在本机能复现，这次**只有换一台机器才看得见**。
2. **凡是"只在别人机器上坏"的东西，本机自测永远发现不了。** 唯一的办法是
   **真的造出一个"别人的机器"**：克隆到一个空目录再跑。这次的成本是三分钟，
   收益是发现 CI 从第一次跑就会红。
3. **"测试与环境无关"不是默认状态，是要主动做出来的。** 这次两条设计
   （`load_dotenv`、按约定查凭据）都很好，合起来却让测试悄悄绑上了本机文件。
   默认安全比"记得别依赖"可靠——这是本仓库第三次得出同一个结论，
   前两次分别是 `reset_mock_state` 与生产库那条夹具。
4. **一条守卫"绿"可能绿在两种不同的原因上**：它挡住了，或者根本没有东西可挡。
   这两种绿在输出里长得一模一样，所以**证伪的条件本身也要写下来**。
5. **顺带发现的另一件事**：`docs/images/` 里那几张作品集截图里的品牌名是
   `示例品牌` / `对照品牌`——截图脚本 `shoot_screens.mjs` 选的是"证据条数最多"
   的那份报告，而那份是 mock 报告（见 54.2 里那条"仪器藏起了它本该测的东西"）。
   截图本身在 [`修补文档.md`](修补文档.md) 里另记着。
